--- PR #1087 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:      // get fee | -    const feeAsset = this.getFeeAsset(from, to); |      const specs = this.getSpecs(from, to);
David: Is the method `getFeeAsset` still used somewhere?

--- PR #1087 | src/subdomains/supporting/payment/entities/fee.entity.ts ---
Context: +        (this.fiatList?.length && | +          ((request.from && !this.isAsset(request.from) && !this.fiatList.includes(request.from.id)) || | +            (!this.isAsset(request.to) && !this.fiatList.includes(request.to.id)))) ||
David: Maybe a helper method for this? 🤔 
```ts
private verifyCurrency(currency: Asset | Fiat): boolean {
   const list = this.isAsset(currency) ? this.assetList : this.fiatList;

   return !list?.length || list.includes(currency.id)
}
```

--- PR #1089 | src/subdomains/core/buy-crypto/routes/buy/buy.controller.ts ---
Context:        (e) => e.message?.includes('duplicate key'), | -    ).then((buy) => this.toPaymentInfoDto(jwt.id, buy, dto)); | +    ).then((buy) => this.toPaymentInfoDto(jwt.id, buy, dto).then());
David: Why this?

--- PR #1089 | src/subdomains/core/buy-crypto/routes/buy/buy.controller.ts ---
Context:   | -    return { | +    const buyDto = {
David: Add type annotation `BuyPaymentInfoDto`

--- PR #1089 | src/subdomains/core/buy-crypto/routes/crypto-route/crypto-route.controller.ts ---
Context:   | -    return { | +    const cryptoDto = {
David: Same here

--- PR #1089 | src/subdomains/core/sell-crypto/route/sell.controller.ts ---
Context:   | -    return { | +    const sellDto = {
David: Same here

--- PR #1089 | src/subdomains/supporting/payment/entities/transaction-request.entity.ts ---
Context: + | +  @Column({ nullable: true }) | +  targetId: number;
David: Nullable false + type 'integer' ?

--- PR #1089 | src/subdomains/supporting/payment/entities/transaction-request.entity.ts ---
Context: +  Sell = 'Sell', | +  Crypto = 'Crypto', | +}
David: Rename `Crypto` to `Convert`

--- PR #1092 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context: +    return this.verifiedName | +      ? `https://dilisense.com/en/search/${this.verifiedName?.split(' ')?.join('%20')}` | +      : undefined;
David: Use `encodeURIComponent` (see e.g. https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/encodeURIComponent)

--- PR #1092 | src/subdomains/core/buy-crypto/process/services/buy-crypto-notification.service.ts ---
Context:        try { | -        if (entity.user.userData.mail) { | +        if (entity.user.userData.mail && entity.user.userData.verifiedName) {
David: Do we really just not send the mail, if the user has no verifiedName?

--- PR #1092 | src/subdomains/core/sell-crypto/process/services/buy-fiat-notification.service.ts ---
Context:        try { | -        if (entity.sell.user.userData.mail) { | +        if (entity.sell.user.userData.mail && entity.sell.user.userData.verifiedName) {
David: Same here?

--- PR #1100 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +      relations: { user: true, userData: true }, |      }); | +    if (!existing) throw new BadRequestException('Webhook already created');
David: Invert?

--- PR #1100 | src/subdomains/generic/user/services/webhook/webhook.entity.ts ---
Context: +  // References | +  @ManyToOne(() => UserData, { nullable: true }) | +  user: User;
David: `UserData` copy&paste error?
-> redo migration

--- PR #1100 | src/subdomains/generic/user/services/webhook/webhook-notification.service.ts ---
Context: +      await this.triggerUserWebhook(user, data, type, reason); | +    } | +  }
David: I would create a webhook entity for every webhook to be sent (instead of looping here), so we can keep track of every single webhook. Then you could also remove the UserData field on the webhook.

--- PR #1100 | src/subdomains/generic/kyc/services/kyc-admin.service.ts ---
Context: -      relations: { userData: true }, | +      relations: { userData: { users: { wallet: true } } }, |        order: { updated: 'DESC' },
David: I'm not sure if we should load all users with wallets everywhere. It will probably slow down everything. Maybe we can just load the users when we store the webhook?

--- PR #1100 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +      relations: { user: true }, |      }); | +    if (existing) throw new BadRequestException('Webhook already created');
David: Do we need to check, if the wallet of the user has a webhook URL configured? Doesn't make sense to store the webhook otherwise ... WDYT?

--- PR #1100 | src/subdomains/generic/user/services/webhook/webhook-notification.service.ts ---
Context: +    type: WebhookType, | +    reason?: string, | +  ): Promise<void> {
David: You can just use the entity as a parameter here 😉

--- PR #1100 | src/subdomains/generic/kyc/services/limit-request.service.ts ---
Context: +      where: { id }, | +      relations: { userData: { users: { wallet: true } } }, | +    });
David: Is this change required?

--- PR #1100 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +        where: { userData: { id: userData.id } }, | +        relations: { wallet: true }, | +      });
David: Could do `userData.users ??= ...`

--- PR #1100 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +        where: { userData: { id: userData.id } }, | +        relations: { wallet: true }, | +      });
David: Same here

--- PR #1100 | src/subdomains/generic/user/services/webhook/webhook-notification.service.ts ---
Context: +    if (DisabledProcess(Process.WEBHOOK)) return; | +    await this.sendOpenWebhooks(); | +  }
David: Haha, good method name 😆

--- PR #1100 | src/subdomains/generic/user/services/webhook/webhook-notification.service.ts ---
Context: +    try { | +      if (!webhook.user.wallet.apiUrl) return; | +      if (!webhook.user.wallet.apiKey) throw new Error(`ApiKey for wallet ${webhook.user.wallet.name} not available`);
David: We should probably also throw an error, if apiUrl is not defined 🤔

--- PR #1107 | src/subdomains/supporting/log/log.service.ts ---
Context: + | +  private async maxEntity({ system, subsystem, severity }: CreateLogDto): Promise<Log> { | +    const { maxId } = await this.logRepo
David: Return `Promise<Log | undefined>`

--- PR #1109 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
David: ![image](https://github.com/DFXswiss/api/assets/85513542/c54dfba4-2702-45ef-b0dd-18968869d179)
Should be able to remove the EUR price (same for convertToTarget)?

--- PR #1109 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:   | +    const targetFiat = targetPriceCurrency === 'USD' ? this.usd : targetPriceCurrency === 'EUR' ? this.eur : this.chf; | +    const price = await this.priceProviderService.getPrice(this.chf, targetFiat);
David: I think we can omit this conversion.

--- PR #1109 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:      // get fee | -    const specs = this.getSpecs(from, to); | +    const specs = await this.getSpecs(from, to);
David: Revert?

--- PR #1109 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:      // get fee | -    const specs = this.getSpecs(from, to); | +    const specs = await this.getSpecs(from, to);
David: Revert?

--- PR #1109 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:   |      const maxVolumePrice = |        maxVolume && (await this.priceProviderService.getPrice(from, this.chf).then((p) => p.invert()));
David: Getting the same price twice 😉

--- PR #1109 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: -    const price = await this.priceProviderService.getPrice(this.eur, to); | +    const price = await this.priceProviderService.getPrice(this.chf, to); |      const maxVolumePrice = maxVolume && (await this.priceProviderService.getPrice(this.chf, to));
David: Getting the same price twice 😉

--- PR #1109 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +        amount: Math.max(outSpec.minVolume, inSpec.minVolume), | +        asset: 'CHF', | +      },
David: Do we need to switch to CHF in buy/sell/crypto controllers too?
![image](https://github.com/DFXswiss/api/assets/85513542/46d44e2f-71e6-44f4-b8f1-8b2832d65259)

--- PR #1110 | src/shared/models/asset/asset.entity.ts ---
David: Check the new fields in PaymentInfoService

--- PR #1110 | src/shared/services/payment-info.service.ts ---
Context: -      } | +      if (!dto.currency.sellable) throw new BadRequestException('Currency not sellable'); |      }
David: This check should only be made if PaymentMethod is Bank (`sellable` = `bankSellable` 😉 )

--- PR #1110 | src/shared/services/payment-info.service.ts ---
Context: +      if (!dto.currency.instantSellable) throw new BadRequestException('Currency not sellable per Instant'); | +      if (!dto.asset.instantSellable) throw new BadRequestException('Asset not buyable per Instant'); | +    }
David: There might be no currency in the DTO here?

--- PR #1110 | src/shared/services/payment-info.service.ts ---
Context: +    } else if ('paymentMethod' in dto && dto.paymentMethod === FiatPaymentMethod.INSTANT) { | +      if (!dto.currency.instantSellable) throw new BadRequestException('Currency not sellable per Instant'); | +      if (!dto.asset.instantSellable) throw new BadRequestException('Asset not buyable per Instant');
David: `per` sounds a bit german 😁  maybe use `via`?

--- PR #1110 | src/shared/services/payment-info.service.ts ---
Context: +      if (!dto.asset.instantBuyable) throw new BadRequestException('Asset not buyable via Instant'); | +    } else if (('paymentMethod' in dto && dto.paymentMethod === FiatPaymentMethod.BANK) || !('paymentMethod' in dto)) { | +      if ('currency' in dto && !dto.currency.sellable) throw new BadRequestException('Currency not sellable via Bank');
David: Just use else, without condition?

--- PR #1118 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +    if (notifyUser) { | +      await this.userDataNotificationService.userDataAddedAddressInfo(master, slave); | +      if (master.mail !== slave.mail) await this.userDataNotificationService.userDataChangedMailInfo(master, slave);
David: Check if `slave.mail` is not null

--- PR #1118 | src/shared/i18n/de/mail.json ---
Context: +            "salutation": "Deine Account Email wurde geändert", | +            "message": "Die Email {userMail} wird ab sofort für deinen Account verwendet" | +        }
David: Missing . ? (all languages)

--- PR #1118 | src/shared/utils/util.ts ---
Context: +  static blankMail(value: string, visibleLength = 4): string { | +    const mailSplit = value.split('@'); | +    return `${mailSplit[0].slice(0, visibleLength > value.length ? value.length : visibleLength)}***@${mailSplit[1]}`;
David: I think the check `visibleLength > value.length ? value.length : visibleLength` is not required.

--- PR #1118 | src/subdomains/generic/user/models/user-data/user-data-notification.service.ts ---
Context: +        }); | +      } else { | +        this.logger.warn(`Failed to send userData changed mail info ${slave.id}: user has no email`);
David: master.id ?

--- PR #1118 | src/subdomains/generic/user/models/user-data/user-data-notification.service.ts ---
Context: +      ); | +    } | +  }
David: Send to master mail

--- PR #1122 | src/subdomains/supporting/address-pool/deposit/deposit.service.ts ---
Context:    private async createJellyfishDeposits(blockchain: Blockchain, count: number) { | -    const client = blockchain === Blockchain.DEFICHAIN ? this.inpClient : this.btcInpClient; | +    const client = this.btcInpClient;
David: `createBitcoinDeposits`

--- PR #1122 | src/subdomains/supporting/payin/strategies/register/__tests__/register.registry.spec.ts ---
Context: +    this.addStrategy(Blockchain.POLYGON, polygonStrategy); | +    this.addStrategy(Blockchain.BASE, baseStrategy); | +=======
David: Merge markers

--- PR #1122 | src/subdomains/supporting/payin/strategies/send/__tests__/send.registry.spec.ts ---
Context: +    this.addStrategy({ blockchain: Blockchain.BASE, assetType: AssetType.COIN }, baseCoin); | +    this.addStrategy({ blockchain: Blockchain.BASE, assetType: AssetType.TOKEN }, baseToken); | +=======
David: Merge markers

--- PR #1122 | src/app.controller.ts ---
Context: -      r.reduce((prev, curr) => prev.concat(curr), []), | -    ); | -  }
David: Keep (return empty array?)

--- PR #1122 | src/integration/blockchain/ain/ain.module.ts ---
Context: -  exports: [NodeService, WhaleService, DeFiChainUtil, BtcFeeService, DfiTaxService], | +  providers: [NodeService, BtcFeeService, DfiTaxService], | +  exports: [NodeService, BtcFeeService, DfiTaxService],
David: DfiTaxService can be deleted?

--- PR #1122 | src/integration/blockchain/ain/node/node.service.ts ---
Context: +        message: `${node} node not in sync (active headers: ${info.headers})`, |          nodeType: node, |        });
David: This check does not really make sense? 🤔

--- PR #1122 | src/subdomains/core/monitoring/observers/node-health.observer.ts ---
Context:   | -      await this.azureService.restartWebApp(`node-${node.type}`, node.mode === NodeMode.PASSIVE ? 'stg' : undefined); | +      await this.azureService.restartWebApp(`node-${node.type}`);
David: The restart can be removed

--- PR #1124 | src/integration/bank/services/revolut.service.ts ---
Context: +  private async getTransactions(fromDate: Date, toDate: Date = new Date()): Promise<Transaction[]> { | +    const url = `transactions?from=${Util.isoDate(fromDate)}&to=${Util.isoDate(toDate)}`; | +    return this.callApi<any[]>(url);
David: Delete `<any[]>`

--- PR #1124 | src/integration/bank/services/revolut.service.ts ---
Context: +    const url = `accounts`; | +    const balance = await this.callApi<Account[]>(url); | +    return balance;
David: `return this.callApi<Account[]>(url);`

--- PR #1124 | src/integration/bank/services/revolut.service.ts ---
Context: +    const url = `accounts/${accountId}/bank-details`; | +    const bankDetails = await this.callApi<any>(url); | +    return bankDetails;
David: `return this.callApi<TYPE>(url);`

--- PR #1124 | src/integration/bank/services/revolut.service.ts ---
Context: +    const bankDetails = await this.callApi<any>(url); | +    return bankDetails; | +  }
David: Add return type?

--- PR #1124 | src/integration/bank/services/revolut.service.ts ---
Context: +    return this.http.request<TokenAuth>({ | +      url: `${this.loginUrl}`, | +      method: 'POST',
David: `url: this.loginUrl,` 😉

--- PR #1125 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:      if (dto.countryId) { | -      userData.country = await this.countryService.getCountry(dto.countryId); |        if (!userData.country) throw new BadRequestException('Country not found');
David: Why this?

--- PR #1125 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +        } | +      }) | +      .split(' ');
David: Maybe use Regex like this?
```ts
return name
      .toLowerCase()
      .replace(/[ìíî]/g, 'i')
      .replace(/[úûù]/g, 'u')
...
```

--- PR #1125 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:   | +  checkVerifiedName(input1: string, input2: string): boolean { | +    const array1 = this.getVerifiedNameArray(input1);
David: Rename to `isSameName` or equal? 🤔

--- PR #1125 | src/shared/utils/util.ts ---
Context: +          arr2.every((element) => arr1.includes(element.split(replaceKey).join(replaceString))) | +      : arr1.every((element) => arr2.includes(element)) || arr2.every((element) => arr1.includes(element)); | +  }
David: Duplicate checks

--- PR #1125 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +    if (Util.arraysHaveSameElements(array1, array2, 'ü', 'u')) return true; | + | +    return false;
David: ```ts
const replacements = [
   [undefined, undefined],
   ['ö', 'oe'],
   ['ö', 'o'],
   ['ä', 'ae'],
   ['ä', 'a'],
   ['ü', 'ue'],
   ['ü', 'u'],
];

return replacements.some(([k, v]) => Util.arraysHaveSameElements(array1, array2, k, v));
```

--- PR #1125 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +      [undefined, undefined], | +      ['r', 'n'], | +      ['ö', 'oe'],
David: Remove `['r', 'n'],`

--- PR #1128 | src/subdomains/core/sell-crypto/process/services/buy-fiat-preparation.service.ts ---
Context: +        outputAssetEntity: IsNull(), | +      }, | +      relations: ['sell', 'sell.user', 'sell.user.wallet', 'sell.user.userData', 'cryptoInput'],
David: Do we need all these relations?

--- PR #1128 | src/subdomains/core/sell-crypto/process/services/buy-fiat-preparation.service.ts ---
Context: +        await this.buyFiatRepo.update(entity.id, { outputAssetEntity, outputReferenceAssetEntity }); | +      } catch (e) { | +        this.logger.error(`Error during buy-fiat ${entity.id} output setting:`, e);
David: Maybe a different log than above?

--- PR #1131 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
David: Rename to transaction-error.enum.ts

--- PR #1131 | src/subdomains/supporting/payment/entities/transaction-request.entity.ts ---
Context: + | +  @Column() | +  isComplete: boolean;
David: default false

--- PR #1131 | src/subdomains/supporting/payment/entities/transaction-request.entity.ts ---
David: Add migration

--- PR #1131 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: +      where: { amount, routeId, sourceId, targetId, isComplete: false }, | +      order: { created: 'DESC' }, | +    });
David: Filter by <48h

--- PR #1131 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: + | +  async findTransactionRequest( | +    amount: number,
David: `findAndCompleteRequest`?

--- PR #1131 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: +      order: { created: 'DESC' }, | +    }); | +    await this.transactionRequestRepo.update(transactionRequest.id, { isComplete: true });
David: Only if request is not null

--- PR #1131 | migration/1709042126109-transactionRequestMatch.js ---
Context: +        await queryRunner.query(`CREATE UNIQUE INDEX "REL_40eba1970b47febeab6d8118cd" ON "dbo"."buy_crypto" ("transactionRequestId") WHERE "transactionRequestId" IS NOT NULL`); | +        await queryRunner.query(`ALTER TABLE "dbo"."buy_crypto" ADD CONSTRAINT "FK_40eba1970b47febeab6d8118cd3" FOREIGN KEY ("transactionRequestId") REFERENCES ."transaction_request"("id") ON DELETE NO ACTION ON UPDATE NO ACTION`); | +    }
David: BuyFiat change missing -> recreate migration

--- PR #1131 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: +        targetId, | +        isComplete: false, | +        created: LessThan(Util.daysBefore(2, new Date())),
David: `MoreThan`?
`new Date()` can be removed

--- PR #1131 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: +    }); | +    if (!transactionRequest) await this.transactionRequestRepo.update(transactionRequest.id, { isComplete: true }); | +    return transactionRequest;
David: Haha, invert the if? 😉

--- PR #1131 | migration/1709047257141-transactionRequestMatch.js ---
David: BuyFiat missing

--- PR #1131 | src/subdomains/core/sell-crypto/process/buy-fiat.entity.ts ---
David: Add externalTransactionId

--- PR #1131 | migration/1709049727775-transactionRequestMatch.js ---
David: DEV DB

--- PR #1132 | src/subdomains/core/sell-crypto/process/services/buy-fiat.service.ts ---
Context: -    return Object.assign(buyFiat, { inputAssetEntity, outputAssetEntity }); | +    return Object.assign(buyFiat, { inputAssetEntity, outputAssetEntity: buyFiat.outputAssetEntity }); |    }
David: Haha, this code is a bit useless for outputAsset 🤣  just remove it from BuyFiatExtended.

--- PR #1133 | src/subdomains/supporting/payout/services/payout.service.ts ---
Context: +    const payoutStrategy = this.payoutStrategyRegistry.getPayoutStrategy(feeAsset); | +    const { asset, amount } = await payoutStrategy.estimateBlockchainFee(feeAsset); | +    return { asset, amount };
David: ```ts
return payoutStrategy.estimateBlockchainFee(feeAsset);
```

--- PR #1133 | src/subdomains/supporting/payment/entities/blockchain-fee.ts ---
Context: +  @OneToOne(() => Asset, { nullable: false }) | +  @JoinColumn() | +  asset: Asset;
David: `eager: true` -> remove relation in DB query?

--- PR #1133 | src/subdomains/supporting/payment/entities/blockchain-fee.ts ---
David: Rename to `blockchain-fee.entity.ts`

--- PR #1133 | src/subdomains/supporting/payout/services/payout.service.ts ---
Context: +    const payoutStrategy = this.payoutStrategyRegistry.getPayoutStrategy(feeAsset); | +    return payoutStrategy.estimateBlockchainFee(feeAsset); | +  }
David: Parameter name `feeAsset` is a bit misleading 🤔

--- PR #1136 | migration/1709752564082-blockchainFeeNullable.js ---
David: Can be reverted

--- PR #1136 | src/subdomains/supporting/payment/entities/blockchain-fee.entity.ts ---
Context:   | -  @Column({ type: 'float' }) | +  @Column({ type: 'float', nullable: true })
David: Can be reverted

--- PR #1136 | src/subdomains/supporting/payment/services/fee.service.ts ---
David: Linter warnings

--- PR #1136 | src/config/config.ts ---
Context:    amlCheckMonthlyTradingLimit = 50000; // CHF |    amlCheckLastNameCheckValidity = 90; // days | +  maxBlockchainFee = 50;
David: Add a currency hint `// CHF`

--- PR #1136 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context:    txVolume?: number; | -  blockchainFee: number; |    discountCodes: string[];
David: Adapt calls of `getUserFee` and `getDefaultFee` in TransactionHelper

--- PR #1136 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context:          payoutRefBonus: customFee.payoutRefBonus, | -        blockchain: customFee.blockchainFactor * blockchainFee, | +        blockchain: Math.min(customFee.blockchainFactor * blockchainFee, Config.maxBlockchainFee),
David: Same change is needed 2x below (line 255 and line 268)

--- PR #1136 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context: + | +      if (!fee) return this.getBlockchainMaxFee(active.blockchain); | +      return fee.amount;
David: `return fee ? fee.amount : this.getBlockchainMaxFee(active.blockchain); `

--- PR #1136 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context:          payoutRefBonus: true, | -        blockchain: blockchainFee * baseFee.blockchainFactor, | +        blockchain: Math.min(customFee.blockchainFactor * blockchainFee, Config.maxBlockchainFee),
David: `baseFee.blockchainFactor`

--- PR #1136 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context: +      .where('asset.blockchain = :blockchain', { blockchain }) | +      .getRawOne<{ fee: number }>(); | +    return fee;
David: Filter for update more than 70 minutes before and return 0 as fallback `return fee ?? 0;`

--- PR #1136 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context:    @Lock(1800) |    async updateBlockchainFees() { |      const blockchainFees = await this.blockchainFeeRepo.find({ relations: ['asset'] });
David: Manually set updated field

--- PR #1136 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context:          blockchainFee.amount = price.convert(amount); | -        await this.blockchainFeeRepo.save(blockchainFee); | +        await this.blockchainFeeRepo.save({ updated: new Date(), ...blockchainFee });
David: Just `blockchainFee.updated = new Date();`?

--- PR #1147 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: -      (!OlkypayIban.includes(this.bankTx.iban) || this.userData.olkypayAllowed) | +      ((!RevolutIban.includes(this.bankTx.iban) && !OlkypayIban.includes(this.bankTx.iban)) || | +        this.userData.olkypayAllowed) &&
David: Wrong bracket placement?

--- PR #1147 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:    ): Promise<number> { | -    if (!user) return inputAmount; | +    if (users.length === 0) return inputAmount;
David: `users` is nullable?

--- PR #1147 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +      Util.daysBefore(1), | +      user.userData.users ?? [user], | +    );
David: User is nullable?

--- PR #1147 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +      Util.daysBefore(1), | +      user.userData.users ?? [user], | +    );
David: Does this fallback make sense?

--- PR #1147 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: -      (!OlkypayIban.includes(this.bankTx.iban) || this.userData.olkypayAllowed) | +      ((!RevolutIban.includes(this.bankTx.iban) && !OlkypayIban.includes(this.bankTx.iban)) || | +        this.userData.olkypayAllowed) &&
David: Ah, shouldn't we check the accountIban?

--- PR #1147 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +    bankDataUserDataId: number, | +    last24hVolume: number, | +    monthlyAmountInChf: number,
David: `last30dVolume`?

--- PR #1147 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:      minVolume: number, | +    last24hVolume: number, |      monthlyAmountInChf: number,
David: Same here?

--- PR #1147 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +    const txAmountChf = await this.getVolumeChfSince(target.sourceAmount, from, allowExpiredPrice, Util.daysBefore(1), [ | +      user, | +    ]);
David: This will result in `[undefined]` (user) and cause exceptions.

--- PR #1147 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +  ): Promise<number> { | +    if (!users || users?.length === 0) return inputAmount; | +
David: `if (!users?.length) ...`

--- PR #1147 | src/subdomains/supporting/bank-tx/bank-tx/bank-tx.entity.ts ---
Context:  export const BicBlacklist = ['FPELFR21XXX']; |  export const OlkypayIban = ['LU116060002000005040', 'GB77REVO00996972945099']; | +export const RevolutIban = ['GB77REVO00996972945099'];
David: Duplicate IBAN? 🤔  Maybe get the IBANs from DB?

--- PR #1149 | src/subdomains/supporting/bank/special-external-iban/special-external-iban.service.ts ---
Context: +  async getAllSpecialExternalIban(type: SpecialExternalIbanType): Promise<SpecialExternalIban[]> { | +    return this.specialExternalIbanRepo.findBy({ type }); | +  }
David: Maybe just have a method `getMultiAccountIbans`?

--- PR #1149 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context:   | -    const multiAccountIban = await this.multiAccountIbanService.getAllMultiAccountIban(); | +    const multiAccountIban = await this.specialExternalIbanService.getMultiAccountIban();
David: `const multiAccountIbans = await this.specialExternalIbanService.getMultiAccountIbans();`

--- PR #1149 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:      if (dto.verifiedName) { | -      const multiAccountIban = await this.multiAccountIbanService.getAllMultiAccountIban(); | +      const multiAccountIban = await this.specialExternalIbanService.getMultiAccountIban();
David: Same here

--- PR #1149 | src/subdomains/supporting/bank/special-external-iban/special-external-iban.service.ts ---
Context: +      { type: SpecialExternalIbanType.BANNED_IBAN }, | +      { type: SpecialExternalIbanType.BANNED_BIC }, | +    ]);
David: Could use `{ type: In([SpecialExternalIbanType.BANNED_IBAN, SpecialExternalIbanType.BANNED_BIC]) },`

--- PR #1149 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:        monthlyAmountInChf <= Config.amlCheckMonthlyTradingLimit && | -      !BicBlacklist.includes(this.bankTx.bic) && | +      !blacklist.some((b) => b.bic === this.bankTx.bic || b.iban === this.bankTx.iban) &&
David: BicBlacklist variable can be removed?

--- PR #1154 | src/subdomains/core/transaction/transaction.service.ts ---
David: Would it make sense to move this to `/subdomains/supporting/payment`?

--- PR #1154 | src/subdomains/supporting/payment/dto/input/create-transaction.dto.ts ---
Context: +  @IsEnum(TransactionType) | +  type?: TransactionType; | +}
David: Do we need the validators here?

--- PR #1154 | src/subdomains/supporting/payment/dto/input/update-transaction.dto.ts ---
Context: +  @IsEnum(TransactionType) | +  type: TransactionType; | +}
David: And here?

--- PR #1154 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
David: Maybe we should keep the external name clean and change the internal one (e.g. `TransactionTypeInternal`)? 🤔

--- PR #1154 | src/subdomains/supporting/payment/payment.module.ts ---
Context:      forwardRef(() => BuyCryptoModule), | +    TransactionJobModule, |    ],
David: Is the TransactionJobModule part of the PaymentModule, but the TransactionModule isn't?

--- PR #1154 | src/subdomains/core/core.module.ts ---
Context:      LiquidityManagementModule, | +    TransactionModule, |    ],
David: Why this?

--- PR #1154 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
Context: + | +  @OneToOne(() => CheckoutTx, (checkoutTx) => checkoutTx.transaction, { eager: true, nullable: true }) | +  checkoutTx: CheckoutTx;
David: I'm not sure if we should load them all eagerly. Even though most of them are undefined, SQL will try to join the tables and reading will be much slower.

--- PR #1154 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
David: Add migration

--- PR #1154 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: + | +  constructor(private repo: TransactionRepository) {} | +
David: readonly

--- PR #1154 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: +  async update(sourceId: number, sourceType: TransactionSourceType, dto: UpdateTransactionDto): Promise<Transaction> { | +    const entity = await this.repo.findOneBy({ sourceId, sourceType }); | +    if (!entity) throw new BadRequestException('Transaction not found');
David: Use the get method from below?

--- PR #1154 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: +    } catch (e) { | +      this.logger.error('Exception during create transaction:', e); | +    }
David: Should we really catch the transaction? 🤔

--- PR #1154 | src/subdomains/supporting/payment/services/transaction-job.service.ts ---
Context: +        type: this.getType(unassignedTx), | +      }); | +    }
David: Hm, we also need to update the source entity and BuyFiat/BuyCrypto?

--- PR #1154 | src/subdomains/supporting/payment/services/transaction-job.service.ts ---
Context: +      ], | +      'created', | +    );
David: RefRewards missing

--- PR #1154 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
Context: +  BUY_CRYPTO_RETURN = 'BuyCryptoReturn', | +  STAKING = 'Staking', | +}
David: Should we add a CryptoCrypto type?

--- PR #1154 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
Context: +export class Transaction extends IEntity { | +  @Column({ type: 'integer' }) | +  sourceId: number;
David: Can be removed

--- PR #1154 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +          type: TransactionType.BUY_CRYPTO, | +        }) | +      : null;
David: Update with transaction ID?
`.update(bankTx.transaction.id, ...)`?

--- PR #1154 | src/subdomains/supporting/fiat-payin/services/fiat-payin-sync.service.ts ---
Context: +            sourceId: checkoutTx.id, | +            sourceType: TransactionSourceType.CHECKOUT_TX, | +          });
David: Save transaction to checkout TX?

--- PR #1154 | src/subdomains/supporting/payin/strategies/register/impl/base/register.strategy.ts ---
Context: +          sourceId: cryptoInput.id, | +          sourceType: TransactionSourceType.CRYPTO_INPUT, | +        });
David: Save transaction to crypto input?

--- PR #1154 | src/subdomains/supporting/bank-tx/bank-tx/bank-tx.service.ts ---
Context: +        const bankTx = await this.create(transaction); | +        if (!DisabledProcess(Process.CREATE_TRANSACTION)) | +          await this.transactionService.create({ sourceId: bankTx.id, sourceType: TransactionSourceType.BANK_TX });
David: Save transaction to bank TX?

--- PR #1154 | src/subdomains/supporting/payment/services/transaction.service.ts ---
David: Linter warning

--- PR #1154 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: +  async update(id: number, dto: UpdateTransactionDto): Promise<Transaction> { | +    const entity = await this.repo.findOneBy({ id }); | +    if (!entity) throw new BadRequestException('Transaction not found');
David: I would throw a normal Error, not a BadRequestException

--- PR #1154 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
Context: + | +  @OneToOne(() => CheckoutTx, (checkoutTx) => checkoutTx.transaction, { eager: false, nullable: true }) | +  checkoutTx: CheckoutTx;
David: `eager: false` is the default

--- PR #1154 | src/subdomains/core/referral/reward/ref-reward.service.ts ---
David: We need to create a transaction when creating a ref reward?

--- PR #1154 | src/subdomains/supporting/payment/services/transaction-job.service.ts ---
Context: +  @Cron(CronExpression.EVERY_30_MINUTES) | +  @Lock(7200) | +  async addFiatOutputs(): Promise<void> {
David: Copy&paste? 😉

--- PR #1154 | src/subdomains/supporting/payment/services/transaction-job.service.ts ---
Context: +        sourceType: this.getSourceType(unassignedTx), | +        ...this.getTypeAndEntity(unassignedTx), | +      });
David: Are you sure this updates the entities?

--- PR #1154 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +      ? await this.transactionService.update(cryptoInput.transaction.id, { | +          type: TransactionTypeInternal.BUY_CRYPTO, | +        })
David: `TransactionTypeInternal.CRYPTO_CRYPTO`?

--- PR #1154 | src/subdomains/supporting/payin/entities/crypto-input.entity.ts ---
Context: + | +  @OneToOne(() => BuyCrypto, (buyCrypto) => buyCrypto.cryptoInput, { eager: false, nullable: true }) | +  buyCrypto: BuyCrypto;
David: `eager: false` is default

--- PR #1154 | src/subdomains/supporting/payin/strategies/register/impl/base/register.strategy.ts ---
Context: +        payIn.transaction = await this.transactionService.create({ | +          sourceType: TransactionSourceType.CRYPTO_INPUT, | +        });
David: Add the transaction before saving? 🤔

--- PR #1157 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +        if (dto.identificationprocess.result === IdentResult.CANCELED) | +          await this.kycNotificationService.identFailed(kycStep, reason); |          break;
David: Split the two cases

--- PR #1158 | src/config/config.ts ---
Context: +      zchfUsdtPool: process.env.UNISWAP_POLYGON_ZCHF_USDT_POOL, | +    }, | +  };
David: The pool addresses can be calculated using the following code:
```ts
  import { Token } from '@uniswap/sdk-core';
  import { FeeAmount, Pool } from '@uniswap/v3-sdk';

  async getPoolAddress(asset1: Asset, asset2: Asset): Promise<string> {
    const token1 = await this.getToken(asset1);
    const token2 = await this.getToken(asset2);

    if (token1 instanceof Token && token2 instanceof Token) {
      return Pool.getAddress(token1, token2, FeeAmount.LOWEST);
    } else {
      throw new Error(`Only tokens can be in a pool`);
    }
  }
```

--- PR #1158 | src/subdomains/core/trading/controllers/trading-order.controller.ts ---
David: I think we don't need any controllers

--- PR #1158 | src/subdomains/core/trading/controllers/trading-rule.controller.ts ---
David: Same here

--- PR #1158 | src/subdomains/core/trading/dto/output/mappers/trading-order-output-dto.mapper.ts ---
David: Same for mappers and DTOs

--- PR #1158 | src/subdomains/core/trading/services/trading-uniswap.service.ts ---
Context: +        priceImpact: 0, | +      }, | +    };
David: Do we need all this information? 🤔

--- PR #1158 | src/subdomains/core/trading/services/trading-uniswap.service.ts ---
Context: +    if (price1.isValid && price2.isValid) { | +      const ratio = price1.price / price2.price; | +      const priceImpact = (ratio - 1) * 100;
David: Always use factors, not percentages (0.012 instead of 1.2%)

--- PR #1158 | src/subdomains/core/trading/services/trading-order.service.ts ---
Context: + | +        this.logger.verbose(`Trading order ${order.id} complete`); | +      }
David: We need to complete the liquidity reservation order, maybe also in case of failure?

--- PR #1158 | src/subdomains/core/trading/services/trading-rule.service.ts ---
Context: +        this.logger.info(message); | +        return; | +      }
David: Can be an error log

--- PR #1158 | src/integration/blockchain/shared/evm/evm-client.ts ---
Context: +      balance1: EvmUtil.fromWeiAmount(token1PoolBalance, token1.decimals), | +    }; | +  }
David: Remove this

--- PR #1158 | src/subdomains/core/trading/entities/trading-order.entity.ts ---
Context: + | +  @Column({ type: 'float', nullable: false }) | +  amountIn: number;
David: Add amountOut?

--- PR #1162 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +      this.bankTx | +      ? this.isBankTxAmlPass(blacklist, instantBanks) | +      : this.isCheckoutTxAmlPass(blacklist);
David: Bracket around this?

--- PR #1163 | src/subdomains/supporting/notification/entities/mail.entity.ts ---
David: Can we combine it with the existing notification entity?

--- PR #1163 | src/subdomains/supporting/notification/entities/notification.entity.ts ---
Context:   |    @Column({ length: 'MAX', nullable: false }) |    correlationId: string;
David: Optional

--- PR #1163 | src/subdomains/supporting/notification/entities/notification.entity.ts ---
Context: -  sendDate: Date; | +  @Column({ length: 'MAX', default: '-' }) | +  data: string;
David: Why not default null?

--- PR #1163 | src/subdomains/supporting/notification/entities/notification.entity.ts ---
Context: + | +  @Column({ default: true }) | +  isComplete: boolean;
David: Remove this default later?

--- PR #1163 | src/subdomains/supporting/notification/dto/create-notification.dto.ts ---
Context: +  lastTryDate: Date; | +  isComplete: boolean; | +}
David: Is this used?

--- PR #1163 | src/subdomains/supporting/notification/entities/notification.entity.ts ---
Context: +      if (this.suppressRecurring) return true; | +      if (this.isDebounced(existingNotification)) return true; |      }
David: Combine into one return?

--- PR #1163 | src/subdomains/supporting/notification/notification.module.ts ---
Context:    controllers: [NotificationController], | -  exports: [NotificationService], | +  exports: [NotificationService, MailService],
David: Why this export?

--- PR #1163 | src/subdomains/supporting/notification/services/notification-job.service.ts ---
Context: +        await this.notificationService.updateNotification(notification, { isComplete: true }); | +      } catch (e) { | +        this.logger.error('Error in sendMail', e);
David: Maybe we should have a different message here than for the initial send?
`'Error during mail send retry:', e`?

--- PR #1163 | src/subdomains/generic/user/models/account-merge/account-merge.service.ts ---
Context:        }, | +      options: { debounce: 60000 }, | +      metadata: { context: MailContext.ACCOUNT_MERGE_REQUEST, correlationId: `AccountMerge&${master.id}&${slave.id}` },
David: correlationId: `${request.id}`

--- PR #1163 | src/subdomains/generic/user/models/account-merge/account-merge.service.ts ---
Context:        }, | +      options: { debounce: 60000 }, | +      metadata: { context: MailContext.ACCOUNT_MERGE_REQUEST, correlationId: `AccountMerge&${master.id}&${slave.id}` },
David: Replace metadata with correlationId?

--- PR #1165 | src/subdomains/supporting/payin/entities/crypto-input.entity.ts ---
Context: -      this.status = PayInStatus.WAITING_FOR_PRICE_REFERENCE; | -      return this; | -    }
David: Status `WAITING_FOR_PRICE_REFERENCE` can be removed

--- PR #1165 | src/subdomains/supporting/payin/strategies/register/impl/base/evm.strategy.ts ---
David: Methods `getReferenceAssets` and `getSourceAssetRepresentation` can be removed in this and every implementing strategy.

--- PR #1166 | src/subdomains/core/buy-crypto/routes/buy/buy.service.ts ---
Context: +    this.cache.push({ id: entity.id, bankUsage: entity.bankUsage }); | + | +    return entity;
David: What if this is called before the cache is initialized?

--- PR #1172 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +      this.cryptoInput.amlCheck === CheckStatus.PASS && | +      this.cryptoInput.isConfirmed && | +      this.userData.cryptoCryptoAllowed
David: Could it be that it takes more than 10 minutes to get confirmed and AML check will switch to GSheet?

--- PR #1172 | src/subdomains/core/buy-crypto/process/services/buy-crypto-preparation.service.ts ---
Context:        try { | +        if (!entity.cryptoInput?.isConfirmed || !entity.cryptoInput?.amlCheck) continue; | +
David: This will block all buys?

--- PR #1177 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:   | +  @Column({ type: 'float', nullable: true }) | +  blockchainFee: number;
David: Hm, should we use `nullable: false, default: 0`?

--- PR #1177 | src/subdomains/core/buy-crypto/routes/buy/dto/buy-payment-info.dto.ts ---
Context:   | +  @ApiProperty({ description: 'Fee dto in source asset' }) | +  feeSource: FeeDto;
David: `Fee infos in source currency`? Add type (everywhere)

--- PR #1177 | src/subdomains/core/sell-crypto/process/buy-fiat.entity.ts ---
Context:   | +  @Column({ type: 'float', nullable: true }) | +  blockchainFee: number;
David: Hm, should we use `nullable: false, default: 0`?

--- PR #1177 | src/subdomains/supporting/payment/dto/tx-spec.dto.ts ---
Context: +  blockchainFee?: number; | +  totalFee?: number; |  }
David: Maybe we should create a new interface like this?
```ts
export interface TxSpecExtended {
  volume: {
    min?: number;
    max?: number;
  },
  fee: {
    fixed?: number;
    blockchain?: number;
    total?: number;
  }
}
```

--- PR #1177 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:        maxVolume: [paymentMethodIn, paymentMethodOut].includes(FiatPaymentMethod.CARD) |          ? Math.min(user?.userData.availableTradingLimit ?? Infinity, Config.tradingLimits.cardDefault) |          : user?.userData.availableTradingLimit ?? Config.tradingLimits.yearlyDefault,
David: totalFee missing?

--- PR #1177 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +        min: txSpecSource.minFee, | +        total: txSpecSource.totalFee, | +      },
David: This will assign all props of `fee` to the result and return it (for an API call). There is the payoutRefBonus in the fee?

--- PR #1177 | src/subdomains/core/buy-crypto/routes/buy/dto/buy-payment-info.dto.ts ---
Context:   | +  @ApiProperty({ type: FeeDto, description: 'Fee infos in source currency' }) | +  feeSource: FeeDto;
David: Maybe call it `fees` and `feesTarget` for all DTOs? Then we could use the same field names for quote and payment info.

--- PR #1177 | src/subdomains/supporting/payin/strategies/send/impl/base/send.strategy.ts ---
Context: -    return this.transactionHelper.getInSpecs(asset, true).then((r) => r.minFee); | +    return this.transactionHelper.getInSpecs(asset, true).then((r) => r.fee.min); |    }
David: We need to update this, it should check for the blockchain fee.

--- PR #1177 | src/subdomains/supporting/payment/dto/fee.dto.ts ---
Context: +export class FeeDto extends BaseFeeDto { | +  @ApiProperty({ description: 'Min fee amount' }) | +  min: number;
David: `Minimum fee amount`

--- PR #1177 | src/subdomains/supporting/payment/dto/fee.dto.ts ---
Context: + | +  @ApiProperty({ description: 'Dfx fee amount' }) | +  dfx: number;
David: `DFX fee amount`

--- PR #1177 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +      allowExpiredPrice, | +    ); |    }
David: These methods are a bit overcomplicated 🤔 the only thing they do is return the min Volume in source/target. Maybe we should rename them to `getMinVolumeIn` and `getMinVolumeOut` and just return a number?

--- PR #1177 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: -    const txSpecReferenceSource = await this.convertToSource( | +    const { fee: txSpecReferenceSourceFee, volume: txSpecReferenceSourceVolume } = await this.convertToSource( | +      inputReferenceAmount,
David: Maybe we should just name them `feeSource` and `volumeSource`? It's hardly readable like this 🤣

--- PR #1177 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:          total: this.round(feeAmount, isFiat(from)), | +        dfx: undefined, |        },
David: I would calculate the DFX fee, maybe we'll use it later?

--- PR #1177 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +      percentFeeAmount + txSpecReferenceSourceFee.fixed + txSpecReferenceSourceFee.blockchain, | +      txSpecReferenceSourceFee.min, | +    );
David: Min. fee is part of the DFX fee:
```ts
const dfxFee = Math.max(percentFeeAmount + txSpecReferenceSourceFee.fixed, txSpecReferenceSourceFee.min);
const totalFee = dfxFee + txSpecReferenceSourceFee.blockchain;
```

--- PR #1177 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +        dfx: this.round(sourceAmount * fee.rate + fixedFee, isFiat(from)), | +        total: totalFee, | +      },
David: Same here

--- PR #1177 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +        dfx: this.round(targetAmount * fee.rate + fixedFee, isFiat(to)), | +        total: totalFee, | +      },
David: Same here

--- PR #1177 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context: +  async getBlockchainFee(asset: Active, allowBlockchainFeeFallback: boolean): Promise<number> { | +    return this.getBlockchainFeeInternal(asset, allowBlockchainFeeFallback); | +  }
David: Why this wrapper method?

--- PR #1187 | src/subdomains/core/sell-crypto/process/buy-fiat.entity.ts ---
Context: +      address: this.sell.deposit?.address, | +      asset: this.sell.fiat, | +      trimmedReturnAddress: this.sell?.iban ? Util.blankStart(this.sell.iban) : null,
David: Is this correct? Shouldn't the address be the IBAN? And trimmedReturnAddress the user address?

--- PR #1190 | infrastructure/bicep/dfx-api.bicep ---
David: Was this a Bicep autoformatter?

--- PR #1190 | src/subdomains/supporting/pricing/services/integration/pricing-ebel2x.service.ts ---
Context: + | +    return Price.create(from, to, Util.round(1 / mkxPrice, 8)); | +  }
David: How can it be that we get a single price for all assets?

--- PR #1194 | src/subdomains/core/aml/aml.service.ts ---
Context: +      blacklist.some((b) => b.type === SpecialExternalAccountType.BANNED_MAIL && entity.userData.mail.includes(b.value)) | +    ) | +      errors.push(AmlError.SUSPICIOUS_MAIL);
David: Maybe we should use Regex here? Then we can remove the `hasSuspiciousMail` check in the user-data.

--- PR #1194 | src/subdomains/core/aml/aml.service.ts ---
Context: +      blacklist.some((b) => b.type === SpecialExternalAccountType.BANNED_MAIL && entity.userData.mail.includes(b.value)) | +    ) | +      errors.push(AmlError.SUSPICIOUS_MAIL);
David: Mail might be undefined?

--- PR #1194 | src/subdomains/core/aml/aml.service.ts ---
Context: +          blacklist.some( | +            (b) => b.type === SpecialExternalAccountType.BANNED_BIC && new RegExp(b.value).test(entity.bankTx.bic), | +          )
David: Maybe we should create a helper method in the SpecialExternalAccount entity?
```ts
matches(type: SpecialExternalAccountType, value: string): boolean {
  return this.type === type && new RegExp(this.value).test(value);
}
```

--- PR #1198 | src/integration/blockchain/shared/services/crypto.service.ts ---
Context:        if (blockchain === Blockchain.MONERO) return await this.verifyMonero(message, address, signature); |        if (blockchain === Blockchain.LIQUID) return this.verifyLiquid(message, address, signature); | +      if (blockchain === Blockchain.ARWEAVE) return this.verifyArweave(message, signature, key);
David: Linter warning

--- PR #1200 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context: +  ANNUAL_LIMIT_EXCEEDED = 'AnnualLimitExceeded', | +  BANK_ACCOUNT_NOT_MATCHING = 'BankAccountNotMatching', | +  IBAN_CHECK = 'IbanCheck',
David: BankAccountMismatch

--- PR #1200 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context: +  BANK_ACCOUNT_NOT_MATCHING = 'BankAccountNotMatching', | +  IBAN_CHECK = 'IbanCheck', | +  KYC_REJECTED = 'KycRejected',
David: What is IbanCheck?

--- PR #1200 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context: +  STAKING_DISCONTINUED = 'StakingDiscontinued', | +  BANK_NOT_ALLOWED = 'BankNotAllowed', | +  COUNTRY_NOT_ALLOWED = 'CountryNotAllowed',
David: Can we summarize these two reasons?

--- PR #1200 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context:   | +  @ApiProperty({ enum: TransactionError }) | +  error: TransactionError;
David: Optional?

--- PR #1200 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context: +  [AmlReason.HIGH_RISK_BLOCKED]: TransactionError.BANK_NOT_ALLOWED, | +  [AmlReason.COUNTRY_NOT_ALLOWED]: TransactionError.COUNTRY_NOT_ALLOWED, | +  [AmlReason.MANUAL_CHECK]: TransactionError.MANUAL_CHECK,
David: We probably should map AmlReason.NA and AmlReason.MANUAL_CHECK to null? These are no errors.

--- PR #1200 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context: +  INSTANT_PAYMENT = 'InstantPayment', | +  MANUAL_CHECK = 'ManualCheck', | +}
David: MANUAL_CHECK can be removed

--- PR #1201 | src/main.ts ---
Context:   |    const app = await NestFactory.create(AppModule); | +  useContainer(app.select(AppModule), { fallbackOnErrors: true });
David: What is this? 😉

--- PR #1201 | src/subdomains/supporting/bank/bank-account/is-dfx-iban.validator.ts ---
Context: -  validate(_: string, args: ValidationArguments) { | +  private blockedIban: string[] = []; | +
David: blockedIbans

--- PR #1208 | src/subdomains/supporting/payment/services/transaction.service.ts ---
David: Linter warning

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.entity.ts ---
Context: +  @ManyToOne(() => Transaction, (transaction) => transaction.supportIssues, { nullable: false, eager: true }) | +  transaction: Transaction; | +}
David: We will have other issues in the future
- Add a type column (with `TransactionIssue` for now)
- `transaction` relation is one to one
- `transaction` is nullable

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +@ApiTags('SupportIssue') | +@Controller('supportIssue') | +export class SupportIssueController {
David: Use `@Controller('support/issue')`

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +  async updateSupportIssue(@Param('id') id: string, @Body() dto: UpdateSupportIssueDto): Promise<SupportIssue> { | +    return this.supportIssueService.updateSupportIssue(+id, dto); | +  }
David: Use `@Post('transaction')` and `@Put('transaction/:id')`

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.service.ts ---
Context: + | +  async createSupportIssue(dto: CreateSupportIssueDto): Promise<SupportIssue> { | +    const existing = await this.supportIssueRepo.findOneBy({
David: Rename to `createTransactionIssue`

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.service.ts ---
Context: +    }); | +    if (existing) throw new BadRequestException('SupportIssue already exists'); | +
David: `ConflictException('There is already a support issue for this transaction');` or similar

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.service.ts ---
Context: + | +      entity.fileUrl = await this.storageService.uploadFile( | +        entity.transaction.user.id,
David: Don't load the user and get it from the JWT (maybe check with the transaction user?)

--- PR #1208 | src/subdomains/supporting/support-issue/dto/create-support-issue.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsString() | +  fileName?: string;
David: `{ description: 'Name of the file' }`

--- PR #1208 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
David: Maybe it's time to link the UserData in the TransactionEntity?

--- PR #1208 | src/subdomains/supporting/support-issue/dto/create-support-issue.dto.ts ---
Context: +  @ValidateNested() | +  @Type(() => EntityDto) | +  transaction: Transaction;
David: I think I would have `transactionId` as a query parameter instead of in the DTO

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +  @ApiBearerAuth() | +  @UseGuards(AuthGuard(), new RoleGuard(UserRole.ADMIN)) | +  @ApiExcludeEndpoint()
David: `UserRole.SUPPORT`?

--- PR #1208 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: + | +  async getTransactionWithoutUser(): Promise<Transaction[]> { | +    return this.repo.find({
David: `getTransactionsWithoutUser`

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.entity.ts ---
Context: + | +  @Column({ length: 'MAX' }) | +  description: string;
David: Should be `nullable: true` if it's optional in the DTO?

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +  async updateSupportIssue(@Param('id') id: string, @Body() dto: UpdateSupportIssueDto): Promise<SupportIssue> { | +    return this.supportIssueService.updateSupportIssue(+id, dto); | +  }
David: Sorry, my bad. Update method is not specifically for transaction issues. So it's better to have it under `@Put(':id')`.

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +    @Query('transactionId') transactionId: string, | +    @Body() dto: CreateSupportIssueDto, | +  ): Promise<SupportIssue> {
David: `createTransactionIssue`?

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +    @GetJwt() jwt: JwtPayload, | +    @Query('transactionId') transactionId: string, | +    @Body() dto: CreateSupportIssueDto,
David: Would it be enough to have `@Query('id') transactionId: string,`, WDYT? URL would then look like `support/issue/transaction?id=1234`

--- PR #1208 | src/subdomains/supporting/support-issue/support-issue.service.ts ---
Context: +    if (!entity.transaction) throw new NotFoundException('Transaction not found'); | +    if (!entity.transaction.user || entity.transaction.user.id !== userId) | +      throw new ConflictException('You can only create support issue for your own transaction');
David: `ForbiddenException`?

--- PR #1209 | src/shared/utils/async-cache.ts ---
Context: +  EVERY_1_MINUTE = 60, | +  EVERY_5_MINUTE = 5 * 60, | +  EVERY_6_HOURS = 3600 * 6,
David: `EVERY_5_MINUTES`

--- PR #1209 | src/shared/utils/async-cache.ts ---
Context: +export enum CacheItemResetPeriod { | +  EVERY_SECOND = 0, | +  EVERY_10_SECONDS = 10,
David: `EVERY_SECOND` -> `ALWAYS`?

--- PR #1209 | src/shared/models/asset/asset.service.ts ---
David: Cache the "getAll" methods as well?

--- PR #1209 | src/shared/utils/async-cache.ts ---
Context: +  } | + | +  private async updateInternal(id: string, update: () => Promise<T>, fallbackToCache: boolean) {
David: Maybe we should have a method `invalidate(id: string)`?

--- PR #1209 | src/subdomains/supporting/bank/bank/bank.service.ts ---
Context:    async getBankInternal(name: BankName, currency: string): Promise<Bank> { | -    return this.bankRepo.findOneBy({ name, currency }); | +    return this.cache.get(`${name}-${currency}`, () => this.bankRepo.findBy({ name, currency }))?.[0];
David: Why not `findOneBuy`?

--- PR #1209 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context: +    this.fees = await this.cache.get('all', () => this.feeRepo.find()); | +    return this.fees; | +  }
David: Doesn't really make sense to have a second cache?

--- PR #1209 | src/shared/models/asset/asset.service.ts ---
Context: +          this.assetRepo.findBy({ blockchain: In(blockchains) }), | +        ) | +      : this.arrayCache.get(`all-${includePrivate}`, () => this.assetRepo.findBy(search));
David: `return this.arrayCache.get(JSON.stringify(search), () => this.assetRepo.findBy(search))`?

--- PR #1209 | src/shared/models/asset/asset.service.ts ---
Context: +          .where('asset.sellable = 1') | +          .distinct() | +          .getMany(),
David: You can use a normal find here

--- PR #1209 | src/shared/utils/async-cache.ts ---
Context: + | +    this.cache.get(id).updated = new Date(0); | +  }
David: `this.cache.delete(id)`?

--- PR #1209 | src/subdomains/supporting/payment/services/special-external-account.service.ts ---
Context: +          ]), | +      }), | +    );
David: type parameter to cache key?

--- PR #1209 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context:    private readonly logger = new DfxLogger(FeeService); | +  private readonly cache = new AsyncCache<BlockchainFee>(CacheItemResetPeriod.EVERY_5_MINUTES); | +  private readonly arrayCache = new AsyncCache<Fee[]>(CacheItemResetPeriod.EVERY_5_MINUTES);
David: `blockchainFeeCache` and `feeCache`?

--- PR #1209 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context: +        .andWhere({ updated: MoreThan(Util.minutesBefore(FeeValidityMinutes)) }) | +        .orderBy('fee.amount', 'DESC') | +        .getOne(),
David: You can use a normal find?

--- PR #1209 | src/shared/models/asset/asset.service.ts ---
Context: +          .where('asset.sellable = 1') | +          .distinct() | +          .getMany(),
David: Yes, but you are getting all assets in this query, and assets are distinct by design 😉

--- PR #1209 | src/shared/models/asset/asset.service.ts ---
Context: +      ) | +      .then((assets) => assets.map((a) => a.blockchain)); |    }
David: ```ts
.then((assets) => Array.from(new Set(assets.map((a) => a.blockchain))));
```

--- PR #1210 | src/subdomains/core/trading/entities/trading-rule.entity.ts ---
Context:   | +  @Column({ type: 'int', default: FeeAmount.LOWEST }) | +  feeAmount: FeeAmount;
David: Maybe we should call it `poolFee`?

--- PR #1213 | src/subdomains/generic/kyc/services/kyc-client.service.ts ---
David: Linter warnings

--- PR #1213 | src/subdomains/generic/kyc/services/kyc-client.service.ts ---
Context:    // --- HELPER METHODS --- // | +  private async getWebhookData(userId: number, dateFrom: Date, dateTo: Date): Promise<PaymentWebhookData[]> { | +    const txList = [
David: Why `getWebhookData`? 🤔

--- PR #1216 | src/subdomains/core/aml/aml-helper.service.ts ---
Context: +@Injectable() | +export class AmlHelperService { | +  static getAmlErrors(
David: Doesn't need to be injectable

--- PR #1216 | src/subdomains/core/aml/enums/aml-error.enum.ts ---
Context: -  OUTDATED_NAME_CHECK = 'OutdatedNameCheck', | +  NAME_CHECK_WITHOUT_KYC = 'NameCheckWithoutKYC', | +  NAME_CHECK_DESPITE_BIRTHDAY = 'NameCheckDespiteBirthday',
David: Should we name it `NAME_CHECK_WITH_BIRTHDAY`? Would fit better to `NAME_CHECK_WITHOUT_KYC` 😁

--- PR #1216 | src/subdomains/core/buy-crypto/process/services/buy-crypto-preparation.service.ts ---
Context: +          }, | +        }, | +      ],
David: Haha, crazy 🤣

--- PR #1216 | src/subdomains/core/buy-crypto/process/services/buy-crypto-preparation.service.ts ---
Context: +          }, | +        }, | +      ],
David: Maybe we should share some data? 😉 
```ts
    const search: FindOptionsWhere<BuyCrypto> = {
      amlCheck: IsNull(),
      amlReason: IsNull(),
      inputAmount: Not(IsNull()),
      inputAsset: Not(IsNull()),
      status: IsNull(),
      isComplete: false,
    };

    const entities = await this.buyCryptoRepo.find({
      where: [
        {
          ...search,
          buy: { user: { userData: { lastNameCheckDate: IsNull() } } },
        },
        ...
      ],
    });
```

--- PR #1216 | src/subdomains/core/aml/enums/aml-error.enum.ts ---
Context: +  AmlError.NAME_CHECK_WITHOUT_KYC, | +  AmlError.NAME_CHECK_WITH_BIRTHDAY, | +];
David: You can remove this and just check, if the map entry below is not null for this error.

--- PR #1220 | src/subdomains/supporting/payment/services/special-external-account.service.ts ---
Context:   | -  async getBlacklist(type?: SpecialExternalAccountType): Promise<SpecialExternalAccount[]> { | +  async getBlacklist(type?: SpecialExternalAccountType[]): Promise<SpecialExternalAccount[]> {
David: `types`

--- PR #1220 | src/subdomains/core/aml/aml-helper.service.ts ---
Context: +            (b) => | +              b.matches(SpecialExternalAccountType.BANNED_IBAN, entity.bankTx.iban) || | +              b.matches(SpecialExternalAccountType.BANNED_IBAN_BUY, entity.bankTx.iban),
David: Allow list of types

--- PR #1221 | src/subdomains/generic/kyc/dto/input/ident-result.dto.ts ---
Context: +  ID_DAMAGED_OTHER = 'ID_DAMAGED_OTHER', | +  ID_SECURITY_FEATURE_NOT_VISIBLE_NOT_FRAUD = 'ID_SECURITY_FEATURE_NOT_VISIBLE_NOT_FRAUD	', | +  ID_SECURITY_FEATURE_VIDEO_SHORT = 'ID_SECURITY_FEATURE_VIDEO_SHORT',
David: Spaces?

--- PR #1221 | src/subdomains/generic/kyc/dto/input/ident-result.dto.ts ---
Context: +  SELFIE_GLARE = 'SELFIE_GLARE', | +  SELFIE_DARKNESS = 'SELFIE_DARKNESS	', | +  SELFIE_PERSPECTIVE = 'SELFIE_PERSPECTIVE',
David: Spaces?

--- PR #1223 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:          : Util.minutesDiff(this.created) >= 10 |          ? { amlCheck: CheckStatus.GSHEET, comment } |          : { comment };
David: Can we simplify this? 😉

--- PR #1223 | src/subdomains/core/sell-crypto/process/buy-fiat.entity.ts ---
Context:          : Util.minutesDiff(this.created) >= 10 |          ? { amlCheck: CheckStatus.GSHEET, comment } |          : { comment };
David: Same here

--- PR #1223 | src/subdomains/core/aml/aml-helper.service.ts ---
Context: +    // Fail | +    const failedError = amlErrors.find((e) => FailedAmlErrors.includes(e)); | +    if (failedError && AmlErrorReasons[failedError])
David: ```ts
    const failedError = amlErrors.find((e) => FailedAmlErrors.includes(e) && AmlErrorReasons[e]);
    if (failedError) ...
```?

--- PR #1230 | src/subdomains/core/aml/aml-helper.service.ts ---
Context: +    // Crucial error aml | +    const crucialErrorResult = amlResults.find((r) => r?.errorProperty === AmlErrorProperty.CRUCIAL_ERROR); | +    if (crucialErrorResult) return { ...crucialErrorResult, comment };
David: Why ? in `r?.errorProperty`?

--- PR #1230 | src/subdomains/core/aml/enums/aml-error.enum.ts ---
Context: +  SAME_CHECK = 'SameCheck', // All errors must have the same amlCheck | +  CRUCIAL_ERROR = 'CrucialError', // Prioritized error | +}
David: Maybe we can find a better naming 😉

--- PR #1230 | src/subdomains/core/aml/enums/aml-error.enum.ts ---
Context: +export const AmlErrorResult: { | +  [b in AmlError]: { errorProperty: AmlErrorType; amlCheck: CheckStatus; amlReason: AmlReason }; | +} = {
David: Rename `errorProperty` to `type`?

--- PR #1231 | src/subdomains/core/buy-crypto/routes/swap/dto/get-crypto-payment-info.dto.ts ---
David: Rename?

--- PR #1231 | src/subdomains/core/buy-crypto/routes/swap/dto/get-crypto-quote.dto.ts ---
David: Rename?

--- PR #1231 | src/subdomains/core/buy-crypto/routes/swap/swap.controller.ts ---
Context: -  async getCryptoQuote(@Body() dto: GetCryptoQuoteDto): Promise<CryptoQuoteDto> { | +  @ApiOkResponse({ type: SwapQuoteDto }) | +  async getCryptoQuote(@Body() dto: GetCryptoQuoteDto): Promise<SwapQuoteDto> {
David: Remove this comment and the `ApiExcludeEndpoint`

--- PR #1231 | src/subdomains/core/buy-crypto/routes/swap/swap.controller.ts ---
Context: -  @ApiOkResponse({ type: CryptoPaymentInfoDto }) | +  @ApiOkResponse({ type: SwapPaymentInfoDto }) |    async createCryptoWithPaymentInfo(
David: Remove `ApiExcludeEndpoint` above

--- PR #1231 | src/subdomains/core/buy-crypto/routes/swap/swap.service.ts ---
David: Rename methods?

--- PR #1231 | src/subdomains/core/aml/aml-helper.service.ts ---
Context:        if (!entity.cryptoInput.isConfirmed) errors.push(AmlError.INPUT_NOT_CONFIRMED); | -      if (entity instanceof BuyCrypto && !entity.userData.cryptoCryptoAllowed) | -        errors.push(AmlError.CRYPTO_CRYPTO_NOT_ALLOWED);
David: Remove `cryptoCryptoAllowed` on UserData? Or maybe in a second step?

--- PR #1231 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:        user && |        !user.userData.hasBankTxVerification && |        txAmountChf > Config.tradingLimits.dailyDefault
David: Move the amount checks up (above isSwapTx) and remove the `QuoteError.DEPOSIT_LIMIT_REACHED`. `(txAmountChf > Config.tradingLimits.dailyDefault && user?.userData.kycLevel < KycLevel.LEVEL_50)` can also be removed.

--- PR #1231 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +    // amount checks | +    if (sourceAmount < txSourceMinVolume) return QuoteError.AMOUNT_TOO_LOW; | +    if (txAmountChf > maxVolumeChf) return QuoteError.AMOUNT_TOO_HIGH;
David: Move down

--- PR #1233 | src/subdomains/generic/kyc/services/name-check.service.ts ---
Context:   | +    if (sanctionData.found_records.every((s) => !s.date_of_birth || s.date_of_birth.length === 0)) { | +      await this.createNameCheckLog(bankData, JSON.stringify(sanctionData), RiskStatus.MATCH_WITHOUT_BIRTHDAY);
David: Can be simplified to `(s) => !s.date_of_birth?.length` if `date_of_birth` is an array?

--- PR #1243 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: +  private async txConfirmedMail(entity: Transaction): Promise<void> { | +    try { | +      if (entity.mailTarget?.userData.mail && !DisabledProcess(Process.TX_MAIL)) {
David: Maybe `sendTxAssignedMail` would be a better name? Confirmed sounds like it's already finished 😉

--- PR #1243 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
Context: + | +  confirmSentMail(): UpdateResult<Transaction> { | +    const update: Partial<BuyCrypto> = {
David: `mailSent`?

--- PR #1243 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
Context: +  get mailTarget(): BuyCrypto | BuyFiat | undefined { | +    return this.buyCrypto ?? this.buyFiat ?? undefined; | +  }
David: `target` is a bit weird ... but I don't have a better name yet 😆 🤔

--- PR #1243 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
Context: +  get mailTarget(): BuyCrypto | BuyFiat | undefined { | +    return this.buyCrypto ?? this.buyFiat ?? undefined; | +  }
David: Use `targetEntity`?

--- PR #1243 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
Context: +  get url(): string { | +    return `${Config.frontend.services}/tx/${this.id}`; | +  }
David: UID?

--- PR #1243 | src/subdomains/supporting/notification/factories/mail.factory.ts ---
Context:    BLACK_SQUAD = 'mail.black_squad', | +  UNVERIFIED_FIAT_INPUT = 'mail.payment.fiat_input.unverified', |  }
David: unassigned?

--- PR #1243 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: + | +    await this.transactionNotificationService.sendTxAssignedMail(entity); | +
David: Only send mail if type was set?

--- PR #1244 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: +    const hash = Util.createHash( | +      `${sourceEntity.id}` + entity.created + entity.sourceType + sourceEntity.created, | +    ).toUpperCase();
David: Does the entity already have a `created` date? I think I would take only fields from source entity.

--- PR #1244 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: +    ).toUpperCase(); | +    entity.uid = `${hash.slice(0, 8)}-${hash.slice(8, 16)}-${hash.slice(16, 24)}`; | +
David: 24 digits seems to be a bit long for me 😁  and do you see some benefit using these dashes?

--- PR #1244 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: +  @Get('uid/:uid') | +  @ApiOkResponse({ type: TransactionDto }) | +  async getSingleTransaction(@Param('uid') uid: string): Promise<TransactionDto> {
David: Should we just use `@Get(':uid')` or do you want to reserve this for a future call with the "real" ID?

--- PR #1244 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: +    if (!transaction.txTarget) throw new BadRequestException('Unsupported transaction type'); | + | +    switch (transaction.txTarget.constructor) {
David: Remove the check and change the switch to
`switch (transaction.txTarget?.constructor) {`

--- PR #1244 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: + | +      default: | +        throw new BadRequestException('Unsupported transaction type');
David: What about referral transactions?

--- PR #1244 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: +        throw new BadRequestException('Unsupported transaction type'); | +    } | +  }
David: Ah, this code is already there below, maybe you can reuse it?

--- PR #1244 | src/subdomains/supporting/payment/entities/transaction.entity.ts ---
Context: + | +  get txTarget(): BuyCrypto | BuyFiat | RefReward | undefined { | +    return this.buyCrypto ?? this.buyFiat ?? this.refReward ?? undefined;
David: Maybe `txEntity` would be a better name (similar to sourceEntity)?

--- PR #1245 | src/subdomains/core/aml/aml.service.ts ---
Context: +          bankData.userData.kycLevel < entity.userData.kycLevel | +            ? await this.userDataService.mergeUserData(entity.userData.id, bankData.userData.id, true) | +            : await this.userDataService.mergeUserData(bankData.userData.id, entity.userData.id, true);
David: Do we need to resync the entity?

--- PR #1245 | src/subdomains/core/aml/aml.service.ts ---
Context: + | +          const slave = | +            bankData.userData.kycLevel < entity.userData.kycLevel ? bankData.userData.id : entity.userData.id;
David: Maybe this? 😉 
```ts
const [master, slave] = bankData.userData.kycLevel < entity.userData.kycLevel
   ? [entity.userData.id, bankData.userData.id]
   : [bankData.userData.id, entity.userData.id];
```

--- PR #1247 | src/subdomains/supporting/bank-tx/bank-tx/bank-tx.service.ts ---
Context: +  async reset(id: number): Promise<void> { | +    const bankTx = await this.bankTxRepo.findOne({ where: { id }, relations: { buyCrypto: true, buyFiat: true } }); | +    if (!bankTx) throw new NotFoundException('BankTx not found');
David: Loading the buyFiat is not necessary?

--- PR #1247 | src/subdomains/supporting/bank-tx/bank-tx/bank-tx.controller.ts ---
Context: + | +  @Delete('reset/:id') | +  @ApiBearerAuth()
David: `@Put(':id/reset')`

--- PR #1251 | src/shared/i18n/de/mail.json ---
Context: -                "high_risk_blocked": "Wir können nicht sicherstellen, dass es sich bei dieser Transaktion nicht um einen Betrug handelt und mussten daher die Transaktion leider ablehnen.<br>Einzahlungen per Banktransaktion stehen dir weiterhin zur Verfügung." | +                "high_risk_blocked": "Wir können nicht sicherstellen, dass es sich bei dieser Transaktion nicht um einen Betrug handelt und mussten daher die Transaktion leider ablehnen.<br>Einzahlungen per Banktransaktion stehen dir weiterhin zur Verfügung.", | +                "fee_too_high": "Die Netzwerk Gebühren sind derzeit zu hoch für die Menge die du geschickt hast"
David: `den Betrag`

--- PR #1253 | src/subdomains/core/buy-crypto/process/entities/buy-crypto-batch.entity.ts ---
Context:      for (const tx of this.transactions) { | -      if (tx.fee.estimatePayoutFeePercent > tx.fee.allowedTotalFeePercent) { | +      if (tx.fee.estimatePayoutFeeAmount > tx.fee.allowedTotalFeeAmount) {
David: Use 1% buffer?

--- PR #1257 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context:   | +  @ApiPropertyOptional({ type: FeeDto, description: 'Fee infos in source currency' }) | +  fees?: FeeDto;
David: `Fee infos in input asset`?

--- PR #1257 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context: +          buyCrypto.totalFeeAmount && | +          Util.roundReadable(buyCrypto.totalFeeAmount * referencePrice, isFiat(buyCrypto.inputAssetEntity)), | +      },
David: This could be done in a helper method and combined with the buy-fiat?
```ts
const referencePrice = buyFiat.inputAmount / buyFiat.inputReferenceAmount;
const fees = this.mapFees(buyFiat);

const dto: TransactionDto = {
   ...
   feeAmount: fees.total,
   fees,
   ...
};
```

```ts
private mapFees(entity: BuyCrypto | BuyFiat, referencePrice: number): FeeDto {
   ...
}
```

--- PR #1257 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context:    static mapBuyFiatTransaction(buyFiat: BuyFiatExtended): TransactionDto { | +    const referencePrice = buyFiat.inputAmount / buyFiat.inputReferenceAmount; |      const dto: TransactionDto = {
David: Calculating the referencePrice here is kind of useless. I would do it the same way as for buy-crypto 😉

--- PR #1257 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context: +        entity.totalFeeAmount && | +        Util.roundReadable(entity.totalFeeAmount * referencePrice, isFiat(entity.inputAssetEntity)), | +    };
David: Check for null (e.g. `entity.absoluteFeeAmount != null`) since we want to have 0, if it's 0

--- PR #1258 | src/shared/services/payment-info.service.ts ---
Context: + | +    if ('blockchain' in dto) { | +      if (!SwapInputBlockchains.includes(dto.blockchain)) throw new BadRequestException('Blockchain is not swappable');
David: Error messages are a bit weird 😆  blockchains are not swappable normally.

--- PR #1260 | src/subdomains/core/trading/dto/update-trading-rule.dto.ts ---
Context: +  @IsOptional() | +  @IsNumber() | +  reactivationTime: number;
David: Integer

--- PR #1260 | src/subdomains/core/trading/services/trading-rule.service.ts ---
Context: +    if (!tradingRule) throw new NotFoundException('Trading rule not found'); | + | +    return this.ruleRepo.save({ ...tradingRule, ...dto });
David: Maybe better update?

```ts
await this.ruleRepo.update(id, dto);
```

--- PR #1263 | src/shared/i18n/de/mail.json ---
Context: +                "fee_too_high": "Die Netzwerk Gebühren sind derzeit zu hoch für den Betrag, den du geschickt hast", | +                "receiver_rejected_tx": "Der Empfänger Node hat die Transaktion abgelehnt" |              },
David: `Die Empfänger-Node`?

--- PR #1263 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context:    INSTANT_PAYMENT = 'InstantPayment', |    FEE_TOO_HIGH = 'FeeTooHigh', | +  RECEIVER_REJECTED_TRANSACTION = 'ReceiverRejectedTransaction',
David: Maybe `ReceiverRejected` is enough?

--- PR #1275 | src/subdomains/supporting/support-issue/dto/create-support-issue.dto.ts ---
David: Services need to be udpated

--- PR #1275 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +  async createSupportMessage(@GetJwt() jwt: JwtPayload, @Body() dto: CreateSupportMessageDto): Promise<void> { | +    return this.supportIssueService.createSupportMessage({ ...dto, author: SupportMessageAuthor.CUSTOMER }, jwt.id); | +  }
David: `Post(':id/message')` and remove the id from the DTO

--- PR #1275 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +  async createSupportMessageReply(@Body() dto: CreateSupportMessageDto): Promise<void> { | +    return this.supportIssueService.createSupportMessage(dto); | +  }
David: `Post(':id/reply')`

--- PR #1275 | src/subdomains/supporting/support-issue/dto/create-support-message.dto.ts ---
Context: +  @IsString() | +  fileName?: string; | +}
David: Maybe we should have a separate DTO for the reply, where the author can be specified? And maybe it should just be a string instead of enum?

--- PR #1275 | src/subdomains/supporting/support-issue/dto/create-support-message.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsString() | +  fileName?: string;
David: This can be shared with `CreateSupportIssueDto`?

--- PR #1275 | src/subdomains/supporting/support-issue/entities/support-issue.entity.ts ---
Context:   | +  @Column({ length: 256, nullable: true }) | +  name: string;
David: What is this for?

--- PR #1275 | src/subdomains/supporting/support-issue/entities/support-issue.entity.ts ---
Context: + |    @Column({ length: 'MAX', nullable: true }) |    description: string;
David: Remove description as well (later)?

--- PR #1275 | src/subdomains/supporting/support-issue/entities/support-message.entity.ts ---
Context: + | +  @ManyToOne(() => SupportIssue, (supportIssue) => supportIssue.messages, { nullable: false, eager: true }) | +  supportIssue: SupportIssue;
David: `issue: SupportIssue`

--- PR #1275 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +        }, | +        userId, | +      );
David: `if (dto.message || dto.file)`?

--- PR #1275 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: + | +    entity.supportIssue = await this.supportIssueRepo.findOneBy({ id: dto.supportIssue.id }); | +    if (!entity.supportIssue) throw new NotFoundException('Support Issue not found');
David: A bit more consistency in error messages would be good 😉 
- Support **i**ssue not found
- Support **m**essage already exists
- Support **i**ssue not found

--- PR #1275 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context:        const { contentType, buffer } = Util.fromBase64(dto.file); |   | +      const user = await this.userService.getUser(userId, { userData: true });
David: Maybe you should get the userDataId from existing support issue? This would be always the same.

--- PR #1275 | src/subdomains/supporting/support-issue/entities/support-issue.entity.ts ---
Context:   | +  @Column({ length: 256, nullable: true }) | +  name: string;
David: Then we should maybe rename it to `user`? Or `userName`? And I didn't find a usage of this field.

--- PR #1275 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +    const entity = this.messageRepo.create(dto); | +    if (!entity.author) entity.author = 'Customer'; | +
David: Maybe `entity.author ??= 'Customer'; `?

--- PR #1275 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +      +id, | +      jwt.role === UserRole.SUPPORT || jwt.role === UserRole.ADMIN ? dto : { ...dto, author: 'Customer' }, | +    );
David: `[UserRole.SUPPORT, UserRole.ADMIN].includes(jwt.role)`?

--- PR #1275 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: -    return this.supportIssueRepo.save(entity); | +    await this.messageRepo.save(entity); |    }
David: Send a mail to the user, if the author is a support member

--- PR #1275 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +      relations: { transaction: { user: { userData: true } } }, | +    }); | +    if (!entity.issue) throw new NotFoundException('Support issue not found');
David: Only allowed if the issue belongs to the calling user (or for support)

--- PR #1275 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +      entity.fileUrl = await this.storageService.uploadFile( | +        entity.issue.transaction.user.userData.id, | +        FileType.SUPPORT_ISSUE,
David: `entity.userData.id`?

--- PR #1275 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: + | +    if (dto.author === 'Customer' && entity.issue.user.id !== userId) | +      throw new BadRequestException('You can only create support messages for your own transaction');
David: `ForbiddenException`

--- PR #1275 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: + | +    if (dto.author !== 'Customer') await this.supportIssueNotificationService.newSupportMessage(entity); | +  }
David: Maybe we should define `Customer` as constant somewhere?

--- PR #1275 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: + | +    if (dto.author === CustomerAuthor && entity.issue.user.id !== userId) | +      throw new ForbiddenException('You can only create support messages for your own transaction');
David: We need to compare user data IDs (you can create support issues for all your users)

--- PR #1282 | src/subdomains/generic/user/models/user-data/user-data-notification.service.ts ---
Context: +            suffix: [{ key: MailKey.SPACE, params: { value: '4' } }, { key: MailKey.DFX_TEAM_CLOSING }], | +          }, | +        });
David: Does it make sense to have this in a `if (master.mail)`?

--- PR #1284 | src/config/config.ts ---
Context: +  checkout = { | +    ckoEntityId: process.env.CKO_ENTITY_ID, | +  };
David: ```ts
  checkout = {
    entityId: process.env.CKO_ENTITY_ID,
  };
```

--- PR #1284 | src/subdomains/core/monitoring/observers/checkout.observer.ts ---
Context: +      }); | +    } | +    return checkoutData;
David: ```ts
return balances.map((b) => ({
        name: 'Checkout',
        currency: b.holding_currency,
        balance: b.balances,
        description: b.descriptor,
}));
```

--- PR #1284 | src/subdomains/core/monitoring/observers/checkout.observer.ts ---
Context: +    let data = []; | + | +    data = data.concat(await this.getCheckout());
David: ```ts
const data = await this.getCheckout();
```

--- PR #1285 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:      paymentMethodOut: PaymentMethod, | -    allowExpiredPrice: boolean, | +    allowCachedBlockchainFee: boolean,
David: Here we still want to select, if the price can be expired or not. For payment info calls, we always use cached blockchain fees.

--- PR #1285 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context: +        ? await this.blockchainFeeRepo.findOneCachedBy(`${active.id}`, where) | +        : await this.blockchainFeeRepo.findOneBy(where); | +      if (!fee && !allowCached) throw new Error(`No blockchain fee found for asset ${active.id}`);
David: "Not cached" means we need to fetch it from the blockchain directly, not from DB 😉

--- PR #1285 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:        discountCodes, | -      allowBlockchainFeeFallback: allowExpiredPrice, | +      allowCachedBlockchainFee: allowCachedBlockchainFee,
David: Simplify to `allowCachedBlockchainFee,` 😉

--- PR #1285 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:      discountCodes: string[], | -    allowExpiredPrice: boolean, | +    allowCachedBlockchainFee: boolean,
David: Set this parameter to true when calling from `getTxDetails` (for payment infos)

--- PR #1285 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context: +    } catch (e) { | +      this.logger.error(`Failed to get blockchain fee of asset id ${asset.id}:`, e); | +    }
David: I think we can/should remove this try-catch?

--- PR #1285 | src/subdomains/supporting/payment/services/fee.service.ts ---
Context: +        : await this.calculateBlockchainFee(active, false); | +      if (!feeAmount && !allowCached) | +        throw new Error(`No blockchain fee found for asset ${active.id} and allowCached: ${allowCached}`);
David: It's not really useful to add `allowCached` to the message, it's always false in this case 😉

--- PR #1286 | infrastructure/bicep/dfx-api.bicep ---
Context:          } | +        { | +          name: 'CKO_ENTITY_ID'
David: `SIFT_API_KEY`?

--- PR #1286 | src/integration/sift/services/sift.service.ts ---
Context: +        $ip: user.ip, | +        $time: Date.now(), | +        blockchain_address: user.address,
David: `$time: user.created,` ?

--- PR #1286 | src/integration/sift/services/sift.service.ts ---
Context: +      this.logger.error('Error during account creation', error); | +    } | +  }
David: We should update all the KYC data and only the things that changed. These things here are not going to change.

--- PR #1286 | src/subdomains/generic/user/models/user/user.service.ts ---
David: I think the update account should be called from UserDataService. There are a bunch of methods there that update user data.

--- PR #1286 | src/subdomains/core/sell-crypto/route/sell.controller.ts ---
Context:   | -    void this.transactionRequestService.createTransactionRequest(TransactionRequestType.Sell, dto, sellDto); | +    void this.transactionRequestService.createTransactionRequest(TransactionRequestType.Sell, dto, sellDto, userId);
David: `user.id` would be the same as in the other two controllers 😉

--- PR #1286 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: +        throw new Error(`No payment type for fiat payment ${fiatPaymentMethod}`); | +    } | +  }
David: Easier with a map:
```ts
const PaymentMethodMap: { [method in PaymentMethod]: PaymentType } = {
  [FiatPaymentMethod.BANK]: PaymentType.SEPA_CREDIT,
  [FiatPaymentMethod.INSTANT]: PaymentType.SEPA_INSTANT_CREDIT,
  [FiatPaymentMethod.CARD]: PaymentType.CREDIT_CARD,
  [CryptoPaymentMethod.CRYPTO]: PaymentType.CRYPTO_CURRENCY,
};
```
and this will generate a build error, if a PaymentMethod is not mapped.

--- PR #1286 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +          $name: `${data.firstname} ${data.surname}`, | +          $address_1: data.street, | +          $city: data.location,
David: Add house number to address (if available)

--- PR #1286 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +        $ip: ip, | +      } as CreateAccount); | +    }
David: Remove `ip`

--- PR #1286 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +          $zipcode: data.zip, | +        }, | +        kyc_level: userData.kycLevel,
David: Doesn't really make sense to update the KYC level here 🤔 let's leave it away for now.

--- PR #1286 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +    const updateSiftAccount: CreateAccount = {}; | + | +    if (dto.phone != userData.phone) updateSiftAccount.$phone = dto.phone;
David: `if (dto.phone && dto.phone !== userData.phone)`

--- PR #1288 | src/subdomains/supporting/pricing/domain/entities/price.ts ---
David: When creating a price, it normally has a single step. So I would just create the first step in the `create` method (no additional params needed).
When joining the prices with the `join` method, the steps should be appended (and inverting steps should be removed).

--- PR #1288 | src/subdomains/supporting/pricing/domain/entities/price.ts ---
Context: +  @ApiProperty({ type: Date }) | +  timestamp: Date; | +
David: Is `{ type: Date }` required?

--- PR #1288 | src/subdomains/core/sell-crypto/process/buy-fiat.entity.ts ---
Context: +  addPriceSteps(steps: PriceStep[]): void { | +    this.priceSteps = JSON.stringify([...this.priceStepsObject, ...steps]); | +  }
David: Use a setter and move to `get priceStepsObject`?

--- PR #1288 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +  addPriceSteps(steps: PriceStep[]): void { | +    this.priceSteps = JSON.stringify([...this.priceStepsObject, ...steps]); | +  }
David: Same here?

--- PR #1288 | src/subdomains/supporting/pricing/domain/entities/price.ts ---
Context:      price.isValid = _isValid; |      price.timestamp = _timestamp; | +    price.steps = priceSource ? [PriceStep.create(priceSource, source, target, _price, _timestamp)] : [];
David: `steps` should be undefined, if no source provided?

--- PR #1288 | src/subdomains/supporting/pricing/domain/entities/price.ts ---
Context: + | +    const priceSteps = prices.map((p) => p.steps).flat(); | +    price.steps.push(...priceSteps);
David: Remove unnecessary steps?

--- PR #1288 | src/subdomains/supporting/pricing/domain/entities/price.ts ---
Context:      price.isValid = _isValid; |      price.timestamp = _timestamp; | +    price.steps = priceSource ? [PriceStep.create(priceSource, source, target, _price, _timestamp)] : [];
David: Ok 🤔  we can keep it as is and we'll see how it works 👍

--- PR #1288 | src/subdomains/core/sell-crypto/process/services/buy-fiat-preparation.service.ts ---
Context:          await this.buyFiatRepo.update( | -          ...entity.setOutput(price.convert(entity.inputReferenceAmountMinusFee), currency, price.steps), | +          ...entity.setOutput(price.convert(entity.inputReferenceAmountMinusFee), currency, price?.steps),
David: This `?` is useless 🤔

--- PR #1288 | src/subdomains/supporting/pricing/domain/entities/price.ts ---
Context: -      priceSteps.some((p2) => p1.source === p2.source && p1.to === p2.from && p1.from === p2.to), | +      priceSteps.every((p2) => p1?.source !== p2?.source || p1?.to !== p2?.from || p1?.from !== p2?.to), |      );
David: ```ts
    const filteredPriceSteps = priceSteps.filter((p1) =>
      !priceSteps.some((p2) => p1.source === p2.source && p1.to === p2.from && p1.from === p2.to),
    );
```
would be a bit simpler to understand 😉

--- PR #1288 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:      feeConstraints: BuyCryptoFee, | +    priceSteps: PriceStep[], |    ): UpdateResult<BuyCrypto> {
David: I think the price steps should be set during output amount calculation `calculateOutputReferenceAmount`

--- PR #1288 | src/subdomains/core/sell-crypto/process/buy-fiat.entity.ts ---
Context:      totalFeeAmountChf: number, | +    priceSteps: PriceStep[], |    ): UpdateResult<BuyFiat> {
David: Same here

--- PR #1288 | src/subdomains/supporting/payment/dto/transaction-helper/tx-fee-details.dto.ts ---
Context:    fee: InternalFeeDto & FeeDto; | +  priceSteps: PriceStep[]; |  }
David: This could then be removed again

--- PR #1288 | src/subdomains/supporting/payment/dto/transaction-helper/tx-fee-details.dto.ts ---
Context:    fee: InternalFeeDto & FeeDto; | +  priceSteps: PriceStep[]; |  }
David: - For BuyFiat, inputReferenceAsset is always the same as inputAsset
- For BuyCrypto, they might be different but the price source for conversion from input- to inputReferenceAsset is our bank

--- PR #1288 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:    calculateOutputReferenceAmount(price: Price): this { |      this.outputReferenceAmount = price.convert(this.inputReferenceAmountMinusFee, 8); | +    this.addPriceSteps(price?.steps);
David: `?` can be removed

--- PR #1288 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +    this.addPriceSteps(price?.steps); |      return this; |    }
David: If inputAsset is not the same as inputReferenceAsset, add a price step (first one) with source Bank

--- PR #1288 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +  addPriceSteps(steps: PriceStep[]): void { | +    this.priceStepsObject = [...this.priceStepsObject, ...(steps ?? [])]; | +  }
David: This method is useless?

--- PR #1288 | src/subdomains/core/sell-crypto/process/buy-fiat.entity.ts ---
Context: +  addPriceSteps(steps: PriceStep[]): void { | +    this.priceStepsObject = [...this.priceStepsObject, ...(steps ?? [])]; | +  }
David: This method is useless?

--- PR #1292 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +    const errors = []; | +    const resultJson = JSON.parse(entity.result); | +
David: ```ts
const result = entity.getResult<IdentResultDto>();
```

--- PR #1292 | src/subdomains/generic/kyc/enums/kyc.enum.ts ---
David: I would rename
- CHECK_PENDING to REVIEW_EXTERNAL
- IN_REVIEW to REVIEW_INTERNAL
- INTERNAL_REVIEW to REVIEW_MANUAL
and adapt the corresponding entity methods (KycStep, UserData).

WDYT?

--- PR #1292 | src/subdomains/generic/kyc/dto/mapper/kyc-step.mapper.ts ---
Context:      [EntityStatus.OUTDATED]: DtoStatus.OUTDATED, | +    [EntityStatus.INTERNAL_REVIEW]: DtoStatus.IN_REVIEW, |    };
David: Move to IN_REVIEW

--- PR #1292 | src/subdomains/generic/kyc/entities/kyc-step.entity.ts ---
Context:   | -export type KycStepResult = string | object; | +export type KycStepResult = string | object | KycStepResultDto;
David: Is this required?

--- PR #1292 | src/subdomains/generic/kyc/entities/kyc-step.entity.ts ---
Context: +  value: string; | +  original?: string; | +}
David: This is the same as `IdentResultDto`?

--- PR #1292 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +      ) | +    ) | +      errors.push(IdentCheckError.LAST_NAME_NOT_MATCHING);
David: I think this shouldn't be a null-fallback. Either birthname or lastname should match.

--- PR #1292 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +  LAST_NAME_NOT_MATCHING = 'LastNameNotMatching', | +  INVALID_TYPE = 'InvalidType', | +  IDENTIFICATION_NUMBER_MISSING = 'IdentificationNumberMissing',
David: `INVALID_TYPE` -> `INVALID_DOCUMENT_TYPE`

--- PR #1292 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: + | +    if (resultJson.identificationprocess?.type !== 'IDCARD' && resultJson.identificationprocess?.type !== 'PASSPORT') | +      errors.push(IdentCheckError.INVALID_TYPE);
David: Use `.includes()`?

--- PR #1292 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: + | +      if (errors.includes(IdentCheckError.USER_DATA_BLOCKED) || errors.includes(IdentCheckError.USER_DATA_MERGED)) { | +        entity.cancel();
David: Fail?

--- PR #1292 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +      resultJson.identificationprocess?.result !== 'SUCCESS' | +    ) | +      errors.push(IdentCheckError.INVALID_RESULT);
David: Use `.includes()`?

--- PR #1292 | src/subdomains/generic/kyc/dto/mapper/kyc-step.mapper.ts ---
Context:      [EntityStatus.OUTDATED]: DtoStatus.OUTDATED, | +    [EntityStatus.INTERNAL_REVIEW]: DtoStatus.IN_REVIEW, |    };
David: 😅 
```ts
  private static StepMap: Record<EntityStatus, DtoStatus> = {
    [EntityStatus.NOT_STARTED]: DtoStatus.NOT_STARTED,
    [EntityStatus.IN_PROGRESS]: DtoStatus.IN_PROGRESS,
    [EntityStatus.FINISHED]: DtoStatus.IN_REVIEW,
    [EntityStatus.CHECK_PENDING]: DtoStatus.IN_REVIEW,
    [EntityStatus.IN_REVIEW]: DtoStatus.IN_REVIEW,
    [EntityStatus.INTERNAL_REVIEW]: DtoStatus.IN_REVIEW,
    [EntityStatus.FAILED]: DtoStatus.FAILED,
    [EntityStatus.COMPLETED]: DtoStatus.COMPLETED,
    [EntityStatus.CANCELED]: DtoStatus.FAILED,
    [EntityStatus.OUTDATED]: DtoStatus.OUTDATED,
  };
```

--- PR #1292 | src/subdomains/generic/kyc/entities/kyc-step.entity.ts ---
Context:   | -export type KycStepResult = string | object; | +export type KycStepResult = string | object | KycStepResultDto;
David: Ok 😉

--- PR #1292 | src/subdomains/generic/kyc/enums/kyc.enum.ts ---
David: Yes, and would need to update the GS probably. But would be a more comprehensive naming.

--- PR #1292 | src/subdomains/generic/kyc/enums/kyc.enum.ts ---
David: Rename the review stati

--- PR #1292 | src/subdomains/generic/kyc/enums/kyc.enum.ts ---
David: Yes, I'm ok with it 😉

--- PR #1292 | src/subdomains/generic/kyc/enums/kyc.enum.ts ---
Context: -  IN_REVIEW = 'InReview', | +  EXTERNAL_REVIEW = 'ExternalPending', | +  INTERNAL_REVIEW = 'InternalReview',
David: `EXTERNAL_REVIEW = 'ExternalReview',`?

--- PR #1294 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +    if (DisabledProcess(Process.BANK_DATA_VERIFICATION)) return; | +    await this.bankDataVerification(); | +  }
David: Maybe we could (also) trigger the verification directly during creation?

--- PR #1294 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +    else if (!entity.name) errors.push(BankDataVerificationError.NAME_MISSING); | +    else if (Util.isSameName(entity.name, entity.userData.verifiedName)) | +      errors.push(BankDataVerificationError.VERIFIED_NAME_NOT_MATCHING);
David: `!Util.isSameName(`?

--- PR #1294 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: + | +        await this.bankDataRepo.update(...entity.deactivate(errors.join(';'))); | +      }
David: Can't we move the `existing.type === BankDataType.BANK_IN` check into the getErrors method?

--- PR #1294 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +      errors.push(BankDataVerificationError.USER_DATA_NOT_MATCHING); | + | +    if (existingActive?.type === BankDataType.BANK_IN) errors.push(BankDataVerificationError.ALREADY_ACTIVE_EXISTS);
David: ```ts
if (existingActive) {
   if (entity.userData.id !== existingActive.userData.id) errors.push(BankDataVerificationError.USER_DATA_NOT_MATCHING);
   if (existingActive.type === BankDataType.BANK_IN || entity.type !== BankDataType.BANK_IN) errors.push(BankDataVerificationError.ALREADY_ACTIVE_EXISTS);
}
```

--- PR #1294 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +    } else { | +      await this.bankDataRepo.update(...entity.deactivate(errors.join(';'))); | +    }
David: ```ts
if (errors.length === 0) {   
   await this.bankDataRepo.update(...entity.activate());

   if (existing) {
      const existingError = [...(existing.comment?.split(';') ?? []), BankDataVerificationError.NEW_BANK_IN_ACTIVE];
      await this.bankDataRepo.update(...existing.deactivate(existingError.join(';')));
   }
} else {
   await this.bankDataRepo.update(...entity.deactivate(errors.join(';')));
}
```

--- PR #1294 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +        const existingError = [...(existing.comment?.split(';') ?? []), BankDataVerificationError.NEW_BANK_IN_ACTIVE]; | + | +        await this.bankDataRepo.update(...existing.deactivate(existingError.join(';')));
David: Maybe we should just overwrite the existing error?

--- PR #1294 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +        const existingError = [...(existing.comment?.split(';') ?? []), BankDataVerificationError.NEW_BANK_IN_ACTIVE]; | + | +        await this.bankDataRepo.update(...existing.deactivate(existingError.join(';')));
David: Ok 😉

--- PR #1296 | src/subdomains/core/aml/aml.service.ts ---
Context: -    if (bankData) { | +    if (bankData && isNaN(+bankData.iban)) { |        if (!entity.userData.hasValidNameCheckDate) await this.checkNameCheck(entity, bankData);
David: Check with IbanTools.

--- PR #1299 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +      $order_id: entity.transactionRequest?.id.toString(), | +      $user_id: entity.user.toString(), | +      $time: entity.created.getTime(),
David: `entity.user.id.toString(),`?

--- PR #1299 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +      $site_country: 'CH', | +      $payment_methods: [{ $payment_type: SiftPaymentMethodMap[entity.paymentMethodIn] }], | +      $digital_orders: [
David: Add payment method infos (IBAN or card details)?

--- PR #1299 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
David: Tests fail

--- PR #1299 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +      $transaction_type: TransactionType.BUY, | +      $transaction_status: TransactionStatus.SUCCESS, | +      $order_id: entity.transactionRequest?.id.toString(),
David: Transaction status should be pending and updated afterwards to fail or success.

--- PR #1305 | src/shared/i18n/de/mail.json ---
Context: +        "fiat_output": { | +            "title": "Auszahlung verschickt", | +            "salutation": "Deine Auszahlung wurde an deinen Bank-Account verschickt"
David: `dein Bankkonto`?

--- PR #1305 | src/subdomains/core/sell-crypto/process/buy-fiat.entity.ts ---
David: Do we still need `mail1SendDate`, `mail2SendDate` and `mail3SendDate`? If yes, we could maybe rename them to something meaningful? 😉

--- PR #1311 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:      master.relatedAccountRelations = master.relatedAccountRelations.concat(slave.relatedAccountRelations); | +    master.amlListAddedDate = slave.amlListAddedDate; | +    master.kycFileId = slave.kycFileId;
David: Only if `amlListAddedDate` is null on master?

--- PR #1331 | src/subdomains/generic/user/models/wallet/wallet.entity.ts ---
Context: +  paymentWebhook: WebhookConfigOption; | +  kycWebhook: WebhookConfigOption; | +}
David: Simplify?
```ts
export interface WebhookConfig {
  payment: WebhookConfigOption;
  kyc: WebhookConfigOption;
}
```

--- PR #1331 | src/subdomains/generic/user/models/wallet/wallet.entity.ts ---
Context: + | +  @Column({ length: 256, nullable: true }) | +  webhookConfig: string; // JSON string
David: Length max?

--- PR #1331 | src/subdomains/generic/user/models/wallet/wallet.entity.ts ---
Context: +          (this.webhookConfigObject.paymentWebhook === WebhookConfigOption.CONSENT_ONLY && consented) | +        ); | +    }
David: `this.webhookConfigObject` mit be undefined

--- PR #1331 | src/subdomains/generic/user/models/wallet/wallet.entity.ts ---
Context: +          this.webhookConfigObject.kycWebhook === WebhookConfigOption.TRUE || | +          (this.webhookConfigObject.kycWebhook === WebhookConfigOption.CONSENT_ONLY && consented) | +        );
David: Maybe we could build a helper here 😉

--- PR #1331 | src/subdomains/generic/user/services/webhook/dto/webhook.dto.ts ---
Context:    KYC_FAILED = 'KycFailed', | +  ACCOUNT_MERGE = 'AccountMerge', |  }
David: Maybe we should call it `AccountChanged` (DTOs as well)?

--- PR #1331 | src/subdomains/generic/user/services/webhook/dto/account-merge-webhook.dto.ts ---
Context: +  slave: number; | +} | +
David: Master and slave doesn't really make sense from a integrator perspective. Maybe just call it `old` and `new` or similar?

--- PR #1331 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: + | +  async removeKycClient(userData: UserData, walletId: number): Promise<void> { | +    if (!userData.kycClientList?.includes(walletId)) throw new BadRequestException('Kyc client already removed');
David: Just return in this case?

--- PR #1331 | src/subdomains/generic/user/services/webhook/webhook.entity.ts ---
Context: + | +  @ManyToOne(() => Wallet, { nullable: true, eager: true }) | +  wallet: Wallet;
David: Load the `user` also eager?

--- PR #1331 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: + | +    await this.userDataService.removeKycClient(userData, wallet.id); | +  }
David: `wallet.isKycClient` should be checked?

--- PR #1331 | src/subdomains/generic/user/models/wallet/wallet.entity.ts ---
Context: +  FALSE = 'False', | +  CONSENT_ONLY = 'ConsentOnly', | +}
David: Maybe we should also have the option `WalletOnly` 😉

--- PR #1331 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +    for (const client of webhooks) { | +      await this.createAndSendWebhook(client); | +    }
David: Do we need to load the wallet of the user, in case it's not loaded?

--- PR #1331 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context:   | +  @Column({ length: 256, nullable: true }) | +  kycClients: string; // semicolon separated wallet id's
David: KYC client names need to be added to the `KycInfoDto`

--- PR #1331 | src/subdomains/generic/kyc/controllers/kyc.controller.ts ---
Context: +  async removeKycClient(@GetJwt() jwt: JwtPayload, @Query('client') walletName: string) { | +    return this.kycService.removeKycClient(jwt.account, walletName); | +  }
David: Use the KYC code (`@Headers(CodeHeaderName) code: string`), no JWT authentication (see above)

--- PR #1331 | src/subdomains/generic/user/services/webhook/webhook.entity.ts ---
Context: + | +  @ManyToOne(() => Wallet, { nullable: true, eager: true }) | +  wallet: Wallet;
David: Ah ok

--- PR #1331 | src/subdomains/generic/user/services/webhook/dto/webhook.dto.ts ---
Context:    KYC_FAILED = 'KycFailed', | +  ACCOUNT_MERGE = 'AccountMerge', |  }
David: Hm yes 🤔 I was thinking from the "slave" perspective. As a slave, the only change you see is a new account id 😄

--- PR #1331 | src/subdomains/generic/user/services/webhook/dto/account-merge-webhook.dto.ts ---
Context: +  slave: number; | +} | +
David: Slave doesn't make sense IMO since the slave doesn't have any functionality after the merge ... it's just gone 💥 maybe we should even not include the slave ID in the data and set something like this:
```ts
export class AccountMergeWebhookData {
  @ApiProperty()
  accountId: number;
}
```
Then we could name it `AccountChanged` and just add some further data to this DTO, if there is the need for it in the future.
WDYT?

--- PR #1331 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context:   | +  @Column({ length: 256, nullable: true }) | +  kycClients: string; // semicolon separated wallet id's
David: Haha, true, it's `KycLevelDto`.

--- PR #1331 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context:   | +  @Column({ length: 256, nullable: true }) | +  kycClients: string; // semicolon separated wallet id's
David: Yes, I would set the names.

--- PR #1331 | src/subdomains/generic/user/models/wallet/wallet.entity.ts ---
Context: +          this.webhookConfigObject.kycWebhook === WebhookConfigOption.TRUE || | +          (this.webhookConfigObject.kycWebhook === WebhookConfigOption.CONSENT_ONLY && consented) | +        );
David: Just pushed

--- PR #1331 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +    for (const client of webhooks) { | +      await this.createAndSendWebhook(client); | +    }
David: Just pushed

--- PR #1331 | src/subdomains/generic/kyc/dto/mapper/kyc-info.mapper.ts ---
Context:        twoFactorEnabled: userData.totpSecret != null, | +      kycClients: kycClients?.map((kc) => kc.name), |        language: LanguageDtoMapper.entityToDto(userData.language),
David: Add `?? []`?

--- PR #1331 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +    const kycClients = await this.getKycClients(user); | + | +    return KycInfoMapper.toDto(user, false, kycClients);
David: Would be easier to put these two lines into a helper method 😉

--- PR #1331 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +  private async getKycClients(userData: UserData): Promise<Wallet[]> { | +    return Promise.all(userData.kycClientList?.map((k) => this.walletService.getByIdOrName(k)) ?? []); | +  }
David: It's probably faster to fetch all wallets and do the filtering (with userData.kycClientList) in the mapper.

--- PR #1331 | src/subdomains/generic/kyc/dto/mapper/kyc-info.mapper.ts ---
Context:        tradingLimit: userData.tradingLimit, |        twoFactorEnabled: userData.totpSecret != null, | +      kycClients: userKycClients?.map((kc) => kc.name) ?? [],
David: `?` and fallback could probably be removed with the new implementation? Also above.

--- PR #1331 | src/subdomains/generic/kyc/dto/mapper/kyc-info.mapper.ts ---
Context:   | +    const userKycClients = kycClients?.filter((kc) => userData.kycClientList.includes(kc.id)); | +
David: `userData.kycClientList` can be undefined. Maybe we should add a fallback in the getter, would be safer.

--- PR #1331 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: -        lastTryDate: IsNull(), | +        userData: { id: dto.userData.id }, | +        wallet: { id: dto.wallet.id },
David: Why did you remove the `lastTryDate`?

--- PR #1332 | src/subdomains/supporting/notification/templates/userV2.hbs ---
David: `user-v2.hbs`?

--- PR #1333 | src/subdomains/core/buy-crypto/process/services/buy-crypto-preparation.service.ts ---
Context: +        const ibanCountry = entity.bankTx?.iban | +          ? await this.countryService.getCountryWithSymbol(entity.bankTx.iban?.substring(0, 2)) | +          : undefined;
David: `?` can be removed in `entity.bankTx.iban?.substring`

--- PR #1336 | src/subdomains/supporting/fiat-payin/services/fiat-payin-sync.service.ts ---
Context: + | +        const buy = await this.buyService.getByBankUsage(checkoutTx.reference); | +        if (checkoutTx.authStatusReason && buy) await this.siftService.transactionCheckoutDeclined(checkoutTx, buy);
David: Put into an `else`?

--- PR #1336 | src/integration/sift/services/sift.service.ts ---
Context: +          $bank_country: checkoutTx.cardIssuerCountry, | +        }, | +      ],
David: Maybe this can be done with a helper method (and reused above)?

--- PR #1336 | src/subdomains/core/buy-crypto/process/services/buy-crypto-preparation.service.ts ---
Context: -          } as Transaction); | +          await this.siftService.transaction(entity, TransactionStatus.FAILURE, DeclineCategory.OTHER); |        } catch (e) {
David: Add decline category (for all transaction events)?

--- PR #1336 | src/subdomains/supporting/fiat-payin/services/fiat-payin-sync.service.ts ---
Context: +          const buy = await this.buyService.getByBankUsage(checkoutTx.reference); | +          if (buy) await this.siftService.transactionCheckoutDeclined(checkoutTx, buy); | +        }
David: Only if `checkoutTx.authStatusReason`?

--- PR #1336 | src/subdomains/supporting/payment/entities/transaction-request.entity.ts ---
Context: + | +  @Column({ length: 'MAX', nullable: true }) | +  siftScore: string;
David: Maybe we should call it `siftResponse? (also for BuyCrypto)

--- PR #1336 | migration/1718094174304-addAuthStatus.js ---
David: Remove this migration

--- PR #1337 | src/subdomains/generic/kyc/entities/kyc-step.entity.ts ---
Context: +      .map((n) => n.trim()) | +      .join(' '); | +  }
David: `get userName(): string`?

--- PR #1337 | src/subdomains/generic/kyc/entities/kyc-step.entity.ts ---
Context: +  get completeName(): string { | +    const result = this.getResult<IdentResultDto>(); | +    return [result?.userdata?.firstname?.value, result?.userdata?.lastname?.value, result?.userdata?.birthname?.value]
David: Just return undefined, if result is not defined?

--- PR #1337 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +            iban: `Ident${entity.identDocumentId}`, | +            type: BankDataType.IDENT, | +          });
David: User won't have KYC level 30?

--- PR #1337 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +    ) { | +      const nationality = await this.countryService.getCountryWithSymbol(result.userdata.nationality.value); | +      return await this.userDataService.updateUserDataInternal(userData, {
David: Linter warning

--- PR #1337 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context: +      identDocumentId, | +    }; | +  }
David: Is this used somewhere?

--- PR #1337 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: -        const errors = this.getIdentCheckErrors(entity); | +        const result = entity.getResult<IdentResultDto>(); | +        const errors = this.getIdentCheckErrors(entity, result);
David: Why this separated?

--- PR #1337 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: + | +  private async setUserDataIdentCompleted(result: IdentResultDto, userData: UserData): Promise<UserData> { | +    if (
David: Rename to `completeIdent`?

--- PR #1337 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context:          await this.kycStepRepo.save(entity); | +        entity.userData = await this.setUserDataIdentCompleted(result, entity.userData); | +
David: Only if step is complete?

--- PR #1337 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: -    if (kycChanged) await this.kycNotificationService.kycChanged(userData, userData.kycLevel); | +    if (dto.kycLevel && dto.kycLevel !== userData.kycLevel) | +      await this.kycNotificationService.kycChanged(userData, userData.kycLevel);
David: Is a bit hard to detect a change, if the DTO is already applied to the entity 😉

--- PR #1337 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: + | +    if (dto.kycLevel && dto.kycLevel !== userData.kycLevel) | +      await this.kycNotificationService.kycChanged(userData, userData.kycLevel);
David: Same here

--- PR #1337 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +      }); | +      if (existing) throw new ConflictException('A user with the same nationality and identDocumentId already exists'); | +    }
David: Error messages should be in regular english 😉

--- PR #1337 | src/subdomains/supporting/bank-tx/bank-tx/bank-tx.entity.ts ---
Context:   | +  get bankDataName(): string { | +    if (this.name.startsWith('/C/')) return this.addressLine1;
David: The `'/C/'` should be handled in the SepaParserService 🤔

--- PR #1337 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +            iban: `Ident${entity.identDocumentId}`, | +            type: BankDataType.IDENT, | +          });
David: Move the bank data creation also into the if?

--- PR #1346 | src/subdomains/generic/user/services/webhook/webhook.entity.ts ---
Context:    // References |    @ManyToOne(() => User, { nullable: true, eager: true }) |    user: User; // TODO: remove?
David: Remove?

--- PR #1346 | src/subdomains/generic/user/services/webhook/webhook.entity.ts ---
Context: -  wallet: Wallet; // TODO: nullable: false | +  @ManyToOne(() => Wallet, { eager: true }) | +  wallet: Wallet;
David: `nullable: false` (is not default for relations)

--- PR #1346 | src/subdomains/generic/user/services/webhook/webhook-notification.service.ts ---
Context: -      return false; | +      return error; |      }
David: `error` is an Error object, so you need to stringify it, or use `error.message`

--- PR #1351 | src/subdomains/core/aml/aml-helper.service.ts ---
Context: +    const crucialErrorResults = amlResults.filter((r) => r.type === AmlErrorType.CRUCIAL); | +    if (crucialErrorResults) { | +      const crucialErrorResult =
David: `if (crucialErrorResults.length)`

--- PR #1353 | src/integration/blockchain/frankencoin/frankencoin.service.ts ---
Context:      for (const position of positions) { | +      this.logger.info(`Position: ${position.id}`); | +
David: Remove logs?

--- PR #1356 | src/integration/blockchain/frankencoin/frankencoin.service.ts ---
Context: + | +    const priceUsdToChf = await this.pricingService.getPrice(fiatUsd, fiatChf, false); | +    const priceZchfToChf = await this.pricingService.getPrice(assetZchf, fiatChf, false);
David: You can ignore `priceZchfToChf`, it will always be 1

--- PR #1356 | src/integration/blockchain/frankencoin/frankencoin.service.ts ---
Context: +      totalSupplyZchf: frankencoinLog.totalSupply, | +      totalValueLockedInChf: frankencoinLog.totalValueLocked / priceUsdToChf.price, | +      fpsMarketCapInChf: frankencoinLog.poolShares.marketCap,
David: `totalValueLockedInChf: priceUsdToChf.convert(frankencoinLog.totalValueLocked), `

--- PR #1358 | src/shared/i18n/de/mail.json ---
Context: +            "salutation": "Online-Identifikation gestartet", | +            "line1": "Du hast den Online Id Prozess bei DFX gestartet. Sollte es zu Unterbechungen kommen, kannst du den Prozess jederzeit mit diesem Link fortführen:<br>[url:{urlText}]", | +            "line2": "Sollte es zu technischen Problemen kommen, so steht dir alternativ unser Video Ident Service zur Verfügung. Dabei prüft ein Mitarbeiter deinen Ausweis manuell. Bitte beachte dass dieser Service nur während den Öffnungszeiten zur Verfügung steht und deutlich höhere Kosten für DFX beinhaltet als der vollautomatische Prozess mit dem Button oben. Wir bitten dich daher diesen Link nur zu verwenden wenn es mit dem Button oben ein technisches Problem gibt:<br>[url:Video Id Link]"
David: Online Id**ent** Prozess (also for other languages)

--- PR #1358 | src/shared/i18n/de/mail.json ---
Context: +            "salutation": "Online-Identifikation gestartet", | +            "line1": "Du hast den Online Id Prozess bei DFX gestartet. Sollte es zu Unterbechungen kommen, kannst du den Prozess jederzeit mit diesem Link fortführen:<br>[url:{urlText}]", | +            "line2": "Sollte es zu technischen Problemen kommen, so steht dir alternativ unser Video Ident Service zur Verfügung. Dabei prüft ein Mitarbeiter deinen Ausweis manuell. Bitte beachte dass dieser Service nur während den Öffnungszeiten zur Verfügung steht und deutlich höhere Kosten für DFX beinhaltet als der vollautomatische Prozess mit dem Button oben. Wir bitten dich daher diesen Link nur zu verwenden wenn es mit dem Button oben ein technisches Problem gibt:<br>[url:Video Id Link]"
David: Missing commas 😉

--- PR #1358 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context: +  get kycVideoUrl(): string { | +    return `${Config.frontend.services}/kyc?code=${this.kycHash}&step=ident/video`; | +  }
David: ```ts
return `${this.kycUrl}&step=ident/video`;
```

--- PR #1358 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context:          kycStep.sessionId = await this.identService.initiateIdent(user, kycStep); | +        await this.kycNotificationService.sendIdentStartedMail(kycStep.userData); |          break;
David: Only send mail if it's the first ident step?

--- PR #1358 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: + | +        if (!user.getStepsWith(KycStepName.IDENT).length) | +          await this.kycNotificationService.sendIdentStartedMail(kycStep.userData);
David: `sendIdentStartedMail(user)`

--- PR #1359 | src/subdomains/generic/gs/dto/support-data.dto.ts ---
Context:    userData: UserData; | +  supportIssues: SupportIssue[]; | +  supportMessages: SupportMessage[];
David: Shouldn't we attach the messages to the respective ticket?

--- PR #1359 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +      supportIssues, | +      supportMessages: await this.messageRepo.findBy({ issue: { id: In(supportIssueIds) } }), | +    };
David: `supportIssues` cannot be undefined?
```ts
const supportIssues = await this.supportIssueRepo.findBy({ userData: { id: userDataId } });
return {
      supportIssues,
      supportMessages: await this.messageRepo.findBy({ issue: { id: In(supportIssues.map((i) => i.id) } }),
    };
```

--- PR #1365 | src/subdomains/core/buy-crypto/routes/buy/buy.controller.ts ---
Context: +      bankInfo, | +      request, | +    );
David: Duplicate `await this.fiatService.getFiat(request.sourceId)`

--- PR #1365 | src/subdomains/core/buy-crypto/routes/buy/dto/qr-bill.dto.ts ---
Context: +  @ApiProperty() | +  base64Enc: string; | +}
David: Maybe rename to just `InvoiceDto`?

--- PR #1365 | src/subdomains/core/buy-crypto/routes/buy/dto/qr-bill.dto.ts ---
Context: +  @ApiProperty() | +  base64Enc: string; | +}
David: Maybe something like this? WDYT?

```ts
  @ApiProperty({ description: 'Base64 encoded file' })
  invoidePdf: string;
```

--- PR #1368 | src/integration/checkout/dto/checkout.dto.ts ---
Context:    REFUNDED = 'Refunded', | +  REFUNDED_PENDING = 'Refunded Pending', |    DECLINED = 'Declined',
David: Maybe add a comment `DFX custom status`?

--- PR #1368 | src/integration/checkout/services/checkout.service.ts ---
Context: + | +    return payments; | +  }
David: I think we should remove the `Refunded` in the method name. This method has nothing to do with refunds 😉

--- PR #1368 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: + | +    buyCrypto.chargebackDate = new Date(); | +    buyCrypto.chargebackRemittanceInfo = JSON.stringify(chargebackRemittanceInfo);
David: Can we just use the reference for `chargebackRemittanceInfo` instead of storing the whole thing as JSON?

--- PR #1368 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +    buyCrypto.checkoutTx.status = CheckoutPaymentStatus.REFUNDED_PENDING; | + | +    await this.checkoutTxService.save(buyCrypto.checkoutTx);
David: Implement a method `paymentRefunded()` on `CheckoutTxService` instead?

--- PR #1368 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: + | +    await this.checkoutTxService.paymentRefunded(buyCrypto.checkoutTx); | +    await this.buyCryptoRepo.save(buyCrypto);
David: Better to use the update method:
```ts
await this.buyCryptoRepo.update(buyCrypto.id, { chargebackDate: new Date(), chargebackRemittanceInfo: chargebackRemittanceInfo.reference});
```

--- PR #1368 | src/subdomains/supporting/fiat-payin/services/checkout-tx.service.ts ---
Context: +    entity.status = CheckoutPaymentStatus.PARTIALLY_REFUNDED; | +    return this.checkoutTxRepo.save(entity); | +  }
David: Same here

--- PR #1368 | src/subdomains/supporting/fiat-payin/services/checkout-tx.service.ts ---
Context: +    await this.checkoutTxRepo.update(entityId, { | +      status: CheckoutPaymentStatus.PARTIALLY_REFUNDED, | +    });
David: Oh, shouldn't it be `CheckoutPaymentStatus.REFUNDED_PENDING`? 😄

--- PR #1369 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:   | +    const language = await this.languageService.getLanguageBySymbol(ipCountryToLanguage[ip.toLowerCase()] || 'EN'); | +
David: Create a method `getLanguageByIpCountry` in `LanguageService`

--- PR #1369 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:   | +    const language = await this.languageService.getLanguageBySymbol(ipCountryToLanguage[ip.toLowerCase()] || 'EN'); | +
David: Need to get the country with this ip first?

--- PR #1369 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:   | +    const language = await this.languageService.getLanguageBySymbol(ipCountryToLanguage[ip.toLowerCase()] || 'EN'); | +
David: Use `Config.defaultLanguage` as fallback

--- PR #1369 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: +    const language = await this.languageService.getLanguageBySymbol( | +      ipCountryToLanguage[user.ipCountry.toLowerCase()] || 'EN', | +    );
David: Use new method

--- PR #1369 | src/shared/models/language/language.service.ts ---
Context: +  async getLanguageByIpCountry(ipCountry: string): Promise<Language> { | +    const symbol = this.ipCountryToLanguage[ipCountry] ?? Config.defaultCountry; | +    return this.languageRepo.findOne({ where: { symbol } });
David: `Config.defaultLanguage`

--- PR #1369 | src/shared/models/language/language.service.ts ---
Context: +    it: 'IT', | +    fr: 'FR', | +  };
David: Countries are also uppercase?

--- PR #1376 | src/subdomains/supporting/payment/services/transaction-job.service.ts ---
Context: +      try { | +        if (!entity.type) continue; | +
David: This should already be checked by the DB query, right?

--- PR #1376 | src/subdomains/supporting/payment/services/transaction-job.service.ts ---
Context: +            : this.userService.getUser(entity.bankTx.bankTxRepeat.userId); | + | +        return entity.bankTx.buyFiat?.user ?? entity.bankTx.buyCryptoChargeback?.user;
David: The user id would be enough for an update

--- PR #1383 | src/integration/sift/services/sift.service.ts ---
Context:            $card_last4: tx.iban.slice(-4), |            $bank_name: tx.bankName, |            $bank_country: tx.country,
David: IBAN?

--- PR #1385 | src/subdomains/supporting/payment/dto/input/create-special-external-account.dto.ts ---
Context: +  @ValidateIf((dto: CreateSpecialExternalAccountDto) => | +    Boolean(dto.type === SpecialExternalAccountType.MULTI_ACCOUNT_IBAN), | +  )
David: `Boolean(...)` looks unnecessary 🤔

--- PR #1385 | src/subdomains/supporting/bank-tx/bank-tx/bank-tx.entity.ts ---
Context:      return [this.name, this.ultimateName] | -      .filter((n) => n) | +      .filter((n) => n && (!multiAccountName || n !== multiAccountName))
David: `!multiAccountName ||` should be unnecessary

--- PR #1385 | src/subdomains/supporting/bank-tx/bank-tx/bank-tx.entity.ts ---
Context: +    if (multiAccount) return this.completeName(multiAccount.name); | + | +    return this.completeName();
David: ```ts
    const multiAccount = multiAccounts.find((m) => m.value === this.iban);
    return this.completeName(multiAccount?.name);
```
😉

--- PR #1390 | src/config/config.ts ---
Context: +      apiKey: process.env.P2B_KEY, | +      secret: process.env.P2B_SECRET, | +      withdrawKeys: splitWithdrawKeys(process.env.P2B_WITHDRAW_KEYS),
David: Update Bicep

--- PR #1391 | src/subdomains/core/buy-crypto/process/services/buy-crypto-batch.service.ts ---
Context:            outputAsset: { type: Not(AssetType.CUSTOM) }, |            outputReferenceAmount: IsNull(), | +          exchangeRateApprovalDate: Not(IsNull()),
David: outputAmount should be null too?

--- PR #1391 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:    // Pass | +  @Column({ type: 'datetime2', nullable: true }) | +  exchangeRateApprovalDate: Date;
David: Better name?

--- PR #1398 | src/subdomains/core/buy-crypto/routes/buy/dto/buy-payment-info.dto.ts ---
Context:   | +  @ApiProperty({ description: 'Transaction timestamp' }) | +  timestamp: Date;
David: `description: 'Price timestamp'`? (same for sell/swap)

--- PR #1399 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: + | +    return { kycLimit, defaultLimit }; | +  }
David: Good question 😆

--- PR #1399 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: +    const volume24h = | +      user?.userData.kycLevel < KycLevel.LEVEL_50 | +        ? await this.getVolumeSince(Util.daysBefore(1), new Date(), [user])
David: The `Util.daysAfter` in AML check is based on `transaction.created` because a transaction can be quite "old". Here it's always the current day and I hope we'll never have future transactions 😆

--- PR #1399 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:    ): Promise<{ kycLimit: number; defaultLimit: number }> { | +    const volume24h = | +      user?.userData.kycLevel < KycLevel.LEVEL_50
David: True, `user` should be optional.

--- PR #1400 | src/integration/sift/services/sift.service.ts ---
Context:  export class SiftService { | -  private readonly url = 'https://api.sift.com/v205/events?return_workflow_status=true&return_route_info'; | +  private readonly url = 'https://api.sift.com/v205/';
David: `/events`?

--- PR #1400 | src/integration/sift/services/sift.service.ts ---
Context:        $transaction_status: status, | -      $decline_category: declineCategory, | +      $decline_category: status == TransactionStatus.FAILURE ? declineCategory : undefined,
David: `===`

--- PR #1400 | src/integration/sift/services/sift.service.ts ---
Context:   | +    const scoreUrl = 'events?return_workflow_status=true&return_route_info'; | +
David: Without `events`?

--- PR #1401 | src/integration/blockchain/shared/evm/evm-client.ts ---
Context:   | +  async getNativeCoinBalanceForAddress(address: string): Promise<number> { | +    const balance = await this.alchemyService.getNativeCoinBalance(this.chainId, address);
David: Use this method in the method above (`getNativeCoinBalance`)

--- PR #1401 | src/subdomains/core/buy-crypto/process/services/buy-crypto-out.service.ts ---
Context: +        asset: nativeAsset, | +        amount: Config.minEvmGasStarterBalance, | +        destinationAddress: transaction.target.address,
David: Use `gasStarterFeeAmount` as amount

--- PR #1401 | src/config/config.ts ---
Context: +  gasStarterFee = 1; //CHF | +  minEvmGasStarterBalance = 0.00001; //ETH |    blockchainFeeBuffer = 1.2;
David: I wouldn't define a min. balance. Just send if it's 0.

--- PR #1401 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:        true, |      ); | +    const gasStarterFee = await this.getGasStarterFee(to, user);
David: I'm not sure if we should include the gas starter fee in the payment infos, it may slow down the requests 🤔

--- PR #1401 | src/subdomains/core/buy-crypto/process/services/buy-crypto-out.service.ts ---
Context: +      const gasFeeRequest: PayoutRequest = { | +        context: PayoutOrderContext.BUY_CRYPTO_GAS_STARTER, | +        correlationId: transaction.id.toString(),
David: Maybe use `context: PayoutOrderContext.BUY_CRYPTO` and `correlationId: '${transaction.id}-gas-starter'`?

--- PR #1401 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:   | +  @Column({ type: 'float', nullable: true }) | +  gasStarterFeeAmount: number; //inputReferenceAsset
David: Maybe we can find a better name? 🤔

--- PR #1401 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: + | +      return userBalance < Config.minEvmGasStarterBalance ? Config.gasStarterFee : 0; | +    } catch {
David: Only if the asset is not native coin.

--- PR #1401 | src/subdomains/core/buy-crypto/process/services/buy-crypto-out.service.ts ---
Context: +        asset: nativeAsset, | +        amount: startFeePrice.convert(Config.minEvmGasStarterBalance), | +        destinationAddress: transaction.target.address,
David: `networkStartFeeAmount`?

--- PR #1401 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:      const dfx = Math.max(amount * rate + fixed, min); | -    const total = dfx + network; | +    const total = dfx + network + gasStarter;
David: Rename

--- PR #1401 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:      const specs: TxSpec = { | -      fee: { min: minSpecs.minFee, fixed: fee.fixed, network: fee.network }, | +      fee: { min: minSpecs.minFee, fixed: fee.fixed, network: fee.network, networkStart: gasStarterFee },
David: Rename

--- PR #1401 | src/subdomains/core/buy-crypto/process/services/buy-crypto-out.service.ts ---
Context: +        asset: nativeAsset, | +        amount: startFeePrice.convert(Config.minEvmGasStarterBalance), | +        destinationAddress: transaction.target.address,
David: networkStartFeeAmount is in inputReferenceAsset?

--- PR #1402 | src/subdomains/core/history/services/history.service.ts ---
Context: -      (all || query.sell) && (await this.buyFiatService.getUserTransactions(user.id, query.from, query.to)); | +    const transaction = await this.transactionService.getTransactionsForUser(user.id, query.from, query.to); | +
David: `const transactions =  ...`

--- PR #1402 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: +    return this.repo.find({ | +      where: { user: { id: userId }, type: Not(IsNull()), created: Between(from, to) }, | +      relations: {
David: Before we used the output date, maybe we should use the updated field here?

--- PR #1402 | src/subdomains/generic/kyc/services/kyc-client.service.ts ---
Context: +    const txList = await this.transactionService | +      .getTransactionsForUser(userId, dateFrom, dateTo) | +      .then((txs) => txs?.filter((t) => t.buyCrypto || t.buyFiat)?.map((t) => t.buyCrypto || t.buyFiat));
David: `txs` cannot be null -> remove both `?`

--- PR #1405 | src/integration/sift/services/sift.service.ts ---
David: `payment_method` for transaction event?

--- PR #1405 | src/integration/sift/services/sift.service.ts ---
Context:   | -    const scoreUrl = '?return_workflow_status=true&return_route_info'; | +    const scoreUrl = '?return_workflow_status=true&return_route_info&return_score=true';
David: Is `?return_score` enough?

--- PR #1414 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context:   | +  checkAndThrowMergeError(slave: UserData): void { | +    if (!this.isDfxUser) throw new BadRequestException(`Master ${this.id} not allowed to merge. Wrong KYC type`);
David: `checkIfMergePossibleWith`? 😉

--- PR #1417 | src/subdomains/supporting/payment/services/swiss-qr.service.ts ---
David: I think would create a getter in UserData entity
```ts
  get address() {
    return this.accountType === AccountType.BUSINESS
      ? {
          name: this.organizationName,
          street: this.organizationStreet,
          houseNumber: this.organizationHouseNumber,
          city: this.organizationLocation,
          zip: this.organizationZip,
          country: this.organizationCountry,
        }
      : {
          name: `${this.firstname} ${this.surname}`,
          street: this.street,
          houseNumber: this.houseNumber,
          city: this.location,
          zip: this.zip,
          country: this.country,
        };
  }
```

--- PR #1419 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:        true, |      ); | +    const networkStartFee = allowExpiredPrice ? undefined : await this.getNetworkStartFee(to, user);
David: Parallelize calls

--- PR #1424 | src/subdomains/generic/kyc/services/integration/document-storage.service.ts ---
Context: +      const blob = await this.downloadFile(sourceUserDataId, type, name); | +      await this.uploadFile(targetUserDataId, type, name, blob.data, file.contentType, file.metadata); | +    }
David: This uploads spider files to user folder?

--- PR #1424 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:      } | +    if (slave.kycSteps.some((k) => k.type === KycStepType.VIDEO && k.status === KycStepStatus.COMPLETED)) { | +      master.identificationType = KycIdentificationType.VIDEO_ID;
David: `k.isCompleted`

--- PR #1424 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +    // copy all documents | +    await this.documentStorageService.copyFiles(slave.id, master.id); | +
David: This should be done asynchronically (with error handling), could take veery long 🤔

--- PR #1425 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: + | +    const existingIssue = await this.supportIssueRepo.findOneBy({ | +      userData: { id: userData.id },
David: `.existsBy`

--- PR #1425 | src/subdomains/supporting/support-issue/services/limit-request-job.service.ts ---
Context: +      await this.supportIssueService.createIssueInternal(entity.userData, { | +        name: entity.userData.firstname ?? '-', | +        type: SupportIssueType.LIMIT_REQUEST,
David: `completeName`?

--- PR #1425 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context:   | +    // limit request | +    if (dto.limitRequest) await this.limitRequestService.increaseLimitInternal(dto.limitRequest, userData);
David: Create the limit request with support issue? Or is this updated when removing the limit request API?

--- PR #1425 | src/subdomains/supporting/support-issue/services/limit-request-job.service.ts ---
Context: +      await this.supportIssueService.createIssueInternal(entity.userData, { | +        name: entity.userData.firstname ?? '-', | +        type: SupportIssueType.LIMIT_REQUEST,
David: Ok, I just thought that most users will probably enter their full name when creating a support issue.

--- PR #1425 | src/subdomains/supporting/support-issue/services/limit-request.service.ts ---
Context:   | +    return void this.increaseLimitInternal(dto, user); | +  }
David: I think this doesn't wait 🤔  just use `await this.increaseLimitInternal(dto, user);`

--- PR #1429 | src/shared/i18n/de/mail.json ---
Context: +                "line4": "Alternativ kannst du auch über die DFX-Services Webseite <br> oben rechts auf den Menüpunkt und dann auf \"KYC\" klicken <br> und die Verifizierung über den \"Weiter\" Button starten.", | +                "line5": "Solltest du die Verifizierung nicht vornehmen wollen, wird das Geld vollautomatisch nach 2 Tagen retourniert." |              }
David: `... wird das Asset vollautomatisch nach ...`?
(also for other languages)

--- PR #1429 | src/subdomains/core/aml/aml-helper.service.ts ---
Context:        if (!entity.cryptoInput.isConfirmed) errors.push(AmlError.INPUT_NOT_CONFIRMED); | +      if (entity.inputAsset === 'XML' && entity.userData.kycLevel < KycLevel.LEVEL_30) | +        errors.push(AmlError.KYC_LEVEL_FOR_ASSET_NOT_REACHED);
David: Find the error 🤣

--- PR #1435 | src/subdomains/generic/kyc/services/kyc-admin.service.ts ---
Context: -      }); | +    if (kycStep.isCompleted) { | +      kycStep.userData = await this.kycService.completeIdent(kycStep.getResult<IdentResultDto>(), kycStep.userData);
David: Save userData?

--- PR #1435 | src/subdomains/generic/kyc/services/kyc-admin.service.ts ---
Context: -      }); | +    if (kycStep.isCompleted) { | +      kycStep.userData = await this.kycService.completeIdent(kycStep.getResult<IdentResultDto>(), kycStep.userData);
David: Check if ident step?

--- PR #1436 | src/subdomains/supporting/payout/services/payout.service.ts ---
Context:      try { | +      if (request.amount < 0) throw new BadRequestException('Amount is lower 0'); | +
David: `throw new Error(...`

--- PR #1438 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context:    RECEIVER_REJECTED = 'ReceiverRejected', | +  ABROAD_CHF_NOT_ALLOWED = 'AbroadChfNotAllowed', |  }
David: CHF_ABROAD_...?

--- PR #1439 | src/subdomains/generic/kyc/services/integration/ident.service.ts ---
Context:   | +    const pendingVideoIdent = user.getPendingStepWith(KycStepName.IDENT, KycStepType.VIDEO); | +    if (pendingVideoIdent.status !== KycStepStatus.CANCELED && kycStep.type === KycStepType.VIDEO)
David: ```ts
const steps = user.getStepsWith(KycStepName.IDENT, KycStepType.VIDEO);
if (steps.some((s) => !s.isCompleted && !s.isFailed))
```

--- PR #1443 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:      totalFeeAmountChf: number, | -    maxNetworkFee: number, | +    feeConstraints: BuyCryptoFee,
David: Can we remove this parameter completely?

--- PR #1444 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context: +    const referencePrice = entity.inputAmount / entity.inputReferenceAmount; | +    const networkStartFee = (entity instanceof BuyCrypto && entity.networkStartFeeAmount) ?? 0; | +    const blockchainFee = entity.blockchainFee ?? 0;
David: `const networkStartFee = (entity instanceof BuyCrypto && entity.networkStartFeeAmount) || 0;` (also triggers on `false`)

--- PR #1444 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context: -          : 0, | +      network: blockchainFee ? Util.roundReadable(blockchainFee * referencePrice, isFiat(entity.inputAssetEntity)) : 0, |        dfx:
David: Check is unnecessary?

--- PR #1444 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context: +      networkStart: networkStartFee | +        ? Util.roundReadable(networkStartFee * referencePrice, isFiat(entity.inputAssetEntity)) | +        : 0,
David: Check is unnecessary?

--- PR #1445 | src/shared/models/asset/asset.entity.ts ---
Context:   | +  @Column({ default: false }) | +  payment: boolean;
David: `paymentEnabled`?

--- PR #1445 | src/shared/services/process.service.ts ---
David: Revert?

--- PR #1445 | src/subdomains/core/payment-llink/dto/create-payment-link-payment.dto.ts ---
Context: +  @ApiProperty() | +  @IsOptional() | +  expiryDate: Date;
David: ```ts
  @ApiPropertyOptional()
  @IsOptional()
  @IsDate()
  @Type(() => Date)
```

--- PR #1445 | src/subdomains/core/payment-llink/dto/create-payment-link.dto.ts ---
Context: +  @ApiProperty() | +  @IsNumber() | +  route: number;
David: `@IsNotEmpty()`

