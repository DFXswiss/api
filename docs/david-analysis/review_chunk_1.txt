--- PR #474 | src/payment/models/payout/strategies/prepare/impl/ethereum.strategy.ts ---
Context: + | +  protected getFeeAsset(): Promise<Asset> { | +    return this.assetService.getAssetByQuery({ dexName: 'ETH', blockchain: Blockchain.ETHEREUM, type: AssetType.COIN });
David: Should we put the fee asset descriptors in a central place? It felt like I saw them 100 times 😆

--- PR #475 | src/payment/models/crypto-input/crypto-input.entity.ts ---
Context:   | +  @Column({ type: 'float' }) | +  forwardFeeAmount: number;
David: This should probably be nullable: true, since it's set only after creation?

--- PR #475 | src/payment/models/crypto-input/btc-input.service.ts ---
Context:      ); | -    await this.cryptoInputRepo.update({ id: input.id }, { outTxId }); | +    await this.cryptoInputRepo.update({ id: input.id }, { outTxId, forwardFeeAmount: feeAmount });
David: I would do the same also for defi-input.service. It's not really important, but brings consistency.

--- PR #476 | src/user/models/limit-request/limit-request.entity.ts ---
Context:    userData: UserData; | + | +  //
David: ? 🤔

--- PR #476 | src/user/models/limit-request/limit-request.entity.ts ---
Context: + | +  // | +  limitRequestMailSendDate(): UpdateResult<LimitRequest> {
David: `mailSent`?

--- PR #476 | src/notification/entities/mail/base/mail.ts ---
Context: +    return this.#banner; | +  } | +
David: I would move this to personal-mail and try to use it in the body. Would this be possible?

--- PR #476 | src/user/models/limit-request/limit-request-notification.service.ts ---
Context: +  ) {} | + | +  @Interval(60000)
David: Can probably be reduced to five minutes? 😉

--- PR #476 | src/user/models/limit-request/limit-request-notification.service.ts ---
Context: +    }); | + | +    entities.length > 0 && console.log(`Sending ${entities.length} 'limit-request initiated' email(s)`);
David: `limit-request accepted`?

--- PR #476 | src/user/models/limit-request/limit-request-notification.service.ts ---
Context: +          }); | +        } else { | +          console.error(`Failed to send buy fiat mails ${entity.id}: user has no email`);
David: `Failed to send limit request accepted mail ${entity.id}: ...`

--- PR #476 | src/user/models/limit-request/limit-request-notification.service.ts ---
Context: +        await this.limitRequestRepo.update(...entity.limitRequestMailSendDate()); | +      } catch (e) { | +        console.error(e);
David: Add a message:
```ts
console.error('Failed to send limit request accepted mail ${entity.id}:', e);
```

--- PR #476 | src/user/models/limit-request/limit-request.entity.ts ---
Context: + | +  // | +  limitRequestMailSendDate(): UpdateResult<LimitRequest> {
David: Hehe 😄  I meant the method could be renamed to mailSent. The current name is a bit strange 😉

--- PR #476 | src/notification/entities/mail/error-monitoring-mail.ts ---
Context: +          body: ErrorMonitoringMail.createBody(params.errors), | +        }, | +      },
David: ```ts
      templateParams: {
        ...defaultParams,
        salutation: 'Hi DFX Tech Support',
        body: ErrorMonitoringMail.createBody(params.errors),
      },
```
or just
```ts
      templateParams: {
        date: new Date().getFullYear(),
        salutation: 'Hi DFX Tech Support',
        body: ErrorMonitoringMail.createBody(params.errors),
      },
```

--- PR #476 | src/notification/entities/mail/kyc-support-mail.ts ---
Context:    kycCustomerId: number; |    kycStatus: string; | +  date?: number;
David: I think we can remove this.

--- PR #476 | src/notification/entities/mail/error-monitoring-mail.ts ---
Context:    subject: string; |    errors: string[]; | +  date?: number;
David: I think we can remove this

--- PR #476 | src/notification/entities/mail/kyc-support-mail.ts ---
Context: +          body: KycSupportMail.createBody(params), | +        }, | +      },
David: Same as above

--- PR #483 | src/payment/models/history/history.service.ts ---
Context:        all || query.ref != null ? await this.getAllRefRewards(userId, query.from, query.to) : Promise.resolve([]), |        //all || query.lm != null ? await this.getDfiTaxRewards(userAddress, DfiTaxInterval.DAY, query.from, query.to, timeout): Promise.resolve([]), |      ]);
David: Should we do the duplicate check only once on all transactions?
```ts
this.fixDuplicateTx(transactions)
   .reduce(...)
```

--- PR #489 | src/payment/models/payout/strategies/payout/impl/base/jellyfish.strategy.ts ---
Context: +    const ordersWithSamePayoutTxId = await this.payoutOrderRepo.find({ payoutTxId: order.payoutTxId }); | + | +    if (ordersWithSamePayoutTxId.length === 1) return totalPayoutFee;
David: This line is actually not required 😉

--- PR #490 | src/payment/models/crypto-staking/crypto-staking.service.ts ---
Context: +    entity.payoutType = PayoutType.WALLET; | + | +    return await this.cryptoStakingRepo.save({ ...entity, ...dto });
David: Use update

--- PR #496 | src/payment/models/buy/buy.controller.ts ---
Context: +    const { fee, refBonus } = await this.userService.getUserBuyFee(userId, annualVolume); | + | +    return { fee, refBonus };
David: ```ts
return await this.userService.getUserBuyFee(userId, annualVolume);
```

--- PR #496 | src/payment/models/sell/dto/get-sell-payment-info.dto.ts ---
Context:   | +  @ApiProperty() | +  @IsOptional()
David: Add a TODO to switch to IsNotEmptyObject

--- PR #503 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +    for (const user of master.users) { | +      await this.userService.activateUser(user); | +    }
David: Only activate users, if one of the users was already active.

--- PR #503 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +    if (master.hasActiveUser) { | +      for (const user of master.users) { | +        if (user?.status === UserStatus.NA) await this.userRepo.update(user.id, { status: UserStatus.ACTIVE });
David: Move this logic to the UserRepository and reuse it from UserService?

--- PR #504 | src/shared/validators/and.validator.ts ---
Context: +import { ValidatorConstraint, ValidatorConstraintInterface, ValidationArguments } from 'class-validator'; | + | +@ValidatorConstraint({ name: 'AND', async: false })
David: Should we use custom validation decorators?

--- PR #504 | src/shared/validators/and.validator.ts ---
Context: +  validate(propertyValue: any, args: ValidationArguments) { | +    return ( | +      (!!propertyValue && args.object[args.constraints[0]]) || (propertyValue && !!args.object[args.constraints[0]])
David: Why so complicated?

--- PR #504 | src/subdomains/core/liquidity-management/controllers/balance.controller.ts ---
Context: + | +@ApiTags('liquidity-management') | +@Controller('liquidity-management/balance')
David: At DFX we use camelCase for URLs (doesn't really make sense, but ... 😉 )

--- PR #504 | src/subdomains/core/liquidity-management/controllers/order.controller.ts ---
Context: +@ApiTags('liquidity-management') | +@Controller('liquidity-management/order') | +export class LiquidityManagementOrderController {
David: Does it make sense to have a controller for each sub-topic?

--- PR #504 | src/subdomains/core/liquidity-management/adapters/balances/blockchain.adapter.ts ---
Context: +    }; | + | +    const liquidity = await this.dexService.checkLiquidity(request);
David: This is ignoring availability?

--- PR #504 | src/subdomains/core/liquidity-management/adapters/balances/blockchain.adapter.ts ---
Context: + | +  private async getForDeFiChain(asset: Asset): Promise<number> { | +    if (!this.defiChainCacheUpdateCall && Date.now() - this.defiChainCacheTimestamp > 30000) {
David: Could also use timestamp of type Date and use `Util.secondsDiff(this.defiChainCacheTimestamp, new Date())`

--- PR #504 | src/subdomains/core/liquidity-management/adapters/balances/blockchain.adapter.ts ---
Context: +      const { name, type, amount } = balance; | + | +      this.defiChainCache.set(JSON.stringify({ name, type }), amount);
David: A simple string concat would probably be more performant 😉

--- PR #504 | src/subdomains/core/liquidity-management/dto/input/liquidity-management-rule-creation.dto.ts ---
Context: +  @Validate(AND, ['redundancyActions']) | +  @IsNumber() | +  maximal: number;
David: Do we really need all these boolean validators? Can't it be done with a ValidateIf?

--- PR #504 | src/subdomains/core/liquidity-management/entities/liquidity-management-pipeline.entity.ts ---
Context: +    pipeline.ordersProcessed = 0; | +    pipeline.type = this.getPipelineType(verificationResult); | +    pipeline.targetAmount = verificationResult.liquidityDeficit || verificationResult.liquidityRedundancy;
David: `??`?

--- PR #504 | src/subdomains/core/liquidity-management/specifications/liquidity-management-rule-init.specification.ts ---
Context: +    if (rule.maximal && rule.maximal <= rule.optimal) { | +      throw new BadRequestException('Cannot created rule. Maximal liquidity configuration must be higher than optimal'); | +    }
David: Can't it be that minimal or maximal is equal to optimal?

--- PR #504 | src/subdomains/core/liquidity-management/services/liquidity-management-pipeline.service.ts ---
Context: +      this.processOrdersLock.release(); | +    } | +  }
David: No error logs?

--- PR #504 | src/subdomains/core/liquidity-management/services/liquidity-management-pipeline.service.ts ---
Context: +  //*** JOBS ***// | + | +  @Interval(60000)
David: Switch to `@Cron(CronExpression.EVERY_MINUTE)` (is more readable)

--- PR #504 | src/subdomains/core/liquidity-management/services/liquidity-management-rule.service.ts ---
Context: + | +    return this.confirmOrCreateActionTree(redundancyActions, 1); | +  }
David: These are the same?

--- PR #504 | src/subdomains/core/liquidity-management/dto/input/liquidity-management-rule-creation.dto.ts ---
Context: +  @Validate(AND, ['redundancyActions']) | +  @IsNumber() | +  maximal: number;
David: ```ts
  @ValidateIf((dto) => dto.maximal != null || dto.minimal == null || dto.redundancyActions)
  @IsNotEmpty()
  @IsNumber()
  maximal: number;
```

--- PR #508 | migration/1668437287846-removeUnusedBuyCryptoCols.js ---
Context: +        await queryRunner.query(`ALTER TABLE "dbo"."deposit_route" DROP CONSTRAINT "FK_416688df9433a5db75efe220cc1"`); | +        await queryRunner.query(`ALTER TABLE "dbo"."deposit_route" ALTER COLUMN "userId" int`); | +        await queryRunner.query(`ALTER TABLE "dbo"."deposit_route" ADD CONSTRAINT "FK_416688df9433a5db75efe220cc1" FOREIGN KEY ("userId") REFERENCES "user"("id") ON DELETE NO ACTION ON UPDATE NO ACTION`);
David: The change on deposit_route is not wanted. It's weird that these changes got picked up only now 🤔  we need to add a `{ nullable: false }` at the `user` field of ´CryptoRoute´ and re-run the migration

--- PR #510 | src/subdomains/generic/user/models/limit-request/limit-request.entity.ts ---
Context:  } | + | +export const LimitRequestStates = [LimitRequestDecision.ACCEPTED, LimitRequestDecision.PARTIALLY_ACCEPTED];
David: `LimitRequestAcceptedStates`

--- PR #510 | src/subdomains/generic/user/models/limit-request/limit-request.service.ts ---
Context:      const update = this.limitRequestRepo.create(dto); |   | +    if (LimitRequestAccepted(dto.decision)) this.kycWebhookService.kycChanged(entity.userData);
David: Is `userData` loaded?

--- PR #510 | src/subdomains/generic/user/models/limit-request/limit-request.service.ts ---
Context:      const update = this.limitRequestRepo.create(dto); |   | +    if (LimitRequestAccepted(dto.decision)) this.kycWebhookService.kycChanged(entity.userData);
David: Only trigger the webhook, if the decision changed?

--- PR #512 | src/subdomains/supporting/bank/fiat-output/dto/create-fiat-output.dto.ts ---
Context: +  @IsOptional() | +  @Type(() => BuyFiat) | +  buyFiat: BuyFiat;
David: Use `buyFiatId` for Google Sheets

--- PR #512 | src/subdomains/core/sell-crypto/buy-fiat/buy-fiat.service.ts ---
Context: +    try { | +      const buyFiatWithoutOutput = await this.buyFiatRepo.find({ | +        where: { amlCheck: AmlCheck.PASS, fiatOutput: IsNull() },
David: I think we need `fiatOutput` in relations?

--- PR #512 | src/subdomains/core/sell-crypto/buy-fiat/buy-fiat.service.ts ---
Context: + | +    try { | +      const buyFiatWithoutOutput = await this.buyFiatRepo.find({
David: `buyFiatsWithoutOutput` 😉

--- PR #512 | src/subdomains/core/sell-crypto/buy-fiat/buy-fiat.service.ts ---
Context: + | +      for (const buyFiat of buyFiatWithoutOutput) { | +        const fiatOutput = await this.fiatOutputService.create({ buyFiat: buyFiat } as CreateFiatOutputDto);
David: Add a reason (`BuyFiat`)

--- PR #512 | src/subdomains/core/sell-crypto/buy-fiat/buy-fiat.service.ts ---
Context: +        await this.buyFiatRepo.update(buyFiat.id, { | +          fiatOutput, | +        });
David: This shouldn't be necessary 🤔

--- PR #512 | src/subdomains/supporting/bank/fiat-output/fiat-output.entity.ts ---
Context: + | +  @Column({ length: 256, nullable: true }) | +  reason: string;
David: Maybe `type` is a better naming than `reason`, WDYT? Sorry, `reason` was my proposition 😇

--- PR #512 | src/subdomains/supporting/bank/fiat-output/fiat-output.entity.ts ---
Context: + | +  @Column({ length: 256, nullable: true }) | +  reason: string;
David: Reason should be required IMO (also in DTO)

--- PR #512 | src/subdomains/core/sell-crypto/buy-fiat/buy-fiat.service.ts ---
Context: +  // --- CHECK BUY FIAT --- // | +  @Cron(CronExpression.EVERY_10_MINUTES) | +  async checkBuyFiats(): Promise<void> {
David: Use a more descriptive name, e.g. `addFiatOutputs` (also in the log entry)?

--- PR #512 | src/subdomains/core/sell-crypto/buy-fiat/buy-fiat.service.ts ---
Context: +          buyFiat: buyFiat, | +          reason: 'BuyFiat', | +        } as CreateFiatOutputDto);
David: The `as CreateFiatOutputDto` is not required anymore

--- PR #512 | src/subdomains/supporting/bank/fiat-output/fiat-output.entity.ts ---
Context:    @OneToOne(() => BuyFiat, (buyFiat) => buyFiat.fiatOutput, { nullable: true }) | -  buyFiat?: BuyFiat; | +  buyFiatId?: BuyFiat;
David: What? 🤔

--- PR #513 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +  recordFee(fee: BuyCryptoFee): this { | +    this.fee = fee; | +    this.status = BuyCryptoStatus.IN_PROGRESS;
David: This logic is kind of hidden (status change). Maybe we should name the method differently?

--- PR #513 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +  private resetTransaction(): this { | +    this.outputReferenceAmount = null; | +    this.outputReferenceAsset = null;
David: Output reference is set after batching?

--- PR #513 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +  recordFee(fee: BuyCryptoFee): this { | +    this.fee = fee; | +    this.status = BuyCryptoStatus.IN_PROGRESS;
David: Ok, makes sense 👍

--- PR #513 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +  private resetTransaction(): this { | +    this.outputReferenceAmount = null; | +    this.outputReferenceAsset = null;
David: Ok, cool

--- PR #517 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context: +  get isExternalUser(): boolean { | +    return this.kycType !== KycType.DFX; |    }
David: I would invert it to isDfxUser, makes more sense IMO.

--- PR #517 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context:      const completedUser = users.find((data) => KycCompleted(data.kycStatus)); | -    if (completedUser && !user.hasExternalUser) { | +    if (completedUser && !user.isExternalUser) {
David: Both users must be DFX users (master must always be DFX, and slave can only be external on data transfer)

--- PR #517 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context:      const completedUser = users.find((data) => KycCompleted(data.kycStatus)); | -    if (completedUser && !user.hasExternalUser) { | +    if (completedUser && !user.isExternalUser) {
David: Do the check, if the completedUser is DFX, on line 215

--- PR #518 | src/subdomains/generic/user/models/kyc/kyc.controller.ts ---
Context: +  async getKycCountries(@Param('code') code: string): Promise<Country[]> { | +    return await this.kycService.getKycCountries(code); | +  }
David: Add the same API call with JWT instead of code.

--- PR #518 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context:      ]); |      if (!country || (!isPersonalAccount && !organizationCountry)) throw new BadRequestException('Country not found'); | +    if ((!country.dfxEnable && user.kycType === KycType.DFX) || (!country.lockEnable && user.kycType === KycType.LOCK))
David: Maybe move this logic to the country entity?

--- PR #518 | src/shared/models/country/country.entity.ts ---
Context: +    switch (kycType) { | +      case KycType.DFX: | +        return this.dfxEnable ? true : false;
David: `return this.dfxEnable`?

--- PR #518 | src/shared/models/country/country.entity.ts ---
Context: + | +      case KycType.LOCK: | +        return this.lockEnable ? true : false;
David: `return this.lockEnable`?

--- PR #518 | src/subdomains/generic/user/models/kyc/kyc.controller.ts ---
Context: +  @ApiBearerAuth() | +  @UseGuards(AuthGuard(), new RoleGuard(UserRole.USER)) | +  @ApiResponse({ status: 201, type: Country, isArray: true })
David: `status: 200`

--- PR #518 | src/subdomains/generic/user/models/kyc/kyc.controller.ts ---
Context:   | +  @Get(':code/countries') | +  async getKycCountriesByCode(@Param('code') code: string): Promise<Country[]> {
David: Missing ApiResponse docu

--- PR #518 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: + | +    return await this.countryService.getCountriesByKycType(userData.kycType); | +  }
David: Combine these methods and use `this.getUser` to get the user (similar to `updateKycData`)

--- PR #518 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context:    async updateKycData(code: string, data: KycUserDataDto, userId?: number): Promise<KycInfo> { |      let user = await this.getUser(code, userId); | +    if (user.kycStatus !== KycStatus.NA) throw new BadRequestException('Kyc already started');
David: **KYC** already started

--- PR #519 | src/subdomains/supporting/bank/bank-tx-repeat/bank-tx-repeat.service.ts ---
Context: +    if (entity) throw new BadRequestException('BankTx already used'); | + | +    entity = this.bankTxRepeatRepo.create({ bankTx });
David: What? 🤔

--- PR #519 | src/subdomains/supporting/bank/bank-tx-repeat/bank-tx-repeat.service.ts ---
Context: +      if (!update.chargebackBankTx) throw new NotFoundException('ChargebackBankTx not found'); | + | +      const existingReturnForChargeback = await this.bankTxRepeatRepo.findOne({
David: existing**Repeat**ForChargeback?

--- PR #519 | src/subdomains/supporting/bank/bank-tx-repeat/bank-tx-repeat.service.ts ---
Context: +      if (!update.sourceBankTx) throw new NotFoundException('SourceBankTx not found'); | + | +      const existingSourceBankTx = await this.bankTxRepeatRepo.findOne({
David: existingRepeatForSource?

--- PR #519 | src/subdomains/supporting/bank/bank-tx-repeat/bank-tx-repeat.service.ts ---
Context: +      }); | +      if (existingSourceBankTx) throw new BadRequestException('SourceBankTx already used'); | +    }
David: No update on SourceBankTx needed?

--- PR #521 | migration/1669043161178-liquidityOrdersRefactoring.js ---
Context: +        await queryRunner.query(`ALTER TABLE "liquidity_order" ADD "txId" nvarchar(256)`); | +        await queryRunner.query(`ALTER TABLE "liquidity_order" ADD "feeAmount" float`); | +        await queryRunner.query(`ALTER TABLE "liquidity_order" ADD "feeAssetId" int`);
David: Rename instead of recreate?

--- PR #521 | src/subdomains/core/liquidity-management/adapters/balances/blockchain.adapter.ts ---
Context: +      type: e[0].split('_')[1] as AssetType, | +      amount: e[1], | +    }));
David: Why do we need the aggregation?

--- PR #521 | src/subdomains/supporting/dex/strategies/sell-liquidity/impl/defichain-coin.strategy.ts ---
Context: +      type: AssetType.TOKEN, | +    }); | +  }
David: Just an idea. What about adding some commonly used assets directly to asset service? Like `assetService.getDfiToken`?

--- PR #521 | src/subdomains/supporting/dex/strategies/sell-liquidity/impl/defichain-token.strategy.ts ---
Context: +  private getDefaultTargetAsset(sellAsset: Asset): string { | +    return sellAsset.category === AssetCategory.CRYPTO ? 'DFI' : 'DUSD'; | +  }
David: Move this to line 69 😉

--- PR #530 | src/subdomains/supporting/dex/services/dex.service.ts ---
Context: +        { targetAsset: asset, isReady: false }, | +        { swapAsset: asset, isComplete: false }, | +        { swapAsset: asset, isReady: false },
David: It might be more performant to only filter by ID, e.g.
```ts
{ targetAsset: { id: asset.id }, isComplete: false },
```

--- PR #531 | src/subdomains/supporting/log/log.controller.ts ---
Context: +  @ApiBearerAuth() | +  @ApiExcludeEndpoint() | +  @UseGuards(AuthGuard(), new RoleGuard(UserRole.ADMIN))
David: Use `BANKING_BOT` role

--- PR #531 | src/subdomains/subdomains.module.ts ---
Context: +  imports: [TypeOrmModule.forFeature([LogRepository]), CoreModule, GenericModule, SupportingModule], | +  controllers: [LogController], | +  providers: [LogService],
David: Create a Log Module

--- PR #534 | src/subdomains/generic/user/models/limit-request/limit-request.service.ts ---
Context: +          subject: 'LimitRequest', | +          salutation: 'New LimitRequest', | +          body: `<p>Limit: ${entity.limit} EUR</p>Investment date: ${entity.investmentDate}<p>Fund origin: ${entity.fundOrigin}</p>`,
David: Do we need some user info here?

--- PR #534 | src/subdomains/generic/user/models/limit-request/limit-request.service.ts ---
Context: +      }) | +      .catch((error) => console.error(`Failed to send limitRequest created mail:`, error)); | +
David: Maybe send the notification after saving? And add the id to the error log?

--- PR #534 | src/subdomains/supporting/notification/enums/index.ts ---
Context:    USER = 'User', |    PERSONAL = 'Personal', | +  INTERNAL = 'Internal',
David: Is INTERNAL the right name? 😉  KYC_SUPPORT and ERROR_MONITORING are also internal.

--- PR #535 | src/subdomains/generic/gs/dto/db-query.dto.ts ---
Context: +import { IsBoolean, IsDate, IsNotEmpty, IsNumber, IsOptional, IsString } from 'class-validator'; | + | +export class dbQueryDto {
David: `DbQueryDto`

--- PR #535 | src/subdomains/generic/gs/gs.controller.ts ---
Context: +  async getRawData(@Body() query: dbQueryDto): Promise<any> { | +    return await this.gsService.getRawData(query); | +  }
David: I think I would have two separate routes for old and new logic, makes it much easier to read and refactor. WDYT?

--- PR #535 | src/subdomains/generic/gs/gs.controller.ts ---
Context: +  async getSupportData(@Query('id') id: string): Promise<SupportReturnData> { | +    return await this.gsService.getSupportData(+id); | +  }
David: What about the legacy routes in the AdminController. Are they not removed?

--- PR #535 | src/subdomains/generic/gs/gs.controller.ts ---
Context: +  async getRawData(@Body() query: dbQueryDto): Promise<any> { | +    return await this.gsService.getRawData(query); | +  }
David: We can then change all existing to the new scheme and remove the old one.

--- PR #535 | src/subdomains/generic/gs/gs.service.ts ---
Context: +      if (query.select) request.select(query.select); | + | +      for (const where of query?.where) {
David: `?` ist not required

--- PR #535 | src/subdomains/generic/gs/gs.service.ts ---
Context: +      } | + | +      for (const join of query?.join) {
David: `?` ist not required

--- PR #535 | src/subdomains/generic/gs/gs.service.ts ---
Context: +                    .replace('_' + e.split('_')[1].split('')[0], e.split('_')[1].split('')[0].toLocaleUpperCase()) | +                : e, | +            ),
David: This looks very complex 😆  maybe move to a helper method?

--- PR #535 | src/subdomains/generic/gs/dto/db-query.dto.ts ---
Context: +  // Comma separated column names | +  @IsOptional() | +  select?: string[];
David: Why is this `string[]`, if it is comma separated? I would go for string array, as we are using a body here.

--- PR #535 | src/subdomains/generic/gs/gs.service.ts ---
Context: +    switch (query.table) { | +      case 'bank_tx': | +        return this.getExtendedBankTxData(query);
David: Transformation to array missing?

--- PR #535 | src/subdomains/generic/gs/gs.controller.ts ---
Context: +  async getBankTxExtendedData(@Body() query: DbQueryBaseDto): Promise<any> { | +    return await this.gsService.getExtendedData(query); | +  }
David: It's a bit weird to have a dedicated endpoint, but a generic service method and DTO 😉

--- PR #535 | src/subdomains/generic/gs/gs.service.ts ---
Context: +            .replace('_' + e.split('_')[1].split('')[0], e.split('_')[1].split('')[0].toLocaleUpperCase()) | +        : e, | +    );
David: Does this work for the new generic endpoint? What does it actually do? 😆

--- PR #535 | src/subdomains/generic/gs/gs.service.ts ---
Context: +    const arrayData = this.transformResultArray(data, query.table); | + | +    return arrayData;
David: ```ts
return this.transformResultArray(data, query.table);
```

--- PR #536 | src/shared/models/asset/asset.service.ts ---
Context: -    return this.assetRepo.find({ where: { blockchain: In(blockchains) } }); | +    const where = blockchains.length > 0 ? { blockchain: In(blockchains) } : undefined; | +    return where ? this.assetRepo.find({ where: where }) : this.assetRepo.find();
David: ```ts
return blockchains.length > 0 ? this.assetRepo.find({ where: { blockchain: In(blockchains) } }) : this.assetRepo.find();
```
Looks simpler to me 😆

--- PR #536 | thunder-tests/thunderclient.json ---
Context:        "type": "json", | -      "raw": "{\n    \"address\": \"address\",\n    \"signature\": \"signature\"\n}", | +      "raw": "{\n    \"address\": \"df1qgkech2ha26r34mylm523c42d63er9pt5gyvq43\",\n    \"signature\": \"IEASE8bIcQLE7vjnDBvlMvvUWb4pdaiJnvd+qjhPRuzbXiWC1yZF9O7KPDiPz+lK+Tr8/tKASo2P15ly4VBBfZY=\"\n}",
David: Revert?

--- PR #536 | src/shared/auth/optional.guard.ts ---
Context: +export class OptionalJwtAuthGuard extends AuthGuard('jwt') { | +  // Override handleRequest so it never throws an error | +  handleRequest(err: any, user: any, info: any, context: any) {
David: ![image](https://user-images.githubusercontent.com/85513542/204318212-bc6afa10-29dc-4820-b4ba-5cf159c401ad.png)
Use underscored names to avoid warnings.

--- PR #538 | src/subdomains/core/liquidity-management/adapters/balances/blockchain.adapter.ts ---
Context: +    /** | +     * cleanup cache to remove tokens which balances went to 0 | +     */
David: Why this strange comment with ** ? 😉

--- PR #541 | src/app.controller.ts ---
Context:    } |   |    private getAgentDetails(req: Request): Details {
David: Method can be removed

--- PR #541 | src/subdomains/core/buy-crypto/route/buy.controller.ts ---
Context: -    if (asset?.category === AssetCategory.STOCK) return { fee: 0, refBonus: 0 }; | +  async getFees(userId: number, asset: Asset): Promise<{ fee: number }> { | +    if (asset?.category === AssetCategory.STOCK) return { fee: 0 };
David: Move this also into `userService. getUserBuyFee`, to have everything in one place?

--- PR #541 | src/subdomains/core/sell-crypto/sell/sell.controller.ts ---
Context:        asset = await this.assetService.getAssetById(asset.id); | -      if (asset?.category === AssetCategory.STOCK) return 0; | +      if (asset?.category === AssetCategory.STOCK) return { fee: 0 };
David: Move this also into `userService. getUserSellFee`, to have everything in one place?

--- PR #541 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: +        Config.defaultPercentageDecimal, | +      ), | +    };
David: Maybe split this up, to make it more readable?
```ts
const defaultFee = userData.accountType === AccountType.PERSONAL
   ? Config.buy.fee.private[asset.feeTier]
   : Config.buy.fee.organization[asset.feeTier];

return {
   fee: Util.round((buyFee ?? defaultFee) * 100, Config.defaultPercentageDecimal),
}
```

--- PR #541 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: +        Config.defaultPercentageDecimal, | +      ), |      };
David: Same here

--- PR #546 | src/subdomains/core/liquidity-management/services/liquidity-management-balance.service.ts ---
Context:   | -          throw e; | +          return null;
David: So we just ignore the uncertain asset instead of stopping the process?

--- PR #546 | src/subdomains/core/liquidity-management/adapters/balances/blockchain.adapter.ts ---
Context: +      this.setCache(tokensResult); | +    } catch { | +      this.invalidateCache();
David: No log?

--- PR #546 | src/subdomains/core/liquidity-management/services/liquidity-management-balance.service.ts ---
Context:   | -          throw e; | +          return null;
David: Cool, makes sense 👍

--- PR #548 | src/config/config.ts ---
Context:          }, |          default: { |            USD: 1,
David: This whole config doesn't really belong to blockchain, does it? I would move it somewhere else 😉 
(right now it's under `Config.blockchain.default...`

--- PR #548 | src/subdomains/core/buy-crypto/route/buy.controller.ts ---
Context: + | +    return Util.transformToMinDeposit(minAssetVolume[0] ?? Config.blockchain.default.minTransactionVolume.default); | +  }
David: Looks a bit complicated? Maybe just
```ts
const minAssetVolume = Config.blockchain.default.minTransactionVolume[outputAsset.blockchain]?.[outputAsset.name];
return Util.transformToMinDeposit(minAssetVolume ?? Config.blockchain.default.minTransactionVolume.default);
```

--- PR #548 | src/subdomains/core/sell-crypto/sell/sell.controller.ts ---
Context: +        : []; | + | +    return Util.transformToMinDeposit(minAssetVolume[0] ?? Config.blockchain.default.minTransactionVolume.default);
David: Same here.

--- PR #548 | src/subdomains/core/buy-crypto/route/buy.controller.ts ---
Context: + | +    return Util.transformToMinDeposit(minAssetVolume[0] ?? Config.blockchain.default.minTransactionVolume.default); | +  }
David: Can we move the methods `getTransactionVolume` and `transformToMinDeposit` to the config class? Would make more sense there 🤔

--- PR #549 | src/subdomains/generic/user/models/kyc/kyc-webhook.service.ts ---
Context:          }); |        } catch (error) { | -        console.error(`Exception during KYC webhook (${result}) for user ${userData.id}:`, error);
David: I would keep the log

--- PR #549 | src/subdomains/generic/user/models/kyc/kyc-webhook.service.ts ---
Context:          await this.http.post(`${user.wallet.apiUrl}/kyc/update`, data, { |            headers: { 'x-api-key': apiKey }, | +          timeout: 5000,
David: This is the HTTP call timeout, doesn't make much sense here 🤔

--- PR #549 | src/subdomains/generic/user/models/kyc/kyc-webhook.service.ts ---
Context: +            errors: [`Exception during KYC webhook (${result}) for user ${userData.id}:`, error], | +          }, | +        });
David: Not related to this change but it would generally be good to also log (and have in mail message) the user ID (not only user data ID).

--- PR #549 | src/subdomains/generic/user/models/kyc/kyc-webhook.service.ts ---
Context:          await this.http.post(`${user.wallet.apiUrl}/kyc/update`, data, { |            headers: { 'x-api-key': apiKey }, | +          timeout: 5000,
David: If we want to have a retry delay, we would need to adapt `HttpService`

--- PR #549 | src/shared/services/http.service.ts ---
Context:   | -export type HttpRequestConfig = AxiosRequestConfig & { tryCount?: number }; | +export type HttpRequestConfig = AxiosRequestConfig & { tryCount?: number; delay?: number };
David: `retryDelay`

--- PR #554 | src/subdomains/core/sell-crypto/sell/sell.service.ts ---
Context: +      relations: ['user', 'user.userData', 'user.userData.users', 'deposit'], | +    }); | +  }
David: Method already exists

--- PR #554 | src/subdomains/generic/gs/gs.service.ts ---
Context: +      return buy.user.userData; | +    } | +  }
David: Maybe remove all the `NotFoundException`, always use `?`s? Check is anyways done at line 76. Something like:
```ts
if (query.userDataId) {
      return await this.userDataService.getUserData(+query.userDataId);
    } else if (query.userAddress) {
      return await this.userService.getUserByAddress(query.userAddress).then((u) => u?.userData);
    } else if (query.depositAddress) {
      return await this.sellService.getSellByDepositAddress(query.depositAddress).then((s) => s?.user.userData);
    } ...
```

--- PR #554 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: +  async getUserByRef(ref: string): Promise<User> { | +    return await this.userRepo.findOne({ where: { ref }, relations: ['userData', 'userData.users'] }); | +  }
David: Method already exists

--- PR #556 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context:      try { | -      result = await this.http.get<{ kycId: string }>(`${wallet.apiUrl}/kyc/check`, { | +      result = await this.http.get<{ kycId: string }>(`${wallet.kycUrl}/kyc/check`, {
David: The `/kyc` is now part of the URL in the database?

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +    for (const user of userData.users) { | +      try { | +        const currentUrl = data instanceof KycWebhookDto ? user.wallet.kycUrl : user.wallet.paymentUrl;
David: For kycUrl we have `check` and `update` endpoints 🤔

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +    for (const user of userData.users) { | +      try { | +        const currentUrl = data instanceof KycWebhookDto ? user.wallet.kycUrl : user.wallet.paymentUrl;
David: `data instanceof KycWebhookDto` will probably not work, because we are not creating class instances.

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +          : null, | +    }; | +  }
David: Maybe easier (and more readable) to have a single switch? 😉 Like
```ts
return payment instanceof BuyFiat
   ? { ... }
   : { ... };
```

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +      paymentReference: | +        payment instanceof BuyFiat | +          ? payment.sell.deposit.address
David: For FiatFiat payments we need to get the input bank usage. Maybe create a todo here?

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: + | +export enum KycWebhookResult { | +  STATUS_CHANGED = 'StatusChanged',
David: We should rename this to `DataChanged`. Also on LOCK side 🤔

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +      return KycWebhookStatus.NA; | +    } | +  }
David: This can be simplified (without spider data). KYC status is full, if DFX KYC was done, else light.

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +  async paymentUpdate(userData: UserData, payment: BuyFiat | BuyCrypto, state: PaymentWebhookState): Promise<void> { | +    await this.triggerWebhook(userData, this.getPaymentWebhookData(payment, state)); | +  }
David: Maybe use two methods, one for BuyFiat and one for BuyCrypto? Then we don't need to do the `xxx instanceof yyy` thing.

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +  outputAsset: string; | +  paymentReference: string; | +}
David: I think it would be good to have the same DTO structure for all webhook DTOs

--- PR #556 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +    if (dto.inputAmount && dto.inputAsset) { | +      await this.webhookService.paymentUpdate( | +        entity.buy ? entity.buy.user.userData : entity.cryptoRoute.user.userData,
David: `entity.user.userData`

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +  outputAsset: string; | +  paymentReference: string; | +}
David: Maybe something like
```ts
export enum WebhookType {
  // TODO
}

export class WebhookDto<T> {
  id: string;
  type: WebhookType;
  data: T;
}

export class KycChangedWebhookDto extends WebhookDto<KycWebhookData> {}

export class KycFailedWebhookDto extends WebhookDto<KycWebhookData> {
  reason: string;
}

export class PaymentWebhookDto extends WebhookDto<PaymentWebhookData> {}
```

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +  private getPaymentWebhookData(payment: BuyFiat | BuyCrypto, state: PaymentWebhookState): PaymentWebhookDto { | +    return { | +      type: payment instanceof BuyFiat ? PaymentWebhookType.BUY_FIAT : PaymentWebhookType.BUY_CRYPTO,
David: Add id

--- PR #556 | src/subdomains/core/sell-crypto/buy-fiat/buy-fiat.service.ts ---
Context: +        ? await this.webhookService.cryptoFiatUpdate(entity.sell.user.userData, entity, PaymentWebhookState.COMPLETED) | +        : null; | +    }
David: Hm. We will send a both webhooks, if outputAmount and outputAsset is set, won't we? Maybe something like
```ts
if (dto.outputAmount && dto.outputAsset && entity.sell) {
   await this.webhookService.cryptoFiatUpdate(entity.sell.user.userData, entity, PaymentWebhookState.COMPLETED);
    } else if (dto.inputAmount && dto.inputAsset && entity.sell) {
   await this.webhookService.cryptoFiatUpdate(entity.sell.user.userData, entity, PaymentWebhookState.CREATED);
}
```

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +      } | +    } | +  }
David: Use generics:
```ts
private async triggerWebhook<T extends PaymentWebhookData | KycWebhookData>(
    userData: UserData,
    data: T,
    type: WebhookType,
    reason?: string,
  ): Promise<void> {
    userData.users = await this.userRepo.find({
      where: { userData: { id: userData.id } },
      relations: ['wallet', 'userData'],
    });

    for (const user of userData.users) {
      try {
        if (!user.wallet.isKycClient || !user.wallet.apiUrl) continue;

        const apiKey = this.walletService.getApiKeyInternal(user.wallet.name);
        if (!apiKey) throw new Error(`ApiKey for wallet ${user.wallet.name} not available`);

        const webhookDto: WebhookDto<T> = {
          id: user.address,
          type: type,
          data: data,
          reason: reason,
        };

        await this.http.post(user.wallet.apiUrl, webhookDto, {
          headers: { 'x-api-key': apiKey },
          retryDelay: 5000,
          tryCount: 3,
        });
      } catch (error) {
        const errMessage = `Exception during ${type} webhook for user ${user.id} & user data ${userData.id}:`;

        console.error(errMessage, error);

        await this.notificationService.sendMail({
          type: MailType.ERROR_MONITORING,
          input: {
            subject: `${type} webhook failed`,
            errors: [errMessage, error],
          },
        });
      }
    }
  }
```

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +} | + | +export class KycWebhookDto extends WebhookDto<KycWebhookData> {}
David: This is actually not needed 🤔

--- PR #556 | src/subdomains/generic/user/services/webhook/webhook.service.ts ---
Context: +} | + | +export class PaymentWebhookDto extends WebhookDto<PaymentWebhookData> {}
David: This is actually not needed 🤔

--- PR #559 | src/subdomains/supporting/bank/fiat-output/dto/update-fiat-output.dto.ts ---
Context: +  @IsOptional() | +  @IsNumber() | +  typeId?: number;
David: `typeId` is not really a fitting name, sounds like id of the type 😁 maybe `originEntityId` or something?

--- PR #559 | src/subdomains/supporting/bank/fiat-output/dto/update-fiat-output.dto.ts ---
Context: +  @IsOptional() | +  @IsString() | +  postalCode?: string;
David: We normally use `zip` (American english)

--- PR #563 | src/config/__tests__/config.spec.ts ---
Context: +    ]); | +  }); | +});
David: Add more tests?

--- PR #567 | src/shared/models/asset/asset.controller.ts ---
Context: -    return this.assetService.getAllAsset(queryBlockchains ?? jwt?.blockchains ?? []); | +  async getAllAsset(@GetJwt() jwt?: JwtPayload, @Query('blockchain') blockchain?: string): Promise<Asset[]> { | +    return this.assetService.getAllAsset(blockchain ? (blockchain as Blockchain) : jwt?.blockchain);
David: The query parameter should still be a list of blockchains

--- PR #567 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:    async signUp(dto: CreateUserDto, userIp: string): Promise<{ accessToken: string }> { | -    const existingUser = await this.userRepo.getByAddress(dto.address); | +    if (!dto.blockchain) dto.blockchain = this.cryptoService.getBlockchainsBasedOn(dto.address);
David: `dto.blockchain ??= this.cryptoService.getBlockchainsBasedOn(dto.address)`

--- PR #567 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context: -    const user = await this.userRepo.getByAddress(address); | +  async signIn({ address, signature, blockchain }: AuthCredentialsDto): Promise<{ accessToken: string }> { | +    if (!blockchain) blockchain = this.cryptoService.getBlockchainsBasedOn(address);
David: `dto.blockchain ??= this.cryptoService.getBlockchainsBasedOn(dto.address)`

--- PR #567 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:    async changeUser(id: number, changeUser: LinkedUserInDto): Promise<{ accessToken: string }> { | -    const user = await this.getLinkedUser(id, changeUser.address); | +    if (!changeUser.blockchain) changeUser.blockchain = this.cryptoService.getBlockchainsBasedOn(changeUser.address);
David: Can be removed here, as blockchain will be required in this case (see comment below)

--- PR #567 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:        role: user.role, | -      blockchains: this.cryptoService.getBlockchainsBasedOn(user.address), | +      blockchain: this.cryptoService.getBlockchainsBasedOn(user.address),
David: Take blockchain from user

--- PR #567 | src/subdomains/generic/user/models/auth/dto/auth-credentials.dto.ts ---
Context: +  @IsOptional() | +  @IsEnum(Blockchain) | +  blockchain: Blockchain;
David: I would use `@ApiProperty()` to make it clear that it should be required

--- PR #567 | src/subdomains/generic/user/models/user/dto/create-user.dto.ts ---
Context: +  @IsOptional() | +  @IsEnum(Blockchain) | +  blockchain: Blockchain;
David: Same here

--- PR #567 | src/subdomains/generic/user/models/user/dto/linked-user.dto.ts ---
Context: +  @IsOptional() | +  @IsEnum(Blockchain) | +  blockchain: Blockchain;
David: In this case we can make it required. This functionality is only used by payment page.

--- PR #567 | src/subdomains/generic/user/models/user/dto/linked-user.dto.ts ---
Context:    isSwitchable: boolean; |   | +  // TODO: Two times blockchain
David: Can be removed then

--- PR #567 | src/subdomains/generic/user/models/user/user.entity.ts ---
Context:   |  @Entity() | +@Index('blockchainAddress', (user: User) => [user.address, user.blockchain], { unique: true })
David: Remove the index name, is not really helpful
`@Index((user: User) => [user.address, user.blockchain], { unique: true })`

--- PR #567 | src/subdomains/generic/user/models/user/user.entity.ts ---
Context:    address: string; |   | +  @Column({ length: 256, nullable: true })
David: Should not be nullable. Maybe add a TODO to change this later?

--- PR #567 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: -  ) {} | +  ) { | +    this.setAllUserBlockchain();
David: Add a TODO to remove the code again

--- PR #567 | src/subdomains/generic/user/models/user/user.service.ts ---
Context:          return linkedUsers.map((u) => { | -          return { ...u, blockchains: this.cryptoService.getBlockchainsBasedOn(u.address) }; | +          return { ...u, blockchain: this.cryptoService.getBlockchainsBasedOn(u.address) };
David: Get blockchain from user

--- PR #567 | src/subdomains/generic/user/models/link/link.service.ts ---
Context: +      this.cryptoService.getBlockchainsBasedOn(linkAddress.newAddress), | +      true, | +    );
David: I think we need to extend the LinkedAddress entity with the blockchains.

--- PR #567 | src/subdomains/generic/user/models/auth/auth.controller.ts ---
Context:    @Get('signMessage') | -  getSignMessage(@Query('address') address: string): { message: string; blockchains: Blockchain[] } { | +  getSignMessage(@Query('address') address: string): { message: string; blockchain: Blockchain } {
David: I think we still need to return a list of blockchains here, because this address may be used to login for multiple chains.

--- PR #567 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:   |    // --- AUTH METHODS --- // |    async signUp(dto: CreateUserDto, userIp: string): Promise<{ accessToken: string }> {
David: We need to verify the provided blockchain fits the address. Maybe add as parameter to verifySignature?

--- PR #567 | src/integration/blockchain/ain/services/crypto.service.ts ---
Context: +    if (this.isBitcoinAddress(address)) return Blockchain.BITCOIN; | +    return Blockchain.DEFICHAIN; |    }
David: I would revert this change, because the address is still valid on multiple chains. Maybe add a second method for the default chain?
```ts
public getDefaultBlockchainBasedOn(address: string): Blockchain {
   return this.getBlockchainsBasedOn(address)[0];
}

--- PR #567 | src/integration/blockchain/ain/services/crypto.service.ts ---
Context:    public verifySignature(message: string, address: string, signature: string): boolean { | -    const blockchains = this.getBlockchainsBasedOn(address); | +    const blockchain = this.getBlockchainsBasedOn(address);
David: As stated before, it would probably make sense to add a blockchain parameter here, to include it in the validation.

--- PR #567 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: +      } | +    } | +  }
David: This does not really work, since it would assign all BSC users to Ethereum. Maybe we can find the right blockchain looking at the buy routes of the user?

--- PR #569 | src/mix/models/crypto-input/defi-input.service.ts ---
Context: +            //send back | +            try { | +              if (input.route.user.userData.mail) {
David: Did you check if UserData is loaded?

--- PR #569 | src/shared/i18n/de/mail.json ---
Context: +    "return": { | +      "salutation": "Das Enddatum des DFX-Staking-Service ist der 31. Dezember 2022 - neue Einzahlungen werden erstattet", | +      "body": "<table style=\"font-family:Open Sans,Helvetica,Arial,sans-serif;width:60%;padding:8px;font-size:85%\" align=\"center\"><tr><td colspan=\"3\" align=\"center\">Wir haben deine letzte DFX-Staking-Einzahlung an deine Wallet-Adresse zurückerstattet</td></tr><tr><td colspan=\"3\" align=\"center\">Neue Staking-Einzahlungen sind nicht mehr möglich.</td></tr><tr><td align=\"right\">Erstattet</td><td style=\"width:2%;\"></td><td align=\"left\">{inputAmount} {inputAsset}</td></tr><tr><td align=\"right\">Wallet Adresse</td><td style=\"width:2%;\"></td><td align=\"left\">***{userAddressTrimmed}</td></tr><tr><td colspan=\"3\" align=\"center\">Dies ist die Transaktions-ID: <a style=\"color:white\" href=\"{transactionLink}\">Link</a></td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"font-family:Open Sans,Helvetica,Arial,sans-serif\"><td colspan=\"3\" align=\"center\">Bei Fragen helfen wir dir gerne unter <a href=\"mailto:support@dfx.swiss\">support@dfx.swiss</a> weiter.</td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr></tr><tr style=\"font-family:Open Sans,Helvetica,Arial,sans-serif\"><td colspan=\"3\" align=\"center\">Herzlichen Dank für dein entgegengebrachtes Vertrauen</td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"font-family:Open Sans,Helvetica,Arial,sans-serif\"><td colspan=\"3\" align=\"center\">Dein freundliches DFX Team</td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr><td colspan=\"3\" align=\"center\"><span style=\"font-family:Zapfino\">Bitcoiners by heart ♥️</span></td></tr></table>",
David: Maybe we need to update the text for input transaction ID?

--- PR #572 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:   | -  complete(payoutTxId: string, payoutFee: number): this { | +  complete(payoutTxId: string, payoutFee: number | null): this {
David: Can it be null, if TX is complete?

--- PR #572 | src/subdomains/core/buy-crypto/process/entities/buy-crypto-fees.entity.ts ---
Context: +      this.actualPayoutFeeAmount = payoutFeeAmount; | +      this.actualPayoutFeePercent = Util.round(payoutFeeAmount / transaction.outputReferenceAmount, 8); | +    }
David: Can it still be null after payout is complete?

--- PR #572 | src/subdomains/core/buy-crypto/process/entities/buy-crypto-fees.entity.ts ---
Context: +      this.actualPayoutFeeAmount = payoutFeeAmount; | +      this.actualPayoutFeePercent = Util.round(payoutFeeAmount / transaction.outputReferenceAmount, 8); | +    }
David: ![image](https://user-images.githubusercontent.com/85513542/208694834-cd777c1c-62e0-40de-99b1-ff99493f0721.png)
Ok, maybe it would make sense to not call this method, if fee is null? Do the check in BuyCrypto?

--- PR #576 | src/subdomains/core/buy-crypto/process/services/buy-crypto-pricing.service.ts ---
Context:   | -    return Util.round(sourceAmount * result.price.price, 8); | +    return result.price.price ? Util.round(sourceAmount / result.price.price, 8) : 0;
David: Is it possible that price is 0 or undefined?

--- PR #586 | src/subdomains/supporting/bank/bank-tx/bank-tx.service.ts ---
Context: -    bankTx.type = dto.type; | - |      switch (bankTx.type) {
David: I think this is not a good idea, we will create entities (BuyCrypto, BankTxReturn, or BankTxRepeat) multiple times. Bank Tx type should not be changeable IMO 🤔

--- PR #594 | src/subdomains/generic/user/models/auth/dto/auth-credentials.dto.ts ---
Context:    signature: string; | + | +  @ApiProperty()
David: `@ApiPropertyOptional()`

--- PR #594 | src/subdomains/generic/user/models/auth/dto/auth-credentials.dto.ts ---
Context: +  @IsString() | +  @Matches(GetConfig().keyFormat) | +  key?: string;
David: Theoretically, we could use conditional validation:
```ts
  @ApiPropertyOptional()
  @IsRequired()
  @IsString()
  @Matches(GetConfig().keyFormat)
  @ValidateIf((dto: AuthCredentialsDto) => /stake[a-z0-9]{54}/.test(dto.address))
  key?: string;
```

--- PR #594 | src/subdomains/generic/user/models/auth/dto/auth-credentials.dto.ts ---
Context: +  @IsString() | +  @Matches(GetConfig().keyFormat) | +  key?: string;
David: Or make address methods (`isCardanoAddress`) on CryptoService `public static` and use them here.

--- PR #594 | src/subdomains/generic/user/models/user/dto/create-user.dto.ts ---
Context: +  @IsString() | +  @Matches(GetConfig().keyFormat) | +  key?: string;
David: Same here

--- PR #596 | src/subdomains/supporting/bank/bank-account/is-dfx-iban.validator.ts ---
Context: +import { | +  registerDecorator, | +  ValidationArguments,
David: Unused import

--- PR #596 | src/subdomains/supporting/bank/bank-account/is-dfx-iban.validator.ts ---
Context: +import ibantools from 'ibantools'; | + | +export const REQUEST_CONTEXT = '_requestContext';
David: Not necessary?

--- PR #596 | src/subdomains/supporting/bank/bank-account/is-dfx-iban.validator.ts ---
Context: + | +@ValidatorConstraint({ name: 'IsDfxIban', async: true }) | +@Injectable()
David: `@Injectable()` not necessary?

--- PR #596 | src/subdomains/supporting/bank/bank-account/is-dfx-iban.validator.ts ---
Context: +    } catch (e) { | +      return false; | +    }
David: Why this try-catch?

--- PR #596 | src/subdomains/supporting/bank/bank-account/is-dfx-iban.validator.ts ---
Context: +  async validate(iban: string) { | +    try { | +      iban = iban.split(' ').join('');
David: Is this required? Isn't this done by the `@Transform` before?

--- PR #596 | src/subdomains/supporting/bank/bank-account/is-dfx-iban.validator.ts ---
Context: + | +      // check blocked IBANs | +      const blockedIban = this.blockedIbans.find((i) => i.split(' ').join('').toLowerCase() === iban);
David: ```ts
const isBlocked = this.blockedIbans.some((i) => iban.toLowerCase().match(i.toLowerCase()) != null)
```

--- PR #596 | src/subdomains/supporting/bank/bank-account/is-dfx-iban.validator.ts ---
Context: +export const REQUEST_CONTEXT = '_requestContext'; | + | +@ValidatorConstraint({ name: 'IsDfxIban', async: true })
David: `async: false`?

--- PR #596 | src/subdomains/supporting/bank/bank-account/is-dfx-iban.validator.ts ---
Context: +    return `IBAN not valid`; | +  } | +}
David: If you want, you can return different messages for different errors. Add a validation function, which will return a string in case of error and undefined otherwise. Then call this method from `validate` and `defaultMessage`(use `ValidationArguments`).

--- PR #596 | src/subdomains/supporting/bank/bank-account/bank-account.service.ts ---
Context:      copyFrom?: BankAccount, |    ): Promise<BankAccount> { | +    if (!this.isValidIbanCountry(iban, kycType))
David: Missing `await`

--- PR #599 | src/config/config.ts ---
Context:      }, | +    Talium: { | +      apiKey: process.env.TALIUM_API_KEY,
David: Update Bicep?

--- PR #611 | src/config/config.ts ---
Context: +      minDeposit: { | +        USD: 1000, | +      },
David: These are already under `transaction.minVolume`

--- PR #611 | src/config/config.ts ---
Context: +      }, | +    }, | +  };
David: This is already under `blockchain.default.minDeposit`

--- PR #611 | src/subdomains/supporting/payin/services/base/payin-jellyfish.service.ts ---
Context: + | +    return { headers, blocks }; | +  }
David: Use method on NodeClient

--- PR #611 | src/integration/blockchain/shared/evm/evm-client.ts ---
Context: + | +  async getTokenBalance(token: Asset): Promise<number> { | +    return this.getTokenBalanceOfAddress(this.#dfxAddress, token);
David: Should we hard code the address in the client?

--- PR #611 | src/shared/models/asset/asset.service.ts ---
Context: +    return assets.find((a) => { | +      const queryMatch = a.dexName === dexName && a.blockchain === blockchain && a.type === type; | +      const chainIdMatch = !a.chainId ? true : chainId ? a.chainId.toLowerCase() === chainId.toLowerCase() : true;
David: `const chainIdMatch = a.chainId && chainId ? a.chainId.toLowerCase() === chainId.toLowerCase() : true;`?

--- PR #611 | src/shared/models/blockchain-address.ts ---
Context: + | +  @Column({ nullable: true }) | +  blockchain: Blockchain;
David: Should be `nullable: false`?

--- PR #611 | src/subdomains/core/buy-crypto/process/services/buy-crypto-registration.service.ts ---
Context: +      throw e; | +    } | +  }
David: Isn't it better to create the buy crypto before acknowledging the pay in?

--- PR #611 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: -  async createFromCrypto(cryptoInput: CryptoInput): Promise<BuyCrypto> { | -    const entity = this.buyCryptoRepo.create(); | +  // TODO -> remove commented code
David: TODO

--- PR #611 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: -  private readonly lock = new Lock(1800); | +  private readonly registerLock = new Lock(1800); | +  private readonly processLock = new Lock(1800);
David: Just thinking - maybe we should increase the lock timeouts?

--- PR #611 | src/subdomains/core/buy-crypto/routes/crypto-route/dto/crypto-history.dto.ts ---
Context: +import { HistoryDto } from '../../../../history/dto/history.dto'; | + | +export class CryptoHistoryDto extends HistoryDto {}
David: What is this for?

--- PR #611 | src/subdomains/core/monitoring/observers/payment.observer.ts ---
Context:    // *** HELPER METHODS *** // |   | +  // TODO -> clarify this method with Matthias
David: TODO

--- PR #611 | src/subdomains/core/sell-crypto/process/buy-fiat-registration.service.ts ---
Context: +      throw e; | +    } | +  }
David: Same here (acknowledging)

--- PR #611 | src/subdomains/core/sell-crypto/process/buy-fiat.service.ts ---
Context: +  @Cron(CronExpression.EVERY_MINUTE) | +  async checkCryptoPayIn() { | +    if ((await this.settingService.get('sell-crypto')) !== 'on') return;
David: Maybe we should also switch to disabled processes?

--- PR #611 | src/subdomains/core/sell-crypto/process/buy-fiat.service.ts ---
Context:        outputAmount: buyFiat.outputAmount, |        outputAsset: buyFiat.outputAsset, | +      // txId: buyFiat.cryptoInput.inTxId,
David: Why this?

--- PR #611 | src/subdomains/core/sell-crypto/process/buy-fiat.service.ts ---
Context:        date: v.outputDate, | +      // cryptoAmount: v.cryptoInput?.amount, | +      // cryptoCurrency: v.cryptoInput?.asset?.name,
David: Why this?

--- PR #611 | src/subdomains/core/sell-crypto/sell-crypto.module.ts ---
Context: +    // BankModule, | +    forwardRef(() => BankModule), | +    // PayInModule,
David: Remove commented code?

--- PR #611 | src/subdomains/core/staking/services/staking.service.ts ---
Context:    } |   | +  // stay
David: ?

--- PR #611 | src/subdomains/supporting/address-pool/deposit/dto/random-deposit.dto.ts ---
Context: +  @IsEnum(Blockchain) | +  blockchain: Blockchain; | +}
David: Good idea! Maybe add a count to it?

--- PR #611 | src/subdomains/supporting/address-pool/address-pool.module.ts ---
Context: +    SharedModule, | +  ], | +  controllers: [RouteController, DepositController],
David: Hm, RouteController does not really belong here 🤔

--- PR #611 | src/subdomains/supporting/address-pool/deposit/deposit.entity.ts ---
Context: +  //*** FACTORY METHODS ***// | + | +  static create(address: string, key: string, blockchain: Blockchain): Deposit {
David: `key` should be optional?

--- PR #611 | src/subdomains/supporting/payin/entities/crypto-input.entity.ts ---
Context: +  amlCheck: AmlCheck; | + | +  // TODO -> rename from type.
David: TODO

--- PR #611 | src/subdomains/supporting/payout/services/payout.service.ts ---
Context:   | -    const totalFeeAmount = Util.round(prepareFee.amount + payoutFee.amount, 8); | +    const totalFeeAmount = Util.round(prepareFee.amount + payoutFee.amount, 16);
David: Why this?

--- PR #611 | src/subdomains/supporting/payin/entities/crypto-input.entity.ts ---
Context: + | +    this.status = PayInStatus.FORWARDED; | +    this.isConfirmed = true;
David: Logic for confirmation was different before.

--- PR #611 | src/subdomains/supporting/payin/entities/crypto-input.entity.ts ---
Context: + | +@Entity() | +@Index('txAssetRouteTxSequence', (input: CryptoInput) => [input.inTxId, input.asset, input.route, input.txSequence], {
David: No custom naming for indexes

--- PR #611 | src/subdomains/supporting/payin/services/payin-bitcoin.service.ts ---
Context: +  } | + | +  async getUTXO(): Promise<UTXO[]> {
David: `getUtxo`?

--- PR #611 | src/subdomains/supporting/payin/services/payin.service.ts ---
Context: +      // relations: ['buyFiat', 'cryptoStaking', 'route', 'route.user'], | +    }); | +  }
David: TODO

--- PR #611 | src/subdomains/supporting/payin/services/payin.service.ts ---
Context: + | +    return prices.filter((p) => p).map((p) => p.price); | +  }
David: Unused method?

--- PR #611 | src/subdomains/supporting/payin/strategies/register/impl/base/evm.strategy.ts ---
Context: +          blockchain: this.blockchain, | +          type: AssetType.TOKEN, | +          chainId: tx.contractAddress,
David: Is the chainId required here?

--- PR #611 | src/subdomains/supporting/payin/strategies/register/impl/bitcoin.strategy.ts ---
Context: +        const btcAmount = entry.amount; | + | +        // TODO -> not sure if we should restrict this for Bitcoin (what if defichain node is OFF)
David: TODO

--- PR #611 | src/subdomains/supporting/payin/strategies/register/impl/defichain.strategy.ts ---
Context: +        const usdtAmount = | +          entry.asset.dexName === 'DUSD' | +            ? entry.amount
David: Temporary DUSD fix can be removed

--- PR #611 | src/subdomains/supporting/payin/strategies/register/impl/defichain.strategy.ts ---
Context: + | +    return p; | +  }
David: Where do we check for sellable on other blockchains?

--- PR #611 | src/subdomains/supporting/payin/strategies/register/impl/optimism.strategy.ts ---
Context: +          blockchain: Blockchain.ETHEREUM, | +          type: entry.asset.type, | +        });
David: All assets from Ethereum blockchain?

--- PR #611 | src/subdomains/supporting/payin/strategies/send/impl/arbitrum-coin.strategy.ts ---
Context: +    return this.assetService.getAssetByQuery({ | +      dexName: asset.dexName, | +      blockchain: Blockchain.ETHEREUM,
David: Ethereum blockchain?

--- PR #611 | src/subdomains/core/buy-crypto/routes/crypto-route/__tests__/crypto-route.controller.spec.ts ---
Context:   | -  it('should return a min deposit of 0.0005 for a default crypto route', async () => { | +  it.skip('should return a min deposit of 0.0005 for a default crypto route', async () => {
David: TODO?

--- PR #611 | src/subdomains/supporting/payin/strategies/send/impl/base/evm.strategy.ts ---
Context: +            nativeFee > Config.blockchain.evm.minimalPreparationFee | +              ? nativeFee | +              : Config.blockchain.evm.minimalPreparationFee;
David: `const effectivePreparationFee = Math.max(nativeFee, Config.blockchain.evm.minimalPreparationFee);`

--- PR #611 | src/subdomains/supporting/payin/strategies/send/impl/defichain-token.strategy.ts ---
Context: +        await this.deFiChainService.sendToken(savedPayIn, (outTxId: string) => | +          // TODO -> this have to be tested carefully | +          this.updatePayInWithSendData(savedPayIn, type, outTxId),
David: No send?

--- PR #611 | src/subdomains/supporting/payin/strategies/send/impl/defichain-token.strategy.ts ---
Context: + | +        const savedPayIn = await this.payInRepo.save(payIn); | +        await this.deFiChainService.sendToken(savedPayIn, (outTxId: string) =>
David: Switch to prepare as well?

--- PR #611 | src/subdomains/supporting/address-pool/deposit/deposit.entity.ts ---
Context: + | +@Entity() | +@Index('oneDepositPerBlockchain', (deposit: Deposit) => [deposit.address, deposit.blockchain], { unique: true })
David: No named indexes

--- PR #611 | src/shared/models/asset/asset.service.ts ---
Context: +    return assets.find((a) => { | +      const queryMatch = a.dexName === dexName && a.blockchain === blockchain && a.type === type; | +      const chainIdMatch = !a.chainId ? true : chainId ? a.chainId.toLowerCase() === chainId.toLowerCase() : true;
David: Ok I see, makes sense.

--- PR #619 | src/mix/models/crypto-route/dto/crypto-route.dto.ts ---
Context:    active: boolean; | + | +  @ApiProperty({ enum: BuyType, enumName: 'BuyType' })
David: `enumName: 'BuyType'` should not be required 🤔

--- PR #619 | src/mix/models/crypto-route/dto/crypto-route.dto.ts ---
Context:    staking: StakingDto; | + | +  @ApiProperty({ enum: Blockchain, enumName: 'Blockchain' })
David: `enumName: 'Blockchain'` should not be required 🤔 (for all enums)

--- PR #619 | src/mix/models/crypto-route/crypto-route.controller.ts ---
Context: +  ApiResponse, | +  ApiTags, | +} from '@nestjs/swagger';
David: Unused imports

--- PR #619 | src/shared/models/asset/dto/asset-dto.mapper.ts ---
Context: +      dto.push(this.entityToDto(asset)); | +    } | +    return dto;
David: ```ts
return assets.map(this.entityToDto);
```

--- PR #619 | src/shared/models/country/dto/country-dto.mapper.ts ---
Context: +      dto.push(this.entityToDto(country)); | +    } | +    return dto;
David: Same here

--- PR #619 | src/shared/models/fiat/dto/fiat-dto.mapper.ts ---
Context: +      dto.push(this.entityToDto(fiat)); | +    } | +    return dto;
David: Same here

--- PR #619 | src/shared/models/language/dto/language-dto.mapper.ts ---
Context: +      dto.push(this.entityToDto(language)); | +    } | +    return dto;
David: Same here

--- PR #619 | src/subdomains/supporting/bank/bank/dto/bank-dto.mapper.ts ---
Context: +      dto.push(this.entityToDto(bank)); | +    } | +    return dto;
David: Same here

--- PR #619 | src/shared/models/setting/setting.controller.ts ---
Context: +  @ApiOkResponse({ type: CfpSettings }) |    async getFrontendSettings(): Promise<FrontendSettings> { |      const cfpSettings = await this.settingService.getObj<CfpSettings>('cfp');
David: This will be unused (no CFP voting at DFX) => best to remove it from Swagger

--- PR #619 | src/subdomains/core/statistic/statistic.controller.ts ---
Context:    @Get('transactions') | +  @ApiOkResponse() |    async getTransactions(@Query('dateFrom') dateFrom: Date, @Query('dateTo') dateTo: Date): Promise<any> {
David: Maybe we should add some DTOs here? 😉

--- PR #619 | src/subdomains/generic/user/models/auth/dto/auth-response.dto.ts ---
Context: +export class AuthResponseDto { | +  @ApiProperty({ | +    description: 'Access token of LOCK API',
David: Hups 😉

--- PR #619 | src/subdomains/generic/user/models/user/user.controller.ts ---
Context:    @ApiBearerAuth() |    @UseGuards(AuthGuard(), new RoleGuard(UserRole.USER)) | +  @ApiOkResponse({ type: HistoryFilter, isArray: true })
David: Is this correct?

--- PR #619 | src/subdomains/generic/user/models/user/user.controller.ts ---
Context:    @ApiBearerAuth() |    @UseGuards(AuthGuard(), new RoleGuard(UserRole.USER)) | +  @ApiOkResponse()
David: Missing type?

--- PR #619 | src/subdomains/generic/user/models/user/user.controller.ts ---
Context:    @ApiBearerAuth() |    @UseGuards(AuthGuard(), new RoleGuard(UserRole.USER)) | +  @ApiOkResponse()
David: Missing type?

--- PR #619 | src/subdomains/supporting/bank/bank/bank.controller.ts ---
Context: +  async getAllBanks(): Promise<BankDto[]> { | +    return BankDtoMapper.entitiesToDto(await this.bankService.getAllBanks()); |    }
David: I personally would prefer the following syntax (for all mappings):
```ts
return this.bankService.getAllBanks().then(BankDtoMapper.entitiesToDto);
```
WDYT?

--- PR #619 | src/mix/models/crypto-route/dto/crypto-route.dto.ts ---
Context: + | +  @ApiProperty({ type: Asset }) |    asset: Asset;
David: Map to DTO (everywhere)

--- PR #619 | src/shared/models/asset/dto/asset-dto.mapper.ts ---
Context: + | +  static entitiesToDto(assets: Asset[]): AssetDto[] { | +    return assets.map(this.entityToDto);
David: I think I would replace this with `AssetDtoMapper.entityToDto` (in all mappers), just to be sure.

--- PR #619 | src/mix/models/crypto-route/dto/crypto-route.dto.ts ---
Context: + | +  @ApiProperty({ type: AssetDto }) |    asset: Asset;
David: Missing mapping?

--- PR #619 | src/subdomains/core/buy-crypto/route/dto/buy.dto.ts ---
Context: + | +  @ApiProperty({ type: AssetDto }) |    asset: Asset;
David: Missing mapping?

--- PR #619 | src/subdomains/core/sell-crypto/sell/dto/sell.dto.ts ---
Context: + | +  @ApiProperty({ type: Deposit }) |    deposit: Deposit;
David: Do we have a DTO and documentation for Deposit?

--- PR #619 | src/subdomains/core/sell-crypto/sell/dto/sell.dto.ts ---
Context: + | +  @ApiProperty({ type: Fiat }) |    fiat: Fiat;
David: Missing mapping?

--- PR #619 | src/mix/models/deposit/dto/deposit-dto.mapper.ts ---
Context: +      blockchain: deposit.blockchain, | +      address: deposit.address, | +      route: deposit.route ? DepositRouteDtoMapper.entityToDto(deposit.route) : undefined,
David: `route` can be removed

--- PR #619 | src/mix/models/deposit/dto/deposit.dto.ts ---
Context: @@ -0,0 +1,13 @@ | +import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
David: Unused import

--- PR #619 | src/subdomains/core/statistic/dto/cfp.dto.ts ---
Context: +  @ApiProperty() | +  endDate: string; | +}
David: These settings are not exposed through API? So `ApiProperty` can be removed.

--- PR #619 | src/subdomains/core/statistic/dto/cfp.dto.ts ---
Context: +  body: string; | +  created_at: string; | +}
David: Unused?

--- PR #619 | src/subdomains/core/statistic/dto/cfp.dto.ts ---
Context: +  mintedBlocks: number; | +  state: State; | +}
David: Unused?

--- PR #619 | src/subdomains/core/statistic/dto/cfp.dto.ts ---
Context: +  ENABLED = 'ENABLED', | +  PRE_ENABLED = 'PRE_ENABLED', | +}
David: Unused

--- PR #619 | src/subdomains/core/statistic/dto/cfp.dto.ts ---
Context: + | +export class CfpResult { | +  @ApiProperty()
David: Maybe add `{ description: 'Proposal ID' }`?

--- PR #620 | package.json ---
Context: +    "@defichain/jellyfish-api-core": "^2.70.0", | +    "@defichain/jellyfish-api-jsonrpc": "^2.70.0", |      "@defichain/jellyfish-network": "^2.18.3",
David: Update `jellyfish-network` as well

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context:      no: number; |    }; | -  dfxVotes: {
David: DFX votes should stay (because of previous results).

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context:  export interface CfpResult { | -  number: number; | +  number?: number;
David: We should keep this identifier. Maybe use the proposal ID? UI's use this for voting.

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: -  private readonly issuesUrl = 'https://api.github.com/repos/DeFiCh/dfips/issues'; | +  private readonly myDefichainUrl = 'https://api.mydeficha.in/v1/listmasternodes/'; | +  private readonly lockUrl = 'https://api.lock.space/v1/analytics/staking/masternodes';
David: URL needs to be updated?

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: +      } catch (e) { | +        console.error('Failed to get lock/cake masternodes', e); | +      }
David: Does it make sense to have this try-catch? I think if one step fails, all should fail.

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: +      } catch (e) { | +        console.error('Failed to get lock/cake masternodes', e); | +      }
David: Can be move below (only required if in progress)?

--- PR #620 | src/integration/blockchain/ain/node/defi-client.ts ---
Context:  import BigNumber from 'bignumber.js'; |  import { HttpService } from 'src/shared/services/http.service'; | +import { Proposal, ProposalVote } from 'src/subdomains/core/statistic/cfp.service';
David: I think `Proposal` and `ProposalVote` need to be placed in blockchain folder, not in CFP.

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: +    for (const proposal of currentProposals) { | +      proposal.proposalVotes = await this.getVotes(await this.client.listVotes(proposal.proposalId)); | +    }
David: I would move this to the `getCfpResult` method and remove the `proposalVotes` field on `Proposal`. It's misleading because one could think that the votes are returned with the `listProposal`.

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: +  async getCurrentProposals(): Promise<Proposal[]> { | +    const currentProposals = await this.client.listProposal(); | +    this.masternodeCount = await this.client.getProposal(currentProposals[0].proposalId).then((p) => p.votesPossible);
David: Remove this method and move the two lines to the update method.

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context:   | -    return this.getCfpResult(cfp, comments); | +  private async getVotes(proposalVote: ProposalVote[]): Promise<Vote[]> {
David: `proposalVote`

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: +          this.lockMasternodes.find((mn) => mn === this.allMasternodes[m.masternodeId]['ownerAuthAddress']) != null, | +      })), | +    );
David: This is not `async`?

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: +        vote: m.vote, | +        isCake: | +          Object.values(this.cakeMasternodes).find(
David: Why do we need a `Object.values` if it's an array?

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: - | +  private masternodeCount: number; | +  private allMasternodes;
David: Add type

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: +        isCake: | +          Object.values(this.cakeMasternodes).find( | +            (n) => n.address === this.allMasternodes[m.masternodeId]['ownerAuthAddress'],
David: `this.allMasternodes[m.masternodeId].ownerAuthAddress` (same below)

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: -        neutral: neutralVotesDfx.length, | -        no: noVotesDfx.length, | -      },
David: Should we keep this?

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: -    return this.http.get<T>(`${baseUrl}${url}`, { headers: { Authorization: `Bearer ${Config.githubToken}` } }); | +  private async callApi<T>(url: string): Promise<T> { | +    return this.http.get<T>(`${url}`, { headers: { Authorization: `Bearer DFX` } });
David: `return this.http.get<T>(url);`?

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context:   | -    return this.getCfpResult(cfp, comments); | +  private async getVotes(proposalVote: ProposalVote[]): Promise<Vote[]> {
David: Sorry, I meant `proposalVotes` 😆

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: +        neutral: neutralVotesLock.length, | +        no: noVotesLock.length, |        },
David: Hups ... find the error 😉

--- PR #620 | src/subdomains/core/statistic/cfp.service.ts ---
Context: -    const type = cfp.labels.map((a) => a.name).includes(VotingType.CFP) ? VotingType.CFP : VotingType.DFIP; | +  private async getCfpResult(proposal: Proposal): Promise<CfpResult> { | +    const proposalVotes = await this.getVotes(await this.client.listVotes(proposal.proposalId));
David: Unnecessary (outer) await

--- PR #628 | src/shared/models/setting/dto/cake-flow.dto.ts ---
Context: @@ -0,0 +1,38 @@ | +import { ApiProperty } from '@nestjs/swagger';
David: `ApiProperty` not required, it's no public (documented) API.

--- PR #628 | src/shared/models/setting/setting.controller.ts ---
Context: +  async getCakeFlowSettings(): Promise<CakeFlow> { | +    return this.settingService.getCakeFlow(); | +  }
David: `getCakeSettings`?

--- PR #628 | src/shared/models/setting/dto/cake-flow.dto.ts ---
Context: + | +export class CakeFlow { | +  [x: string]: any;
David: What is `[x: string]: any;`?

--- PR #628 | src/shared/models/setting/dto/cake-flow.dto.ts ---
Context: +} | + | +export class CakeFlow {
David: `CakeSettings`?

--- PR #628 | src/shared/models/setting/dto/cake-flow.dto.ts ---
Context: +  @IsNotEmpty() | +  threshold: number; | +}
David: `extends CakeFlowAssetDto`?

--- PR #628 | src/shared/models/setting/setting.controller.ts ---
Context: +  @Get('cake') | +  async getCakeFlowSettings(): Promise<CakeFlow> { | +    return this.settingService.getCakeFlow();
David: Use
```ts
return this.settingService.getObj<CakeFlow>('cake');
```

--- PR #628 | src/shared/models/setting/setting.service.ts ---
Context: +  async setCakeFlow(dto: CakeFlowDto): Promise<void> { | +    let currentCakeFlow = await this.getCakeFlow(); | +    currentCakeFlow ??= { assets: {} };
David: ```ts
const cakeSettings = await this.getObj<CakeFlow>('cake', { assets: {} });
```

--- PR #628 | src/shared/models/setting/setting.service.ts ---
Context: +    currentCakeFlow.assets[dto.asset] = { direction: dto.direction, threshold: dto.threshold }; | + | +    await this.settingRepo.save({ key: 'CakeFlow', value: JSON.stringify(currentCakeFlow) });
David: ```ts
await this.setObj('cake', cakeSettings);
```

--- PR #631 | src/mix/models/crypto-route/dto/get-crypto-payment-info.dto.ts ---
Context: @@ -11,6 +11,12 @@ export class GetCryptoPaymentInfoDto { |    @IsEnum(Blockchain) |    blockchain: Blockchain;
David: Remove blockchain

--- PR #631 | src/subdomains/core/sell-crypto/sell/sell.service.ts ---
Context:      const fiat = await this.fiatService.getFiat(dto.fiat.id); |      if (!fiat) throw new BadRequestException('Fiat not found'); | +    if (!fiat.sellable || !fiat.enable) throw new BadRequestException('Fiat not buyable');
David: `enable` column will be removed, can ignore it

--- PR #631 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +    // check if organization country is enabled | +    if (user.organizationCountry && !user.organizationCountry.isEnabled(user.kycType)) | +      throw new ConflictException('Organization country is not supported');
David: This should be (and is already partially) done during user data update. See `updateKycData` in `UserDataService`.

--- PR #631 | src/shared/services/payment-info.service.ts ---
Context: +  constructor(private readonly fiatService: FiatService, private readonly assetService: AssetService) {} | + | +  async buyPaymentInfoCheck(dto: GetBuyPaymentInfoDto): Promise<void> {
David: I would return the DTO, to make it clear it's getting updated.

--- PR #631 | src/shared/services/payment-info.service.ts ---
Context: + | +@Injectable() | +export class PaymentInfoService {
David: Maybe rename to PaymentCheckService?

--- PR #631 | src/shared/services/payment-info.service.ts ---
Context: +  constructor(private readonly fiatService: FiatService, private readonly assetService: AssetService) {} | + | +  async buyPaymentInfoCheck(dto: GetBuyPaymentInfoDto): Promise<void> {
David: I would remove `PaymentInfo` from name, is already included in the service name.

--- PR #631 | src/subdomains/core/buy-crypto/route/buy.controller.ts ---
Context:   |  @ApiTags('Buy') |  @Controller('buy')
David: Unused imports in this file

--- PR #632 | src/subdomains/supporting/dex/strategies/check-liquidity/impl/defichain-default.strategy.ts ---
Context:   | +    const targetAvailableAmount = availableAmount > 0 ? availableAmount : 0; | +    const targetMaxPurchasableAmount = maxPurchasableAmount > 0 ? maxPurchasableAmount : 0;
David: Could use `const targetAvailableAmount = Math.max(availableAmount, 0);` if you like 😉  (everywhere)

--- PR #633 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context:        outputAsset: buyCrypto.outputAsset?.dexName, |        txId: buyCrypto.txId, | +      txUrl: `${BlockchainExplorerUrls[buyCrypto.outputAsset.blockchain]}/${buyCrypto.txId}`,
David: outputAsset and txId might be null

--- PR #633 | src/subdomains/core/sell-crypto/buy-fiat/buy-fiat.service.ts ---
Context:        outputAsset: buyFiat.outputAsset, |        txId: buyFiat.cryptoInput.inTxId, | +      txUrl: `${BlockchainExplorerUrls[buyFiat.cryptoInput.asset.blockchain]}/${buyFiat.cryptoInput.inTxId}`,
David: Use `buyFiat.inputAsset.blockchain`

--- PR #634 | src/mix/models/crypto-input/crypto-input.service.ts ---
Context: +    paramName: KeyType<CryptoInput, any>, | +    param: CryptoInput[keyof CryptoInput], | +  ): Promise<CryptoInput> {
David: Use generics (easier and more type safe):
```ts
async getByKey<T>(key: KeyType<CryptoInput, T>, value: T): Promise<CryptoInput>
```

--- PR #634 | src/mix/models/crypto-input/crypto-input.service.ts ---
Context: +    paramName: KeyType<CryptoInput, any>, | +    param: CryptoInput[keyof CryptoInput], | +  ): Promise<CryptoInput> {
David: For all of these methods

--- PR #634 | src/mix/models/crypto-input/crypto-input.entity.ts ---
Context:   | -  @Column({ length: 256, nullable: false }) | +  @Column({ length: 256, nullable: true })
David: Hm?

--- PR #634 | src/subdomains/generic/gs/gs.service.ts ---
Context: +        return await this.buyCryptoService | +          .getBuyCryptoByKey(query.key, query.value) | +          .then((buyCrypto) => (buyCrypto?.buy ? buyCrypto?.buy.user.userData : buyCrypto?.cryptoRoute?.user.userData));
David: Can we use `buyCrypto?.user.userData` here?

--- PR #635 | src/subdomains/core/statistic/dto/cfp.dto.ts ---
Context:   | +  @ApiPropertyOptional({ enum: ProposalStatus }) | +  status?: ProposalStatus;
David: Why nullable?

--- PR #635 | src/subdomains/core/statistic/cfp.service.ts ---
Context:      const currentResult = | -      yesVotes.length / (yesVotes.length + noVotes.length) > requiredVotes | +      Util.round((proposalVotes.length / this.masternodeCount) * 100, Config.defaultPercentageDecimal) > quorum &&
David: `proposalVotes.length / this.masternodeCount > quorum`

--- PR #635 | src/subdomains/core/statistic/cfp.service.ts ---
Context: -      yesVotes.length / (yesVotes.length + noVotes.length) > requiredVotes | +      Util.round((proposalVotes.length / this.masternodeCount) * 100, Config.defaultPercentageDecimal) > quorum && | +      yesVotes.length / (yesVotes.length + noVotes.length + neutralVotes.length) > requiredVotes
David: `yesVotes.length / proposalVotes.length > requiredVotes `

--- PR #635 | src/subdomains/core/statistic/dto/cfp.dto.ts ---
Context:    currentResult: ResultStatus; |   |    @ApiPropertyOptional({ enum: ProposalStatus })
David: Not optional

--- PR #638 | src/subdomains/core/liquidity-management/entities/liquidity-management-rule.entity.ts ---
Context: +    return ( | +      this.status === LiquidityManagementRuleStatus.PAUSED && | +      Util.minutesDiff(this.updated, new Date()) > this.reactivationTime
David: Yes, makes sense 👍

--- PR #638 | src/subdomains/core/liquidity-management/services/liquidity-management-rule.service.ts ---
Context: +        await this.ruleRepo.save(rule); | + | +        console.log(`Reactivated liquidity management rule ${rule.id}`);
David: It would be cool to have an email notification (equivalent to 'rule paused' mail).

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context:    } | + | +  // --- GET COMPANY KCY //
David: Typo 😉

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.controller.ts ---
Context:   | +  // --- KYC COMPANY Calls // | +  @Get('kycData')
David: Maybe `@Get('users')`?

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.controller.ts ---
Context: +  @ApiOkResponse({ type: KycDataDto, isArray: true }) | +  async getAllKycData(@GetJwt() jwt: JwtPayload): Promise<KycDataDto[]> { | +    return this.walletService.getAllKycData(jwt.id).then((l) => this.toKycDataDtoList(l));
David: I think this method should also be moved to KycService.

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.controller.ts ---
Context: +  ): Promise<any> { | +    return this.kycService.getKycFile(userId, jwt.id, type); | +  }
David: Path should be `:id/documents` where `id` is the user address.

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +    const wallet = await this.walletRepo.findOne({ where: { id: walletId } }); | +    const user = await this.userRepo.findOne({ where: { id: userId }, relations: ['userData', 'wallet'] }); | +    if (user.wallet.id != wallet.id) throw new ForbiddenException('User not from wallet');
David: ```ts
const user = await this.userRepo.findOne({ where: { id: userId, wallet: { id: walletId }, relations: ['userData', 'wallet'] });
if (!user) throw new NotFoundException('User not found');
```

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +    const customerInfos = await this.spiderApiService.getDocumentInfos( | +      user.userData.id, | +      user.userData.accountType != AccountType.PERSONAL,
David: Documents are always saved on the person not the organization, so just set it to false here.

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +    const customerInfos = await this.spiderApiService.getDocumentInfos( | +      user.userData.id, | +      user.userData.accountType != AccountType.PERSONAL,
David: Same here

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +    if (user.wallet.id != wallet.id) throw new ForbiddenException('User not from wallet'); | + | +    const customerInfos = await this.spiderApiService.getDocumentInfos(
David: `const allDocuments = ...`?

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +        ) | +        .map((d) => d.document), | +    };
David: Why not just return an array instead of wrapping into an object? And add some more information (e.g. document name + content type).

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +        ) | +        .map((d) => d.document), | +    };
David: I would add a separate document enum for output, to decouple it from Spider.

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +    if (user.wallet.id != wallet.id) throw new ForbiddenException('User not from wallet'); | + | +    const customerInfos = await this.spiderApiService.getDocumentInfos(
David: `const allDocuments = ...`?

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +        ) | +        .map((d) => d.document), | +    };
David: Maybe create a mapping method with the new DocumentType enum?
```ts
private getDocumentInfoFor(type: KycDocumentType, documents: DocumentInfo[]): DocumentInfo | undefined {
  switch (type) {
    case KycDocumentType.IDENTIFICATION:
      return documents.find((d) => d.document === KycDocument.ONLINE_IDENTIFICATION && d.label === 'Report');
    case ...
  }
}
```

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +        ) | +        .map((d) => d.document), | +    };
David: And we need to get the video identification document, if no online id is found.

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.service.ts ---
Context: +    const wallet = await this.walletRepo.findOne({ where: { id: walletId } }); | +    const user = await this.userRepo.findOne({ where: { id: userId }, relations: ['userData', 'wallet'] }); | +    if (user.wallet.id != wallet.id) throw new ForbiddenException('User not from wallet');
David: ```ts
const user = await this.userRepo.findOne({ where: { id: userId, wallet: { id: walletId }, relations: ['userData', 'wallet'] });
if (!user) throw new NotFoundException('User not found');
```

--- PR #647 | src/subdomains/generic/user/models/kyc/kyc.controller.ts ---
Context: + | +  @Get(':id/documents') | +  @ApiOkResponse({ type: KycFilesDto })
David: `isArray: true`

--- PR #647 | src/subdomains/generic/user/models/kyc/dto/kyc-files.dto.ts ---
Context: +  contentType: KycContentType; | +  kycType: KycDocumentType; | +}
David: Add documentation

--- PR #647 | src/subdomains/generic/user/models/kyc/dto/kyc-files.dto.ts ---
Context: +export class KycFilesDto { | +  name: string; | +  contentType: KycContentType;
David: I think I would set `contentType: string`, this is a general standard.

--- PR #663 | src/subdomains/generic/gs/gs-evm.controller.ts ---
Context: +import { GsEvmService } from './gs-evm.service'; | + | +@Controller('gsEvm')
David: Maybe better use `gs/evm`?

--- PR #664 | src/subdomains/generic/user/models/user-data/is-dfx-phone.validator.ts ---
Context: +      return 'Phone number not valid'; | +    } | +  }
David: I think we can simplify this to:
```ts
  validate(phoneNumber: string) {
    try {
      return phoneNumber && phoneNumber.match(/^\+[\d ]*$/)) && phoneUtil.isValidNumber(phoneUtil.parseAndKeepRawInput(phoneNumber));
    } catch() {
      return false;
    }
```

--- PR #664 | src/subdomains/generic/user/models/user-data/is-dfx-phone.validator.ts ---
Context: +      return ( | +        phoneNumber && | +        phoneNumber.match(/^\+[\d ]*$/) &&
David: Is the regex still required?

--- PR #666 | src/shared/auth/ip.guard.ts ---
Context: +import { IpRepository } from 'src/subdomains/generic/user/models/ip/ip.repository'; | +import { CountryService } from '../models/country/country.service'; | +import * as requestIp from '@supercharge/request-ip';
David: `import { getClientIp } from '@supercharge/request-ip';`

--- PR #666 | src/shared/auth/ip.guard.ts ---
Context: +  async canActivate(context: ExecutionContext): Promise<boolean> { | +    const req = context.switchToHttp().getRequest(); | +    const ip = await requestIp.getClientIp(req);
David: Unnecessary `await`

--- PR #666 | src/shared/auth/ip.guard.ts ---
Context: +    await this.ipRepo.save(ipObject); | +    if (!result) throw new ForbiddenException('The country of IP address is not allowed'); | +    return result;
David: Throwing the error is not required, returning `false` will throw a ForbiddenException

--- PR #666 | src/shared/auth/ip.guard.ts ---
Context: +    const countryObject = await this.countryService.getCountryWithSymbol(country); | + | +    return { country, result: countryObject?.ipEnable };
David: Maybe just return country `undefined` and result `false`, if no country? Country should then be nullable in entity.

--- PR #666 | src/subdomains/generic/user/models/ip/ip.entity.ts ---
Context: + | +@Entity() | +export class Ip extends IEntity {
David: Maybe rename it to SignInLog or similar?

--- PR #666 | migration/1677015014694-add-ip-logger.js ---
Context: +        await queryRunner.query(`ALTER TABLE "dbo"."wallet" DROP COLUMN "mail"`); | +        await queryRunner.query(`ALTER TABLE "dbo"."wallet" DROP CONSTRAINT "UQ_8a21f1713dbe0211d8493128774"`); | +        await queryRunner.query(`ALTER TABLE "dbo"."wallet" DROP COLUMN "signature"`);
David: Unwanted changes?

--- PR #666 | src/subdomains/generic/user/models/ip-log/ip-log.repository.ts ---
Context: + | +@EntityRepository(IpLog) | +export class IpRepository extends Repository<IpLog> {}
David: IpLogRepository

--- PR #677 | src/subdomains/generic/user/models/user/user.service.ts ---
Context:    async activateUser(user?: User): Promise<void> { | +    user.ref = await this.getNextRef(); |      await this.userRepo.activateUser(user);
David: Add the retry logic from above.

--- PR #677 | src/subdomains/generic/user/user.module.ts ---
Context: +    LinkService, | +    WebhookService, | +    GeoLocationService,
David: `GeoLocationService` should not be provided and exported here, but exported from other module

--- PR #677 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: -      return this.userRepo.save(user); | -    }, 3); | +    await this.userRepo.save(user);
David: ```ts
user = await this.userRepo.save(user);
```

--- PR #677 | src/subdomains/generic/user/models/user/user.repository.ts ---
Context: -    if (user?.status === UserStatus.NA) await this.update(user.id, { status: UserStatus.ACTIVE }); | +    if (user?.status === UserStatus.NA) await this.update(user.id, { status: UserStatus.ACTIVE, ref: user.ref }); |    }
David: Maybe add the ref as parameter to make sure we do not forget to set it?

--- PR #677 | src/subdomains/core/statistic/statistic.module.ts ---
Context:  @Module({ |    imports: [ | +    TypeOrmModule.forFeature([IpLogRepository]),
David: This is probably wrong here? IpGuard should be exported from other module.

--- PR #677 | src/config/config.ts ---
Context:    apiKeyVersionCT = '0'; // single digit hex number |    azureIpSubstring = '169.254'; | +  knownRequestIps = process.env.KNOWN_REQUEST_IPS?.split(',');
David: Add `?? []`

--- PR #677 | src/shared/auth/ip.guard.ts ---
Context: +      }); | +      if (callCount > callLimit) throw new HttpException(`Too many requests ${req.url}`, 429); | +    }
David: I think it would be good to have an environment setting to disable rate limiting for some special cases (events, etc).

--- PR #677 | src/shared/auth/ip.guard.ts ---
Context: +        created: MoreThan(Util.daysBefore(1)), | +      }); | +      if (callCount > callLimit) throw new HttpException(`Too many requests ${req.url}`, 429);
David: `if (callCount >= callLimit)` => current call will be above limit.

--- PR #677 | src/shared/shared.module.ts ---
Context:      HttpModule, |      ConfigModule, | +    GeoLocationModule,
David: Add `GeoLocationModule` to exports and remove import in UserModule, StatisticModule and IntegrationModule.

--- PR #677 | infrastructure/bicep/dfx-api.bicep ---
Context: +          name: 'REQUEST_LIMIT_CHECK' | +          value: limitCheck | +        }
David: Missing param?

--- PR #677 | infrastructure/bicep/parameters/dev.json ---
Context: +    }, | +    "limitCheck": { | +      "value": "xxx"
David: `"value": "true"`

--- PR #677 | infrastructure/bicep/parameters/loc.json ---
Context: +    }, | +    "limitCheck": { | +      "value": "xxx"
David: `"value": "false"`

--- PR #677 | infrastructure/bicep/parameters/prd.json ---
Context: +    }, | +    "limitCheck": { | +      "value": "xxx"
David: `"value": "true"`

--- PR #677 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:        for (const user of master.users) { | -        await this.userRepo.activateUser(user); | +        await this.userRepo.activateUser(user, user.ref);
David: Use the `activateUser` method from UserService (need to assign a ref code).

--- PR #677 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: +    }, 3); | +    await this.userRepo.activateUser(user, user.ref); | +    await this.userDataRepo.activateUserData(user.userData);
David: Do not overwrite the ref. Save/activate at once.

```ts
// retry (in case of ref conflict)
await Util.retry(async () => {
  const ref = user.ref ?? await this.getNextRef();
  await this.userRepo.activateUser(user, ref);
}, 3);

if (user.userData.status === UserDataStatus.NA)
  await this.userDataRepo.activateUserData(user.userData);

--- PR #677 | src/subdomains/generic/user/models/user/user.service.ts ---
Context:    } |   |    async activateUser(user?: User): Promise<void> {
David: `user` should be not nullable (no `?`)

--- PR #677 | src/shared/models/ip-log/ip-log.service.ts ---
Context: +    const ipWithoutLastElement = ip.split('.').slice(0, -1).join('.'); | +    const callCount = await this.getCallCount(url, ipWithoutLastElement); | +    return callLimit >= callCount;
David: `callLimit > callCount` (condition has been inverted 😉)

--- PR #678 | thunder-tests/thunderEnvironment.json ---
Context: +      { | +        "name": "token", | +        "value": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MTc1LCJhZGRyZXNzIjoidGYxcXlqbDZwbjJ4ODJoanNqcWM2MjYwN2d5ejV0cWw3ajA5NnBnZWE4Iiwicm9sZSI6IkFkbWluIiwiYmxvY2tjaGFpbnMiOlsiRGVGaUNoYWluIl0sImlhdCI6MTY3NzU5Mjc2MSwiZXhwIjoxNjc3NzY1NTYxfQ.v4NhE6H3Ppa0ART5yhMOVjjCXS_u8jgO-NmXPORb73Y"
David: Remove

--- PR #678 | thunder-tests/thunderEnvironment.json ---
Context: +      { | +        "name": "token", | +        "value": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MjE1LCJhZGRyZXNzIjoidGYxcXd1ZnVoZnJreWhwcm5zeWx4Y2o1NjZsYXg3bnRuemEzMDVzbmE3Iiwicm9sZSI6IkFkbWluIiwiYmxvY2tjaGFpbnMiOlsiRGVGaUNoYWluIl0sImlhdCI6MTY3NzU4ODE4NywiZXhwIjoxNjc4Nzk3Nzg3fQ.245OHQ0xsrCEaxmqGcagUlFAzxgclObqxDvVIP7CI3c"
David: Remove

--- PR #678 | src/subdomains/core/liquidity-management/adapters/actions/binance.adapter.ts ---
Context: +      default: | +        return null; | +    }
David: It might be better to put this into a map (on all exchange adapters), where all blockchains are required, e.g. 
```ts
const ccxtNetworks: { [b in Blockchain]: string } { ...}
```

--- PR #678 | src/subdomains/core/liquidity-management/entities/liquidity-management-pipeline.entity.ts ---
Context: +  unique: true, | +  where: `status IN ('${LiquidityManagementPipelineStatus.CREATED}', '${LiquidityManagementPipelineStatus.IN_PROGRESS}')`, | +})
David: Is it ok to have two pipelines for the same rule, one in CREATED and the other in IN_PROGRESS state?

--- PR #678 | src/subdomains/core/liquidity-management/entities/liquidity-management-pipeline.entity.ts ---
Context:   |    continue(currentActionOrderStatus: LiquidityManagementOrderStatus): this { | +    this.previousAction = Object.assign({}, this.currentAction);
David: `new LiquidityManagementAction()` instead of `{}`?

--- PR #678 | src/integration/exchange/services/exchange.service.ts ---
Context: +export type ExchangeExtended = Exchange & { depositAddresses: { [key in Blockchain]?: string } } & { | +  withdrawalAddressesKeys: { [key in Blockchain]?: string }; | +};
David: Is this used somewhere?

--- PR #678 | src/subdomains/core/liquidity-management/services/liquidity-management.service.ts ---
Context: + | +    return this.executeRule(rule, liquidityState); | +  }
David: Should we check for current liquidity state and combine both inputs?

--- PR #678 | src/subdomains/supporting/dex/services/dex-bitcoin.service.ts ---
Context: +  async transferMinimalUtxo(address: string): Promise<string> { | +    return this.sendUtxoToMany([{ addressTo: address, amount: Config.blockchain.default.minDeposit.Bitcoin.BTC / 2 }]); | +  }
David: Is this used? `Config.blockchain.default.minDeposit.Bitcoin.BTC / 2` is currently more than 5 USD 😉

--- PR #678 | src/subdomains/supporting/dex/interfaces/index.ts ---
Context: +  amount: number; | +  since: Date; | +}
David: I think it would make sense to add the address to this query.

--- PR #678 | src/integration/blockchain/arbitrum/arbitrum-client.ts ---
Context: + | +    // eslint-disable-next-line @typescript-eslint/no-floating-promises | +    this.initL2Network();
David: `void this.initL2Network();`

--- PR #678 | src/integration/blockchain/arbitrum/arbitrum-client.ts ---
Context: +      l1Signer: this.#l1Wallet, | +      l2Provider: this.provider, | +    });
David: How long does this approval take? What if the service restarts at this point?

--- PR #678 | src/integration/blockchain/arbitrum/arbitrum-client.ts ---
Context: +      value: this.convertToWeiLikeDenomination(amount, 'ether'), | +    }); | +  }
David: Why are there two places for coin gas estimation in this class?

--- PR #679 | src/subdomains/core/buy-crypto/routes/buy/dto/create-buy.dto.ts ---
Context:    @ValidateNested() |    @Type(() => EntityDto) |    asset?: Asset;
David: Not optional
-> `@ApiProperty`
-> No `?`

--- PR #679 | src/subdomains/core/buy-crypto/routes/buy/buy.controller.ts ---
Context:      return this.buyService | -      .createBuy(jwt.id, jwt.address, { ...dto, type: BuyType.WALLET }, true) | +      .createBuy(jwt.id, jwt.address, { ...dto }, true)
David: `.createBuy(jwt.id, jwt.address, dto, true)`?

--- PR #679 | src/subdomains/core/buy-crypto/routes/crypto-route/crypto-route.service.ts ---
Context:          user: { id: userId }, |          deposit: { blockchain: dto.blockchain }, |        },
David: ```ts
      where: {
        asset: targetAsset,
        targetDeposit: IsNull(),
        user: { id: userId },
        deposit: { blockchain: dto.blockchain },
      },
```

--- PR #679 | src/subdomains/core/buy-crypto/routes/crypto-route/dto/create-crypto-route.dto.ts ---
Context:    @ValidateNested() |    @Type(() => EntityDto) |    asset: Asset;
David: Not optional

--- PR #679 | src/subdomains/generic/user/models/user/dto/user.dto.ts ---
Context:   | -  @ApiProperty() | -  stakingBalance: number;
David: Add hard-coded value 0

--- PR #679 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: - | -  async activateUser(user: User): Promise<void> { | +  async activateUser(user?: User): Promise<void> {
David: I think this is an unwanted merge change

--- PR #679 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:      const user = await this.getLinkedUser(id, changeUser.address); |      if (!user) throw new NotFoundException('User not found'); | -    if (user.stakingBalance > 0) throw new ForbiddenException('Change user not allowed');
David: Unused import

--- PR #680 | src/subdomains/generic/user/models/auth/auth.controller.ts ---
Context: +  @UseGuards(RateLimitGuard, IpGuard) |    @ApiCreatedResponse({ type: AuthResponseDto }) |    signIn(@Body() credentials: AuthCredentialsDto): Promise<AuthResponseDto> {
David: No rate limit on sign in.

--- PR #684 | src/subdomains/supporting/dex/services/dex-bitcoin.service.ts ---
Context:    private async getPendingAmount(): Promise<number> { | -    const pendingOrders = (await this.liquidityOrderRepo.find({ isReady: true, isComplete: false })).filter( | +    const pendingOrders = (await this.liquidityOrderRepo.find({ isComplete: false })).filter(
David: Should we also change to summing up `estimatedTargetAmount`? And add a small cap?

--- PR #684 | src/subdomains/supporting/dex/services/dex-evm.service.ts ---
Context:    private async getPendingAmount(assetName: string): Promise<number> { | -    const pendingOrders = (await this.liquidityOrderRepo.find({ isReady: true, isComplete: false })).filter( | +    const pendingOrders = (await this.liquidityOrderRepo.find({ isComplete: false })).filter(
David: Should we also change to summing up `estimatedTargetAmount`? And add a small cap?

--- PR #684 | src/subdomains/supporting/payin/strategies/send/impl/base/evm.strategy.ts ---
Context: -      quantityOfTransactions: 1, |      }; |    }
David: Why async?

--- PR #684 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:    PAYING_OUT = 'PayingOut', |    COMPLETE = 'Complete', |  }
David: Are there any other interesting statuses?
- Pricing Mismatch
- Missing Liquidity

--- PR #687 | src/subdomains/generic/user/models/user/user.entity.ts ---
Context: -  @Column({ length: 256, unique: true, nullable: true }) | +  @Column({ length: 256, nullable: true }) | +  @Index({ unique: true, where: 'ref IS NOT NULL' })
David: Add a migration

--- PR #691 | src/subdomains/generic/user/models/user/user.service.ts ---
Context:        refCount: await this.userRepo.count({ usedRef: user.ref }), |        refCountActive: await this.userRepo.count({ usedRef: user.ref, status: Not(UserStatus.NA) }), | +      bsLink: user.buyVolume > 50000 ? Config.bsLink : undefined,
David: Use total volume (buy + sell)

--- PR #698 | src/subdomains/generic/user/models/kyc/kyc.controller.ts ---
Context:  import { KycDataDto } from './dto/kyc-data.dto'; | +import { CountryDto } from 'src/shared/models/country/dto/country.dto'; | +import { CountryDtoMapper } from 'src/shared/models/country/dto/country-dto.mapper';
David: Remove the `enabled` from CountryDto, is not used anymore.

--- PR #704 | src/integration/exchange/exchange.module.ts ---
David: Need to add the repository to the module?

--- PR #704 | src/integration/exchange/entities/exchange-tx.entity.ts ---
Context: +  @Column({ type: 'float', nullable: true }) | +  fees: number; | +}
David: Missing migration

--- PR #704 | src/integration/exchange/entities/exchange-tx.entity.ts ---
Context: + | +  @Column({ length: 256, nullable: true }) | +  txid: string;
David: txId

--- PR #704 | src/integration/exchange/entities/exchange-tx.entity.ts ---
Context: + | +  @Column({ length: 256, nullable: true }) | +  refid: string;
David: refId

--- PR #704 | src/integration/exchange/entities/exchange-tx.entity.ts ---
Context: + | +  @Column({ length: 256, nullable: true }) | +  orderTxid: string;
David: orderTxId

--- PR #704 | src/integration/exchange/entities/exchange-tx.entity.ts ---
Context: + | +  @Column({ length: 256, nullable: true }) | +  posTxid: string;
David: posTxId

--- PR #704 | src/integration/exchange/entities/exchange-tx.entity.ts ---
Context: + | +  @Column({ length: 256, nullable: true }) | +  ordertype: string;
David: orderType

--- PR #705 | src/config/config.ts ---
Context: +      mailAddress: process.env.BLACK_SQUAD_MAIL, | +      mailBanner: process.env.BLACK_SQUAD_BANNER, | +    },
David: There is already a black squad config (bs) -> merge them

--- PR #705 | src/config/config.ts ---
Context: +  BLACK_SQUAD_MAIL = 'BlackSquadMail', | +  BUY_CRYPTO_MAIL = 'BuyCryptoMail', | +  BUY_FIAT_MAIL = 'BuyFiatMail',
David: Maybe 1 single `Notifications` process?

--- PR #705 | src/shared/i18n/de/mail.json ---
Context: +    "invitation": { | +      "salutation": "", | +      "body": "<p>Hi {firstname},</p><p>zunächst möchte ich mich kurz vorstellen. Mein Name ist Oli und ich bin CMO bei DFX.swiss.</p><p>Wir pflegen seit einiger Zeit ein Telegram-Gruppe für Personen mit größeren Investments über DFX.swiss und möchten dir mit dieser Gruppe</p><p>- ein direktes Sprachrohr zum C-Level der DFX bieten<br>- eine Plattform für Diskussionen bereitstellen<br>- genau hinhören, wenn ihr euch neue Services wünscht<br>- und euch neue Themen und Services vorab vorstellen</p><p>Wir würden uns freuen, wenn du dieser Gruppe beitreten möchtest.</p><p>Wir haben dir hierfür einen Direktzugang zu dieser Gruppe auf deiner persönlichen DFX Payment Seite hinterlegt. Klicke hierfür einfach in der DFX App auf MyDFX und folge dem Link.</p><p>Vielen Dank für dein Vertrauen in uns.<br>Oli</p>",
David: Typo _ein**e** Telegram-Gruppe_

--- PR #705 | src/subdomains/generic/user/models/user-data/user-data-notification.service.ts ---
Context: +      .andWhere('userData.buyVolume + userData.sellVolume + userData.cryptoVolume >= :limit', { | +        limit: Config.support.blackSquad.limit, | +      })
David: In UserService.getUserDetails, we only use buy and sell volume (without crypto) and have `>` instead of `>=`. Should be the same for both.

--- PR #705 | src/subdomains/generic/user/models/user-data/user-data-notification.service.ts ---
Context: +  ) {} | + | +  @Interval(300000)
David: Lower interval? Every hour should be sufficient. Use `@Cron`

--- PR #707 | src/shared/auth/ip.guard.ts ---
Context:      const ipLog = await this.ipLogService.create(ip, req.url, req.body.address); | +    const user = await this.userService.getUserByKey('address', req.body.address); | +    if (user?.role != UserRole.USER) return true;
David: I would move this to ip-log service. Now, in the IP log the result would be false, even though the user was allowed.

--- PR #707 | src/shared/auth/ip.guard.ts ---
Context:      const ipLog = await this.ipLogService.create(ip, req.url, req.body.address); | +    const user = await this.userService.getUserByKey('address', req.body.address); | +    if (user?.role != UserRole.USER) return true;
David: And better use a new method on userService to get the user. `getUserByKey` is not typed and only for GS sync 😉

--- PR #707 | src/shared/auth/ip.guard.ts ---
Context:   |      const ipLog = await this.ipLogService.create(ip, req.url, req.body.address); | +
David: Revert changes

--- PR #710 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +    // set slave status | +    slave.status = UserDataStatus.MERGED; | +    await this.userDataRepo.save(slave);
David: Does this reassign the child entities to the slave again?

--- PR #713 | src/integration/exchange/mappers/exchange-tx.mapper.ts ---
Context: +        // tagTo: d.tagTo, | +        // tagFrom: d.tagFrom, | +      }));
David: TODO: adapt to entity (remove unused, fix order)

--- PR #713 | src/integration/exchange/dto/exchange-tx-transaction.dto.ts ---
Context: +  side?: string; | +  takeOrMaker?: string; | +}
David: Why not use the entity directly?

--- PR #713 | src/integration/exchange/services/exchange.service.ts ---
Context: +  async getWithdrawals(token: string, since: Date): Promise<Transaction[]> { | +    return this.callApi((e) => e.fetchWithdrawals(token, since.getTime(), 50)); | +  }
David: `since` should be optional

--- PR #713 | src/integration/exchange/mappers/exchange-tx.mapper.ts ---
Context: + | +export class ExchangeTxMapper { | +  static getDeposits(transactions: Transaction[], exchange: string): ExchangeTxTransactionDto[] {
David: `mapDeposits`?

--- PR #713 | src/integration/exchange/entities/exchange-tx.entity.ts ---
Context: + | +export const ExchangeSyncs = ['Kraken']; | +export const ExchangeToken = ['BTC', 'EUR', 'CHF', 'USDT', 'USDC', 'LTC', 'ETH', 'USD'];
David: ExchangeTokens

--- PR #713 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: +        .then((t) => ExchangeTxMapper.getTrades(t, exchange)); | + | +      const transactionArray: ExchangeTxTransactionDto[][] = [];
David: Get exchange only once?
```ts
    for (const exchangeName of ExchangeSyncs) {
      const exchange = this.registryService.getExchange(exchange);

      // Trades
      const transactions = await exchange
        .getTrades()
        .then((t) => ExchangeTxMapper.getTrades(t, exchange));
```

--- PR #713 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: +        const entity = this.exchangeTxRepo.create(transaction); | + | +        this.exchangeTxRepo.save(entity);
David: Missing await

--- PR #713 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: +            .getDeposits(asset, Util.minutesBefore(120)) | +            .then((d) => ExchangeTxMapper.getDeposits(d, exchange)), | +        );
David: ```ts
        // Deposit
        transations.push(
          ...(await exchange
            .getDeposits(asset, Util.minutesBefore(120))
            .then((d) => ExchangeTxMapper.getDeposits(d, exchange))),
        );
```

--- PR #713 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: +        const existing = await this.exchangeTxRepo.find({ | +          where: { exchange: exchange, externalId: transaction.externalId }, | +        });
David: Filter by type?

--- PR #713 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: +          where: { exchange: exchange, externalId: transaction.externalId }, | +        }); | +        if (existing) continue;
David: Update existing

--- PR #713 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: + | +  @Lock(1800) | +  @Cron(CronExpression.EVERY_MINUTE)
David: ```ts
  @Cron(CronExpression.EVERY_MINUTE)
  @Lock(1800)
```

--- PR #713 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: +  @Cron(CronExpression.EVERY_MINUTE) | +  async syncExchanges() { | +    for (const exchange of ExchangeSyncs) {
David: Add possibility to disable sync

--- PR #715 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:   | +    if (userData.letterSentDate) dto.letterSentDate = userData.letterSentDate; | +    if (userData.amlListAddedDate) dto.amlListAddedDate = userData.amlListAddedDate;
David: Maybe add a comment, why this is done (columns are not updatable)

--- PR #716 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:      return this.userDataRepo.find({ | -      where: { mail: mail }, | +      where: { mail: mail, status: In([UserStatus.ACTIVE, UserStatus.NA]) },
David: `UserDataStatus`?

--- PR #720 | src/subdomains/core/sell-crypto/route/dto/sell.dto.ts ---
Context:   | -  @ApiProperty({ enum: Blockchain }) | +  @ApiProperty({ enum: Blockchain, deprecated: true })
David: Add TODO to remove

--- PR #720 | src/subdomains/generic/user/models/user/user.service.spec.ts ---
Context: -    await expect(service.getUserCryptoFee(1)).resolves.toStrictEqual({ fee: 0.5, refBonus: 0 }); | +    await expect(service.getUserCryptoFee(1)).resolves.toStrictEqual({ fee: 0.5 }); |    });
David: Remove refBonus in description

--- PR #720 | src/subdomains/generic/user/models/user/user.service.spec.ts ---
Context: -    await expect(service.getUserCryptoFee(1)).resolves.toStrictEqual({ fee: 0.5, refBonus: 0 }); | +    await expect(service.getUserCryptoFee(1)).resolves.toStrictEqual({ fee: 0.5 }); |    });
David: Remove useless tests

--- PR #720 | src/subdomains/core/buy-crypto/routes/crypto-route/dto/crypto-route.dto.ts ---
Context: - | -  @ApiProperty({ enum: Blockchain }) | +  @ApiProperty({ enum: Blockchain, deprecated: true })
David: Add TODO to remove

--- PR #720 | src/subdomains/core/buy-crypto/routes/crypto-route/dto/crypto-payment-info.dto.ts ---
Context: - |    @ApiProperty() |    depositAddress: string;
David: Equal to SellPaymentInfoDto

--- PR #729 | src/subdomains/core/buy-crypto/routes/buy/buy.service.ts ---
Context:      const existing = await this.buyRepo.findOne({ |        where: { | -        iban: dto.iban,
David: Do we still need this check, if IBAN is not null?

--- PR #729 | src/subdomains/core/buy-crypto/routes/buy/buy.service.ts ---
Context:      // create hash | -    const hash = Util.createHash(userAddress + asset.uniqueName + buy.iban).toUpperCase(); | +    const hash = Util.createHash(userAddress + asset.uniqueName).toUpperCase();
David: Do we need to keep this?

--- PR #729 | src/subdomains/core/buy-crypto/routes/buy/dto/create-buy.dto.ts ---
Context:  import { Transform, Type } from 'class-transformer'; | -import { IsNotEmpty, IsNotEmptyObject, ValidateNested } from 'class-validator'; | +import { IsNotEmpty, IsNotEmptyObject, IsOptional, ValidateNested } from 'class-validator';
David: Unused import

--- PR #729 | src/subdomains/supporting/bank/bank/bank.service.ts ---
Context:      } | -    if (!account && ibanCodeCountry.maerkiBaumannEnable) { | +    if (!account && ibanCodeCountry && ibanCodeCountry.maerkiBaumannEnable) {
David: MB default, if no IBAN

--- PR #733 | src/subdomains/core/history/dto/history.dto.ts ---
Context: +    [BuyCryptoStatus.WAITING_FOR_LOWER_FEE]: PaymentStatus.FEE_TOO_HIGH, | +    ['Incomplete']: PaymentStatus.PENDING, | +  };
David: Maybe better not put it in the DTO but outside with `export constant PaymentStatusMapper`?

--- PR #733 | src/subdomains/core/history/dto/history.dto.ts ---
Context:   |    @ApiProperty() |    isComplete: boolean;
David: `deprecated: true`?

--- PR #733 | src/subdomains/core/sell-crypto/process/buy-fiat.service.ts ---
Context:        amlCheck: buyFiat.amlCheck, |        isComplete: buyFiat.isComplete, | +      status: TypedHistoryDto.PaymentStatusMapper[buyFiat.isComplete ? 'Complete' : 'Incomplete'],
David: `status: buyFiat.isComplete ? PaymentStatus.COMPLETE : PaymentStatus.PENDING` and remove the `Incomplete` entry on the mapper?

--- PR #734 | src/subdomains/core/buy-crypto/process/services/buy-crypto-notification.service.ts ---
Context:                    buyTransactionLink: `${BlockchainExplorerUrls[tx.target.asset.blockchain]}/${tx.txId}`, | +                  minFeeAmount: tx.minFeeAmount, | +                  inputReferenceAsset: tx.inputReferenceAsset,
David: Rename inputReferenceAsset to minFeeAsset

--- PR #734 | src/subdomains/core/sell-crypto/process/buy-fiat-notification.service.ts ---
Context:                  outputAsset: entity.outputAsset, | +                minFeeAmount: entity.minFeeAmount, | +                outputReferenceAsset: entity.outputReferenceAsset,
David: Rename outputReferenceAsset to minFeeAsset

--- PR #734 | src/subdomains/core/history/history.service.ts ---
Context:            sellAsset: c.inputAsset, | -          fee: c.percentFee ? c.percentFee * c.inputAmount : null, | +          fee: (c.totalFeeAmount / c.inputReferenceAmount) * c.inputAmount,
David: `c.totalFeeAmount ? (c.totalFeeAmount / c.inputReferenceAmount) * c.inputAmount : null`

--- PR #734 | src/subdomains/core/history/history.service.ts ---
Context:            sellAsset: this.getAssetSymbol(c.cryptoInput.asset?.dexName), | -          fee: c.percentFee ? c.percentFee * c.inputAmount : null, | +          fee: (c.totalFeeAmount / c.inputReferenceAmount) * c.inputAmount,
David: `c.totalFeeAmount ? (c.totalFeeAmount / c.inputReferenceAmount) * c.inputAmount : null`

--- PR #734 | src/subdomains/core/history/history.service.ts ---
Context: -          fee: c.percentFee ? c.percentFee * c.inputAmount : null, | +          fee: c.totalFeeAmount ? (c.totalFeeAmount / c.inputReferenceAmount) * c.inputAmount : null, |            feeAsset: c.percentFee ? c.inputAsset : null,
David: `feeAsset: c. totalFeeAmount ? c.inputAsset : null,`

--- PR #734 | src/subdomains/core/history/history.service.ts ---
Context: -          fee: c.percentFee ? c.percentFee * c.inputAmount : null, | +          fee: c.totalFeeAmount ? (c.totalFeeAmount / c.inputReferenceAmount) * c.inputAmount : null, |            feeAsset: c.percentFee ? this.getAssetSymbol(c.inputAsset) : null,
David: `feeAsset: c. totalFeeAmount ? c.inputAsset : null,`

--- PR #734 | src/shared/i18n/pt/mail.json ---
David: inputReferenceAsset, outputReferenceAsset => minFeeAsset

--- PR #734 | src/shared/i18n/it/mail.json ---
David: inputReferenceAsset, outputReferenceAsset => minFeeAsset

--- PR #734 | src/shared/i18n/en/mail.json ---
David: inputReferenceAsset, outputReferenceAsset => minFeeAsset

--- PR #734 | src/shared/i18n/es/mail.json ---
David: inputReferenceAsset, outputReferenceAsset => minFeeAsset

--- PR #734 | src/shared/i18n/fr/mail.json ---
David: inputReferenceAsset, outputReferenceAsset => minFeeAsset

--- PR #735 | src/shared/payment/api/transaction-specification.controller.ts ---
David: Delete

--- PR #735 | migration/1681725057644-add-transaction-specification.js ---
Context: +        await queryRunner.query(`DROP TABLE "transaction_specification"`); | +    } | +}
David: Adapt

--- PR #735 | src/shared/payment/services/transaction-specification.service.ts ---
Context: +  async dailyUpdate() { | +    this.transactionSpecifications = await this.transactionSpecificationRepo.find(); | +  }
David: Do every hour

--- PR #735 | src/shared/shared.module.ts ---
Context:      ConfigModule, |      GeoLocationModule, | +    PaymentModule,
David: Remove

--- PR #738 | src/subdomains/supporting/payout/services/payout-defichain.service.ts ---
Context:    async getUtxoForAddress(address: string): Promise<number> { | -    return parseFloat(await this.whaleService.getClient().getBalance(address)); | +    return parseFloat(await this.whaleService.getCurrentClient().getBalance(address));
David: Fetch the whale client in constructor

--- PR #738 | src/subdomains/supporting/pricing/services/integration/price-provider-defichain.service.ts ---
Context:    constructor(readonly whaleService: WhaleService, private readonly assetService: AssetService) { | -    this.client = whaleService.getClient(); | +    this.client = whaleService.getCurrentClient();
David: Subscribe to observable instead of getting the current client (see LOCK)

--- PR #746 | src/subdomains/core/buy-crypto/routes/buy/buy.service.ts ---
Context: +  async get(id: number): Promise<Buy> { | +    return this.buyRepo.findOneBy({ id }); | +  }
David: Also filter by user ID

--- PR #746 | src/subdomains/core/buy-crypto/routes/crypto-route/crypto-route.service.ts ---
Context: +  async get(id: number): Promise<CryptoRoute> { | +    return this.cryptoRepo.findOneBy({ id }); | +  }
David: Also filter by user ID

--- PR #746 | src/subdomains/core/sell-crypto/route/sell.service.ts ---
Context: +  async get(id: number): Promise<Sell> { | +    return this.sellRepo.findOneBy({ id }); | +  }
David: Also filter by user ID

--- PR #759 | src/integration/blockchain/ain/services/crypto.service.ts ---
Context: +  ): boolean { | +    return true; | +  }
David: Add a TODO here

--- PR #759 | src/integration/blockchain/ain/services/crypto.service.ts ---
Context:   |    private verifyBitcoinBased(message: string, address: string, signature: string, prefix: string | null): boolean { | +    if (this.isLightningAddress(address)) return this.verifyLightningBased(message, address, signature, prefix);
David: Why this?

--- PR #770 | src/subdomains/generic/user/models/user/user.service.ts ---
Context:   | +  async getOpenRefCreditUser(): Promise<User[]> { | +    return await this.userRepo
David: Remove `await`

--- PR #770 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: +    return await this.userRepo | +      .createQueryBuilder('user') | +      .select('user', 'user')
David: The `.select` shouldn't be required.

--- PR #770 | src/subdomains/generic/admin/dto/payout-request.dto.ts ---
Context:    BUY_FIAT_RETURN = 'BuyFiatReturn', |    MANUAL = 'Manual', | +  REF_PAYOUT = 'RefPayout',
David: I think this is not required here?

--- PR #770 | src/subdomains/core/referral/reward/ref-reward.entity.ts ---
Context:  @Entity() | -@Index('oneRewardPerUserCheck', (reward: RefReward) => [reward.txId, reward.user], { unique: true }) | +@Index('oneRewardPerUserCheck', (reward: RefReward) => [reward.txId, reward.user, reward.status], { unique: true })
David: I think this index doesn't really make sense any more?

--- PR #770 | src/subdomains/core/referral/reward/ref-reward-out.service.ts ---
Context: +          successfulRequests.push(transaction); | + | +          await this.updatePaidRefCredit([transaction.user?.id]);
David: I would only update the paid ref credit after completion, wouldn't you?

--- PR #770 | src/subdomains/core/referral/reward/ref-reward-out.service.ts ---
Context: +          await this.updatePaidRefCredit([transaction.user?.id]); | +        } catch (e) { | +          console.error(`Failed to initiate buy-crypto payout. Transaction ID: ${transaction.id}`);
David: Log typo

--- PR #770 | src/subdomains/core/referral/reward/ref-reward-out.service.ts ---
Context: +      console.error(e); | +    } | +  }
David: I think I would split this method into two methods (e.g. payoutNewTransactions and checkPendingTransactions) which are called independently from ref-reward.service, makes it more readable.

--- PR #770 | src/subdomains/core/referral/reward/ref-reward-dex.service.ts ---
Context: +    } catch (e) { | +      console.error(e); | +    }
David: This try-catch can be removed

--- PR #770 | src/subdomains/core/referral/reward/ref-reward-dex.service.ts ---
Context: +        newRefRewards, | +        (o) => o.targetBlockchain, | +      );
David: Here you could also use the simpler groupBy method:
```ts
const groupedRewardsByBlockchain = Util.groupBy<RefReward, Blockchain>(newRefRewards, 'targetBlockchain');
```

--- PR #770 | src/subdomains/core/referral/reward/ref-reward-dex.service.ts ---
Context: +  amount: number; | +  asset: Asset; | +  batchId: string;
David: Rename `batchId` to `rewardId`

--- PR #770 | src/subdomains/core/referral/reward/ref-reward-dex.service.ts ---
Context: +      targetAsset: request.asset, | +    }; | +  }
David: Can be made non-async

--- PR #770 | src/subdomains/core/referral/reward/ref-reward-dex.service.ts ---
Context: +      throw new Error(`Error in checking liquidity for a ref reward, ID: ${request.batchId}. ${e.message}`); | +    } | +  }
David: I would remove the try-catch in the `checkLiquidity` method and the `if (liquidity !== 0)` in the `processRefRewards`

--- PR #770 | src/subdomains/core/referral/reward/ref-reward.service.ts ---
Context: +        from: 'EUR', | +        to: payoutAsset.dexName, | +      });
David: We can only set the payout asset amount just before payout. Means we need to move this code to the ref-reward-dex.service (do it before liquidity check).

--- PR #770 | src/subdomains/core/referral/reward/ref-reward-dex.service.ts ---
Context: +        const assetPrice = await this.priceService.getPrice({ | +          context: PriceRequestContext.REF_REWARD, | +          correlationId: `Ref reward ${new Date().toISOString()}`,
David: `Ref reward` in correlation ID doesn't make much sense (context is already ref reward). But I forgot the blockchain, so I would use 
```ts
correlationId: `${blockchain}&${Util.isoDate(new Date())}`, 
```

--- PR #770 | src/subdomains/core/referral/reward/ref-reward-out.service.ts ---
Context:        await this.checkCompletion(transactionsPaidOut); | +    } catch (e) { | +      console.error('Failed to check paid transactions', e);
David: Minor detail: use a separator (colon) for logs to improve readability in the log:
```ts
console.error('Failed to check paid transactions:', e);
```
(same below)

--- PR #771 | src/subdomains/core/buy-crypto/process/entities/__tests__/buy-crypto.entity.spec.ts ---
Context: -      expect(updatedEntity).toBeInstanceOf(BuyCrypto); | +      expect(entity.fee).toBeInstanceOf(BuyCryptoFee); |      });
David: I think this unit test doesn't really make sense 🤔

--- PR #771 | src/subdomains/core/buy-crypto/process/services/buy-crypto-batch.service.ts ---
Context:        ); |   |        const txWithAssets = await this.defineAssetPair(txInput);
David: I think this is not saved?

--- PR #771 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
David: There are a bunch of update methods, which are not changed to the new way.

--- PR #771 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
David: For `defineAssetExchangePair()` updates are not saved if `null` is returned.

--- PR #771 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
David: `calculateOutputReferenceAmount` updates are not saved

--- PR #771 | src/subdomains/core/buy-crypto/process/services/buy-crypto-batch.service.ts ---
Context: +      for (const tx of txWithAssets) { | +        const fee = BuyCryptoFee.create(tx); | +        await this.buyCryptoRepo.update(...tx.setFeeConstraints(fee));
David: Revert

--- PR #771 | src/subdomains/core/buy-crypto/process/services/buy-crypto-out.service.ts ---
David: Fix merge conflicts

--- PR #777 | infrastructure/config/lightning/lnbits/loc.conf ---
David: I think I would remove the example settings and descriptions

--- PR #777 | infrastructure/config/lightning/thunderhub/loc.conf ---
David: I think I would remove the example settings and descriptions

--- PR #777 | src/integration/lightning/lightning.service.ts ---
David: Use the DFX logger instead of `console.log`:
```ts
private readonly logger = new DfxLogger(LightningService);

 // ... //

this.logger.info(...);
```

--- PR #777 | src/config/config.ts ---
Context: +    lightning: { | +      lnBitsApiUrl: process.env.LIGHTNING_LNBITS_API_URL, | +      lnBitsLnUrlPApiUrl: process.env.LIGHTNING_LNBITS_LNURLP_API_URL,
David: What is the difference between `lnBitsApiUrl` and `lnBitsLnUrlPApiUrl`?

--- PR #777 | src/integration/lightning/lightning-client.ts ---
Context: + | +    return true; | +  }
David: This solution works 👍 just an example, how I would have solved this (maybe it's more the JavaScript way 😉):
```ts
async getPayments(checkingId: string): Promise<PaymentDto[]> {
  const batchSize = 5;

  const result: PaymentDto[] = [];

  // for-loop to avoid infinite looping
  for (let i = 0; i < 100; i++) {
    const url = `${Config.blockchain.lightning.lnBitsApiUrl}/payments?limit=${batchSize}&offset=${i * batchSize}&sortby=time&direction=desc`;
    const payments = await this.http.get<PaymentDto[]>(url, this.httpLnBitsConfig);

    const checkItemIndex = payments.findIndex((p) => p.checking_id === checkingId);
    if (checkItemIndex => 0) {
      result.push(...payments.slice(0, checkItemIndex));
      break;
    }

    result.push(...payments);
  }

  return result;
}
```

--- PR #783 | thunder-tests/thunderEnvironment.json ---
David: Changes to `thunderEnvironment.json` should never be committed (access tokens!)

--- PR #783 | thunder-tests/thunderclient.json ---
David: Changes to `thunderclient.json` should only be committed, if the changes are useful. In this case I would just revert it.

--- PR #785 | src/config/config.ts ---
Context: +        apiKey: process.env.LIGHTNING_LNBITS_API_KEY, | +        lnUrlPUrl: process.env.LIGHTNING_LNBITS_LNURLP_URL, | +      },
David: `lnUrlPApiUrl` is very hard to read, maybe handle LNURLP as one word, like `lnurlpApiUrl`? (same for `lnUrlPUrl`)

--- PR #785 | src/integration/blockchain/shared/util/blockchain.util.ts ---
David: <img width="506" alt="image" src="https://github.com/DFXswiss/api/assets/85513542/cf77c9ac-3732-423e-b104-89ced10fd8bb">

These changes can be reverted, Lightning is already there.

--- PR #785 | src/config/config.ts ---
Context: +        apiKey: process.env.LIGHTNING_LNBITS_API_KEY, | +        lnUrlPUrl: process.env.LIGHTNING_LNBITS_LNURLP_URL, | +      },
David: I think it would be good to do this for all variable names (e.g. `LnUrlPayRequestDto` -> `LnurlPayRequestDto`).

--- PR #785 | src/integration/lightning/lightning-client.ts ---
Context:    async getBalance(): Promise<number> { | -    return this.getWallet().then((w) => w.balance); | +    return this.getWallet().then((w) => w.balance / 10 ** 3);
David: Divide by `10 ** 8` to get the BTC balance?

--- PR #785 | src/integration/lightning/lightning-client.ts ---
Context: +    for (let i = 0; i < 100; i++) { | +      const url = `${Config.blockchain.lightning.lnbits.apiUrl}/payments?limit=${batchSize}&offset=${offset}&sortby=time&direction=desc`; | +      const paymentDtoArray = await this.http.get<PaymentDto[]>(url, this.httpLnBitsConfig);
David: We normally use less "technical" variable/method names. In this case I would just use `const payments = await ...`. This makes it more readable IMO.

--- PR #785 | src/integration/lightning/lightning-client.ts ---
Context:   |    async addLnUrlPLink(description: string): Promise<LnUrlPLinkDto> { | +    if (!description) return null;
David: I would generally rather throw an error, if a required parameter is missing.
```ts
if (!description) throw new Error('Description is undefined');
```

--- PR #785 | src/integration/lightning/lnurl-forward.service.ts ---
Context: @@ -0,0 +1,28 @@ | +import { Injectable } from '@nestjs/common'; | +import { Config } from 'src/config/config';
David: Unused import

--- PR #785 | src/integration/lightning/lnurl-forward.controller.ts ---
Context: +import { LnUrlPayRequestDto } from './dto/lnurlp-payrequest.dto'; | + | +@ApiTags('LnUrlP')
David: `@ApiTags('LNURLp')`?

--- PR #785 | src/integration/lightning/lnurl-forward.service.ts ---
Context: + | +    payRequest.callback = lnbitsCallbackUrl; | +    payRequest.metadata = metadata;
David: Why don't you assign it directly?

```ts
    payRequest.callback = `${LightningHelper.lnUrlPCallbackDfxApiBasePath}/${id}`;
    payRequest.metadata = '[["text/plain", "DFX Deposit Address"]]';;
```

--- PR #785 | src/integration/lightning/lnurl-forward.controller.ts ---
Context: +  async lnUrlPCallbackForward(@Param('id') id: string, @Param('amount') amount: number): Promise<void> { | +    return this.forwardService.lnUrlPCallbackForward(id, amount); | +  }
David: Return `Promise<any>`?

--- PR #785 | src/subdomains/supporting/payin/strategies/register/impl/lightning.strategy.ts ---
Context: +      .then((input) => input?.inTxId ?? ''); | + | +    this.logger.info('lastCheckedTxId=' + lastCheckedTxId);
David: This log can be removed

--- PR #785 | src/subdomains/supporting/payin/strategies/register/impl/lightning.strategy.ts ---
Context: +        blockchain: this.blockchain, | +        type: AssetType.COIN, | +      }) ?? null;
David: Remove `supportedAssets` and get asset by
```ts
const asset = this.assetService.getLightningCoin();
```

--- PR #800 | src/subdomains/supporting/dex/strategies/check-liquidity/impl/base/check-liquidity.strategy-registry.ts ---
Context: +      // Check for 'DeFiChainDefaultStrategy' | +      strategy = super.getStrategy({ blockchain: asset.blockchain }); | +    }
David: Could be done with the nullish coalescing operator (`??`):
```ts
const strategy = super.getStrategy({ blockchain: asset.blockchain, assetType: asset.type })
   ?? super.getStrategy({ blockchain: asset.blockchain, assetCategory: asset.category })
   ?? super.getStrategy({ blockchain: asset.blockchain });
```

--- PR #800 | src/subdomains/supporting/dex/strategies/purchase-liquidity/impl/base/purchase-liquidity.strategy-registry.ts ---
Context: +      // Check for 'BitcoinStrategy' | +      strategy = super.getStrategy({ blockchain: asset.blockchain }); | +    }
David: Same here

--- PR #800 | src/subdomains/supporting/dex/strategies/sell-liquidity/impl/base/sell-liquidity.strategy-registry.ts ---
Context: +      // Check for 'BitcoinStrategy' | +      strategy = super.getStrategy({ blockchain: asset.blockchain }); | +    }
David: Same here

--- PR #800 | src/subdomains/supporting/payin/strategies/send/impl/base/send.strategy-registry.ts ---
Context: +      // Check for 'LightningStrategy' | +      strategy = super.getStrategy({ blockchain: asset.blockchain }); | +    }
David: Same here

--- PR #800 | src/subdomains/supporting/payout/strategies/payout/impl/base/payout.strategy-registry.ts ---
Context: +      // Check for 'BitcoinStrategy' | +      strategy = super.getStrategy({ blockchain: asset.blockchain }); | +    }
David: Same here

--- PR #815 | src/shared/i18n/de/mail.json ---
David: Add TX ID also to on-ramp mail

--- PR #819 | src/subdomains/supporting/bank/bank-tx/bank-tx.service.ts ---
Context: + | +    for (const tx of unassignedBankTx) { | +      const match = /^[0-9A-Z]{4}-[0-9A-Z]{4}-[0-9A-Z]{4}$/.test(tx.remittanceInfo);
David: Put the regex to `Config.formats`

--- PR #819 | src/subdomains/supporting/bank/bank-tx/bank-tx.service.ts ---
Context: +      if (match) { | +        const buy = await this.buyService.getBuyRepo().findOneBy({ bankUsage: tx.remittanceInfo }); | +        if (buy) await this.update(tx.id, { type: BankTxType.BUY_CRYPTO, buyId: buy.id });
David: Maybe add a `getByBankUsage` method to buy service and remove the `getBuyRepo`?

--- PR #819 | src/subdomains/supporting/bank/bank-tx/bank-tx.service.ts ---
Context: +      if (match) { | +        const buy = await this.buyService.getBuyRepo().findOneBy({ bankUsage: tx.remittanceInfo }); | +        if (buy) await this.update(tx.id, { type: BankTxType.BUY_CRYPTO, buyId: buy.id });
David: You need to find by the regex match. Means you need to remove the start/end conditions on the regex and adapt the check to
```ts
const match = /[0-9A-Z]{4}-[0-9A-Z]{4}-[0-9A-Z]{4}/.exec(tx.remittanceInfo);
if (match) {
   const buy = await this.buyService.getBuyRepo().findOneBy({ bankUsage: match[0] });
   ...
}
```

--- PR #819 | src/config/config.ts ---
Context:      key: /^[a-f0-9]{84}$/, |      ref: /^(\w{1,3}-\w{1,3})$/, | +    bankUsage: /^[0-9A-Z]{4}-[0-9A-Z]{4}-[0-9A-Z]{4}$/,
David: Remove start/end conditions `/[0-9A-Z]{4}-[0-9A-Z]{4}-[0-9A-Z]{4}/`

--- PR #819 | src/subdomains/supporting/bank/bank-tx/bank-tx.service.ts ---
Context:   |    // --- TRANSACTION HANDLING --- // |    @Cron(CronExpression.EVERY_MINUTE)
David: Decrease to every 30s (requested by @DFXAdmin 😉)

--- PR #821 | src/subdomains/supporting/notification/interfaces/index.ts ---
Context: +  URL = 'url', | +  DEFAULT = 'default', | +}
David: DEFAULT can be removed

--- PR #821 | src/subdomains/core/sell-crypto/process/buy-fiat-notification.service.ts ---
Context: +                { key: MailKey.SPACE, params: { value: '3' } }, | +                { key: `${MailTranslationKey.BUY_FIAT}.initiated.next_step` }, | +                { key: MailKey.DFX_TEAM_CLOSING, params: { default: 'true' } },
David: `params: { default: 'true' }` can be removed (also below)

--- PR #821 | src/subdomains/supporting/notification/entities/mail/user-mail.ts ---
Context: +  style: string; | +  key?: string; | +}
David: I think `key` should be non-nullable?

--- PR #821 | src/subdomains/supporting/notification/factories/mail.factory.ts ---
Context: +  style: string; | +  key?: string; | +}
David: This looks the same as `UserMailSuffix` 🤔

--- PR #821 | src/subdomains/supporting/notification/factories/mail.factory.ts ---
Context: +      key: this.tNew(element[0], lang), | +      value: element[1], | +    }));
David: Maybe easier with destructuring:
```ts
    return Object.entries(table).map(([key, value]) => ({
      key: this.tNew(key, lang),
      value: value,
    }));
```

--- PR #821 | src/subdomains/supporting/notification/factories/mail.factory.ts ---
Context: +          { key: '', style: `${MailDefaultStyle};padding:1px` }, | +          { key: this.tNew(`${MailTranslationKey.GENERAL}.dfx_closing_message`, lang), style: 'Zapfino' }, | +        ];
David: Maybe store the space element (`{ key: '', style: '${MailDefaultStyle};padding:1px' }`) in a class variable?

--- PR #821 | src/subdomains/supporting/notification/entities/mail/user-mail.ts ---
Context: +  style: string; | +  key?: string; | +}
David: And maybe rename `key` to `text` or similar? It's no more a key but a text (already translated).

--- PR #821 | src/subdomains/supporting/notification/factories/mail.factory.ts ---
Context: +            key: element.params?.url ? this.tNew(element.key, lang)?.split('[url:')[0] : this.tNew(element.key, lang), | +          }, | +        ];
David: Maybe do the splitting with regex and in a general method?
Parser method:
```ts
private parseSpecialTag(text: string): {text: string, tag: string, value: string} | undefined {
   const match = /^(.*)\[(\w*):(\w*)\]$/.exec(text);
   return match
      ? { text: match[1], tag: match[2], value: match[3] }
      : undefined;
}
```
Usage:
```ts
default:
   const text = this.tNew(element.key, lang);
   const specialTag = this.parseSpecialTag(text);

   return {
      url: specialTag?.tag === 'url' && element.params?.url ? {
         link: element.params.url,
         text: specialTag.value,
      } : undefined,
      style: element.params?.style ?? MailDefaultStyle,
      key: specialTag?.text ?? text;
   };
```

--- PR #821 | src/subdomains/supporting/notification/templates/userNew.hbs ---
Context: +                  {{#unless this.url}} | +                    {{this.key}} | +                  {{/unless}}
David: Can this be simplified to the following?
```hbs
{{this.key}}{{#if this.url}}<a style='color:white' href='{{this.url.link}}'>{{this.url.text}}</a>{{/if}}
```

--- PR #821 | src/subdomains/supporting/notification/factories/mail.factory.ts ---
Context: +            key: element.params?.url ? this.tNew(element.key, lang)?.split('[url:')[0] : this.tNew(element.key, lang), | +          }, | +        ];
David: Maybe you could also add an enum for special tags ...

--- PR #822 | src/subdomains/supporting/bank/bank-tx/dto/update-bank-tx.dto.ts ---
Context:   | -  @IsNotEmpty() | +  @IsOptional()
David: Needs to be `@IsNotEmpty()` (conditional validation)

--- PR #826 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +      const activeBankData = await this.bankDataRepo.findOneBy({ iban: bankData.iban, active: true }); | +      if (activeBankData) throw new BadRequestException('Active bankData with same iban found'); | +    }
David: This will find the same bank data, if it was already active.

--- PR #828 | src/integration/lightning/lightning-client.ts ---
Context: +      data, | +      this.httpLnBitsConfig(), | +    );
David: Why no use the update endpoint (PUT)?
https://vm-dfx-btc-inp-loc.westeurope.cloudapp.azure.com:5000/docs#/lnurlp/api_link_create_or_update_lnurlp_api_v1_links__link_id__put

--- PR #837 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +        where: { iban: entity.bankTx.iban, active: true }, | +        relations: ['userData'], | +      });
David: Add a method on the service and remove the repo export

--- PR #837 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context:    // --- HELPER METHODS --- // |   | +  private async saveAmlCheck() {
David: Maybe rename to `doAmlCheck`?

--- PR #837 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +      this.user.userData.status === UserDataStatus.ACTIVE && | +      this.user.userData.riskState === RiskState.C && | +      monthlyAmountInEur <= Config.amlCheckMonthlyTradingLimit
David: Maybe move this giant `if` to a separate method `isAmlPass` or similar?

--- PR #837 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +    const userBuyFee = this.user.buyFee; | +    const assetFee = Config.buy.fee.get(this.target.asset.feeTier, this.user.userData.accountType); | +    const fee = userBuyFee && userBuyFee < assetFee ? userBuyFee : assetFee;
David: This logic already exists in user.service. Maybe move it to user.entity and use it here as well?

--- PR #837 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +    const inputReferenceAmount = this.bankTx.amount + this.bankTx.chargeAmount; | +    const percentFeeAmount = fee * inputReferenceAmount; | +    const totalFeeAmount = percentFeeAmount < minVolumeInCurrency ? minVolumeInCurrency : percentFeeAmount;
David: I think this shouldn't be minVolumeInCurrency but minFeeInCurrency?

--- PR #837 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +    const percentFeeAmount = fee * inputReferenceAmount; | +    const totalFeeAmount = percentFeeAmount < minVolumeInCurrency ? minVolumeInCurrency : percentFeeAmount; | +    const usedRef = userBuyFee ? '000-000' : this.user.usedRef;
David: Move this ref logic to the user entity?

--- PR #837 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: + | +    return [this.id, update]; | +  }
David: Why not do this in a single step?

--- PR #837 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +    }); | + | +    const assetSpecifications = await this.transactionSpecificationRepo.find({ where: { system: Not('Fiat') } });
David: Remove the repo and use the TransactionHelper. If you use the `getTxDetails` method, you will have the minVolume/minFee in input fiat.

--- PR #839 | src/shared/utils/util.ts ---
Context: +  static parseMailKey(mailKey: MailTranslationKey, amlReason: AmlReason): string { | +    return `${mailKey}.${amlReason.replace(/([a-z])([A-Z])/g, '$1_$2').toLowerCase()}`; | +  }
David: This looks like some mail-specific logic, maybe move it there?

--- PR #839 | src/subdomains/supporting/notification/entities/mail/user-mail.ts ---
Context: +  mail?: { | +    mailAddress: string; | +    textSuffix?: string;
David: `mailAddress` could be renamed to `address` (it's already enclosed in mail 😉 )

--- PR #839 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +    if (!this.isCryptoCryptoTransaction) return MailTranslationKey.CRYPTO_RETURN; | +    return MailTranslationKey.FIAT_RETURN; |    }
David: This looks wrong 🤔  buy cryptos aren't always returns.

--- PR #839 | src/subdomains/supporting/notification/factories/mail.factory.ts ---
Context: +    } | +    return suffix; | +  }
David: I wouldn't use this combine logic, but translation params. E.g.:
```ts
`{ key: `${MailTranslationKey.RETURN}.introduction`, params: { reason: `${Util.parseMailKey(MailTranslationKey.RETURN, entity.amlReason)}` } }`
```
and in the translation file
```json
"introduction": "Grund, warum wir zurückerstattet haben:<br>{reason}",
```
then translate
```ts
 this.tNew(element.key, lang, params);
```
where we should also translate all params.

