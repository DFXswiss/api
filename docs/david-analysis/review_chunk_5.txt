--- PR #1835 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +    await this.userDataRepo.save(userData); | +  } | +
David: Unused?

--- PR #1835 | src/subdomains/generic/user/models/user/user.service.ts ---
Context:      user.usedRef = await this.checkRef(user, usedRef); |      user.origin = userOrigin; | +    user.status = userIsActive ? UserStatus.ACTIVE : UserStatus.NA;
David: Maybe this is better (not setting the default)?
`userIsActive && (user.status = UserStatus.ACTIVE);`

--- PR #1840 | src/subdomains/core/sell-crypto/process/services/buy-fiat-preparation.service.ts ---
Context: +        { ...request, percentFee: IsNull() }, | +        { ...request, cryptoInput: { status: PayInStatus.ACKNOWLEDGED } }, | +      ],
David: `cryptoInput` on line 167 will overwrite the one on line 162

--- PR #1840 | src/subdomains/supporting/payin/entities/crypto-input.entity.ts ---
Context:   | -    const maxFee = Math.max(totalAmount * Config.payIn.forwardFeeLimit, minInputFee); | +    const maxFee = Math.max(totalAmount * Config.payIn.forwardFeeLimit, blockchainFee);
David: We should remove the `forwardFeeLimit` and use the same mechanism as for buy crypto:
`const maxNetworkFee = fee.network ? fee.network : Config.maxBlockchainFee;` (with required currency conversion)

--- PR #1840 | src/subdomains/supporting/payin/entities/crypto-input.entity.ts ---
Context: +  get blockchainFee(): number { | +    return this.buyCrypto?.blockchainFee ?? this.buyFiat?.blockchainFee; | +  }
David: Rename to `maxForwardFee`?

--- PR #1840 | src/subdomains/supporting/payin/strategies/send/impl/base/bitcoin-based.strategy.ts ---
Context: +        if (e.message.includes('No blockchain fee provided')) continue; | + |          const logLevel = e instanceof FeeLimitExceededException ? LogLevel.INFO : LogLevel.ERROR;
David: Why is this?

--- PR #1840 | src/subdomains/supporting/payin/strategies/send/impl/base/bitcoin-based.strategy.ts ---
Context:   | -        CryptoInput.verifyEstimatedFee(targetFee, minInputFee, payIn.amount); | +        CryptoInput.verifyEstimatedFee(referenceFee, payIn.blockchainFee, payIn.amount);
David: `referenceFee` is a bit misleading. It's the fee in input asset.

--- PR #1840 | src/subdomains/supporting/payin/strategies/send/impl/base/send.strategy.ts ---
Context: -    return price.convert(fromAmount, 8); | +    return { referenceAmount: price.convert(fromAmount, 8), maxAmount: price.convert(Config.maxBlockchainFee, 8) }; |    }
David: It doesn't make sense to convert the `maxBlockchainFee` in this method, it's in CHF.

--- PR #1840 | src/subdomains/supporting/payin/strategies/send/impl/base/bitcoin-based.strategy.ts ---
Context: +        if (e.message.includes('No blockchain fee provided')) continue; | + |          const logLevel = e instanceof FeeLimitExceededException ? LogLevel.INFO : LogLevel.ERROR;
David: Ok

--- PR #1844 | src/shared/models/ip-log/ip-log.service.ts ---
Context: +  async getIpLog(ip: string): Promise<IpLog> { | +    return this.ipLogRepo.findOneBy({ ip }); | +  }
David: Looks unused

--- PR #1844 | src/shared/auth/ip-country.guard.ts ---
Context:   | +    if (req.body?.region === '1' && ipLog.country !== 'CH') | +      throw new ForbiddenException('The country of IP address is not allowed');
David: Maybe put to config?
```ts
loginCountries = {
   '1': ['CH'],
};
```

--- PR #1847 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +  private getNationalityErrors(entity: KycStep, nationality: Country): KycError[] { | +    const errors = [...this.getStepDefaultErrors(entity)]; | +    if (!nationality.nationalityEnable) errors.push(KycError.NATIONALITY_NOT_ALLOWED);
David: Why this `[...x]`?

--- PR #1847 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +  private getIdentCheckErrors(entity: KycStep, data: IdentResultData, nationality?: Country): KycError[] { | +    const errors = [...this.getStepDefaultErrors(entity)]; |      const nationalityStepResult = entity.userData
David: Same here

--- PR #1854 | src/subdomains/supporting/bank/bank-account/bank-account.controller.ts ---
Context: +    // ); |      return this.bankDataService.getValidBankDatasForUser(jwt.account).then((l) => this.toDtoList(l)); |    }
David: ?

--- PR #1854 | src/subdomains/supporting/bank/bank-account/bank-account.controller.ts ---
Context: +      .filter((b) => b.active && Array.from(new Map(bankDatas.map((item) => [item.iban, item])).values())) | +      .map((b) => this.toDto(b)); |    }
David: ?

--- PR #1854 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +            where: { userData: { id: userDataId }, iban: entity.iban }, | +            relations: { userData: true }, | +          })) ?? (await this.createBankDataInternal(entity.userData, { iban: entity.iban, type: BankDataType.USER }));
David: Why is `|| dto.active != null`?

--- PR #1860 | src/subdomains/generic/kyc/services/integration/sum-sub.service.ts ---
Context: -  private readonly kycLevel = 'CH-Standard'; | +  private readonly kycLevelOnline = 'CH-Standard'; | +  private readonly kycLevelVideo = 'CH-Standard-Video';
David: `kycLevelAuto`

--- PR #1860 | src/subdomains/generic/kyc/services/integration/sum-sub.service.ts ---
Context: +    await this.createApplicant(kycStep.transactionId, user, KycStepType.SUMSUB_VIDEO); | +    return this.generateAccessToken(kycStep.transactionId, KycStepType.SUMSUB_VIDEO).then((r) => r.token); |    }
David: Get type from KYC step

--- PR #1860 | src/subdomains/generic/kyc/services/integration/sum-sub.service.ts ---
Context: +      `/resources/applicants?levelName=${ | +        kycStepType == KycStepType.SUMSUB_AUTO ? this.kycLevelOnline : this.kycLevelVideo | +      }`,
David: `===`

--- PR #1860 | src/subdomains/generic/kyc/services/integration/sum-sub.service.ts ---
Context: +      `/resources/accessTokens?userId=${transactionId}&levelName=${ | +        kycStepType == KycStepType.SUMSUB_AUTO ? this.kycLevelOnline : this.kycLevelVideo | +      }&ttlInSecs=${expirySecs}`,
David: `===`

--- PR #1860 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: -    if (kycStep.type === KycStepType.SUMSUB_AUTO) | +    if (kycStep.type === KycStepType.SUMSUB_AUTO || kycStep.type === KycStepType.SUMSUB_VIDEO) |        throw new BadRequestException('Ident step sync is only available for IDnow');
David: `kycStep.isSumSub`?

--- PR #1860 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:   | -    return isVipUser ? KycStepType.VIDEO : customIdent ?? (defaultIdent as KycStepType); | +    return isVipUser ? KycStepType.SUMSUB_VIDEO : customIdent ?? (defaultIdent as KycStepType);
David: Revert (until we definitely switch to SumSub)

--- PR #1862 | src/subdomains/generic/user/models/bank-data/bank-data.repository.ts ---
Context: +    const currentDefault = await this.findOne({ where: { userData: { id: userData.id }, default: true } }); | +    if (currentDefault) await super.save({ ...currentDefault, default: false }); | +  }
David: Can be done in a single SQL update call 😉

--- PR #1862 | src/subdomains/generic/user/models/bank-data/bank-data.repository.ts ---
Context: + | +    return super.update(criteria, update); | +  }
David: This looks unused?

--- PR #1868 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context: +  @ManyToOne(() => Wallet, { nullable: true }) | +  wallet: Wallet; | +
David: Should we add some could to update this data for existing user datas?

--- PR #1868 | src/subdomains/supporting/notification/enums/index.ts ---
Context:    EMAIL_VERIFICATION = 'EmailVerification', | +  IDENT_STARTED = 'IDENT_STARTED', |  }
David: Is this used somewhere? Wrong format.

--- PR #1868 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +      try { | +        if (!entity.users.length) continue; | +
David: Add this check to the SQL query

--- PR #1879 | src/shared/utils/util.ts ---
Context: +  static blankCentre(value: string, visibleLength = 4): string { | +    return value.slice(0, visibleLength) + this.blankStart(value, visibleLength); | +  }
David: `Center`?

--- PR #1879 | src/shared/utils/util.ts ---
Context: +  static blankCentre(value: string, visibleLength = 4): string { | +    return value.slice(0, visibleLength) + this.blankStart(value, visibleLength); | +  }
David: True, we normally use American english 😉

--- PR #1888 | src/integration/blockchain/eurocoin/eurocoin.module.ts ---
David: Rename all eurocoin to deuro

--- PR #1888 | src/integration/blockchain/deuro/dto/deuro.dto.ts ---
David: There are some ZCHFs and XCHFs in this file 🤔

--- PR #1893 | src/subdomains/core/sell-crypto/route/dto/sell-payment-info.dto.ts ---
Context:   | +  @ApiProperty({ description: 'UID of the transaction request' }) | +  transactionRequestUid?: string;
David: Just name it `uid` and set it below `id` (for all payment infos)

--- PR #1893 | src/subdomains/core/buy-crypto/routes/swap/swap.controller.ts ---
Context:   | -    return swapDto; | +    return { ...swapDto, transactionRequestUid };
David: Set it in `transactionRequestService.create()` (same as id)

--- PR #1893 | src/subdomains/supporting/support-issue/dto/create-support-issue.dto.ts ---
Context: +  @IsOptional() | +  @IsString() | +  transactionRequestUid?: string;
David: I would name it `quoteUid`

--- PR #1893 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +          ) | +            throw new ForbiddenException('You can only create support issue for your own transactionRequest'); |          }
David: `for your own quote`

--- PR #1893 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +  ): Promise<SupportIssueDto> { | +    return this.supportIssueService.createTransactionRequestIssue(uid, { ...dto, author: CustomerAuthor }); | +  }
David: Remove?

--- PR #1893 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context:   | +      if (transactionRequest) newIssue.transactionRequest = transactionRequest; | +
David: This looks useless

--- PR #1893 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +          ) | +            throw new ForbiddenException('You can only create support issue for your own quote'); |          }
David: Also assign the transaction, if there is already one linked to the request

--- PR #1893 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +    dto: CreateSupportIssueDto, | +    transactionRequest?: TransactionRequest, | +  ): Promise<SupportIssueDto> {
David: Linter warning

--- PR #1899 | src/config/config.ts ---
Context:        zchfGatewayUrl: process.env.ZCHF_GATEWAY_URL, | +      zchfWalletPrivateKey: process.env.ZCHF_WALLET_PRIVATE_KEY, |        zchfApiKey: process.env.ALCHEMY_API_KEY,
David: This is our normal liquidity wallet

--- PR #1899 | src/config/config.ts ---
Context:        deuroGatewayUrl: process.env.DEURO_GATEWAY_URL, | +      deuroWalletPrivateKey: process.env.DEURO_WALLET_PRIVATE_KEY, |        deuroApiKey: process.env.ALCHEMY_API_KEY,
David: Same here

--- PR #1899 | src/integration/blockchain/deuro/deuro-client.ts ---
David: Hm, could we somehow use the EvmClient as a variable instead of extending it? The EvmClient has some important caching which is duplicated if we extend it.
Maybe remove the DEuroClient completely and move the logic to the DEuroService?

--- PR #1899 | src/integration/blockchain/frankencoin/frankencoin-client.ts ---
David: Same here?

--- PR #1899 | src/subdomains/core/liquidity-management/adapters/actions/base/frankencoin-based.adapter.ts ---
Context: +      throw new OrderNotProcessableException(`Buy order ${order.id} failed: ${e.message}`); | +    } | +  }
David: This will probably not work for DEuro? 😉

--- PR #1899 | src/subdomains/core/liquidity-management/adapters/actions/base/frankencoin-based.adapter.ts ---
Context: +      this.logger.error(`Buy order ${order.id} failed:`, e); | +      throw new OrderNotProcessableException(`Buy order ${order.id} failed: ${e.message}`); | +    }
David: Throw `OrderFailedException`?
`OrderNotProcessableException` -> order currently not processable due to missing liquidity
`OrderFailedException` -> unknown or unrecoverable error

--- PR #1899 | src/subdomains/core/liquidity-management/adapters/actions/base/frankencoin-based.adapter.ts ---
Context: +export enum FrankencoinBasedAdapterCommands { | +  BUY = 'buy', | +}
David: Maybe call it `mint`?

--- PR #1899 | src/subdomains/core/liquidity-management/adapters/actions/base/frankencoin-based.adapter.ts ---
Context: +    if (!txReceipt) return false; | + | +    return !!txReceipt.status;
David: `return client.isTxComplete(txHash);`?

--- PR #1913 | src/subdomains/generic/user/models/user-data-relation/user-data-relation.controller.ts ---
Context:    @ApiBearerAuth() | +  @ApiExcludeEndpoint() |    @UseGuards(AuthGuard(), new RoleGuard(UserRole.ADMIN))
David: The entire controller is excluded from Swagger

--- PR #1916 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context: -      return { limit: Config.tradingLimits.dailyDefault, period: LimitPeriod.DAY }; | +      return { limit: Config.tradingLimits.monthlyDefaultWoKyc, period: LimitPeriod.DAY }; |      }
David: `LimitPeriod.MONTH`? Need to update the Services 🤔

--- PR #1916 | src/shared/i18n/de/mail.json ---
Context: +            "monthly_limit": { | +                "title": "30d Limit überschritten", | +                "salutation": "Du hast dein 30d Volumenlimit überschritten",
David: `30 Tage` (same for other languages)?

--- PR #1916 | src/shared/i18n/es/mail.json ---
Context:                  "line3": "Alternativamente, también puede hacer clic en el elemento de menú en la parte superior derecha de la página web de DFX-Services <br> y luego en \"KYC\" <br> e iniciar la verificación a través del botón \"Continuar\".", | -                "line4": "Sin la verificación, puedes continuar usando nuestro banco en Suiza hasta 1,000 euros al día sin el KYC con transferencias SEPA ordinarias.", | +                "line4": "Sin verificación, puede seguir utilizando nuestra cuenta bancaria de Suiza hasta 1.000 euros al mes sin KYC a través de Sepa estándar.",
David: Should we just switch `dia` -> `mes` and keep the rest the same?

--- PR #1916 | src/shared/i18n/es/mail.json ---
Context: +                "title": "Límite 30d superado", | +                "salutation": "Ha superado el límite de volumen de 30 días", |                  "line1": "Por favor, verificate a través de KYC (conoce a tu cliente) para incrementar tu límite.",
David: Same here

--- PR #1916 | src/shared/i18n/fr/mail.json ---
Context:                  "line3": "Vous pouvez également cliquer sur l'élément de menu en haut à droite du site web de DFX-Services <br> puis sur \"KYC\" <br> et lancer la vérification en cliquant sur le bouton \"Continuer\".", | -                "line4": "Sans vérification, vous pouvez continuer à utiliser notre compte bancaire suisse jusqu'à 1 000 euros par jour en SEPA standard.", | +                "line4": "Sans vérification, vous pouvez continuer à utiliser notre compte bancaire en Suisse jusqu'à 1 000 EUR par mois sans KYC via le système standard Sepa.",
David: Same here

--- PR #1916 | src/shared/i18n/it/mail.json ---
Context: +                "title": "Limite 30d superato", | +                "salutation": "Avete superato il limite di volume di 30 giorni", |                  "line1": "Per favore, completa la verifica KYC (conosci il tuo cliente) per incrementare il tuo limite.",
David: Same here

--- PR #1916 | src/shared/i18n/it/mail.json ---
Context:                  "line3": "In alternativa, potete anche cliccare sulla voce di menu in alto a destra del sito DFX-Services <br>e poi su \"KYC\" <br>e avviare la verifica tramite il pulsante \"Continua\".", | -                "line4": "Senza verifica, puoi continuare a usare il nostro conto corrente svizzero fino a 1000 EUR al giorno senza KYC utilizzando il SEPA standard.", | +                "line4": "Senza verifica, potete continuare a utilizzare il nostro conto bancario dalla Svizzera fino a 1.000 euro al mese senza KYC tramite la Sepa standard.",
David: Same here

--- PR #1916 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context: -    return { kycLimit, defaultLimit }; | +    return { kycLimit: kycLimit < 0 ? 0 : kycLimit, defaultLimit }; |    }
David: `max(0, kycLimit)`

--- PR #1916 | src/shared/i18n/es/mail.json ---
Context: +            "monthly_limit": { | +                "title": "Límite de 30 días superado", | +                "salutation": "Ha superado su límite de volumen de 30 días",
David: Switched from `tu` (dein) to `su` (Ihr)? 🤔

--- PR #1916 | src/shared/i18n/it/mail.json ---
Context: +            "monthly_limit": { | +                "title": "Superato il limite di 30 giorni", | +                "salutation": "Avete superato il limite di volume di 30 giorni",
David: Same here

--- PR #1916 | src/shared/i18n/it/mail.json ---
Context: +            "monthly_limit": { | +                "title": "Superato il limite di 30 giorni", | +                "salutation": "Avete superato il tuo limite di volume di 30 giorni",
David: `Hai superato il tuo limite di volume di 30 giorni` 😉

--- PR #1929 | src/subdomains/supporting/support-issue/dto/support-log.dto.ts ---
Context: + | +export class SupportLogDto { | +  type: SupportLogType;
David: interface?

--- PR #1929 | src/subdomains/supporting/support-issue/support-issue.module.ts ---
Context: +    LimitRequestLog, | +    LimitRequestLogRepository, | +    SupportLogService,
David: Add entities to TypeOrmModule (above)?

--- PR #1929 | src/subdomains/supporting/support-issue/services/limit-request.service.ts ---
Context: +      limitRequest: entity, | +      ...update, | +    });
David: This doesn't work as far as I know. You need to use the LimitRequestLogRepo to create a limit request log.

--- PR #1929 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +      supportIssue: entity, | +      ...dto, | +    });
David: Same here

--- PR #1929 | src/subdomains/supporting/support-issue/services/limit-request.service.ts ---
Context: +      limitRequest: entity, | +      ...update, | +    });
David: And the type was correct?

--- PR #1931 | src/subdomains/core/buy-crypto/process/services/buy-crypto-notification.service.ts ---
Context:          if (entity.userData.mail) { | +          const mailWarningWalletIds = await this.settingService.getObj<number[]>('mailWarningWalletIds', []); | +
David: Why not store it in the wallet table?

--- PR #1931 | src/subdomains/core/buy-crypto/process/services/buy-crypto-notification.service.ts ---
Context:          if (entity.userData.mail) { | +          const mailWarningWalletIds = await this.settingService.getObj<number[]>('mailWarningWalletIds', []); | +
David: Yes, I would prefer this

--- PR #1932 | src/subdomains/generic/gs/gs.service.ts ---
David: Wouldn't it be better to just add a cron job for checking the connection pool periodically, maybe every second?

--- PR #1940 | src/subdomains/core/buy-crypto/routes/buy/buy.controller.ts ---
Context: +  @ApiOkResponse() | +  async confirmTransactionRequest(@GetJwt() jwt: JwtPayload, @Param('id') id: string): Promise<void> { | +    const request = await this.transactionRequestService.getOrThrow(+id, jwt.user);
David: Let's call it `confirmBuy()`? Would be similar to sell.

--- PR #1940 | src/subdomains/core/buy-crypto/routes/buy/buy.controller.ts ---
Context: +      throw new ConflictException('Transaction request is already confirmed'); | +    if (Util.daysDiff(request.created) >= Config.txRequestWaitingExpiryDays) | +      throw new BadRequestException('Transaction request too old to confirm');
David: `Transaction request is expired`

--- PR #1940 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: +      } catch (e) { | +        this.logger.error(`Error in TxRequest waiting expiry check for id ${entity.id}`, e); | +      }
David: This try-catch is a bit useless 😉

--- PR #1940 | src/subdomains/supporting/payment/entities/transaction-request.entity.ts ---
Context:   | +  // TODO: remove and replace with entity method isComplete |    @Column({ default: false })
David: TODO

--- PR #1940 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +          if (!newIssue.transaction.user || newIssue.transaction.user.userData.id !== newIssue.userData.id) | +            throw new ForbiddenException('You can only create support issue for your own transaction'); |          }
David: What is this?

--- PR #1940 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context: +      feeAmount: txRequest.totalFee | +        ? Util.roundReadable(txRequest.totalFee * txRequest.amount, feeAmountType(txRequest.sourceAssetEntity)) | +        : null,
David: Total fee is an amount?
Use value from `mapFees`?

--- PR #1940 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context: +      targetAccount: txRequest.type === TransactionRequestType.BUY ? txRequest.user?.address : null, // TODO: load route relation for iban? | +    }; | +  }
David: TODO

--- PR #1940 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: + | +    const entities = await this.transactionRequestRepo.find({ where: { isComplete: true }, take: 5000 }); | +
David: Filter by status?

--- PR #1940 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: + | +    return Object.assign(txRequest, { sourceAssetEntity, targetAssetEntity }); | +  }
David: Can we use this in other places in our code?

--- PR #1940 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context: + | +      return { | +        rate: entity.rate,
David: I think `entity.rate` is the total exchange rate, and rate here is the fee rate?

--- PR #1940 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
David: If a transaction arrives for a pending transaction request, but the amount does not match, the transaction request should be marked as not pending

--- PR #1940 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: -    const dto = await this.txToTransactionDto(transaction); | +    const dto = | +      tx instanceof Transaction ? await this.txToTransactionDto(tx) : await this.waitingTxRequestToTransactionDto(tx);
David: Put this into a helper method

--- PR #1940 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: +        return this.waitingTxRequestToTransactionDto(txRequest, true); | +      }).then((list) => list.filter((dto) => dto))), | +    ];
David: Use this method here?

--- PR #1940 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context: +        ? Util.roundReadable(fees.total * txRequest.amount, feeAmountType(txRequest.sourceAssetEntity)) | +        : null, | +      feeAsset: fees.total ? txRequest.sourceAssetEntity.name : null,
David: Is fees.total an amount?

--- PR #1940 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: +      } | +    } | +  }
David: This try-catch is useless

--- PR #1940 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: +      } | +    } | +  }
David: Should we also update the ones that are not completed?

--- PR #1940 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: +      ? await this.txToTransactionDto(tx, detailed) | +      : await this.waitingTxRequestToTransactionDto(tx, detailed); | +  }
David: Linter errors

--- PR #1940 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: +        return this.getTransactionDto(txRequest, true); | +      }).then((list) => list.filter((dto) => dto))), | +    ];
David: Haha, this is completely unreadable 🤣 

Can't we do this?
```ts
    const txList = await this.transactionService.getTransactionsForAccount(userDataId, query.from, query.to)
        .then((l) => l.filter((tx) => tx.targetEntity));
    const waitingTxRequestList = await this.transactionRequestService.getWaitingTransactionRequest(
      userDataId,
      query.from,
      query.to,
    );

    // map to DTO
    return Util.asyncMap([...txList, ...waitingTxRequestList], (tx) => this.getTransactionDto(tx, true))
        .then((l) => l.filter((tx) => tx)));
```

--- PR #1940 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: +    tx: Transaction | TransactionRequest, | +    detailed = false, | +  ): Promise<UnassignedTransactionDto | TransactionDto> {
David: Return value can be undefined

--- PR #1944 | src/shared/utils/util.ts ---
Context: +        return Util.delay(random * 60); // 0 .. 60 sec | +    } | +  }
David: I would move this as a private method to the DfxCronService

--- PR #1944 | src/shared/services/dfx-cron.service.ts ---
Context: +    cronJob.start(); | + | +    this.logger.info(`CronJob ${cronJobName} started`);
David: I would remove this log 🤔

--- PR #1944 | src/shared/services/dfx-cron.service.ts ---
Context: +      await data.methodRef.apply(data.instance, args); | +    }; | +  }
David: This method name does not really make sense 😁

--- PR #1955 | src/subdomains/core/monitoring/observers/exchange.observer.ts ---
Context: +  @Cron(CronExpression.EVERY_10_SECONDS) | +  @Lock(1800) | +  async fetch() {
David: Every 10 seconds is a bit often 🤔  maybe 10 minutes?

--- PR #1955 | src/subdomains/core/monitoring/observers/exchange.observer.ts ---
Context: +  @Cron(CronExpression.EVERY_10_SECONDS) | +  @Lock(1800) | +  async fetch() {
David: Use the new `@DfxCron` decorator from develop

--- PR #1955 | src/subdomains/core/monitoring/observers/exchange.observer.ts ---
Context: +      type: ExchangeTxType.TRADE, | +      created: MoreThan(Util.daysBefore(30)), | +    });
David: Didn't know this method exists 🤯

--- PR #1955 | src/subdomains/core/monitoring/observers/exchange.observer.ts ---
Context: +  private async getVolume30(exchange: ExchangeName): Promise<number> { | +    return this.repos.exchangeTx.sum('vol', { | +      exchange,
David: `vol` is probably in some currency? We need to convert everything to CHF or EUR or USD.

--- PR #1955 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: +            ExchangeTxType.TRADE ? entity.cost : entity.amount, | +            Config.defaultVolumeDecimal, | +          );
David: Move this out of the `if (entity.feeAmount && !entity.feeAmountChf) {...}` and place it in its own `if (entity.amount && !entity.amountChf) {...}`

--- PR #1955 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: +              type: undefined, | +              name: entity.type == ExchangeTxType.TRADE ? entity.symbol.split('/')[1] : entity.symbol.split('/')[0], | +            }));
David: ```ts
const currencyName = entity.type === ExchangeTxType.TRADE ? entity.symbol.split('/')[0] : entity.currency;
const currency = await this.fiatService.getFiatByName(assetName) ?? await this.assetService.getAssetByQuery({
              blockchain: undefined,
              type: undefined,
              name: currencyName,
            });
```

--- PR #1955 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: +            })); | +          const amountChf = await this.pricingService.getPrice(volAsset, this.chf, true); | +
David: `const priceChf = ...`

--- PR #1955 | src/integration/exchange/services/exchange-tx.service.ts ---
Context: +            ExchangeTxType.TRADE ? entity.cost : entity.amount, | +            Config.defaultVolumeDecimal, | +          );
David: ```ts
entity.amountChf = priceChf.convert(entity.amount, Config.defaultVolumeDecimal);
```

--- PR #1956 | src/shared/auth/user.guard.ts ---
Context: +export class UserGuard implements CanActivate { | +  async canActivate(context: ExecutionContext): Promise<boolean> { | +    const req = context.switchToHttp().getRequest();
David: Maybe we should rename this to `UserActiveGuard`?

--- PR #1956 | src/shared/auth/user.guard.ts ---
Context: +      userDataStatus !== UserDataStatus.BLOCKED && | +      userDataStatus !== UserDataStatus.DEACTIVATED | +    );
David: We should activate this in a second step. Because all existing JWTs will not have the new status fields.

--- PR #1956 | src/shared/auth/jwt-payload.interface.ts ---
Context: +  userStatus?: UserStatus; | +  userDataStatus?: UserDataStatus; |  }
David: `accountStatus`

--- PR #1956 | src/shared/auth/user.guard.ts ---
Context: +      userDataStatus !== UserDataStatus.BLOCKED && | +      userDataStatus !== UserDataStatus.DEACTIVATED | +    );
David: True 👍  could do `[UserStatus.BLOCKED, UserStatus.DELETED].includes(userStatus) && ...`

--- PR #1956 | src/shared/auth/user-active.guard.ts ---
Context: +      userDataStatus !== UserDataStatus.BLOCKED && | +      userDataStatus !== UserDataStatus.DEACTIVATED | +    );
David: accountStatus?

--- PR #1956 | src/shared/auth/user-active.guard.ts ---
Context: +    const req = context.switchToHttp().getRequest(); | +    const userStatus = req.user.userStatus; | +    const accountStatus = req.user.userDataStatus;
David: `accountStatus`

--- PR #1960 | src/shared/utils/queue-handler.ts ---
Context: +      this.workParallelCounter = 0; | +    } | +  }
David: ```ts
  private parallelWork(work: Promise<void>): void {
    this.workParallelCounter++;

    void work.finally(() => this.workParallelCounter--);
  }
```

--- PR #1960 | src/shared/utils/queue-handler.ts ---
Context:        try { |          if (this.queue.length > 0) { |            const work = this.queue.shift().doWork(this.itemTimeout);
David: ```ts
if (this.queue.length > 0 && this.workParallelCounter < this.maxWorkParallel) {
```

--- PR #1960 | src/shared/utils/queue-handler.ts ---
Context: -    private workParallel = false, | +    private readonly workParallel = false, | +    private readonly maxWorkParallel = Infinity,
David: Remove `workParallel`, `maxWorkParallel = 1`

--- PR #1960 | src/subdomains/core/payment-link/services/payment-webhook.service.ts ---
Context: -    this.webhookSendQueue = new QueueHandler(undefined, undefined, true); | +    this.webhookSendQueue = new QueueHandler(undefined, undefined, 10); |    }
David: Use `QueueHandler. createParallelQueueHandler`?

--- PR #1962 | src/subdomains/supporting/bank-tx/bank-tx/entities/bank-tx.entity.ts ---
Context: +      this.buyCryptoChargeback?.user ?? | +      (this.buyFiats.length === 1 ? this.buyFiats?.[0].user : undefined) | +    );
David: `this.buyFiats?.[0]?.user`
Or should it return undefined if there are multiple buyFiats?

--- PR #1964 | src/subdomains/generic/kyc/services/integration/sum-sub.service.ts ---
Context: +  async getApplicantMetadata(applicantId: string): Promise<any> { | +    return this.callApi(`/resources/applicants/${applicantId}/metadata/resources`, 'GET'); | +  }
David: Do we know the response type?

--- PR #1964 | src/subdomains/generic/kyc/dto/sum-sub.dto.ts ---
Context: + | +export interface SUmSubReviewResult { | +  reviewAnswer: ReviewAnswer;
David: Typo `SUmSub...`

--- PR #1965 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:      // verification checks | +    if (paymentMethodIn === FiatPaymentMethod.CARD && user && !user.userData.completeName) | +      return QuoteError.NAME_REQUIRED;
David: No error if user is active

--- PR #1967 | migration/1739872103563-ChangeWalletAmlRule.js ---
Context: +        await queryRunner.query(`ALTER TABLE "dbo"."wallet" DROP CONSTRAINT "DF_acb672652f95b21fb90e025dce0"`); | +        await queryRunner.query(`ALTER TABLE "dbo"."wallet" ADD CONSTRAINT "DF_19a183bac114682a3a8920e7961" DEFAULT 0 FOR "amlRules"`); | +        await queryRunner.query(`ALTER TABLE "dbo"."wallet" DROP CONSTRAINT "DF_19a183bac114682a3a8920e7961"`);
David: Drop these lines?

--- PR #1972 | src/subdomains/supporting/bank-tx/bank-tx/services/sepa-parser.service.ts ---
Context:   | +    const referenceAsset = await this.fiatService.getFiatByName(charges[0].Amt['@_Ccy']); | +
David: Don't we want the charge amount in the sender asset? It's not really defined, what the currency of the first charge is, is it?

--- PR #1972 | src/subdomains/supporting/bank-tx/bank-tx/services/sepa-parser.service.ts ---
Context: +        senderChargeAmountChf += chargeChfPrice.convert(amount, Config.defaultVolumeDecimal); | +        senderChargeCurrencies.add(currency); | +      }
David: I think it doesn't make sense to list all sender charge currencies if we anyway convert it to the reference currency?

--- PR #1973 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context:    @Get('single') | -  @ApiExcludeEndpoint() |    async getSingleTransaction(
David: Add `@ApiOkResponse`?

--- PR #1973 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context:      @Query('external-id') externalId?: string, |    ): Promise<TransactionDto | UnassignedTransactionDto> { | -    const transaction = await this.getTransaction({ id, uid, requestId, externalId });
David: Add `request-uid` query parameter here (just for completeness)

--- PR #1973 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context:   | +  @ApiPropertyOptional({ description: 'UID of the request of the transaction' }) | +  requestUid?: string;
David: Maybe we should call it `orderUid`?

--- PR #1973 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: -    if (externalId) transaction = await this.transactionService.getTransactionByExternalId(externalId, relations); | +    if (externalId) | +      transaction = await this.transactionService.getTransactionByExternalId(externalId, accountId, relations);
David: `if (externalId && accountId)`?

--- PR #1973 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context:   | +  @ApiPropertyOptional({ description: 'UID of the order of the transaction' }) | +  orderUid?: string;
David: Also adapt description in  PaymentInfoDtos

--- PR #1983 | src/subdomains/supporting/bank-tx/bank-tx/services/bank-tx.service.ts ---
Context: +          }); | +          continue; | +        }
David: Does this work if they get assigned later on?

--- PR #1983 | src/subdomains/supporting/bank-tx/bank-tx/services/bank-tx.service.ts ---
Context: +            : entity.type === BankTxType.BUY_FIAT | +            ? entity.buyFiats[0].amountInChf | +            : undefined;
David: Maybe create a single if-else (with entity.type === BankTxType.BUY_CRYPTO) and assign all values together?

--- PR #1983 | src/subdomains/supporting/bank-tx/bank-tx/services/bank-tx.service.ts ---
Context: +        const update: Partial<BankTx> = { | +          accountingFeePercent: entity.buyCrypto?.percentFee ?? entity.buyFiats?.[0]?.percentFee, | +        };
David: Why not move the `accountingFeePercent` also into the if-else below?

--- PR #1987 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context:            identDocumentId: kycStep.identDocumentId, | +          olkypayAllowed: userData.olkypayAllowed == null ? true : userData.olkypayAllowed, |            nationality,
David: `userData.olkypayAllowed ?? true`?

--- PR #1988 | src/subdomains/core/custody/services/custody-service.ts ---
Context: +    ); | + | +    await this.siftService.createAccount(custodyUser);
David: Move sift to `createUser`

--- PR #1988 | src/subdomains/core/custody/custody.module.ts ---
Context: +  providers: [CustodyService], | +  exports: [CustodyService], | +})
David: Remove SiftModule

--- PR #1988 | src/subdomains/core/custody/services/custody-service.ts ---
Context: + | +    const user = await this.userService.getUser(userId, { userData: true }); | +    if (!user) throw new NotFoundException('User not exist');
David: Get UserData instead (with jwt.accountId)

--- PR #1988 | src/subdomains/generic/user/models/user/user.service.ts ---
Context:      try { | +      await this.siftService.createAccount(user); |        if (specialCode) await this.feeService.addSpecialCodeUser(user, specialCode);
David: Move out of try-catch

--- PR #1988 | src/subdomains/generic/user/models/user/user.service.ts ---
Context:      try { | +      await this.siftService.createAccount(user); |        if (specialCode) await this.feeService.addSpecialCodeUser(user, specialCode);
David: Remove from all calls of createUser

--- PR #1989 | src/subdomains/core/sell-crypto/process/services/buy-fiat-preparation.service.ts ---
Context: +        isComplete: false, | +        transaction: { userData: { kycStatus: In([KycStatus.NA, KycStatus.COMPLETED]) } }, | +        chargebackAddress: Not(IsNull()),
David: Check KYC level also?

--- PR #1989 | src/subdomains/core/buy-crypto/process/services/buy-crypto-preparation.service.ts ---
Context: +      isComplete: false, | +      transaction: { userData: { kycStatus: In([KycStatus.NA, KycStatus.COMPLETED]) } }, | +    };
David: Same here?

--- PR #1994 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @IsBoolean() | +  answer: boolean; | +
David: hasBeneficialOwners

--- PR #1994 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: + | +  @ApiProperty({ isArray: true }) | +  @ValidateIf((d: KycBeneficialData) => d.hasBeneficialOwners)
David: `@ApiPropertyOptional`

--- PR #1994 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +      }); | + | +      const bankData = await this.bankDataService.createVerifyBankData(beneficialOwner, {
David: Unused variable?

--- PR #1994 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @ValidateIf((d: KycBeneficialData) => d.hasBeneficialOwners) | +  @IsNotEmpty() | +  beneficialOwners: BeneficialOwnerData[];
David: ```ts
@ApiPropertyOptional({ type: BeneficialOwnerData, isArray: true })
@ValidateIf((d: KycBeneficialData) => d.hasBeneficialOwners)
@IsNotEmpty()
@IsArray()
@ValidateNested({ each: true })
@Type(() => BeneficialOwnerData)
```

--- PR #1995 | src/subdomains/generic/kyc/enums/kyc.enum.ts ---
Context: +    (userData.legalEntity !== LegalEntity.LIMITED_LIABILITY_COMPANY || | +      !userData.organizationCountry || | +      userData.organizationCountry.symbol !== 'CH')
David: Invert

--- PR #1999 | migration/1739907449972-AddAmountChf.js ---
David: Revert?

--- PR #1999 | migration/1740525329755-AddCustodyOrder.js ---
David: Is this correct? We cannot edit a migration after it's merged to DEV.

--- PR #1999 | src/subdomains/core/custody/entities/custody-order.entity.ts ---
Context:   | +  @OneToMany(() => CustodyOrderStep, (step) => step.order, { nullable: false }) | +  steps: CustodyOrderStep;
David: `steps: CustodyOrderStep[];`

--- PR #1999 | src/subdomains/core/custody/services/custody-service.ts ---
Context: +          if (await this.dfxOrderStepAdapter.isComplete(step)) | +            await this.custodyOrderStepRepo.update(...step.complete()); | +          await this.nextStep(step);
David: Missing {} brackets?

--- PR #1999 | src/subdomains/core/custody/services/custody-service.ts ---
Context: + | +  async nextStep(step: CustodyOrderStep): Promise<void> { | +    const nextIndex = step.index + 1;
David: `startNextStep`

--- PR #1999 | src/subdomains/core/custody/services/custody-service.ts ---
Context: +        context: nextStep.context, | +      }); | +      await this.custodyOrderStepRepo.save(orderStep);
David: Move this to helper method and reuse on line 69-75

--- PR #2002 | src/subdomains/core/payment-link/services/payment-activation.service.ts ---
Context: +    return { paymentRequest }; | +  } | +
David: This logic is actually already implemented in CryptoService, also for other chains (Monero is missing)

--- PR #2016 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +export class KycOperationalData { | +  @ApiProperty({ description: 'Is organization operational activity?' }) | +  @IsNotEmpty()
David: `Is the organization operationally active?`

--- PR #2016 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsBoolean() | +  isOperationalActive: boolean;
David: `isOperationallyActive` or `isOperational`

--- PR #2016 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @ValidateNested({ each: true }) | +  @Type(() => BeneficialOwnerData) | +  managingDirector: BeneficialOwnerData;
David: isArray? {each: true}?

--- PR #2016 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @ValidateNested({ each: true }) | +  @Type(() => BeneficialOwnerData) | +  managingDirector: BeneficialOwnerData;
David: Maybe we should rename `BeneficialOwnerData` to something more generic?

--- PR #2016 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @ValidateNested({ each: true }) | +  @Type(() => BeneficialOwnerData) | +  managingDirector: BeneficialOwnerData;
David: Maybe `OrganizationMemberData`?

--- PR #2016 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: @@ -156,8 +156,7 @@ export class KycBeneficialData { |    @ApiPropertyOptional({ type: BeneficialOwnerData, isArray: true }) |    @ValidateIf((d: KycBeneficialData) => !d.hasBeneficialOwners && !d.accountHolderIsBeneficialOwner)
David: isArray

--- PR #2016 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @ValidateNested({ each: true }) | +  @Type(() => BeneficialOwnerData) | +  managingDirector: BeneficialOwnerData;
David: It's all about users linked to an organization, right?

--- PR #2018 | src/integration/exchange/controllers/exchange.controller.ts ---
Context:    async trade(@Param('exchange') exchange: string, @Body() { from, to, amount }: TradeOrder): Promise<number> { | +    if (DisabledProcess(Process.EXCHANGE_TRADE)) throw new BadRequestException('Safety module deactivated'); | +
David: I would just write `throw new BadRequestException('Process disabled')` as we don't know if it really is the safety module

--- PR #2018 | src/shared/services/process.service.ts ---
Context:  export class ProcessService implements OnModuleInit { | +  private safetyModuleInactive = false; | +
David: `private safetyModeActive = true;`?

--- PR #2018 | src/shared/services/process.service.ts ---
Context: +  public changeSafetyModuleSetting(deactivate: boolean) { | +    this.safetyModuleInactive = deactivate; | +  }
David: ```ts
  public setSafetyModeActive(active: boolean) {
    this. safetyModeActive = active;
  }
```

--- PR #2018 | src/shared/services/process.service.ts ---
Context:  export class ProcessService implements OnModuleInit { | +  private safetyModuleInactive = false; | +
David: I think the name safety module is misleading. I would call it safety mode (protected mode), that's a common term for systems.
- safety mode inactive (normal mode) -> normal operation
- safety mode active -> no transactions are made

-> you can still call the variable `private safetyModeInactive = true;`

Or maybe you want to call it something like `private paymentOperationsActive = true;`, if that's clearer.

--- PR #2020 | src/subdomains/generic/kyc/services/integration/financial.service.ts ---
Context: +  ): KycFinancialQuestion[] { | +    return FinancialQuestions.filter((q) => q.accountTypes.includes(accountType)).map((q) => ({ |        key: q.key,
David: Maybe adapt the filter like this
```ts
 return FinancialQuestions.filter((q) => !q.accountTypes || q.accountTypes.includes(accountType)).map((q) => ({ ...
```
and remove the accountTypes array from entries which are active for all?

--- PR #2028 | src/subdomains/supporting/payout/services/payout-evm.service.ts ---
Context: -    this.client = service.getDefaultClient(); | +    this.evmClient = service.getDefaultClient(); |    }
David: - Revert the renaming in all implementations
- Make evmClient abstract (`protected abstract client: EvmClient;`)
- Remove constructor in this service

--- PR #2031 | src/subdomains/core/buy-crypto/routes/buy/buy.service.ts ---
Context: -    const user = await this.userService.getUser(userId, { userData: { users: true }, wallet: true }); | +    const user = await this.userService.getUser(userId, { userData: true, wallet: true }); | +    user.userData.users = await this.userService.getAllUserDataUsers(user.userData.id);
David: This is probably counterproductive

--- PR #2031 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +    buy.user.userData.bankDatas = await this.bankDataService.getAllBankDatasForUser(buy.user.userData.id); | + |      return buy;
David: This is probably counterproductive

--- PR #2031 | src/subdomains/core/sell-crypto/route/sell.service.ts ---
Context: -    const user = await this.userService.getUser(userId, { userData: { users: true }, wallet: true }); | +    const user = await this.userService.getUser(userId, { userData: true, wallet: true }); | +    user.userData.users = await this.userService.getAllUserDataUsers(user.userData.id);
David: This is probably counterproductive

--- PR #2031 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context:  import { UserDataService } from '../../user/models/user-data/user-data.service'; | +import { UserService } from '../../user/models/user/user.service'; |  import { WalletService } from '../../user/models/wallet/wallet.service';
David: Unused import

--- PR #2031 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:      }); |      if (!user) throw new NotFoundException('User not found'); | +    user.userData.users = await this.userService.getAllUserDataUsers(user.userData.id);
David: This is probably counterproductive

--- PR #2031 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context: -    const userData = userDataId && (await this.userDataService.getUserData(userDataId, { users: true })); | +    const userData = userDataId && (await this.userDataService.getUserData(userDataId)); | +    if (userData) userData.users = await this.userService.getAllUserDataUsers(userData.id);
David: This is probably counterproductive

--- PR #2031 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context: -      const account = await this.userDataService.getUserData(entry.userDataId, { users: true }); | +      const account = await this.userDataService.getUserData(entry.userDataId); | +      account.users = await this.userService.getAllUserDataUsers(account.id);
David: This is probably counterproductive

--- PR #2031 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context:      if (!userData) throw new NotFoundException('User data not found'); |      if (userData.status === UserDataStatus.MERGED) throw new BadRequestException('User data is merged'); | +    userData.bankDatas = await this.bankDataRepo.findBy({ userData: { id: userData.id } });
David: This is probably counterproductive

--- PR #2031 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: + | +    return userData; |    }
David: Should we switch to single query here?

--- PR #2031 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: +      relations: { wallet: true }, | +    }); | +    userData.kycSteps = await this.kycAdminService.getKycSteps(userData.id);
David: This is overkill

--- PR #2031 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context:    private async getUser(kycHash: string): Promise<UserData> { | -    return this.userDataService.getByKycHashOrThrow(kycHash, { users: true, kycSteps: { userData: true } }); | +    return await this.userDataService.getByKycHashOrThrow(kycHash, { users: true, kycSteps: true });
David: Linter error

--- PR #2031 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context:    private async getUser(kycHash: string): Promise<UserData> { | -    return this.userDataService.getByKycHashOrThrow(kycHash, { users: true, kycSteps: { userData: true } }); | +    return await this.userDataService.getByKycHashOrThrow(kycHash, { users: true, kycSteps: true });
David: Is userData relation not required?

--- PR #2038 | src/subdomains/generic/kyc/services/name-check.service.ts ---
Context: +      contentType as ContentType, | +      false, // TODO protected data or not? | +    );
David: Everything that is not user-uploaded should be protected

--- PR #2041 | src/config/config.ts ---
Context:    refRewardManualCheckLimit = 3000; // EUR |    manualPriceStepSourceName = 'DFX'; // source name for priceStep if price is set manually in buyCrypto | +  exchangeRateFromLiquidityOrder = ['FPS', 'DEPS'];
David: `nDEPS` instead of `DEPS`

--- PR #2041 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +      Config.exchangeRateFromLiquidityOrder.includes(this.outputAsset.name) && | +      this.liquidityPipeline?.orders?.length // TODO more checks needed? Is order always filled with exchangePrice at this point? | +    ) {
David: Is the pipeline loaded in DB query?

--- PR #2041 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +      Config.exchangeRateFromLiquidityOrder.includes(this.outputAsset.name) && | +      this.liquidityPipeline?.orders?.length // TODO more checks needed? Is order always filled with exchangePrice at this point? | +    ) {
David: If the pipeline is still running, we cannot calculate the output amount. Would need to abort here 🤔  if it's failed just use the normal pricing, if it's successful, the price should be there.

--- PR #2041 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +      ]; | + | +      return this;
David: pipelinePrice ist only ZCHF -> FPS, but the price we need might be something different (e.g. BTC -> FPS).
-> update the price steps and recalculate the total price

--- PR #2041 | src/subdomains/core/liquidity-management/adapters/actions/base/frankencoin-based.adapter.ts ---
Context: +        outputAsset: order.target?.name, | +        priceSource: 'SmartContractMint', | +      });
David: Can we use `Frankencoin` or `dEURO` for priceSource?

--- PR #2041 | src/subdomains/core/liquidity-management/entities/liquidity-management-order.entity.ts ---
Context:   | +  @Column({ length: 256, nullable: true }) | +  priceSource?: string;
David: Ah, I think we don't need the price source. We can take `action.system` as the price source.

--- PR #2041 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +      ) | +        throw new InternalServerErrorException('LiquidityPipeline not completed'); | +
David: Just throw a normal error
`throw new Error(...)`

--- PR #2041 | src/subdomains/core/buy-crypto/process/services/buy-crypto-batch.service.ts ---
Context: +              LiquidityManagementPipelineStatus.STOPPED, | +              LiquidityManagementPipelineStatus.COMPLETE, | +            ].includes(t.liquidityPipeline.status),
David: Separate variable?

--- PR #2041 | src/subdomains/core/buy-crypto/process/services/buy-crypto-batch.service.ts ---
Context: +          (t) => | +            (!t.liquidityPipeline && | +              !txWithAssets.find((tx) => t.outputAsset.id === tx.outputAsset.id && tx.liquidityPipeline)) ||
David: Use `some()` instead of `find()`

--- PR #2041 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: +      this.outputReferenceAmount = totalPrice.convert(this.inputReferenceAmountMinusFee, 8); | +      this.priceStepsObject = [...this.inputPriceStep, ...filteredPriceSteps, ...pipelinePrice.steps]; | +
David: `...this.priceStepsObject` missing?

--- PR #2041 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context: + | +      this.outputReferenceAmount = totalPrice.convert(this.inputReferenceAmountMinusFee, 8); | +      this.priceStepsObject = [...this.inputPriceStep, ...filteredPriceSteps, ...pipelinePrice.steps];
David: Maybe it would be simpler to use this?
```ts
const totalPriceValue = [...filteredPriceSteps, pipelinePrice].reduce((prev, curr) => prev * curr.price, 1);
const totalPrice = Price.create(this.inputReferenceAsset, this.outputAsset.name, totalPriceValue);
```

--- PR #2044 | src/integration/blockchain/shared/frankencoin/frankencoin-based.service.ts ---
Context: + | +  abstract getCollateralPrice(collateral: CollateralWithTotalBalance): Promise<number | undefined>; |  }
David: Maybe rename to `getCustomCollateralPrice`?

--- PR #2051 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
Context: -        ? quote.txFailed(transactionResponse.error.message) | -        : quote.txMempool(transactionResponse.result.tx_hash); | +      transferInfo.tx ? quote.txMempool(transferInfo.tx) : quote.txFailed('Transaction Id not found');
David: We need the same thing also for EVM (for MetaMask)

--- PR #2057 | src/subdomains/core/buy-crypto/process/services/buy-crypto-batch.service.ts ---
Context:   | +      // TODO stop running pipeline? | +
David: I think I would leave it for now

--- PR #2060 | src/integration/blockchain/deuro/deuro.service.ts ---
Context: + | +  private async getBridgeTvlFallback(): Promise<number> { | +    const eurcContract = this.deuroClient.getBridgeEURCContract();
David: Maybe the name should include `price` if this method returns a price?

--- PR #2061 | src/subdomains/core/liquidity-management/adapters/actions/deuro.adapter.ts ---
David: Can we move this to the frankencoin-based.adapter? Should be more or less the same for FPS?

--- PR #2066 | src/subdomains/supporting/bank/bank/bank.entity.ts ---
Context: +  @OneToOne(() => Bank, { nullable: true }) | +  @JoinColumn() | +  asset: Asset;
David: `() => Asset`?

--- PR #2066 | src/subdomains/supporting/bank/bank/bank.service.ts ---
Context: +  async getSenderBank(currency: string): Promise<Bank> { | +    return this.bankRepo.findOneCachedBy(`${currency}`, { currency, send: true }); | +  }
David: `'${currency}'` -> `currency`?

--- PR #2066 | src/subdomains/supporting/log/log.service.ts ---
Context: +        }), | +      ), | +    );
David: Does this work? 🤣  looks suspicious.
- Duplicate `id` interpolation?
- No wildcard character (`%`)?

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +      map.get(tx.accountIban)!.push(tx); | +      return map; | +    }, new Map<string, FiatOutput[]>());
David: Linter warning

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +      map.get(tx.accountIban)!.push(tx); | +      return map; | +    }, new Map<string, FiatOutput[]>());
David: Use `Util.groupBy(entities, 'accountIban')`?

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +        return a.amount - b.amount; | +      }), | +    );
David: Double sorting?

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +      group.sort((a, b) => { | +        if (a.accountIban !== b.accountIban) return a.accountIban.localeCompare(b.accountIban); | +        if (a.type !== b.type) return a.type.localeCompare(b.type);
David: Sorting by accountIban is useless, they are grouped by accountIban.

--- PR #2066 | src/config/config.ts ---
Context: +      batchAmountLimit: 9500, | +    }, | +  };
David: Maybe it would be better named something like this:
```ts
liquidityManagement = {
  bankMinBalance: 100, // puffer is german anyways
  fiatOutput: {
    batchAmountLimit: 9500,
  },
}
```

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +    for (const accountIbanGroup of sortedEntities) { | +      let updatedFiatOutputs = 0; | +
David: `updatedFiatOutputAmount`?

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +            ) | +              await this.fiatOutputRepo.update(entity.id, { isReadyDate: new Date() }); | +          }
David: Does it make sense to add the amount, even if the FiatOutput is not set to ready afterwards? What is the following check for?
```ts
if (
              !entity.buyFiats.length ||
              (entity.buyFiats?.[0]?.cryptoInput.isConfirmed &&
                entity.buyFiats?.[0]?.cryptoInput.asset.blockchain &&
                (liqBalance.asset.name !== 'CHF' || ['CH', 'LI'].includes(ibanCountry)))
            )
```

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +            ) | +              await this.fiatOutputRepo.update(entity.id, { isReadyDate: new Date() }); | +          }
David: You could use an entity method to update the `isReadyDate`, then you would probably not need the `updatedFiatOutputs` variable.

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +        } catch (e) { | +          this.logger.error(`Error in fiatOutput fillUp job: ${entity.id}`, e); | +        }
David: It should be clear what the number is, currently it looks like this is the job ID. I would e.g. use
`this.logger.error('Failed to fill up fiat-output ${entity.id}:', e); `

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +      } catch (e) { | +        this.logger.error(`Error in fiatOutput bankTx search job: ${entity.id}`, e); | +      }
David: Same here

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +        currentBatch.push(entity); | +      } | +    }
David: Is this supposed to only create batches if the total amount would be larger than `batchAmountLimit`?

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +        currentBatch.push(entity); | +      } | +    }
David: Maybe it would be clever to have a `batches` array which we fill while looping through the entities and save them all in the end?

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +      } catch (e) { | +        this.logger.error(`Error in fiatOutput fillPreValutaDate job: ${entity.id}`, e); | +      }
David: Could also improve this log

--- PR #2066 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +    await this.setBatchId(); | +    await this.setDates(); | +    await this.bankTxSearch();
David: The job names are not really accurate 🤔  maybe they should be more about the included logic, e.g. more like:
- assignBankAccount
- setReady
- createBatches
- checkTransmission
- searchOutgoingBankTx

--- PR #2066 | src/subdomains/supporting/log/log.service.ts ---
Context: +      .createQueryBuilder('log') | +      .where('subsystem = UploadBank') | +      .andWhere('severity = :severity', { severity: LogSeverity.INFO });
David: I think we need ' around UploadBank?

--- PR #2066 | src/subdomains/supporting/log/log.service.ts ---
Context: +            query.where(`log.message LIKE '%${id}%'`); | +          } else { | +            query.orWhere(`log.message LIKE '%${id}%'`);
David: Hm, I think the best way to do this is probably
`query.where('log.message LIKE :message', { message: '%${id}%'});`

--- PR #2071 | src/subdomains/generic/kyc/services/integration/financial.service.ts ---
David: Map conditions

--- PR #2074 | src/subdomains/generic/kyc/services/name-check.service.ts ---
David: Linter warnings

--- PR #2074 | src/subdomains/generic/kyc/services/name-check.service.ts ---
Context: +        .select('MIN(nameCheckLog.id)', 'minId') | +        .addSelect('nameCheckLog.bankDataId', 'bankDataId') | +        .addSelect('file.id', 'fileId')
David: These selects are not used?

--- PR #2074 | src/subdomains/generic/kyc/services/name-check.service.ts ---
Context: + | +    for (const entity of entities) { | +      if (entity.file) continue;
David: Need to move this if to the SQL query? What does prevent getting the same 5000 entries every time?

--- PR #2082 | src/config/config.ts ---
Context: +      city: 'Zug', | +      country: 'CH', | +    },
David: Is the same as `dfxBankInfo`?

--- PR #2085 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context: +  @ApiProperty({ description: 'Balance in asset' }) | +  balance: number; | +}
David: Add value in selected currency

--- PR #2085 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context: + | +  @ApiProperty({ type: CustodyAssetBalanceDto, description: 'Type of your requested order', isArray: true }) | +  assetBalances: CustodyAssetBalanceDto[];
David: Description?

--- PR #2085 | src/subdomains/core/custody/dto/input/update-custody-order.dto.ts ---
Context: + | +export class UpdateCustodyOrderInternalDto { | +  transactionRequest?: TransactionRequest;
David: interface?

--- PR #2085 | src/subdomains/core/custody/dto/input/create-custody-order.dto.ts ---
Context: + | +export class CreateCustodyOrderInternalDto extends UpdateCustodyOrderInternalDto { | +  user: User;
David: interface?

--- PR #2085 | src/subdomains/core/custody/entities/custody-balance.entity.ts ---
Context: + | +  @Column({ type: 'float', default: 0 }) | +  balanceInChf: number;
David: I don't think this makes sense because this can change any time

--- PR #2085 | src/subdomains/core/custody/services/custody.service.ts ---
Context: +      currency: FiatDtoMapper.toDto(account.currency), | +    }; | +  }
David: Sum balances by asset

--- PR #2085 | src/subdomains/core/buy-crypto/process/services/buy-crypto-out.service.ts ---
Context: +            await this.custodyOrderService.updateCustodyOrderInternal(tx.custodyOrder, { | +              inputAmount: tx.outputAmount, | +            });
David: Do we need to update the status on the custody order?

--- PR #2085 | src/subdomains/core/sell-crypto/process/services/buy-fiat.service.ts ---
Context: +          transaction: entity.transaction, | +          transactionRequest: request, | +        });
David: Should not be possible 🤔

--- PR #2085 | src/subdomains/core/custody/services/custody.service.ts ---
Context: + | +        await this.updateCustodyBalance(custodyBalance, deposit - withdrawal + amount); | +      }
David: Isn't the amount already included in deposit - withdrawal?

--- PR #2085 | src/subdomains/core/custody/services/custody.service.ts ---
Context: + | +        await this.updateCustodyBalance(custodyBalance, deposit - withdrawal + amount); | +      }
David: Hm, this will update the balance for deposit transactions as soon as the user confirmed it 🤔  for withdraws we probably need to update the balance on order start, for deposits on order end.

--- PR #2085 | src/subdomains/core/custody/controllers/custody.controller.ts ---
Context: +  async getUserCustodyBalance(@GetJwt() jwt: JwtPayload): Promise<CustodyBalanceDto> { | +    return this.service.getUserCustodyBalance(jwt.account); | +  }
David: Maybe we should place this endpoint under root (just `custody`)

--- PR #2085 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context: +export class CustodyAssetBalanceDto { | +  @ApiProperty({ type: FiatDto, description: 'Asset' }) | +  asset: CustodyAssetDto;
David: Wrong type

--- PR #2085 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context: + | +  @ApiProperty({ description: 'Balance in user selected currency' }) | +  balanceInCurrency: number;
David: A would name it `value: number; `

--- PR #2085 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context: + | +  @ApiProperty({ type: CustodyAssetBalanceDto, description: 'Asset balances', isArray: true }) | +  assetBalances: CustodyAssetBalanceDto[];
David: `balances`

--- PR #2085 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context: +export class CustodyBalanceDto { | +  @ApiProperty({ description: 'Total balance in user selected currency' }) | +  totalBalance: number;
David: `value` or `totalValue`?

--- PR #2085 | src/subdomains/core/custody/mappers/custody-asset-balance-dto.mapper.ts ---
Context: + | +    return Array.from(groups.values()); | +  }
David: Maybe this is simpler (remove the `mapCustodyBalance` method)?
```ts
const groups = Util.groupByAccessor(custodyBalances, (b) => b.asset.name);

return Array.from(groups.values())
  .map((g) => (
    const asset = g[0].asset;
    const price = Price.create(asset.name, currency.name, asset.getFiatPrice(currency));
    const balance = Util.sumObjValue(g, 'balance');

     const dto: CustodyAssetBalanceDto = {
      asset: { name: asset.name, description: asset.description },
      balance: Util.roundReadable(balance, AmountType.ASSET),
      balanceInCurrency: Util.roundReadable(price.convert(balance), AmountType.FIAT),
    };

    return Object.assign(new CustodyAssetBalanceDto(), dto);
  });
```

--- PR #2085 | src/subdomains/core/custody/services/custody.service.ts ---
Context: +        ), | +      0, | +    );
David: Maybe it makes sense to map custody balances first and then sum up all values?

--- PR #2085 | src/subdomains/core/custody/services/custody.service.ts ---
Context: +    } else { | +      const asset = order.inputAsset ?? order.outputAsset; | +      const amount = order.inputAmount ?? -order.outputAmount;
David: There might be orders with input and output asset/amount -> we need to update both balances?

--- PR #2085 | src/subdomains/core/custody/services/custody.service.ts ---
Context: + | +      this.custodyBalanceRepo.update(custodyBalance.id, { balance: deposit - withdrawal }); | +    }
David: Missing await

--- PR #2085 | src/subdomains/core/custody/services/custody.service.ts ---
Context: +          asset: asset.id, | +        }) | +        .getRawOne<{ deposit: number; withdrawal: number }>();
David: Separate queries

--- PR #2088 | src/integration/lightning/dto/lnurlp.dto.ts ---
Context:    metadata: string; | +  externalId?: string; |  }
David: Remove?

--- PR #2088 | src/subdomains/core/payment-link/dto/payment-link.dto.ts ---
Context:    metadata: string; | +  externalId?: string; |    quote: {
David: ```TS
id: string;
externalId?: string;
```
at the top

--- PR #2088 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context:        recipient: pendingPayment.link.recipient, | +      externalId: pendingPayment.externalId || pendingPayment.link.externalId, |        quote: {
David: Without fallback (only link)

--- PR #2088 | src/subdomains/generic/forwarding/services/lnurl-forward.service.ts ---
Context:   |      payRequest.callback = LightningHelper.createLnurlpCallbackUrl(lnurlpId); | +    payRequest.externalId = (await this.client.getLnurlpLink(lnurlpId))?.id;
David: Remove

--- PR #2093 | src/shared/models/asset/asset.entity.ts ---
Context:   | +  @OneToMany(() => AssetPrice, (assetPrice) => assetPrice.asset) | +  assetPrices: AssetPrice[];
David: `prices: AssetPrice[];`

--- PR #2093 | src/subdomains/supporting/pricing/domain/entities/asset-price.entity.ts ---
Context: + | +  @Column({ type: 'float', nullable: false }) | +  chfPrice: number;
David: I think I would use `priceEur`, `priceUsd` and `priceChf` (that's what we used in other places)

--- PR #2093 | src/subdomains/supporting/pricing/services/asset-prices.service.ts ---
Context: + | +    const result = await this.assetPriceRepo.findOne({ | +      where: {
David: `const todayPrice = await ...`?

--- PR #2093 | src/subdomains/supporting/pricing/services/asset-prices.service.ts ---
Context: +      await this.assetPriceRepo.update(result.id, { | +        asset, | +        usdPrice: meanUsdPrice,
David: asset stays the same, doesn't need to be updated

--- PR #2093 | src/subdomains/supporting/pricing/services/asset-prices.service.ts ---
Context: +      const assetPrice = this.assetPriceRepo.create({ priceUsd, priceChf, priceEur }); | +      await this.assetPriceRepo.save(assetPrice); | +    }
David: Move `asset` to create 😁

--- PR #2095 | src/config/config.ts ---
Context:        apiUrl: process.env.DEURO_API_URL, | +      nDepsEthAddress: process.env.DEURO_NDEPS_ETH_ADDRESS, |      },
David: Get from package

--- PR #2099 | src/subdomains/supporting/payment/dto/transaction-helper/tx-statement-details.dto.ts ---
Context: +  INVOICE = 'INVOICE', | +  RECEIPT = 'RECEIPT', | +}
David: We would normally use
```ts
export enum TxStatementType {
  INVOICE = 'Invoice',
  RECEIPT = 'Receipt',
}
```

--- PR #2100 | src/subdomains/supporting/pricing/services/asset-prices.service.ts ---
Context: +  async getAssetPrices(asset: Asset): Promise<AssetPrice[]> { | +    return this.assetPriceRepo.find({ where: { asset } }); | +  }
David: `where: { asset: { id: asset.id } } `

--- PR #2100 | src/subdomains/core/custody/services/custody.service.ts ---
Context: +      totalValue: custodyHistory, | +    }; | +  }
David: Adapt as discussed

--- PR #2101 | src/subdomains/core/custody/dto/input/get-custody-info.dto.ts ---
Context: +  @IsString() | +  @IsDfxIban(IbanType.BUY) // TODO | +  @Transform(Util.trimAll)
David: TODO

--- PR #2101 | src/subdomains/core/custody/dto/input/create-custody-order.dto.ts ---
Context: +  @ValidateNested() | +  @Type(() => GetCustodyInfoDto) | +  custodyInfo: GetCustodyInfoDto;
David: Why not remove this hierarchy and put the fields directly into the CreateCustodyOrderDto? It is the same for all transactions

--- PR #2101 | src/subdomains/core/custody/dto/input/get-custody-info.dto.ts ---
Context: +  @IsDfxIban(IbanType.BUY) // TODO | +  @Transform(Util.trimAll) | +  iban?: string;
David: Required for Withdrawals?

--- PR #2101 | src/subdomains/core/custody/dto/input/get-custody-info.dto.ts ---
Context: +  @Validate(XOR, ['targetAmount']) | +  @IsNumber() | +  amount: number;
David: sourceAmount?

--- PR #2101 | src/subdomains/core/custody/dto/input/get-custody-info.dto.ts ---
Context: +  @Validate(XOR, ['targetAmount']) | +  @IsNumber() | +  amount: number;
David: description: 'Amount in source asset'

--- PR #2101 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context: + | +  @ApiProperty({ description: 'Balance in USD' }) | +  valueInUsd: number;
David: ```TS
value: {
  eur: number;
  chf: number;
  usd: number;
}
```
=> use DTO (CustodyFiatValueDto) from develop

--- PR #2101 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context: + | +  @ApiProperty({ description: 'Total balance in USD' }) | +  totalValueInUsd: number;
David: Same here

--- PR #2101 | src/subdomains/core/custody/dto/output/custody-order-response.dto.ts ---
Context: + | +  @ApiPropertyOptional() | +  sepaInstant?: boolean;
David: Structure these into an object?

--- PR #2101 | src/subdomains/core/custody/dto/output/custody-order-response.dto.ts ---
Context: + | +  @ApiPropertyOptional() | +  remittanceInfo?: string;
David: Belongs to buy infos

--- PR #2101 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context: -  value: number; | +  @ApiProperty({ description: 'Balances in fiat values' }) | +  value: CustodyFiatValueDto;
David: I think `value` is ok

--- PR #2101 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context:   |    @ApiProperty({ type: FiatDto, description: 'Currency selected by user' }) |    currency: FiatDto;
David: Can be removed?

--- PR #2101 | src/subdomains/core/custody/dto/input/get-custody-info.dto.ts ---
Context: +  @IsDfxIban(IbanType.BUY) // TODO | +  @Transform(Util.trimAll) | +  iban?: string;
David: SAVING_WITHDRAWAL is Withdraw from saving -> no IBAN required

--- PR #2101 | src/subdomains/core/custody/dto/input/get-custody-info.dto.ts ---
Context: +  @IsString() | +  @IsDfxIban(IbanType.BUY) // TODO | +  @Transform(Util.trimAll)
David: I think it's only important for sell?

--- PR #2101 | src/subdomains/core/custody/dto/output/custody-order-response.dto.ts ---
Context: +  // Buy | +  @ApiPropertyOptional({ description: 'Infos for Buy Custody Orders' }) | +  buyInfos?: CustodyOrderBuyResponseDto;
David: Missing type

--- PR #2101 | src/subdomains/core/custody/dto/output/custody-order-response.dto.ts ---
Context: + | +  @ApiProperty({ description: 'Estimated amount in target currency' }) | +  estimatedAmount: number;
David: Descriptions are a mix between currency and asset

--- PR #2103 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: +  async setModerator(user: User, moderator: Moderator): Promise<void> { | +    await this.userRepo.update(user.id, { usedRef: Config.moderators[moderator] }); | +    await this.userDataService.updateUserDataInternal(user.userData, { moderator });
David: Only set moderator ref, if the user doesn't have one?

--- PR #2104 | src/subdomains/supporting/payin/services/payin.service.ts ---
Context: +  async updateFailedPayments() { | +    const checkDate = Util.minutesBefore(10); | +
David: We should probably use 15 minutes before (due to the process timing variations)

--- PR #2104 | src/subdomains/supporting/payin/services/payin.service.ts ---
Context: +          failedPayment.paymentLinkPayment = failedPayment.paymentQuote.payment; | +          failedPayment.status = PayInStatus.COMPLETED; | +          await this.payInRepository.save(failedPayment);
David: `failedPayment.status = PayInStatus.CREATED`?

--- PR #2104 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
Context: + | +        const isComplete = await Util.retry(() => client.isTxComplete(transferInfo.tx, 1), tryCount, 1); | +
David: Retry delay is specified in ms?

--- PR #2104 | src/config/config.ts ---
Context:   |      defaultPaymentTimeout: +(process.env.PAYMENT_TIMEOUT ?? 60), | +    defaultEvmHexPaymentTryCount: +(process.env.PAYMENT_EVM_HEX_TRY_COUNT ?? 60),
David: I would use 15 secs as default

--- PR #2107 | src/subdomains/supporting/support-issue/services/limit-request.service.ts ---
Context:   | -    Util.removeNullFields(entity); | -
David: Why completely remove this?

--- PR #2107 | src/subdomains/supporting/fiat-payin/services/fiat-payin-sync.service.ts ---
Context: -      Object.assign(entity, tx); | +      const txWithoutNulls = Util.removeNullFields(tx); | +      Object.assign(entity, txWithoutNulls);
David: I would just use an inliner `Object.assign(entity, Util.removeNullFields(tx)); ` everywhere

--- PR #2107 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +        (entityWithoutNulls.amlCheck === CheckStatus.FAIL && dto.amlCheck === CheckStatus.GSHEET)) && | +      !entityWithoutNulls.isComplete && | +      (update?.amlCheck !== entityWithoutNulls.amlCheck || update.amlReason !== entityWithoutNulls.amlReason)
David: I think you can revert this and move the `removeNullFields` to the save below. It's just important for the save.

--- PR #2107 | src/shared/utils/util.ts ---
Context: +    Object.keys(result).forEach((k) => result[k] == null && delete result[k]); | +    return result; | +  }
David: Why not the same implementation as in services (using `Object.fromEntries`)? 😉  And maybe also allow undefined  entities => return entity?

--- PR #2107 | src/shared/utils/util.ts ---
Context: + | +    return result; |    }
David: Same here

--- PR #2107 | src/subdomains/core/sell-crypto/process/services/buy-fiat.service.ts ---
Context: +      !entityWithoutNulls.isComplete && | +      (update?.amlCheck !== entityWithoutNulls.amlCheck || update.amlReason !== entityWithoutNulls.amlReason) |          ? { amlCheck: update.amlCheck, mailSendDate: null, amlReason: update.amlReason }
David: Remove `removeNullFields` to save

--- PR #2107 | src/subdomains/core/sell-crypto/process/services/buy-fiat.service.ts ---
Context: -    Util.removeNullFields(where); | +    const where: { user: { id: number }; id?: number } = { user: { id: userId } }; | +    if (sellId !== undefined) where.id = sellId;
David: Why not just `const where = Util.removeNullFields({ user: { id: userId }, id: sellId });`

--- PR #2127 | src/subdomains/core/route/route.controller.ts ---
Context: +    const sellRoute = await this.sellService.getPaymentRoute(id); | +    return this.sellController.toDto(sellRoute); | +  }
David: I think I would place this endpoint in `PaymentLinkController` under `GET paymentLink/recipient`.

Maybe also with query parameters instead of path parameter? I'm not sure 🤔

--- PR #2127 | src/subdomains/core/route/route.controller.ts ---
Context: +    const sellRoute = await this.sellService.getPaymentRoute(id); | +    return this.sellController.toDto(sellRoute); | +  }
David: Maybe we can then somehow share the checks performed on the route? They are already in `PaymentLinkService`.

--- PR #2127 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context: +    const sellRoute = await this.sellService.getPaymentRoute(id); | +    return this.sellController.toDto(sellRoute); | +  }
David: This sell DTO is a legacy thing that nobody uses 🤣  I would create a new one (e.g. `PaymentRecipient`) with the required information.

--- PR #2127 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context:      private readonly paymentLinkPaymentService: PaymentLinkPaymentService, | +    private readonly sellController: SellController, | +    private readonly sellService: SellService,
David: Unused variable `sellController`

--- PR #2127 | src/subdomains/core/sell-crypto/route/sell.controller.ts ---
Context:   | -  private async toDto(sell: Sell): Promise<SellDto> { | +  async toDto(sell: Sell): Promise<SellDto> {
David: Revert

--- PR #2130 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
Context:      try { | -      const client = this.blockchainRegistryService.getClient(method); | +      const client = this.blockchainRegistryService.getClient(method) as EvmClient;
David: `getEvmClient`

--- PR #2130 | src/integration/tatum/dto/tatum.dto.ts ---
Context: +  @IsNotEmpty() | +  addresses: string[]; | +}
David: Validator decorators are not useful for internal-only DTOs (just use an interface)

--- PR #2130 | src/subdomains/core/liquidity-management/adapters/balances/blockchain.adapter.ts ---
Context:      for (const asset of assets) { |        try { |          if (asset.type !== AssetType.COIN) throw new Error(`Only coins are available on ${asset.blockchain}`);
David: Solana has tokens

--- PR #2130 | src/integration/tatum/tatum.provider.ts ---
Context: +    }); | +  }, | +};
David: Can we do this w/o custom provider?

--- PR #2130 | src/config/config.ts ---
Context: +    solana: { | +      solanaWalletAddress: process.env.SOLANA_WALLET_ADDRESS, | +      solanaWalletSeed: process.env.SOLANA_WALLET_SEED,
David: Looks like `solanaWalletAddress` is not used at all?

--- PR #2130 | src/subdomains/supporting/payin/strategies/send/impl/solana-coin.strategy.ts ---
Context: + | +    return this.payInSolanaService.sendNativeCoin(account, payIn.destinationAddress.address, payIn.amount); | +  }
David: Use `payIn.sendingAmount`?

--- PR #2130 | src/subdomains/supporting/payin/strategies/send/impl/solana-token.strategy.ts ---
Context: + | +    return this.payInSolanaService.sendToken(account, payIn.destinationAddress.address, payIn.asset, payIn.amount); | +  }
David: Same here

--- PR #2130 | src/integration/blockchain/solana/solana-client.ts ---
Context: +        const balance = SolanaUtil.fromLamportAmount(tokenAmount.amount, tokenAmount.decimals); | +        tokenBalances.push({ contractAddress: info.mint, balance: balance }); | +      }
David: Is it possible to have multiple token accounts for the same mint? Shouldn't we then sum the balances?

--- PR #2130 | src/integration/blockchain/solana/solana-client.ts ---
Context: + | +    return new Token(-1, mintAddress, decimals); | +  }
David: Should we also use an AsyncCache for tokens as we do for EVM?

--- PR #2130 | src/integration/blockchain/solana/solana-client.ts ---
Context: +  private calculatePriorityFee(): Solana.TransactionInstruction { | +    const priorityRate = 100; | +    return Solana.ComputeBudgetProgram.setComputeUnitPrice({ microLamports: priorityRate });
David: Where does this number come from?

--- PR #2130 | src/integration/blockchain/solana/solana-client.ts ---
Context: +          keypair.publicKey, | +          amount * Math.pow(10, decimals), | +        ),
David: Can't we use `SolanaUtil.toLamportAmount`?

--- PR #2130 | src/integration/blockchain/solana/solana-client.ts ---
Context: + | +    const allSignatures = await this.connection | +      .getSignaturesForAddress(walletInfo.publicKey, { limit }, 'finalized')
David: Just use `this.wallet.publicKey` instead of fetching whole `walletInfo`?

--- PR #2130 | src/integration/tatum/controllers/tatum.controller.ts ---
Context: +      this.logger.warn(`Received Tatum webhook with invalid signature '${tatumSignature}': ${JSON.stringify(dto)}`); | +      throw new BadRequestException('Invalid signature'); | +    }
David: Use `req.body` instead of `JSON.stringify(dto)` in error message and move JSON-parsing into try-catch?

--- PR #2130 | src/subdomains/supporting/payin/strategies/register/impl/solana.strategy.ts ---
Context: + | +    this.solanaPaymentDepositAddress = this.solanaService.getWalletAddress(); | +  }
David: I think it should be a different address?

--- PR #2130 | src/subdomains/supporting/payout/strategies/payout/impl/base/solana.strategy.ts ---
Context: +        this.logger.error(`Error while executing Solana payout order ${order.id}:`, e); | +      } | +    }
David: Does it make sense to look into multi-payout transactions? Maybe not because fees are low.

--- PR #2130 | src/subdomains/core/liquidity-management/adapters/balances/blockchain.adapter.ts ---
Context:   | -  private async getForEvmAsset(assets: Asset[], client: EvmClient): Promise<void> { | +  private async getForTokenAsset(assets: Asset[], client: TokenClient): Promise<void> {
David: Maybe it should rather be `getForTokenChain` or something? It also fetches coin, not only token balance.

--- PR #2130 | src/subdomains/supporting/payin/strategies/register/impl/solana.strategy.ts ---
Context: +    return { | +      senderAddresses: dto.counterAddresses[0], | +      receiverAddress: BlockchainAddress.create(dto.address, this.blockchain),
David: Comma-separate all addresses into senderAddresses?

--- PR #2130 | src/subdomains/supporting/payin/strategies/register/impl/solana.strategy.ts ---
Context: +      amount: Number(dto.amount), | +      asset: isNativeTransaction ? null : this.getTransactionAsset(supportedAssets, dto.asset) ?? null, | +    };
David: Why is the asset null, if it is a native transaction?

--- PR #2130 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:   | +    if (user && isAsset(to) && to.blockchain !== Blockchain.SOLANA) | +      fee.network += await this.getSolanaCreateTokenAccountFee(user, to);
David: `to.blockchain === Blockchain.SOLANA`?

--- PR #2138 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +        ?.balances.filter((b) => b.contractAddress === curr.chainId); | + | +      const totalCustomBalance = customAddressBalances.reduce((sum, result) => sum + result.balance, 0);
David: `Util.sumObjValue`?

--- PR #2138 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +                  customAddressBalances.map((b) => { | +                    return { ...b, balance: this.getJsonValue(b.balance, amountType(curr)) }; | +                  }),
David: ```TS
customAddressBalances.map((b) => ({ ...b, balance: this.getJsonValue(b.balance, amountType(curr)) })),
```
is probably easier to read

--- PR #2146 | src/integration/blockchain/ain/node/btc-client.ts ---
David: We could directly rename `ain` to `btc` (folder, module, ...)

--- PR #2146 | src/integration/blockchain/shared/util/blockchain-client.ts ---
Context:    abstract getToken(asset: Asset): Promise<Currency>; | -  abstract sendSignedTransaction(tx: string): Promise<SignedTransactionResponse>; | +  abstract sendSignedTransaction(tx: string): Promise<EvmSignedTransactionResponse>;
David: BtcClient also extends BlockchainClient but does not return EvmSignedTransactionResponse?

--- PR #2146 | src/integration/blockchain/shared/services/blockchain-registry.service.ts ---
Context: +  getClient(blockchain: Blockchain): EvmClient | MoneroClient | BtcClient { |      return this.getService(blockchain).getDefaultClient(); |    }
David: This will throw an error for bitcoin, as the type is not defined. Maybe we should by default return the Input client?

--- PR #2146 | src/integration/blockchain/ain/node/btc.service.ts ---
David: I think all the subscription logic can be removed. This was used when we had active and passive nodes to improve availability.
Also `addNodeClientPair` does not make sense anymore, it's just a single node.

--- PR #2146 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
Context: +      transactionResponse.error | +        ? quote.txFailed(transactionResponse.error.message) | +        : quote.txInMempool(transactionResponse.hash);
David: Why not do this directly on the client (as for EVM)?

--- PR #2146 | src/integration/blockchain/bitcoin/node/bitcoin.service.ts ---
Context: +  BTC_INPUT = 'btc-inp', | +  BTC_OUTPUT = 'btc-out', | +}
David: I think it should be called `BitcoinNodeType`

--- PR #2148 | src/integration/c2b-payment-link/guards/binance-pay-webhook.guard.ts ---
Context: +    } | + | +    return true;
David: Why not return it directly?
`return this.binancePayService.verifySignature(request.body, { timestamp, nonce, signature, certSN }); `?

--- PR #2148 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context: +    this.paymentLinkPaymentService.handleWebhook(C2BPaymentProvider.BINANCE_PAY, dto); | +    return { returnCode: 'SUCCESS', returnMessage: null }; | +  }
David: Missing await

--- PR #2148 | src/integration/c2b-payment-link/services/binance-pay.service.ts ---
Context: + | +  constructor(private http: HttpService) { | +    this.apiKey = Config.payment.binancePayPublic;
David: `private readonly http: HttpService`

--- PR #2148 | src/integration/c2b-payment-link/services/binance-pay.service.ts ---
Context: +  ): Promise<OrderResult> { | +    const orderDetails: any = { | +      env: {
David: OrderData instead of any?

--- PR #2148 | src/integration/c2b-payment-link/services/binance-pay.service.ts ---
Context: + | +    const decodedSignature = Buffer.from(signature, 'base64'); | +    return verify.verify(cert.certPublic, decodedSignature);
David: Maybe you could use `Util.verifySign`?

--- PR #2148 | src/integration/blockchain/shared/util/blockchain.util.ts ---
Context:    [Blockchain.RAILGUN]: 'https://railgun-explorer.com', | +  [Blockchain.BINANCE_PAY]: '', |  };
David: `[Blockchain.BINANCE_PAY]: undefined,`

--- PR #2148 | src/integration/blockchain/shared/util/blockchain.util.ts ---
Context:    [Blockchain.RAILGUN]: 'transaction', | +  [Blockchain.BINANCE_PAY]: '', |  };
David: Same here

--- PR #2148 | src/integration/c2b-payment-link/c2b-payment-link.service.ts ---
Context: + | +    return webhookResult; | +  }
David: Missing return types.
And why not return the results directly?

--- PR #2148 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context: +  // --- INTEGRATION --- // | +  @Post('integration/binance-pay/webhook') | +  @ApiExcludeEndpoint()
David: Maybe just `@Post('integration/binance/webhook')` (same as Kucoin)?

--- PR #2148 | src/subdomains/core/payment-link/dto/payment-request.mapper.ts ---
Context: +      expiryDate: paymentActivation.expiryDate, | +      blockchain: method, | +      uri: paymentActivation.paymentRequest,
David: I think we can omit the blockchain field in this case 🤔

--- PR #2148 | src/subdomains/core/payment-link/services/payment-link-payment.service.ts ---
Context: + | +      await this.handleQuoteChange(payment, quote); | +    }
David: `await this.handleQuoteChange(quote.payment, quote); ` (no need to load the payment separately)

--- PR #2148 | src/integration/c2b-payment-link/services/binance-pay.service.ts ---
Context: +    const { timestamp, nonce, signature, certSN } = headers; | +    const webhookData = JSON.stringify({ ...body, bizId: body.bizIdStr }).replace(/"bizId":"(\d+)"/g, '"bizId":$1'); | +    const payload = `${timestamp}\n${nonce}\n${webhookData}\n`;
David: Why not just `const webhookData = JSON.stringify({ ...body, bizId: +body.bizIdStr });`? 🤔

--- PR #2148 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context: +    void this.paymentLinkPaymentService.handleWebhook(C2BPaymentProvider.BINANCE_PAY, dto); | +    return { returnCode: 'SUCCESS', returnMessage: null }; | +  }
David: Errors should be handled (and logged) with .catch, if it's not awaited.

--- PR #2148 | migration/1748368553869-AddRegistrationNumberUserData.js ---
Context: @@ -0,0 +1,33 @@ | +module.exports = class AddRegistrationNumberUserData1748368553869 {
David: It's probably because your branch is not up to date with develop. You can either pull develop into your branch and redo the migration, or just remove all lines that are not related to your changes 😉

--- PR #2148 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context: +  @Post('integration/binance/activate/:id') | +  @ApiExcludeEndpoint() | +  async activateBinancePay(@Param('id') id: string): Promise<void> {
David: Use `@UseGuards(AuthGuard(), RoleGuard(UserRole.ADMIN), UserActiveGuard())`

--- PR #2148 | src/integration/c2b-payment-link/services/binance-pay.service.ts ---
Context: +          goodsType: '01', | +          goodsCategory: 'D000', | +          referenceGoodsId: '01',
David: Use enums?

--- PR #2154 | src/subdomains/core/buy-crypto/routes/swap/swap.entity.ts ---
Context:    Blockchain.BASE, | +  Blockchain.GNOSIS, |  ];
David: Fetch update from develop

--- PR #2158 | src/subdomains/generic/kyc/services/kyc-admin.service.ts ---
Context: +            this.kycService.getMailStepName(kycStep.name, kycStep.userData.language.symbol), |              this.kycService.getMailFailedReason(kycStep.comment, kycStep.userData.language.symbol), |            );
David: Why not join this call with the one above?

--- PR #2159 | src/subdomains/core/sell-crypto/route/sell.service.ts ---
Context: +    }, new Map<string, PaymentLink>()); | + | +    return Array.from(latestLinksMap.values());
David: Maybe just add ordering by created to the SQL query (ascending) and then return with something like this?
`return Array.from(new Map((route.paymentLinks || []).map((l) => [l.externalId, l]).values());`

--- PR #2165 | src/subdomains/supporting/payin/strategies/send/impl/solana-coin.strategy.ts ---
Context: +  private async calcSendingAmount(account: WalletAccount, sendingAmount: number, feeAmount: number): Promise<number> { | +    const wallet = SolanaUtil.createWallet(account); | +    const balance = await this.payInSolanaService.getNativeCoinBalanceForAddress(wallet.address);
David: You can use `payIn.address.address` instead of getting the address via account.

--- PR #2165 | src/subdomains/supporting/payin/strategies/send/impl/base/solana.strategy.ts ---
Context: +        if (payIn.status === PayInStatus.PREPARED) { | +          const feeAmount = await this.getFeeAmount(payIn); | +          const outTxId = await this.sendTransfer(payIn, type, feeAmount);
David: Can't you use `feeNativeAsset` instead of calculating the fee amount again?

--- PR #2167 | src/subdomains/generic/kyc/enums/kyc-step-name.enum.ts ---
Context: -  LEGAL_ENTITY = 'LegalEntity', |    OWNER_DIRECTORY = 'OwnerDirectory', |    COMMERCIAL_REGISTER = 'CommercialRegister',
David: I personally would keep `LegalEntity` and remove `CommercialRegister`, is a more meaningful name 🤔

--- PR #2167 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context:   | +export class KycCommercialLegalEntityData extends KycFileData { | +  @ApiProperty({ enum: LegalEntity })
David: Remove `Commercial` from name

--- PR #2167 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @IsEnum(LegalEntity) | +  legalEntity: LegalEntity; | +}
David: Maybe it makes sense to not extend KycFileData and have a description on file/fileName fields (commercial register)?

--- PR #2167 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context:   | +  async updateCommercialLegalData( | +    kycHash: string,
David: Remove `Commercial` from name

--- PR #2167 | src/subdomains/generic/kyc/entities/kyc-step.entity.ts ---
Context: -      case KycStepName.COMMERCIAL_REGISTER: | +      case KycStepName.LEGAL_ENTITY: |          return { url: `${apiUrl}/data/commercial/${this.id}`, type: UrlType.API };
David: Should be `...data/legal/...`

--- PR #2169 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +              }, | +            ] | +          : await this.getCustomBalances(client, a, [targetAddress]).then((b) => b.flat());
David: Solana also has tokens -> move to `getCustomBalances`?

--- PR #2169 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +              }, | +            ] | +          : await this.getCustomBalances(client, a, [targetAddress]).then((b) => b.flat());
David: Monero payment balance is already included in liquidity

--- PR #2169 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +    const paymentDepositBalances = await Promise.all( |        Array.from(paymentAssetMap.entries()).map(async ([e, a]) => { |          const client = this.blockchainRegistryService.getClient(e);
David: Adapt `deposit address balance` comment

--- PR #2169 | src/subdomains/supporting/log/log-job.service.ts ---
Context: + | +        if (![Blockchain.MONERO, Blockchain.BITCOIN].includes(e)) | +          balances.push(...(await this.getCustomBalances(client, a, [targetAddress]).then((b) => b.flat())));
David: Monero is already filtered out above

--- PR #2169 | src/subdomains/supporting/log/log-job.service.ts ---
Context: -      const cryptoInput = pendingPayIns.reduce((sum, tx) => sum + (tx.asset.id === curr.id ? tx.amount : 0), 0); | +      const cryptoInput = | +        curr.blockchain === Blockchain.MONERO
David: Same for Lightning?

--- PR #2169 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +          index: 0, | +        }).address; | +    }
David: Monero can be removed

--- PR #2169 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +            balance: await client.getNativeCoinBalanceForAddress(targetAddress), | +          }, | +        ];
David: Hm, this does not work since coins have no chainId

--- PR #2169 | src/subdomains/supporting/log/log-job.service.ts ---
Context: + | +        if (![Blockchain.MONERO, Blockchain.BITCOIN].includes(e)) | +          balances.push(...(await this.getCustomBalances(client, a, [targetAddress]).then((b) => b.flat())));
David: Only for non-coin assets?

--- PR #2172 | src/subdomains/supporting/payin/strategies/send/impl/solana-token.strategy.ts ---
Context:   | -    const feeAmount = nativeFee + createTokenFee; | +    const feeAmount = nativeFee + rentFee;
David: `const rentFee = Math.max(minRentBalance - coinBalance, 0);`?

--- PR #2172 | src/subdomains/supporting/payin/strategies/send/impl/solana-token.strategy.ts ---
Context:   | -    const feeAmount = nativeFee + createTokenFee; | +    const feeAmount = nativeFee + rentFee;
David: Store minRentBalance to config?

--- PR #2184 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +          // send mail | +          await this.kycStepRepo.update(entity.id, { status: KycStepStatus.IN_PROGRESS }); | +          continue;
David: `send mail`?

--- PR #2184 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: + | +    if (!questions.every((q) => financialStepResult.find((f) => q.key === f.key))) | +      errors.push(KycFinancialDataError.MISSING_QUESTION);
David: Use `FinancialService.isComplete` instead?

--- PR #2184 | src/subdomains/generic/kyc/dto/kyc-error.enum.ts ---
Context: +  // FinancialData errors | +  MISSING_QUESTION = 'MissingQuestion', | +  RISKY_BUSINESS = 'RiskyBusiness',
David: It's more a `MissingResponse` than a `MissingQuestion` 😅

--- PR #2184 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +      errors.push(KycError.MISSING_QUESTION); | +    if (!riskyBusiness?.value || !riskyBusiness.value.includes('no')) errors.push(KycError.RISKY_BUSINESS); | +
David: Maybe this is simpler?

`if (!financialStepResult.some((f) => f.key === 'risky_business' && f.value.includes('no')) errors.push(KycError.RISKY_BUSINESS);`

--- PR #2186 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context: +    if (!externalIds && !ids) { | +      throw new ForbiddenException('Either externalIds or ids parameter must be provided'); | +    }
David: BadRequestException

--- PR #2186 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context: +    } | + | +    const idArray = ids?.split(',').map((id) => +id.trim());
David: trim is probably not required here 😉

--- PR #2186 | src/subdomains/core/sell-crypto/route/sell.service.ts ---
Context: +          ...(ids?.length ? [{ id: In(ids) }] : []), | +        ], | +      },
David: Are you sure this works? Normally we would have `where: [{cond1}, {cond2}]`

--- PR #2187 | src/integration/c2b-payment-link/services/binance-pay.service.ts ---
Context: +      ['registrationNumber', paymentLink.registrationNumber], | +    ]; | +    const missing = requiredFields.filter(([_, value]) => !value).map(([label]) => label);
David: Maybe this would be easier?
```ts
const missing = ['externalId', 'label', 'name', 'merchantMcc', 'country', 'website', 'street', 'houseNumber', 'zip', 'city', 'registrationNumber'].filter((f) => paymentLink[f] == null);
```

--- PR #2187 | src/integration/c2b-payment-link/services/binance-pay.service.ts ---
Context: +          goodsType: payment.link.goodsType || GoodsType.TangibleGoods, // TODO: Remove default values | +          goodsCategory: payment.link.goodsCategory || GoodsCategory.FoodGroceryHealthProducts, |            referenceGoodsId: '01',
David: TODO?

--- PR #2187 | src/subdomains/core/payment-link/dto/create-payment-link.dto.ts ---
Context: +  @IsOptional() | +  @IsEnum(GoodsCategory) | +  goodsCategory?: GoodsCategory;
David: I think this belongs into `config.recipient`

--- PR #2187 | src/subdomains/core/payment-link/dto/create-payment-link.dto.ts ---
Context: +  @IsOptional() | +  @IsEnum(GoodsCategory) | +  goodsCategory?: GoodsCategory;
David: Add the fields to also to `UpdatePaymentLinkInternalDto`

--- PR #2187 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: +      .enrollPaymentLink(paymentLink, C2BPaymentProvider.BINANCE_PAY) | +      .catch((e) => { | +        this.logger.info(`Failed to enroll C2B payment link ${paymentLink.uniqueId}`, e);
David: Could we select the log severity depending on the error type? If it's just missing data, then use LogLevel.INFO, otherwise LogLevel.ERROR.

--- PR #2187 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: +      .catch((e) => { | +        this.logger.info(`Failed to enroll C2B payment link ${paymentLink.uniqueId}`, e); | +      });
David: This should also be called on payment link update (maybe in `PaymentLinkService.updatePaymentLinkInternal`?)

--- PR #2187 | src/integration/c2b-payment-link/services/binance-pay.service.ts ---
Context: +          goodsType: payment.link.goodsType || GoodsType.TangibleGoods, // TODO: Remove default values | +          goodsCategory: payment.link.goodsCategory || GoodsCategory.FoodGroceryHealthProducts, |            referenceGoodsId: '01',
David: Ok, makes sense.

--- PR #2187 | src/integration/c2b-payment-link/services/binance-pay.service.ts ---
Context: +      const config = JSON.parse(paymentLink.config || '{}'); | +      return Boolean(config.binancePayMerchantId || config.binancePaySubMerchantId); | +    } catch (e) {}
David: You can use `paymentLink.linkConfigObj` (uses only local link config)

--- PR #2187 | src/subdomains/core/payment-link/dto/update-payment-link.dto.ts ---
Context: +  @IsOptional() | +  @IsEnum(GoodsCategory) | +  goodsCategory?: GoodsCategory;
David: You can remove the `ApiProperty` tags, this DTO is internal-only and not Swagger-documented.

--- PR #2187 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: +      e instanceof BadRequestException | +        ? this.logger.info(`C2B payment link ${paymentLink.uniqueId} is not eligible for enrollment ${e.message}`) | +        : this.logger.error(`Failed to enroll C2B payment link ${paymentLink.uniqueId}`, e);
David: Add `:` for better readability:
```ts
        ? this.logger.info(`C2B payment link ${paymentLink.uniqueId} is not eligible for enrollment: ${e.message}`)
        : this.logger.error(`Failed to enroll C2B payment link ${paymentLink.uniqueId}:`, e);
```

--- PR #2187 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: -    await this.updateUserPaymentLinksConfig(paymentLink.route.userData.id, { ...ids }); | +    const config = this.getMergedConfig(paymentLink, { ...JSON.parse(paymentLink.config || '{}'), ...ids }); | +    await this.paymentLinkRepo.update(paymentLink.id, { config });
David: This looks like double merged? I think this would be enough?
`    const config = this.getMergedConfig(paymentLink, ids);`

--- PR #2187 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: +      ); | +      if (c2bIds) dto.config = this.getMergedConfig(paymentLink, { ...JSON.parse(dto.config || '{}'), ...c2bIds }); | +    }
David: Same here?

--- PR #2187 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context:   | +    const c2bIds = await this.tryEnrollC2BPaymentLink(paymentLink, C2BPaymentProvider.BINANCE_PAY); | +    if (c2bIds) paymentLink.config = JSON.stringify({ ...JSON.parse(paymentLink.config || '{}'), ...c2bIds });
David: Can we also use `getMergedConfig` here?

--- PR #2187 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: +      ); | +      if (c2bIds) dto.config = this.getMergedConfig(paymentLink, { ...JSON.parse(dto.config || '{}'), ...c2bIds }); | +    }
David: Ah true, makes sense 👍

--- PR #2190 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
David: The props `isDataComplete` and `isDataComplete` do not work anymore

--- PR #2190 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
David: Haha, true, it's `requiredKycFields` 😉

--- PR #2190 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context: +      f.includes('organization') ? this.organization[f.split('organization')[1].toLowerCase()] : this[f], | +    ); |    }
David: I think I would change the organization field strings to `organization.xxx` and then `this.requiredKycFields.every((f) => f.split('.').reduce((prev, curr) => prev[curr], this))` (needs testing 😉 )

--- PR #2190 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context: +      f.includes('organization') ? this.organization[f.split('organization')[1].toLowerCase()] : this[f], | +    ); |    }
David: Update `KycService.initiateStep` (PERSONAL_DATA) accordingly

--- PR #2190 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context: +            'organization.country', | +          ], |      );
David: Update `KycService.initiateStep` (PERSONAL_DATA) accordingly

--- PR #2191 | src/subdomains/core/payment-link/dto/payment-link-config.dto.ts ---
Context: +  @IsString({ each: true }) | +  @IsArray() | +  accessKeys?: string[];
David: I think I wouldn't let the user overwrite access keys, that's a bit dangerous 🤣 => remove it from DTO

--- PR #2191 | src/subdomains/core/payment-link/dto/payment-link-config.dto.ts ---
Context: +  @IsString({ each: true }) | +  @IsArray() | +  accessKeys?: string[];
David: Also remove binancePayMerchantId and binancePaySubMerchantId, as these are now set automatically

--- PR #2191 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context:    async waitForPayment( |      userId: number, |      linkId?: number,
David: userId is optional?

--- PR #2191 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context:      externalLinkId?: string, |      externalPaymentId?: string, | +    key?: string,
David: Same here?

--- PR #2191 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context:   | +  async matchPaymentLinkByAccessKey( | +    key: string,
David: `getPaymentLinkByAccessKey`?

--- PR #2191 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: + | +    if (dto.amount !== 0) | +      existingPaymentLink.payments = [await this.paymentLinkPaymentService.createPayment(existingPaymentLink, dto)];
David: Isn't this already done in `this.create` above?

--- PR #2191 | src/subdomains/core/payment-link/dto/payment-link-config.dto.ts ---
Context: +  @IsString({ each: true }) | +  @IsArray() | +  accessKeys?: string[];
David: Yes 😉

--- PR #2198 | src/subdomains/generic/user/models/wallet/wallet.entity.ts ---
Context: +  get mailConfigObject(): MailContextType[] | undefined { | +    return this.mailConfig ? (this.mailConfig?.split(';') as MailContextType[]) : undefined; | +  }
David: `get disabledMailTypes():`

--- PR #2198 | src/subdomains/supporting/notification/factories/mail.factory.ts ---
Context: +      mailContextType && | +      (wallet.mailConfigObject.includes(mailContextType) || wallet.mailConfigObject.includes(MailContextType.ALL)) | +    )
David: This looks the same as below, maybe a helper method?

--- PR #2204 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context: +      .getHistoryByStatus(+jwt?.user, dto.status, dto.from, dto.to, key, externalLinkId) |        .then(PaymentLinkDtoMapper.toLinkHistoryDtoList); |    }
David: Missing @ApiQuery documentation?

--- PR #2204 | src/subdomains/core/payment-link/dto/payment-link.dto.ts ---
Context:    lnurl: string; | + | +  updatedAt: Date;
David: Is there deliberately no Swagger tag (@ApiProperty())?

--- PR #2206 | src/subdomains/generic/kyc/services/kyc-file.service.ts ---
Context: +                : FileSubType.IDENT_SELFIE; | +            } | +          }
David: Should this really be a for, or just a find?
```ts
const image = applicantMetaData.items.find((i) => i.id === imageId);
if (image) entity.subType = identDocTypes.includes(image.idDocDef.idDocType)
   ? FileSubType.IDENT_DOC
   : FileSubType.IDENT_SELFIE;
```

--- PR #2219 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: +          referId, | +          tx: '', | +          hex: '',
David: Do we need these here?

--- PR #2219 | src/subdomains/core/payment-link/dto/payment-link.dto.ts ---
Context:    quoteUniqueId: string; |    tx: string; |    hex: string;
David: Make optional

--- PR #2223 | src/integration/blockchain/tron/dto/tron-transaction.mapper.ts ---
Context: +    const params = data.slice(8); | +    const receiverAddress = params.slice(0, 64).slice(24); | +    return TronUtil.convertToTronAddress(receiverAddress);
David: Is `.slice(0, 64).slice(24)` not the same as `.slice(24, 64)`?

--- PR #2223 | src/integration/tatum/services/tatum-webhook.service.ts ---
Context: +    const tatum = this.tatumMap.get(tatumNetwork); | +    return tatum ?? this.setupTatumSdk(tatumNetwork); | +  }
David: `tatumMap.set` is never called?

--- PR #2223 | src/integration/blockchain/tron/tron-client.ts ---
Context: +      const addressBalances = allAddressBalanceTokenData.filter((d) => | +        Util.equalsIgnoreCase(d.tokenAddress, asset.chainId), | +      );
David: Why filtering twice for chainId?

--- PR #2223 | src/integration/blockchain/tron/tron-client.ts ---
Context: +    const currentConfirmations = (await this.getBlockHeight()) - (transaction.blockNumber ?? Number.MAX_VALUE); | +    return currentConfirmations > confirmations; | +  }
David: Is it possible to throw an error, if the transaction failed?

--- PR #2223 | src/integration/blockchain/tron/tron-client.ts ---
Context: + | +    return getaccountResponse && Object.keys(getaccountResponse).length > 0; | +  }
David: Can we find a more meaningful name for this method? What do we check?

--- PR #2223 | src/integration/blockchain/tron/tron-client.ts ---
Context: +      const decimals = await this.http | +        .post<any>( | +          Config.blockchain.tron.tronRpcUrl,
David: Can we use Tatum SDK instead of these HTTP calls?

--- PR #2223 | src/integration/blockchain/tron/tron-client.ts ---
Context: +    return { | +      bandwidth: accountResourceResponse.freeNetLimit || 0 + accountResourceResponse.NetLimit || 0, | +      energy: accountResourceResponse.EnergyLimit ?? 0,
David: The + operator has higher precedence than the || operator. Are we missing quotes?

--- PR #2223 | src/integration/blockchain/tron/tron-client.ts ---
Context: +    const createNewAccountFeeInSystemContractParameter = chainParameters.find((cp) => | +      Util.equalsIgnoreCase(cp.key, 'getCreateNewAccountFeeInSystemContract'), | +    );
David: It could be interesting to create a map instead of searching the array 4 times.

--- PR #2223 | src/subdomains/supporting/payin/strategies/send/impl/base/tron.strategy.ts ---
Context: +      } | +    } | +  }
David: Can we apply latest changes from evm.strategy here?

--- PR #2223 | src/subdomains/supporting/payin/strategies/send/impl/tron-token.strategy.ts ---
Context: +    ); | + | +    const feeAmount = nativeFee + transferFee;
David: Why is it native fee + transfer fee?

--- PR #2223 | src/integration/blockchain/tron/tron-client.ts ---
Context: +      const addressBalance = allAddressBalanceTokenData.find((d) => | +        Util.equalsIgnoreCase(d.tokenAddress, asset.chainId), | +      );
David: Improve using a map?

--- PR #2223 | src/integration/blockchain/tron/tron-client.ts ---
Context: +    const currentConfirmations = (await this.getBlockHeight()) - (transaction.blockNumber ?? Number.MAX_VALUE); | +    return currentConfirmations > confirmations; | +  }
David: How does this handle transactions that are still pending? This method should return false if the transaction is pending or doesn't have enough confirmations.
If it's no more pending but failed, it should throw an error.

--- PR #2227 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context:    @Post('integration/binance/activate/:id') |    @ApiExcludeEndpoint() |    @UseGuards(AuthGuard(), RoleGuard(UserRole.ADMIN), UserActiveGuard())
David: I would move both API endpoints to the new controller

--- PR #2227 | src/subdomains/supporting/payin/strategies/register/impl/binance.strategy.ts ---
Context: +      this.logger.error(`Error while mapping binance transaction: ${JSON.stringify(result)}`, e); | +    } | +  }
David: I think we can simplify this by changing the method to `mapBinanceTransaction(result: WebhookResult, assets: Asset[]): PayInEntry[]` and doing something like this:
```ts
return paymentInstructions
   .map((pi) => ({
      senderAddresses: null,
      receiverAddress: null,
      txId: transactionId,
      txType: PayInType.PAYMENT,
      blockHeight: null,
      amount: pi.amount,
      asset: assets.find((a) => Util.equalsIgnoreCase(pi.currency, asset.name)),
   }))
   .filter((p) => p.asset);
```

--- PR #2227 | src/subdomains/supporting/payin/payin.module.ts ---
Context:  import { RegisterStrategyRegistry } from './strategies/register/impl/base/register.strategy-registry'; | +import { BinancePayStrategy as BinanceStrategyR } from './strategies/register/impl/binance-pay.strategy'; |  import { BitcoinStrategy as BitcoinStrategyR } from './strategies/register/impl/bitcoin.strategy';
David: `BinancePayStrategyR`

--- PR #2231 | src/subdomains/core/payment-link/enums/index.ts ---
Context: +  USER = 'user', | +  ACCESS_KEY = 'access-key', | +  PUBLIC = 'public',
David: We normally use PascalCase for enum values

--- PR #2231 | src/subdomains/core/payment-link/guards/create-payment-access-level.guard.ts ---
David: Is it really required to have all this logic with access levels? Can't we just try to create a donation payment, if no key and no JWT are provided?
Currently, for donation payments, the payment link is checked twice 🤔

--- PR #2231 | src/subdomains/core/payment-link/guards/optional-jwt-or-payment-link-key.guard.ts ---
Context: +    return true; | +  } | +}
David: This looks suspicious 🤔

--- PR #2231 | src/subdomains/core/payment-link/enums/index.ts ---
Context: +  MULTIPLE = 'Multiple', | +  DONATION = 'Donation', | +}
David: Donation -> Flexible, Variable?

--- PR #2231 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: +    const existingPaymentLink = await this.getOrThrow(route.user.id, undefined, externalLinkId); | +    return existingPaymentLink.mode === PaymentLinkMode.DONATION; | +  }
David: This looks unused?

--- PR #2231 | src/subdomains/core/payment-link/dto/payment-link.dto.ts ---
Context: +  route?: string; | +  currencyName?: string; |  }
David: Just use `currency?: string;`

--- PR #2231 | src/subdomains/core/payment-link/guards/optional-jwt-or-payment-link-key.guard.ts ---
Context: +    return true; | +  } | +}
David: Is the `OptionalJwtAuthGuard` not enough?

--- PR #2231 | src/subdomains/core/payment-link/enums/index.ts ---
Context: +  MULTIPLE = 'Multiple', | +  DONATION = 'Donation', | +}
David: Yes, works for me 👍

--- PR #2235 | src/config/config.ts ---
Context: +    quoteUidPrefix: 'Q', | +    paymentLinkUidPrefix: 'pl', | +    paymentLinkPaymentUidPrefix: 'plp',
David: Payment quote prefix is missing

--- PR #2249 | src/subdomains/core/custody/services/custody-order.service.ts ---
Context:   | +        if (!this.checkBalance(sourceAsset, orderDto.outputAmount, user.custodyBalances)) | +          new BadRequestException('Amount is not enough');
David: `orderDto.outputAmount` is undefined

--- PR #2249 | src/subdomains/core/custody/services/custody-order.service.ts ---
Context:   | +        if (!this.checkBalance(sourceAsset, orderDto.outputAmount, user.custodyBalances)) | +          new BadRequestException('Amount is not enough');
David: `orderDto.outputAmount` is undefined

--- PR #2249 | src/subdomains/core/custody/services/custody-order.service.ts ---
Context: +    const assetBalance = custodyBalances.find((a) => a.asset.id === asset.id); | +    return assetBalance ? assetBalance.balance >= amount : false; | +  }
David: `assetBalance?.balance >= amount`

--- PR #2249 | src/subdomains/core/custody/services/custody-order.service.ts ---
Context: +    const assetBalance = custodyBalances.find((a) => a.asset.id === asset.id); | +    return assetBalance ? assetBalance.balance >= amount : false; | +  }
David: Throw `BadRequestException('Not enough balance')` directly in this method

--- PR #2252 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +        .then((l) => l.map((log) => JSON.parse(log.message) as FinanceLog)); | +      const meanTotalBalance = Util.sum(logs.map((l) => l.balancesTotal.totalBalanceChf)) / logs.length; | +
David: `Util.avg`

--- PR #2263 | src/subdomains/generic/kyc/enums/review-status.enum.ts ---
Context: +  PAUSED = 'Paused', | +  ON_HOLD = 'OnHold', | +}
David: I'm not sure if it makes sense to combine the two enums into one. There are many status that are only used for KYC so it might be quite confusing for bank datas 🤔

--- PR #2263 | src/subdomains/generic/kyc/enums/review-status.enum.ts ---
Context: +  PAUSED = 'Paused', | +  ON_HOLD = 'OnHold', | +}
David: Haha, yes. As an outsider it's easier to see what's going on when using separate enums. But we can leave it as its is, if you prefer 😉

--- PR #2263 | src/shared/i18n/de/mail.json ---
Context: +                "line4": "Du wirst benachrichtigt sobald die Prüfung abgeschlossen ist und deine Zahlung verarbeitet wurde.", | +                "line5": "Um den aktuellen Stand deiner Transaktion einsehen zu können:<br>[url:Klick hier]" | +            },
David: Can't we reuse the manual_check translations? Look the same 🤔

--- PR #2265 | src/subdomains/supporting/pricing/services/pricing.service.ts ---
Context: + | +  async getDefaultPrice( | +    from: Active,
David: `getFiatPrice`?

--- PR #2265 | src/subdomains/supporting/pricing/services/pricing.service.ts ---
Context: +    from: Active, | +    referenceCurrency: 'EUR' | 'CHF' | 'USD', | +    allowExpired: boolean,
David: Maybe an enum would make sense?

--- PR #2265 | src/subdomains/supporting/pricing/services/pricing.service.ts ---
Context: +  CHF = 'CHF', | +  USD = 'USD', | +}
David: `PriceCurrency` (singular) would probably enough 😉

--- PR #2266 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: +      this.paymentLinkPaymentService.processExpiredPayments(), | +      Util.secondsDiff(actualQuote.expiryDate) * 1000, | +    );
David: I think it doesn't make sense to wait for this timeout when creating a payment request.

--- PR #2266 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: +      this.paymentLinkPaymentService.processExpiredPayments(), | +      Util.secondsDiff(actualQuote.expiryDate) * 1000, | +    );
David: We should start the timer when creating a payment, not for every payment request.

--- PR #2266 | src/subdomains/core/payment-link/services/payment-link.service.ts ---
Context: +      this.paymentLinkPaymentService.processExpiredPayments(), | +      Util.secondsDiff(actualQuote.expiryDate) * 1000, | +    );
David: `Util.secondsDiff(actualQuote.expiryDate)` will give a negative number?

--- PR #2266 | src/subdomains/core/payment-link/services/payment-link-payment.service.ts ---
Context: +    await this.paymentActivationService.processExpiredActivations(); | +    await this.paymentQuoteService.processExpiredQuotes(); | +  }
David: The code below will already cancel all quotes/activations for the expired payment. No need to process them separately. Keep this only for the cron job.
```ts
      await this.doSave(payment.expire(), true);
      await this.cancelQuotesForPayment(payment);
```
-> Keep the cron job as it is (with prolonged execution period)
-> Just call the code above when a payment expires (maybe move to a method)

--- PR #2266 | src/subdomains/core/payment-link/services/payment-link-payment.service.ts ---
Context: +    setTimeout(async () => { | +      await this.expirePaymentIfPending(payment.uniqueId); | +    }, payment.expiryDate.getTime() - new Date().getTime());
David: Add `Config.payment.timeoutDelay`

--- PR #2269 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
Context:          return this.payoutBitcoinService.getCurrentFeeRate(); | +      case Blockchain.SOLANA: | +        return this.payoutSolanaService.getCurrentGasForCoinTransaction();
David: Hm, we don't want the absolute fee amount, but a fee rate. Is there something like this for Solana?

--- PR #2269 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
David: Update to newest changes from develop

--- PR #2270 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context:      @Query('ids') ids: string, | +    @Query('version') version: string, |      @Query('lang') lang: string,
David: Use an enum, e.g.
```TS
enum StickerType {
   CLASSIC = 'Classic',
   BITCOIN = 'Bitcoin',
}

--- PR #2270 | src/subdomains/core/payment-link/controllers/payment-link.controller.ts ---
Context:      @Query('ids') ids: string, | +    @Query('version') version: StickerType, |      @Query('lang') lang: string,
David: Use `type`

--- PR #2276 | src/integration/binance-pay/services/binance-pay.service.ts ---
Context: +  @DfxCron(CronExpression.EVERY_HOUR, { process: Process.BINANCE_PAY_CERTIFICATES_UPDATE }) | +  async updateCertificates(): Promise<void> { | +    try {
David: Call this method in `onModuleInit` method to be ready as fast as possible?

--- PR #2283 | src/subdomains/generic/gs/gs.service.ts ---
David: Only delete field?

--- PR #2283 | src/subdomains/generic/gs/gs.service.ts ---
Context:   | +    // Null all elements which are larger than 50k symbols | +    data.forEach((e) => Object.values(e).map((entity) => (entity.toString().length >= 50000 ? null : entity)));
David: Looks still suspicious 😁

--- PR #2292 | src/shared/models/fiat/fiat.entity.ts ---
Context: +    } else { | +      return true; | +    }
David: `config` might be undefined

--- PR #2292 | src/subdomains/core/aml/enums/aml-error.enum.ts ---
Context:    LIQUIDITY_LIMIT_EXCEEDED = 'LiquidityLimitExceeded', | +  IBAN_COUNTRY_NOT_ALLOWED = 'IbanCountryNotAllowed', |  }
David: Shouldn't it be `IBAN_CURRENCY_MISMATCH`? The country is not blocked per se, just for the selected currency.

--- PR #2292 | src/shared/models/fiat/fiat.entity.ts ---
Context: +      return true; | +    } else if (config.notAllowed.includes(ibanCountry) || config.notAllowed.includes('all')) { | +      return false;
David: Could also use `*` instead of `all` 😉

--- PR #2292 | src/shared/models/fiat/dto/fiat.dto.ts ---
Context: + | +  @ApiProperty() | +  ibanCountryConfig: IbanCountryConfig;
David: I think I would map this to a list of allowed countries. Just mapping the country config is a bit weird.

--- PR #2292 | src/shared/models/fiat/dto/fiat.dto.ts ---
Context: + | +  @ApiProperty() | +  allowedIbanCountry: string[];
David: `allowedIbanCountries`

--- PR #2294 | src/integration/kucoin-pay/kucoin-pay.service.ts ---
Context: +  enrollPaymentLink(): Promise<Record<string, string>> { | +    return Promise.resolve({ kucoinPaySubMerchantId: Util.randomString(8) }); | +  }
David: No enrollment required for Kucoin?

--- PR #2294 | src/integration/kucoin-pay/kucoin-pay.service.ts ---
Context: +      .filter((param) => (param === 'apiKey' ? true : args[param] !== undefined && args[param] !== null)) | +      .map((param) => (param === 'apiKey' ? `${param}=${this.apiKey}` : `${param}=${args[param]}`)) | +      .join('&');
David: Isn't ` args[param] !== undefined && args[param] !== null` the same as `args[param] != null`?

--- PR #2294 | src/integration/kucoin-pay/kucoin-pay.service.ts ---
Context: +      format: 'der', | +      type: 'pkcs8', | +    });
David: Does it make sense to create the private key in constructor instead of on every call?

--- PR #2299 | src/subdomains/supporting/payment/services/transaction.service.ts ---
Context: +      .addSelect('SUM(refReward.amountInEur)', 'credit') | +      .leftJoinAndSelect('transaction.user', 'user') | +      .leftJoinAndSelect('transaction.refReward', 'refReward')
David: Wouldn't `leftJoin` be enough without select? We only want to select volume and credit.

--- PR #2307 | src/subdomains/core/aml/services/aml-helper.service.ts ---
Context:      if (!entity.userData.isPaymentKycStatusEnabled) errors.push(AmlError.INVALID_KYC_STATUS); | +    if (refUser && !refUser.userData.isPaymentKycStatusEnabled) errors.push(AmlError.INVALID_KYC_STATUS); |      if (entity.userData.kycType !== KycType.DFX) errors.push(AmlError.INVALID_KYC_TYPE);
David: `INVALID_KYC_STATUS_REF_USER`?

--- PR #2318 | src/subdomains/core/buy-crypto/routes/swap/swap.entity.ts ---
Context:   | -export const NoSwapBlockchains: Blockchain[] = [Blockchain.MONERO]; | +export const NoSwapBlockchains: Blockchain[] = [Blockchain.MONERO, Blockchain.ZANO];
David: Is there a reason to put ZANO on the no-swap list?

--- PR #2318 | src/integration/blockchain/zano/services/zano.service.ts ---
Context: +      if ('OK' !== status) return false; | + | +      return true;
David: `return 'OK' === status;`

--- PR #2318 | src/integration/blockchain/zano/zano-client.ts ---
Context: +  async getNativeCoinBalanceForAddress(_: string): Promise<number> { | +    throw new Error('Coin balance for address not possible for monero'); |    }
David: Monero?

--- PR #2318 | src/integration/blockchain/zano/zano-client.ts ---
Context: +  async getTokenBalances(_: Asset[], __?: string): Promise<BlockchainTokenBalance[]> { | +    throw new Error('Monero has no token'); | +  }
David: Monero?

--- PR #2318 | src/integration/blockchain/zano/zano-helper.ts ---
Context: +  static zanoToAu(xmrAmount?: number): number | undefined { | +    return xmrAmount && Util.round(xmrAmount * ZanoHelper.AU_ZANO_FACTOR, 0); | +  }
David: xmr?

--- PR #2318 | src/integration/blockchain/zano/services/zano.service.ts ---
Context: +  async getDepositByBlockchainAndIndex(blockchain: Blockchain, index: number): Promise<Deposit> { | +    return this.depositService.getDepositByBlockchainAndIndex(blockchain, index); | +  }
David: Is blockchain as parameter useful in ZanoService?

--- PR #2319 | src/shared/models/country/country.entity.ts ---
Context: +  get enabledKycDocumentList(): KycDocument[] { | +    return this.enabledKycDocuments ? (this.enabledKycDocuments?.split(';') as KycDocument[]) : []; | +  }
David: `return (this.enabledKycDocuments?.split(';') ?? []) as KycDocument[];`?

--- PR #2320 | src/subdomains/core/buy-crypto/process/services/buy-crypto-out.service.ts ---
Context:          try { | +          if (transaction.user.isBlockedOrDeleted || transaction.userData.isBlocked) | +            throw new InternalServerErrorException('Payout stopped for blocked user');
David: `throw new Error(...)`

--- PR #2320 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context:          try { | +          if (entity.user?.isBlockedOrDeleted || entity.userData?.isBlocked) | +            throw new InternalServerErrorException('Payout stopped for blocked user');
David: `throw new Error(...)`

--- PR #2324 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
Context: +    } else { | +      transferAmount.assets = assets.map((asset) => ({ asset: asset.name })); |      }
David: If minFee is null, this method is not available

--- PR #2336 | src/integration/blockchain/shared/evm/evm-client.ts ---
Context:    private readonly alchemyService: AlchemyService; | +  protected readonly goldskyService: any; // Optional Goldsky service |    readonly chainId: ChainId;
David: Is there a type for this service?

--- PR #2336 | src/subdomains/supporting/payout/strategies/prepare/impl/citrea-testnet.strategy.ts ---
Context: +    // This should return the native CitreaTestnet token | +    // For now, returning undefined until proper asset is configured | +    return undefined;
David: TODO

--- PR #2336 | src/subdomains/supporting/payout/strategies/payout/impl/citrea-testnet-token.strategy.ts ---
Context: +    // For now, return a default value until properly implemented | +    return Promise.resolve(0.002); | +  }
David: TODO

--- PR #2336 | src/subdomains/supporting/payout/strategies/payout/impl/citrea-testnet-token.strategy.ts ---
Context: +    // For now, returning undefined until proper asset is configured | +    return undefined; | +  }
David: TODO

--- PR #2336 | src/subdomains/supporting/payout/strategies/payout/impl/citrea-testnet-coin.strategy.ts ---
Context: +    // For now, return a default value until properly implemented | +    return Promise.resolve(0.001); | +  }
David: TODO

--- PR #2336 | src/subdomains/supporting/payout/strategies/payout/impl/citrea-testnet-coin.strategy.ts ---
Context: +    // For now, returning undefined until proper asset is configured | +    return undefined; | +  }
David: TODO

--- PR #2336 | src/subdomains/supporting/payin/strategies/send/impl/citrea-testnet-token.strategy.ts ---
Context: +  protected getForwardAddress(): BlockchainAddress { | +    return BlockchainAddress.create(Config.blockchain.citreaTestnet?.citreaTestnetWalletAddress ?? '', Blockchain.CITREA_TESTNET); | +  }
David: `citreaTestnet` cannot be null

--- PR #2336 | src/subdomains/supporting/payin/strategies/send/impl/citrea-testnet-coin.strategy.ts ---
Context: +  protected getForwardAddress(): BlockchainAddress { | +    return BlockchainAddress.create(Config.blockchain.citreaTestnet?.citreaTestnetWalletAddress ?? '', Blockchain.CITREA_TESTNET); | +  }
David: Same here

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
Context: +    // Note: CitreaTestnet doesn't have Alchemy support yet | +    // Webhook and asset transfer subscriptions would need alternative implementation | +  }
David: TODO

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
Context: +  protected getOwnAddresses(): string[] { | +    return [Config.blockchain.citreaTestnet?.citreaTestnetWalletAddress ?? '']; | +  }
David: `citreaTestnet` cannot be null

--- PR #2336 | src/integration/blockchain/shared/evm/evm.util.ts ---
Context:      [Blockchain.BINANCE_SMART_CHAIN, this.blockchainConfig.bsc.bscChainId], | +    [Blockchain.CITREA_TESTNET, this.blockchainConfig.citreaTestnet?.citreaTestnetChainId ?? 5115], |    ]);
David: `citreaTestnet` cannot be null

--- PR #2336 | src/integration/goldsky/goldsky.service.ts ---
Context: +      'citrea-testnet': GetConfig().blockchain?.citreaTestnet?.goldskySubgraphUrl, | +      'citrea-devnet': undefined, // Add when needed | +    };
David: `citreaTestnet` cannot be null

--- PR #2336 | src/integration/goldsky/goldsky.service.ts ---
Context: +    } | +    return endpoint; | +  }
David: Weird coding

--- PR #2336 | src/integration/blockchain/citrea-testnet/citrea-testnet-client.ts ---
Context: +      this.logger.warn('CitreaTestnet: Goldsky service not configured, transaction history not available'); | +      return []; | +    }
David: Throw an error, if no goldsky

--- PR #2336 | src/integration/goldsky/goldsky.service.ts ---
Context: +      this.logger.warn(`No Goldsky endpoint configured for ${network}`); | +      return []; | +    }
David: Throw an error, if no matching endpoint

--- PR #2336 | src/integration/goldsky/goldsky.service.ts ---
Context: +      this.logger.warn(`No Goldsky endpoint configured for ${network}`); | +      return []; | +    }
David: Same here

--- PR #2336 | migration/1754950663000-citrea-testnet-cbtc-asset.seed.ts ---
David: We do not use seed migrations

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
Context: +     | +    for (const [txHash, txGroup] of txGroups) { | +      for (let i = 0; i < txGroup.length; i++) {
David: Linter warning

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
Context: +import { PayInEntry } from '../../../interfaces'; | +import { AssetService } from 'src/shared/models/asset/asset.service'; | +import { SettingService } from 'src/shared/models/setting/setting.service';
David: Linter warning

--- PR #2336 | src/subdomains/supporting/payin/payin.module.ts ---
David: DEX strategies are still missing

--- PR #2336 | src/subdomains/supporting/payout/payout.module.ts ---
Context: +    CitreaTestnetTokenStrategyPO, | +    ZanoStrategyPR, | +    ZanoStrategyPO,
David: What?

--- PR #2336 | src/integration/goldsky/goldsky.service.ts ---
Context: +    return { | +      'citrea-testnet': GetConfig().blockchain.citreaTestnet.goldskySubgraphUrl, | +      'citrea-devnet': undefined, // Add when needed
David: Create an enum for these networks, use pascal case for enum values

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
Context: +      this.logger.verbose('Previous CitreaTestnet processing still running, skipping this iteration'); | +      return; | +    }
David: This is done by `@DfxCron`

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
Context: +    } | +     | +    return addresses;
David: `return [Config.blockchain.citreaTestnet.citreaTestnetWalletAddress, Config.payment.citreaTestnetAddress];`

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
Context: +      return (this.lastProcessedBlock || 0) + 1; | +    } | +  }
David: Who in the world would create a fallback for this?

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
Context: +      this.logger.error('Failed to fetch new CitreaTestnet transactions:', error); | +      return { entries: [], processedBlock: null }; | +    }
David: Error logging is done in @DfxCron

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
Context: +      } catch (error) { | +        this.logger.warn(`Failed to fetch transactions for address ${address}:`, error); | +      }
David: Doesn't make sense to catch the error

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
David: Does it have to be so complicated? 😆

--- PR #2336 | src/subdomains/supporting/payin/strategies/register/impl/citrea-testnet.strategy.ts ---
Context: +   | +  private static readonly LAST_PROCESSED_BLOCK_KEY = 'citrea_testnet_last_processed_block'; | +  private lastProcessedBlock: number | null = null;
David: We use camel case for setting keys

--- PR #2341 | src/subdomains/core/buy-crypto/routes/swap/dto/get-swap-quote.dto.ts ---
David: Same changes for `targetAsset`

--- PR #2341 | src/shared/models/asset/dto/asset.dto.ts ---
Context: +  }) | +  private readonly _assetIdentifierValidator!: boolean; // Dummy-Property for validation | +}
David: I think this fancy validator thing is not really required 😅  I would do something like this
```TS
export class AssetInDto {
  @ApiPropertyOptional()
  @IsNotEmpty()
  @ValidateIf((a: AssetInDto) => Boolean(a.id || !(a.chainId || a.blockchain)))
  @IsInt()
  id?: number;

  @ApiPropertyOptional()
  @IsNotEmpty()
  @ValidateIf((a: AssetInDto) => Boolean(a.chainId || !a.id))
  @IsString()
  chainId?: string;

  @ApiPropertyOptional()
  @IsNotEmpty()
  @ValidateIf((a: AssetInDto) => Boolean(a.blockchain || !a.id))
  @IsEnum(Blockchain)
  blockchain?: Blockchain;
}
```

--- PR #2341 | src/shared/services/payment-info.service.ts ---
Context: +    dto.asset = dto.asset.id | +      ? await this.assetService.getAssetById(dto.asset.id) | +      : await this.assetService.getAssetByChainId(dto.asset.blockchain, dto.asset.chainId);
David: Create a helper for this

--- PR #2341 | src/shared/services/payment-info.service.ts ---
Context: +    dto.targetAsset = dto.targetAsset.id | +      ? await this.assetService.getAssetById(dto.targetAsset.id) | +      : await this.assetService.getAssetByChainId(dto.targetAsset.blockchain, dto.targetAsset.chainId);
David: Use `resolveAsset`

--- PR #2341 | src/shared/services/payment-info.service.ts ---
Context: +      return this.assetService.getAssetById(asset.id); | +    } | +    return this.assetService.getAssetByChainId(asset.blockchain, asset.chainId);
David: ```TS
return asset.id
   ? this.assetService.getAssetById(asset.id)
   : this.assetService.getAssetByChainId(asset.blockchain, asset.chainId);
```

--- PR #2345 | src/subdomains/core/payment-link/dto/payment-link.dto.ts ---
Context: + | +  @ApiPropertyOptional({ type: FiatValueDto, description: 'Total value of completed payments' }) | +  totalValue?: FiatValueDto;
David: Only total value in route currency

--- PR #2348 | src/subdomains/core/liquidity-management/enums/index.ts ---
Context:    BINANCE = 'Binance', | +  MEXC = 'Mexc', |    DFX_DEX = 'DfxDex',
David: `MEXC = 'MEXC',`

--- PR #2350 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
Context: +    Blockchain.MONERO, | +    Blockchain.ZANO, |      Blockchain.SOLANA,
David: I think Monero should be before Bitcoin in transfer amount order

--- PR #2350 | src/config/config.ts ---
Context: +      paymentWallet: { | +        url: process.env.ZANO_PAYMENT_WALLET_URL, | +        address: process.env.PAYMENT_ZANO_ADDRESS,
David: Use the same wallet for payments as for liquidity -> payment address is address with index 0

--- PR #2350 | src/integration/blockchain/zano/services/zano-payment.service.ts ---
David: Can be reverted?

--- PR #2350 | src/integration/blockchain/zano/services/zano.service.ts ---
Context: +      if ('OK' !== status) return false; | + | +      return true;
David: Revert?

--- PR #2365 | src/subdomains/generic/user/models/user-data/user-data-job.service.ts ---
Context: +      } else { | +        await this.userDataRepo.update(entity.id, { hasBankTx: false }); | +      }
David: ```TS
const hasBankTx = entity.transactions.some((t) => t.buyCrypto?.isComplete && t.sourceType === TransactionSourceType.BANK_TX);
await this.userDataRepo.update(entity.id, { hasBankTx });
```

--- PR #2367 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +        } | +      }), | +    );
David: Why is this async? And what exception can happen during `getWalletAddress`?

--- PR #2367 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +        } | +      }), | +    );
David: Create a map?
```TS
const liqAddresses = new Map(
  Object.values(Blockchain).map((blockchain) => {
      const liqAddress = this.blockchainRegistryService
        .getClient(blockchain)
        ?.getWalletAddress();

      return [blockchain, liqAddress];
  }),
);
```

--- PR #2374 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @Transform(Util.sanitize) | +  fileName: string; | +}
David: Just use `KycFileData` instead?

--- PR #2374 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: + | +    return KycStepMapper.toStepBase(kycStep); | +  }
David: Use `updateFileData()` instead?

--- PR #2374 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: + | +    return KycStepMapper.toStepBase(kycStep); | +  }
David: Ah true. Maybe add a boolean parameter for this? The rest looks the same.

--- PR #2382 | src/subdomains/generic/user/models/bank-data/dto/create-bank-data.dto.ts ---
Context: +  @IsOptional() | +  @IsString() | +  bic?: string;
David: `@Transform(Util.trimAll)`?

--- PR #2382 | src/subdomains/supporting/bank/bank-account/bank-account.service.ts ---
Context: +  private async createBankAccountInternal( | +    iban: string, | +    bic: string,
David: `string | undefined`

--- PR #2385 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:   | +    if (user?.wallet?.amlRuleList?.includes(AmlRule.RULE_SKIP_AML_CHECK) && Config.environment !== Environment.PRD) | +      return;
David: wallet and amlRuleList cannot be undefined (`user?.wallet.amlRuleList.includes(...)` should be ok)

--- PR #2385 | src/subdomains/core/aml/enums/aml-rule.enum.ts ---
Context:  export enum AmlRule { | +  RULE_SKIP_AML_CHECK = -1, // skip amlCheck only possible in dev or loc env |    DEFAULT = 0, // default
David: `SKIP_AML_CHECK = -1,`?

--- PR #2389 | src/subdomains/generic/user/models/user-data/user-data.enum.ts ---
Context: +  BLOCKED = 'Blocked', | +  RELEASED = 'Released', | +}
David: It should be Suspicious or Suspected to match the other terms

--- PR #2393 | src/subdomains/supporting/bank-tx/bank-tx/services/bank-tx.service.ts ---
Context: +    const bank = await this.bankService.getBankByIban(batch.iban); | +    this.bankBalanceSubject.next({ bank, balance: batch.balanceAfterAmount }); | +
David: Check if `CRDT` or `DBIT`?

--- PR #2399 | src/subdomains/core/aml/enums/aml-error.enum.ts ---
Context:    IBAN_CURRENCY_MISMATCH = 'IbanCurrencyMismatch', | +  MERGE_MAIL_PENDING = 'MergeMailPending', |  }
David: Call it `MERGE_PENDING` or `ACCOUNT_MERGE_PENDING?`? Mail is just the type of notification we use to notify the user.

--- PR #2399 | src/subdomains/generic/user/models/bank-data/bank-data.entity.ts ---
Context: +  get isCompleted(): boolean { | +    return this.status === ReviewStatus.COMPLETED; | +  }
David: Maybe `isApproved` or `isReviewed` or similar would be better? A bank data cannot be completed IMO.

--- PR #2399 | src/subdomains/generic/user/models/account-merge/account-merge.service.ts ---
Context: +      { master: { id: referenceUserDataId }, slave: { id: userDataId }, isCompleted: false }, | +    ]); | +  }
David: Maybe `hasPendingMergeRequest(...): Promise<boolean>` would be enough?

--- PR #2399 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +      const pendingMergeRequests = existing | +        ? await this.accountMergeService.hasPendingMergeRequest(entity.userData.id, existing.userData.id) | +        : undefined;
David: `: false;`

--- PR #2401 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: -      ); | +      const pendingFiatOutputs = accountIbanGroup.filter((tx) => tx.isReadyDate && !tx.bankTx); | +      const pendingBalance = Util.sum(pendingFiatOutputs.map((tx) => tx.bankAmount));
David: `Util.sumObjValue`?

