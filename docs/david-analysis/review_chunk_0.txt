--- PR #214 | src/admin/admin.controller.ts ---
Context:    } |   | +  @Put('rename')
David: Würde da noch Spider und Ref in die Route reinbringen, damit's klarer ist.

--- PR #214 | src/monitoring/monitoring.controller.ts ---
Context: +  constructor(private monitoringService: MonitoringService) {} | + | +  @Get()
David: @Get('stakingBalance)?

--- PR #214 | src/monitoring/monitoring.service.ts ---
Context: +  ) {} | +  // --- VOLUMES --- // | +  async checkBalance(): Promise<any> {
David: - checkStakingBalance()
- Typisierung des Return-Werts (auch beim Controller)

--- PR #214 | src/user/services/spider/spider-api.service.ts ---
Context:   | +  async renameCustomerReference(oldReference: string, newReference: string): Promise<Customer> { | +    return this.callApi<Customer>(`customers/${oldReference}/rename?newReference=${newReference}`);
David: Kommt da (und bei renameContractReference) echt der ganze Customer zurück? 😉

--- PR #214 | src/monitoring/monitoring.service.ts ---
Context: +      actualBalance += +(await whaleClient.address.getBalance(masterNode.owner)); | +    } | +    actualBalance += +(await whaleClient.address.getBalance(Config.node.stakingWalletAddress));
David: Mit Promise.all() parallelisieren und dann mit Util.sum() addieren? Ocean sollte das eigentlich handeln können 😄  sowas in der Art:
```TS
const addresses = [...activeMasternodes.map((m) => m.owner), Config.node.stakingWalletAddress];
const balances = await Promise.all(addresses.map((a) => whaleClient.address.getBalance(a).then((b) => +b)));
```

--- PR #214 | src/monitoring/monitoring.service.ts ---
Context: +    } | +    actualBalance += +(await whaleClient.address.getBalance(Config.node.stakingWalletAddress)); | +    const shouldBalance = await this.stakingService.getTotalStakingBalance(new Date());
David: Bei shouldBalance noch `+ 20000` (Startkapital) und `- totalMasternodeCount * 10`.

--- PR #247 | src/payment/models/crypto-input/crypto-input.service.ts ---
Context:   | -  private async saveAndForward(input: CryptoInput): Promise<void> { | +  private async testCompositeSwaps(
David: I would rename this method to something like `getReferenceAmounts()`. USDT, USDC and BTC are reference assets. These are the ones we (virtually) exchange on Kraken.

--- PR #247 | src/payment/models/crypto-input/crypto-input.service.ts ---
Context: +        if (input.route.type === RouteType.STAKING) { | +          await this.cryptoStakingService.create(input); | +        }
David: In my opinion the creation of the CryptoStaking belongs to the saving step.

--- PR #247 | src/payment/models/crypto-input/crypto-input.service.ts ---
Context: +        } | + | +        // forward (only await UTXO)
David: `(only await UTXO)` is an old comment, can be removed :-)

--- PR #251 | src/payment/models/exchange/exchange-utility.service.ts ---
Context: + | +    return krakenPrice; | +  }
David: Should conversion only be possible for the reference assets EUR and BTC, USDT, USDC? And in case of USDC the reference price should be taken from Bitstamp.

And I would return only the price as a number and not the complete DTO. This would also make lines 14-15 unnecessary.

--- PR #251 | src/payment/models/exchange/exchange-utility.service.ts ---
Context: + | +    return krakenPrice; | +  }
David: Should conversion only be possible for the reference assets EUR and BTC, USDT, USDC? And in case of USDC the reference price should be taken from Bitstamp.

And I would return only the price as a number and not the complete DTO. This would also make lines 14-15 unnecessary.

--- PR #256 | src/ain/node/node-client.ts ---
Context:    get utxoFee(): number { | -    return this.chain === Chain.MAIN ? 0.00000132 : 0.0000222; | +    return this.chain === 'main' ? 0.00000132 : 0.0000222;
David: String in environment variable is 'mainnet'.

--- PR #256 | src/ain/node/node-client.ts ---
Context: +  private async getChain(): Promise<string> { | +    return this.callNode((c) => c.blockchain.getBlockchainInfo()).then((i) => i.chain); |    }
David: Can be removed completely.

--- PR #256 | src/ain/node/node.controller.ts ---
Context:   | -  @Post(':node/:mode/rpc') | +  @Post(':node/connected/rpc')
David: Can we omit the 'connected' in the path (also on lines 27 and 41)? Would be nicer without ;-)

--- PR #256 | src/ain/node/node.controller.ts ---
Context:    @ApiExcludeEndpoint() |    @UseGuards(AuthGuard(), new RoleGuard(UserRole.ADMIN)) |    async rpc(@Param('node') node: NodeType, @Param('mode') mode: NodeMode, @Body() command: string): Promise<any> {
David: Unused parameter 'mode' (also on lines 31 and 47).

--- PR #256 | src/config/config.ts ---
Context: +    noReplyMail: process.env.NOREPLY_MAIL || 'noreply@dfx.swiss', | +  }; | +
David: I would propose to cluster all mail related configs into one object, e.g.:
```ts
mail: {
   options: ... // current mail options,
   defaultTemplate: ... // from line 24,
   contact: ... // new mails
}
```

--- PR #256 | src/ain/node/node-client.ts ---
Context:    get utxoFee(): number { | -    return this.chain === Chain.MAIN ? 0.00000132 : 0.0000222; | +    return this.chain === 'main' ? 0.00000132 : 0.0000222;
David: Yes, that's somewhat confusing. The getInfo call returns 'main' whereas the Whale API client requires 'mainnet' 🤔 that's why in the config we have 'mainnet'.

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto-dex.service.ts ---
Context: +        batch.outputAsset, | +        // swapping a bit more output asset to cover commissions | +        DFIAmount + Util.round(DFIAmount * 0.001, 8),
David: Swap amount should be exact for all non-reference assets. For reference assets it should be a few percent more.

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context: +    } | +  } | +
David: Unused method?

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context:    } |   | +  // keep the methods
David: Remove

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context:    } |   | +  // keep the methods
David: Remove

--- PR #269 | src/payment/models/exchange/exchange-utility.service.ts ---
Context: +    } catch { | +      console.log(`Could not find reference price at Bitstamp. From ${fromCurrency} to ${toCurrency}`); | +    }
David: I would remove the two console.log, as they will spam our log with information we already know ;-)

--- PR #269 | src/payment/models/buy-crypto/entities/buy-crypto-batch.entity.ts ---
Context: +  PAYING_OUT = 'paying-out', | +  COMPLETE = 'complete', | +}
David: We normally use PascalCase values for internal enums.

--- PR #269 | src/payment/models/buy-crypto/entities/buy-crypto-batch.entity.ts ---
Context: + | +  @Column({ length: 256, nullable: true }) | +  status: string;
David: `status: BuyCryptoBatchStatus;`

--- PR #269 | src/payment/models/buy-crypto/entities/buy-crypto-batch.entity.ts ---
Context: + | +  private fixRoundingMismatch(): void { | +    const transactionsTotal = this.transactions.reduce((acc, t) => acc + t.outputAmount, 0);
David: Use `Util.sumObj`.

--- PR #269 | src/payment/models/buy-crypto/entities/buy-crypto-batch.entity.ts ---
Context: +    } | + | +    if (Math.abs(mismatch) > 0 && Math.abs(mismatch) < 0.00001) {
David: `Math.abs(mismatch) > 0` is always `true`.

--- PR #269 | src/payment/models/buy-crypto/entities/buy-crypto-batch.entity.ts ---
Context: + | +    return [...result.values()]; | +  }
David: Easier solution?

```ts
  private createPayoutGroups(transactions: BuyCrypto[], maxGroupSize: number): BuyCrypto[][] {
    const result: Map<number, BuyCrypto[]> = new Map();

    transactions.forEach((tx) => {
      // find nearest non-full group without repeating address
      const suitableExistingGroups = [...result.entries()].filter(
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        ([_, transactions]) =>
          transactions.length < maxGroupSize &&
          !transactions.find((_tx) => _tx.buy.user.address === tx.buy.user.address),
      );

      const [key, group] = suitableExistingGroups[0] ?? [result.size, []];
      result.set(key, [...group, tx]);
    });

    return [...result.values()];
  }
```

Disclaimer: no guarantee of correctness ... I did test it [online](https://www.typescriptlang.org/play?#code/ATBmFcDsGMBcEsD2ljQE4FMCGsMAUsBPRcWAcTRIAcBnACljS0hqziRYC5gbH5IA5gG0AugBpgAWywAPCtQDK8AF4ZukcJIBGGNAEpuvNP2EjRwAN4AoECGjJewTDXAAbWNwCyWKgB4N2roSRiaiAHzAALzAkBgA7sDeVHR6ANxWNraMzKzsDgB0oIhoAKJsABZ0DDJ6URHWtrYA9E1g-AAmMdjOsDHIALQQrq7AApTgVMBx8LDlJL2YVNgIgsBY7e3ONJmN9iy9LjNYWq4YJTLwvCbyEzRRwEL5T85usPkYkHwY9HoihfDuXR0HaNYAtYDfVz8WD9dqXY6nfqxGQwqGxYAAAVghCWNHQ8CoMMh0KakEQ-Sg4BoGHa--aB4wB9AB43W434mAAFY9GkgA) though ;-)

--- PR #269 | src/payment/models/buy-crypto/entities/buy-crypto-batch.entity.ts ---
Context: + | +    return this; | +  }
David: Easier solution?

```ts
  addTransaction(tx: BuyCrypto): this {
    tx.batch = this;

    this.transactions = [...(this.transactions ?? []), tx];

    this.outputReferenceAmount = Util.round((this.outputReferenceAmount ?? 0) + tx.outputReferenceAmount, 8);

    return this;
  }
```

--- PR #269 | src/payment/models/exchange/exchange-utility.service.ts ---
Context: +    } catch { | +      console.log(`Could not find reference price at Bitstamp. From ${fromCurrency} to ${toCurrency}`); | +    }
David: For USDC you will never get the price from Binance, for every processed batch you will get the upper log. So the question is, what do you gain from these logs?

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto-batch.service.ts ---
Context: +    for (const tx of transactions) { | +      const outputAsset = tx.buy?.asset?.dexName; | +      tx.defineAssetExchangePair(outputAsset);
David: Why do you get the output asset here and not inside the defineAssetExchangePair method?

--- PR #269 | src/payment/models/buy-crypto/utils/buy-crypto-chain.util.ts ---
Context: + | +      return client | +        .getHistories([Config.node.dexWalletAddress], height, currentHeight)
David: Instead of currentHeight you could just use height + 1.

--- PR #269 | src/payment/models/buy-crypto/utils/buy-crypto-chain.util.ts ---
Context: +            tx.complete(); | +            await this.buyCryptoRepo.save(tx); | +          }
David: Getting the block is not needed anymore.

--- PR #269 | src/payment/models/buy-crypto/utils/buy-crypto-chain.util.ts ---
Context: +  } | + | +  async checkCompletion(batch: BuyCryptoBatch, client: NodeClient) {
David: Is this method really belonging to chain-util?

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto-dex.service.ts ---
Context: +      ); | + | +      const availableAmount = await this.getAvailableTokenAmount(batch);
David: Could be moved to outer method to reduce node calls ... but probably not worth it.

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto-dex.service.ts ---
Context: +    try { | +      const isReferenceAsset = | +        batch.outputAsset === 'BTC' || batch.outputAsset === 'USDC' || batch.outputAsset === 'USDT';
David: This might be something to move to config.ts, since it's used at different places.

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto-dex.service.ts ---
Context: +        // swapping a bit more output asset to cover commissions | +        isReferenceAsset ? DFIAmount : DFIAmount + Util.round(DFIAmount * 0.001, 8), | +      );
David: This logic needs to be rewritten:
- If no reference asset: Do a test swap from output reference to DFI with full amount (to simulate swapping). Use the output as exact input for swap from DFI to output asset.
- If reference asset: Do a test swap from output reference to DFI with a small amount (to get the price). Caution, the amount you specify for the test swap is used as input amount. Currently you would swap 1 BTC, which isn't really a small amount ;-) Use the output with some margin (I'd say 1%) for the swap from DFI to output asset.

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto-out.service.ts ---
Context: +    const assets = [...tokensBalance, utxoBalance]; | + | +    return assets;
David: Can be combined.

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto-out.service.ts ---
Context: +  } | + | +  private async checkNewPayouts(
David: IMO somewhat misleading method name.

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context: +  constructor(private readonly buyCryptoRepo: BuyCryptoRepository, private readonly mailService: MailService) {} | + | +  async sentNotificationMails(): Promise<void> {
David: Typo: sendNotificationMails

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context: +          recipientMail: IsNull(), | +          mailSendDate: IsNull(), | +          txId: Not(IsNull()),
David: Should we check for isComplete instead of txId?

--- PR #269 | src/payment/models/buy-crypto/services/buy-crypto-dex.service.ts ---
Context: +      ); | + | +      const availableAmount = await this.getAvailableTokenAmount(batch);
David: Yes I agree.

--- PR #271 | src/payment/models/bank-account/bank-account.entity.ts ---
Context: +  label: string; | + | +  @ManyToOne(() => User, (user) => user.ibans)
David: Würde das noch 'nullable: false' setzen.

--- PR #271 | src/payment/models/bank-account/bank-account.entity.ts ---
Context: + | +  @OneToMany(() => Buy, (buy) => buy.bankAccount) | +  buy: Buy[];
David: buy -> buys

--- PR #271 | src/payment/models/bank-account/bank-account.entity.ts ---
Context: + | +  @OneToMany(() => Sell, (sell) => sell.bankAccount) | +  sell: Sell[];
David: sell -> sells

--- PR #271 | src/payment/models/bank-account/bank-account.entity.ts ---
Context: +  branch: string; | + | +  @Column({ nullable: true })
David: Explizit keine length?

--- PR #271 | src/payment/models/bank-account/bank-account.entity.ts ---
Context: +  sctInst: boolean; | + | +  @Column({ nullable: true })
David: type: 'datetime2'

--- PR #271 | src/payment/models/bank-account/dto/bank-account.dto.ts ---
Context: @@ -0,0 +1,24 @@ | +export interface BankAccountDto {
David: Würde ich zu BankAccountInfos oder so umbenennen (ist ja kein DTO) und dann die bank-account.entity das Interface implementieren lassen. Dann haben wir da auch mehr Type-Safety.

--- PR #271 | src/shared/services/iban.service.ts ---
Context: +import { HttpService } from './http.service'; | + | +export interface IbanDetails {
David: Das ist jetzt ein DTO ;-)

--- PR #271 | src/shared/services/iban.service.ts ---
Context: +      return result; | +    } catch { | +      throw new ServiceUnavailableException(`sepatools timeout`);
David: Würde die Fehlermeldung noch verbessern, am besten mit dem Fehlertext aus dem Error.

--- PR #271 | src/user/models/user/user.entity.ts ---
Context:   | +  @OneToMany(() => BankAccount, (bankAccount) => bankAccount.user) | +  ibans: BankAccount[];
David: bankAccounts?

--- PR #287 | src/monitoring/metric.observer.ts ---
Context: +  get subscription() { | +    return this.$data.asObservable().pipe(skip(1)); | +  }
David: Missing return types?

--- PR #287 | src/monitoring/monitoring.controller.ts ---
Context: +    @Query('metric') metric: string, | +    @Body() data: unknown, | +  ): Promise<any> {
David: `Promise<void>`?

--- PR #287 | thunder-tests/thunderclient.json ---
Context:      "colId": "62eefab7-84cd-496b-8f93-253c15d4f841", | -    "containerId": "1b37c199-5952-4d91-9455-d1a4151c735f", | -    "name": "create buy",
David: Seems like you didn't refresh from file before updating, deleted the 'create buy' and 'create sell' calls.

--- PR #287 | thunder-tests/thunderclient.json ---
Context: +    "auth": { | +      "type": "bearer", | +      "bearer": "token"
David: Use inherited auth (see other calls).

--- PR #287 | thunder-tests/thunderclient.json ---
Context: +      "type": "bearer", | +      "bearer": "token" | +    },
David: Use inherited auth (see other calls).

--- PR #287 | src/monitoring/monitoring.service.ts ---
Context: +  private subscribeToUpdates(observer: MetricObserver<unknown>): void { | +    observer.subscription.subscribe((data: unknown) => | +      this.updateSystemState.call(this, observer.subsystem, observer.metric, data),
David: Is it required to use the .call method?

--- PR #287 | src/monitoring/monitoring.service.ts ---
Context: +      this.systemStateSnapshotRepo.save({ id: 1, data: JSON.stringify(newState) }); | +    } | +  }
David: Could we use [pairwise()](https://www.learnrxjs.io/learn-rxjs/operators/combination/pairwise) RXJS operator?

--- PR #287 | src/monitoring/monitoring.service.ts ---
Context: -    return { actual, should, difference }; | +  private subscribeToUpdates(observer: MetricObserver<unknown>): void { | +    observer.subscription.subscribe((data: unknown) =>
David: Could this be solved in a more "RXJS" way? ;-)

--- PR #287 | src/monitoring/observers/banking-bot.observer.ts ---
Context: +    if (!(dto instanceof BankingBotDataDto)) { | +      throw new BadRequestException('Invalid banking-bot logs input'); | +    }
David: Isn't this covered by the later validation?

--- PR #287 | src/monitoring/observers/node-balance.observer.ts ---
Context: + | +  constructor(monitoringService: MonitoringService, readonly nodeService: NodeService) { | +    super(monitoringService, 'node', 'nodeBalance');
David: Use 'balance' as metric.

--- PR #287 | src/monitoring/observers/node-balance.observer.ts ---
Context: +  } | + | +  @Interval(60000)
David: Would decrease the interval to 15 minutes or so.

--- PR #287 | src/monitoring/observers/payment.observer.ts ---
Context: +  } | + | +  @Interval(60000)
David: Decrease to every 15 minutes.

--- PR #287 | src/monitoring/observers/staking-balance.observer.ts ---
Context: +export class StakingBalanceObserver extends MetricObserver<StakingData> { | +  constructor(monitoringService: MonitoringService, private whaleService: WhaleService) { | +    super(monitoringService, 'staking', 'stakingBalance');
David: Use 'balance' as metric.

--- PR #287 | src/monitoring/observers/staking-balance.observer.ts ---
Context: +  } | + | +  @Interval(60000)
David: Decrease to every 15 minutes.

--- PR #287 | src/monitoring/observers/user.observer.ts ---
Context: +  } | + | +  @Interval(60000)
David: Same here.

--- PR #287 | src/monitoring/observers/user.observer.ts ---
Context: +export class UserObserver extends MetricObserver<UserData> { | +  constructor(monitoringService: MonitoringService) { | +    super(monitoringService, 'user', 'userData');
David: Use 'kyc' as metric.

--- PR #287 | src/monitoring/observers/node-health.observer.ts ---
Context: +    readonly mailService: MailService, | +  ) { | +    super(monitoringService, 'node', 'nodeHealth');
David: Use 'health' as metric.

--- PR #287 | src/monitoring/observers/node-health.observer.ts ---
Context: +    [key in NodeMode]: NodeHealth; | +  }; | +};
David: This looks a bit weird to me ... let's discuss at the daily.

--- PR #289 | src/ain/node/node.service.ts ---
Context:    INT = 'int', |    REF = 'ref', | +  BTC_INPUT = 'btc_input',
David: btc-input

--- PR #289 | src/config/config.ts ---
Context:        passive: process.env.NODE_REF_URL_PASSIVE, |      }, | +    btc_input: {
David: btcInput

--- PR #289 | src/ain/node/node-client.ts ---
Context:    // --- HELPER METHODS --- // | -  private async callNode<T>(call: (client: ApiClient) => Promise<T>, unlock = false): Promise<T> { | +  async callNode<T>(call: (client: ApiClient) => Promise<T>, unlock = false): Promise<T> {
David: Can be reverted?

--- PR #289 | src/payment/models/crypto-route/crypto-route.entity.ts ---
Context: + | +  @ManyToOne(() => Deposit, { eager: true, nullable: true }) | +  deposit: Deposit;
David: Is already included in DepositRoute.

--- PR #289 | src/payment/models/crypto-route/crypto-route.entity.ts ---
Context: + | +  @ManyToOne(() => Deposit, { eager: true, nullable: true }) | +  staking: Deposit;
David: Rename to targetDeposit, might be something else than a staking route (e.g. LM-Route in future).

--- PR #289 | src/payment/models/crypto-route/crypto-route.entity.ts ---
Context: +@Index('assetDepositUser', (cryptoRoute: CryptoRoute) => [cryptoRoute.asset, cryptoRoute.deposit, cryptoRoute.user], { | +  unique: true, | +})
David: Index should be on asset, targetDeposit and user. But will probably not work, because the user should be able to create same route for different chains.

--- PR #289 | src/payment/models/crypto-route/dto/create-crypto-route.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsObject() | +  staking: StakingDto;
David: ```ts
  @IsNotEmptyObject()
  @ValidateNested()
  @Type(() => EntityDto)
```

--- PR #289 | src/payment/models/deposit/deposit.entity.ts ---
Context: +export enum Blockchain { | +  DEFICHAIN = 'DeFiChain', | +  Bitcoin = 'Bitcoin',
David: BITCOIN = 'Bitcoin'

--- PR #289 | src/payment/models/deposit/deposit.service.ts ---
Context:   | -  async getNextDeposit(): Promise<Deposit> { | +  async getNextDeposit(blockchain: Blockchain = Blockchain.DEFICHAIN): Promise<Deposit> {
David: I would remove the default value.

--- PR #289 | src/user/models/user/user.entity.ts ---
Context:   | +  @OneToMany(() => CryptoRoute, (crypto) => crypto.user) | +  cryptos: CryptoRoute[];
David: cryptoRoutes

--- PR #289 | src/user/models/user/user.service.ts ---
Context: +    if (cryptoFee != null) return baseFee; | + | +    return baseFee;
David: Da kam wohl was durcheinander 😄 

```ts
    const user = await this.userRepo.findOne({
      select: ['id', 'cryptoFee'],
      where: { id: userId },
    });

    return Util.round((user?.cryptoFee ?? Config.crypto.fee) * 100, Config.defaultPercentageDecimal);
```

--- PR #289 | thunder-tests/thunderCollection.json ---
Context: -    "created": "2022-07-04T09:42:04.775Z", | -    "sortNum": 30000, | -    "folders": []
David: Missing sync

--- PR #289 | thunder-tests/thunderclient.json ---
Context: +      "type": "json", | +      "raw": "{\n    \"active\": false\n}", | +      "form": []
David: Missing sync

--- PR #289 | migration/1657179767570-CryptoRoute.js ---
Context: +        await queryRunner.query(`DROP TABLE "crypto_route"`); | +    } | +}
David: Redo after entity changes

--- PR #289 | src/payment/models/crypto-route/crypto-route.service.ts ---
Context: +  async count(): Promise<number> { | +    return this.cryptoRepo.count(); | +  }
David: Should be added to statistics?

--- PR #289 | src/payment/models/crypto-route/crypto-route.service.ts ---
Context: +        ...(dto.buyType === BuyType.WALLET ? { asset: asset, staking: IsNull() } : { staking: staking?.deposit }), | +        user: { id: userId }, | +      },
David: Should be possible to create a route per:
- User and
- Chain and
- Asset or Staking (target deposit)

--- PR #289 | src/payment/models/crypto-route/crypto-route.entity.ts ---
Context: +import { Deposit } from '../deposit/deposit.entity'; | + | +@Entity()
David: @ChildEntity()

--- PR #289 | src/payment/models/crypto-route/crypto-route.service.ts ---
Context: +    crypto.user = { id: userId } as User; | +    crypto.asset = asset; | +    crypto.type = RouteType.CRYPTO;
David: type is automatically set by TypeORM.

--- PR #289 | src/payment/models/crypto-route/crypto-route.service.ts ---
Context: +        user: { id: userId }, | +        blockchain: dto.blockchain, | +      },
David: IMO it should be (please verify)
```ts
      where: {
        ...(dto.buyType === BuyType.WALLET ? { asset: asset, targetDeposit: IsNull() } : { targetDeposit: targetDeposit?.deposit }),
        user: { id: userId },
        blockchain: dto.blockchain,
      },
```

--- PR #289 | src/payment/models/crypto-route/crypto-route.service.ts ---
Context: + | +    // check staking | +    const targetDeposit =
David: Wrong rename (it's a staking)

--- PR #289 | src/payment/models/crypto-route/dto/create-crypto-route.dto.ts ---
Context: +  @Type(() => EntityDto) | +  staking: StakingDto; | +}
David: Sorry, perhaps my comment wasn't clear: 
- @ApiProperty() should stay
- @IsNotEmptyObject(), @ValidateNested(), @Type(() => EntityDto) only for objects, not for enums

=> for the enum properties, all was correct 😉

--- PR #291 | src/ain/node/node-client.ts ---
Context: +    ); | +  } | +
David: Move to BtcClient (merge PR #290 first)

--- PR #291 | src/ain/node/node-client.ts ---
Context: +            'unset', | +            null, | +            { inputs: [{ txid: txId, vout: vout }], replaceable: true },
David: Specify feeRate (see [DEV-369](https://dfxswiss.atlassian.net/browse/DEV-369))

--- PR #291 | src/payment/models/crypto-input/crypto-input.entity.ts ---
Context:  @Entity() | -@Index('txAssetRoute', (input: CryptoInput) => [input.inTxId, input.asset, input.route], { unique: true }) | +@Index('txAssetRoute', (input: CryptoInput) => [input.inTxId, input.asset, input.route, input.amount], { unique: true })
David: Index should be ok without amount, shouldn't it?

--- PR #291 | src/payment/models/crypto-input/crypto-input.entity.ts ---
Context: -@Index('txAssetRoute', (input: CryptoInput) => [input.inTxId, input.asset, input.route], { unique: true }) | +@Index('txAssetRoute', (input: CryptoInput) => [input.inTxId, input.asset, input.route, input.amount], { unique: true }) |  export class CryptoInput extends IEntity {
David: Not related to this feature, but I would set outTxId `nullable: true` and change all places where we filter for `outTxId: ''` with `outTxId: null`.

--- PR #291 | src/payment/models/crypto-input/bitcoin-input.service.ts ---
Context: + | +    return await this.cryptoInputRepo.save({ ...cryptoInput, ...dto }); | +  }
David: This is not BTC related logic. It's probably best to create a new defi-input.service from existing crypto-input.service and have this update method in the crypto-input, whereas DeFi inputs are handled by defi-input.

--- PR #291 | src/payment/models/crypto-input/bitcoin-input.service.ts ---
Context: +    const utxos = await this.client.getUtxo(); | + | +    const newInputs = await this.getAddressesWithFunds(utxos)
David: This line is useless.

--- PR #291 | src/payment/models/crypto-input/bitcoin-input.service.ts ---
Context: + | +    const newInputs = await this.getAddressesWithFunds(utxos) | +      // map to entities
David: Add a filter for new UTXOs.

--- PR #291 | src/payment/models/crypto-input/bitcoin-input.service.ts ---
Context: +      if (input?.route.type === RouteType.STAKING && input.amlCheck === AmlCheck.PASS) { | +        await this.cryptoStakingService.create(input); | +      }
David: This case is not possible, input.route.type **should** always be CRYPTO.

--- PR #291 | src/payment/models/crypto-input/bitcoin-input.service.ts ---
Context: +    const utxos = await this.client.getUtxo(); | + | +    inputs.length > 0 && console.log(`Forwarding inputs (${inputs.length})`);
David: Move above `const utxos = ...` and abort, if no inputs to forward.

--- PR #291 | src/payment/models/crypto-input/bitcoin-input.service.ts ---
Context: +          (i) => | +            i.address == input.route.deposit.address && i.txid == input.inTxId && i.amount.toNumber() == input.amount, | +        );
David: Use `find()` instead of `filter()` and throw an error in case there is matching UTXO found.

--- PR #291 | src/payment/models/crypto-input/crypto-input.entity.ts ---
Context:  @Entity() | -@Index('txAssetRoute', (input: CryptoInput) => [input.inTxId, input.asset, input.route], { unique: true }) | +@Index('txAssetRoute', (input: CryptoInput) => [input.inTxId, input.asset, input.route, input.amount], { unique: true })
David: Hm. It is actually possible to send two inputs with the same asset in a single TX to the same address 🤔  I think we need to add the vout to the table. Index will then be on TX vout, asset and route.

--- PR #291 | src/payment/models/crypto-input/bitcoin-input.service.ts ---
Context: +          (i) => | +            i.address == input.route.deposit.address && i.txid == input.inTxId && i.amount.toNumber() == input.amount, | +        );
David: We will add the vout to the table, therefore this isn't required anymore (I think).

--- PR #291 | src/payment/models/crypto-input/bitcoin-input.service.ts ---
Context: +    const utxos = await this.client.getUtxo(); | + | +    inputs.length > 0 && console.log(`Forwarding inputs (${inputs.length})`);
David: This comment is outdated, if we add vout to the table.

--- PR #291 | src/payment/models/crypto-input/bitcoin-input.service.ts ---
Context: + | +    return await this.cryptoInputRepo.save({ ...cryptoInput, ...dto }); | +  }
David: Shared logic could stay in the crypto-input.service.

--- PR #291 | src/payment/models/crypto-input/bitcoin-input.service.ts ---
Context: +    return ( | +      (await this.cryptoRouteService.getCryptoByAddress(address)) ?? | +      (await this.stakingService.getStakingByAddress(address))
David: Staking is not possible here.

--- PR #291 | src/ain/node/node-client.ts ---
Context: +    return 0.000083; | +  } | +
David: Move this to BtcClient (merge PR #293 first).

--- PR #291 | src/ain/node/node-client.ts ---
Context: +    return 0.000083; | +  } | +
David: Can we calculate the exact fee based on our feeRate?

--- PR #291 | src/payment/models/crypto-input/crypto-input.entity.ts ---
Context:  @Entity() | -@Index('txAssetRoute', (input: CryptoInput) => [input.inTxId, input.asset, input.route], { unique: true }) | +@Index('txAssetRoute', (input: CryptoInput) => [input.inTxId, input.asset, input.route, input.amount], { unique: true })
David: Sorry, forgot the comma: TX, vout, asset and route.

--- PR #291 | src/payment/models/crypto-input/crypto-input.entity.ts ---
Context:  @Entity() | -@Index('txAssetRoute', (input: CryptoInput) => [input.inTxId, input.asset, input.route], { unique: true }) | +@Index('txAssetRoute', (input: CryptoInput) => [input.inTxId, input.asset, input.route, input.vout], { unique: true })
David: Update index name.

--- PR #291 | src/payment/models/crypto-input/btc-input.service.ts ---
Context: +    return this.cryptoInputRepo.create({ | +      inTxId: utxo.txid, | +      outTxId: null, // will be set after crypto forward
David: Can be removed

--- PR #291 | src/payment/models/crypto-input/btc-input.service.ts ---
Context: +    const inputs = await this.cryptoInputRepo.find({ | +      where: { | +        outTxId: null,
David: Use `IsNull()`

--- PR #291 | src/payment/models/crypto-input/defi-input.service.ts ---
Context: +    return this.cryptoInputRepo.create({ | +      inTxId: history.txid, | +      outTxId: null, // will be set after crypto forward
David: Can be removed

--- PR #291 | src/payment/models/crypto-input/defi-input.service.ts ---
Context: +    const inputs = await this.cryptoInputRepo.find({ | +      where: { | +        outTxId: null,
David: Use `IsNull()`

--- PR #291 | src/payment/models/crypto-input/btc-input.service.ts ---
Context: +    newInputs.length > 0 && console.log(`New crypto inputs (${newInputs.length}):`, newInputs); | + | +    // side effect, assuming that cryptoStakingRepo and stakingRepo are faultless on save
David: Comment can be removed.

--- PR #291 | src/payment/models/crypto-input/btc-input.service.ts ---
Context: + | +    // get deposit route | +    const route = await this.getDepositRoute(utxo.address);
David: This does not work. Need to get the CryptoRoute.

--- PR #291 | src/payment/models/crypto-input/crypto-input.service.ts ---
Context:        (await this.sellService.getSellByAddress(address)) ?? (await this.stakingService.getStakingByAddress(address)) |      ); |    }
David: Move to defi-input.service

--- PR #291 | src/payment/models/crypto-input/crypto-input.service.ts ---
Context: -  private async checkNodeInSync(): Promise<{ headers: number; blocks: number }> { | -    const { blocks, headers } = await this.client.getInfo(); | +  async checkNodeInSync(client: NodeClient): Promise<{ headers: number; blocks: number }> {
David: Mark as protected

--- PR #291 | src/payment/models/crypto-input/btc-input.service.ts ---
Context: +    }); | + | +    if (inputs.length == 0) return;
David: Re-add the log

--- PR #291 | src/payment/models/crypto-input/btc-input.service.ts ---
Context: +        select: ['id', 'outTxId', 'isConfirmed'], | +        where: { isConfirmed: false, outTxId: Not(IsNull()) }, | +      });
David: Only verify BTC inputs

--- PR #291 | src/payment/models/crypto-input/defi-input.service.ts ---
Context: +        select: ['id', 'outTxId', 'isConfirmed'], | +        where: { isConfirmed: false, outTxId: Not(IsNull()) }, | +      });
David: Only verify DeFi inputs

--- PR #291 | src/payment/models/crypto-input/crypto-input.service.ts ---
Context: +    readonly cryptoInputRepo: CryptoInputRepository, |      private readonly sellService: SellService, |      private readonly stakingService: StakingService,
David: Remove dependency

--- PR #291 | src/ain/node/btc-client.ts ---
Context:            [ | -            [{ [addressTo]: this.roundAmount(amount - this.btcUtxoFee) }], | +            [{ [addressTo]: this.roundAmount(amount - btcFee) }],
David: Need to multiply btcFee with vByte count. Use e.g. https://bitcoinops.org/en/tools/calc-size/ (single input/output)

--- PR #291 | src/payment/models/crypto-input/crypto-input.service.ts ---
Context: +    return this.http.get<T>(url, { tryCount: 3 }); | +  } | +
David: I would remove this and use http directly in btc-input.service.

--- PR #291 | src/payment/models/crypto-input/btc-input.service.ts ---
Context: +      this.btcFeeUrl, | +    ); | +    const btcFee = fastestFee < Config.crypto.fee * input.amount ? fastestFee : Config.crypto.fee * input.amount;
David: I think this is not related to `Config.crypto.fee`, which is what the user pays. The factor here should be `0.0005% = 0.000005`, the user fee will be much higher.

Code improvement: `const btcFee = Math.min(fastestFee, 0.000005 * input.amount);`

--- PR #291 | src/payment/models/crypto-input/btc-input.service.ts ---
Context: +      this.btcFeeUrl, | +    ); | +    const btcFee = fastestFee < Config.crypto.fee * input.amount ? fastestFee : Config.crypto.fee * input.amount;
David: Move the logic to separate method `getFeeRate()` or similar.

--- PR #291 | src/ain/node/btc-client.ts ---
Context:   | -  async send(addressTo: string, txId: string, amount: number, vout: number): Promise<string> { | +  async send(addressTo: string, txId: string, amount: number, vout: number, btcFee: number): Promise<string> {
David: Rename parameter to `feeRate`

--- PR #291 | src/config/config.ts ---
Context:      outWalletAddress: process.env.OUT_WALLET_ADDRESS, |      stakingWalletAddress: process.env.STAKING_WALLET_ADDRESS, | +    bitcoinWalletAddress: process.env.BITCOIN_WALLET_ADDRESS,
David: Do we need this address?

--- PR #291 | src/payment/models/crypto-input/btc-input.service.ts ---
Context: +    for (const input of inputs) { | +      try { | +        await this.forwardUtxo(input, Config.node.bitcoinWalletAddress);
David: Use DFX's Cake BTC address.

--- PR #309 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context:   |      // activate user |      if (entity.amlCheck === AmlCheck.PASS && entity.buy?.user?.status === UserStatus.NA) {
David: Würde den Check `user?.status === UserStatus.NA` auch in das `activateUser()` verschieben.

--- PR #309 | src/payment/models/buy-fiat/buy-fiat.service.ts ---
Context: + | +    // activate user | +    if (entity.amlCheck === AmlCheck.PASS && entity.sell?.user?.status === UserStatus.NA) {
David: Würde den Check `user?.status === UserStatus.NA` auch in das `activateUser()` verschieben.

--- PR #309 | src/payment/models/buy-fiat/buy-fiat.service.ts ---
Context: +  ): Promise<BuyFiat[]> { | +    return await this.buyFiatRepo.find({ | +      where: { buy: { user: { id: userId } }, outputDate: Between(dateFrom, dateTo) },
David: BuyFiat hat kein Feld `buy`.

--- PR #309 | src/payment/models/buy-fiat/buy-fiat.service.ts ---
Context: +  async getAllUserTransactions(userIds: number[]): Promise<BuyFiat[]> { | +    return await this.buyFiatRepo.find({ | +      where: { buy: { user: { id: In(userIds) } } },
David: BuyFiat hat kein Feld `buy`.

--- PR #309 | src/payment/models/buy-fiat/buy-fiat.service.ts ---
Context: +  async getLastOutputDate(): Promise<Date> { | +    return await this.buyFiatRepo.findOne({ order: { outputDate: 'DESC' } }).then((b) => b.outputDate); | +  }
David: Can be moved to payment.observer.ts

--- PR #309 | src/payment/models/buy-fiat/buy-fiat.entity.ts ---
Context: + | +  @Column({ nullable: true }) | +  instantSEPA: boolean;
David: Wir haben die Variablen normalerweise mit `Sepa` und nicht `SEPA` benannt, wäre jetzt ein Bruch 🤔

--- PR #323 | src/payment/models/crypto-input/btc-input.service.ts ---
Context:      ); | -    return Math.max(Math.min(fastestFee, 500 * amount), 1); | +    return Util.round(Math.max(Math.min(fastestFee, 500 * amount), 1), 0);
David: Better use `Math.floor()` to round off.

--- PR #328 | src/payment/models/bank-tx/bank-tx.service.ts ---
Context: +      throw new ConflictException(`There is already a bank tx with the accountServiceRef: ${bankTx.accountServiceRef}`); | + | +    entity = await this.bankTxRepo.create(bankTx);
David: `await` is not required

--- PR #328 | src/payment/payment.module.ts ---
Context:        BankAccountRepository, |        CryptoRouteRepository, | +      SettingRepository,
David: Can be removed

--- PR #328 | src/payment/payment.module.ts ---
Context:      CryptoRouteController, |      CryptoRouteService, | +    SettingService,
David: Can be removed

--- PR #328 | src/config/config.ts ---
Context:   | +  bank = { | +    olkyPay: {
David: Name of the bank is Olkypay, not OlkyPay 😁  (everywhere)

--- PR #328 | src/payment/models/bank-tx/olky-pay.service.ts ---
Context: +    const qs = require('qs'); | + | +    const data = qs.stringify({
David: Add an import `import { stringify } from 'qs';` at the top of the file, use it like this `const data = stringify({...});` and remove the eslint comment.

--- PR #328 | src/payment/models/bank-tx/olky-pay.service.ts ---
Context: +    }); | + | +    const key = await this.http.request<any>({
David: Add return type

--- PR #328 | src/payment/models/bank-tx/olky-pay.service.ts ---
Context: +      if (getNewAccessToken) this.accessToken = await this.getAccessToken(); | +      return await this.http.request<T>({ | +        url: `${this.baseUrl}${url}`,
David: Remove the trailing `/` at the baseUrl and add it here in between ... makes it more readable

--- PR #328 | src/payment/models/bank-tx/olky-pay.service.ts ---
Context: +    } catch (e) { | +      if (nthTry > 1) { | +        return this.request(url, method, data, nthTry - 1, true);
David: Could we only get a new access token, if error was `Unauthorized` (similar to spider-api.service)?

--- PR #328 | src/payment/models/bank-tx/olky-pay.service.ts ---
Context: +      throw new ServiceUnavailableException(`Failed to get Balance:`, error); | +    } | +  }
David: I think both try-catches are not required?

--- PR #328 | src/payment/models/bank-tx/olky-pay.service.ts ---
Context: +  } | + | +  async getTransactions(fromDate: string): Promise<Transaction[]> {
David: Have `from` and `to` parameter (with type Date) with `to` defaulting to current date.

--- PR #328 | src/payment/models/bank-tx/olky-pay.service.ts ---
Context: +    const settingKey = 'lastOlkyPayDate'; | +    const lastModificationTime = await this.settingService.get(settingKey); | +    const newModificationTime = new Date().toISOString().split('T')[0];
David: Move this logic (`.toISOString().split('T')[0]`) to Util (reuse it also in conversion.service).

--- PR #328 | src/payment/models/bank-tx/olky-pay.service.ts ---
Context: +  // --- TRANSACTION HANDLING --- // | +  @Interval(60000) | +  async checkInputs(): Promise<void> {
David: Add an overall try-catch

--- PR #328 | src/payment/models/bank-tx/olky-pay.service.ts ---
Context: +    const newModificationTime = new Date().toISOString().split('T')[0]; | + | +    const allTransaction = await this.getTransactions(lastModificationTime);
David: Typo: transaction**s**

--- PR #328 | src/payment/models/bank-tx/olky-pay.service.ts ---
Context: +        console.error(`Failed to import Transaction:`, e); | +      } | +    }
David: It will spam our log with errors, if we don't check if the transaction already exists in DB

--- PR #328 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +  id_token: string; | +  session_state: string; | +}
David: `export`s can be removed, if they are not used outside this file

--- PR #328 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +  private readonly baseUrl = 'https://ws.olkypay.com/reporting'; | +  private readonly loginUrl = 'https://stp.olkypay.com/auth/realms/b2b/protocol/openid-connect/token'; | +  private tokenAuth = { access_token: 'access-token-will-be-updated' } as Partial<TokenAuth>;
David: I would only save the accessToken and not the complete TokenAuth. Or do you see an advantage in the latter?

--- PR #328 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +          await this.bankTxService.create(bankTx); | +        } catch (e) { | +          if (e.status != 409) console.error(`Failed to import Transaction:`, e);
David: Use `if (e instanceof ConflictException) ...`

--- PR #328 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +          await this.bankTxService.create(bankTx); | +        } catch (e) { | +          if (e.status != 409) console.error(`Failed to import Transaction:`, e);
David: Typo: Transaction should be lowercase

--- PR #328 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +      await this.settingService.set(settingKey, newModificationTime); | +    } catch (e) { | +      console.error(`Failed to check olkypay transaction:`, e);
David: Typo: transactions

--- PR #328 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +    try { | +      const settingKey = 'lastOlkypayDate'; | +      const lastModificationTime = await this.settingService.get(settingKey);
David: Use a default value, if setting is null

--- PR #328 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +  @Interval(60000) | +  async checkTransactions(): Promise<void> { | +    try {
David: Abort check, if environment is not set (to avoid local error logs)

--- PR #328 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +  async checkTransactions(): Promise<void> { | +    try { | +      if (!process.env.OLKY_CLIENT) return;
David: Use `Config.bank.olkypay.clientId`

--- PR #328 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +      const settingKey = 'lastOlkypayDate'; | +      const lastModificationTime = await this.settingService.get(settingKey); | +      const fromDate = lastModificationTime ? lastModificationTime : new Date(0).toISOString();
David: `get` method has a default value param:
```ts
const lastModificationTime = await this.settingService.get(settingKey, new Date(0).toISOString());
```

--- PR #328 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +      remittanceInfo: tx.line2, | +      type: tx.codeInterbancaireInterne === TransactionType.BILLING ? BankTxType.INTERNAL : null, | +      batch: { id: 1 } as BankTxBatch, // TODO create an olkypay batch and replace the ID
David: Create BankTxBatch and search for it by IBAN. Do the search only once, not per transaction.

--- PR #330 | src/ain/node/defi-client.ts ---
Context: +  async addPoolLiquidity(addressFrom: string, addressTo: string, assetsPair: [string, string]): Promise<string> { | +    return this.callNode((c) => c.poolpair.addPoolLiquidity({ [addressFrom]: assetsPair }, addressTo), true); | +  }
David: One address parameter would be enough IMO.

--- PR #330 | src/payment/models/buy-crypto/services/buy-crypto-out.service.ts ---
Context: -      this.buyCryptoBatchRepo.save(batch); | + | +      await this.buyCryptoBatchRepo.save(batch);
David: Interesting catch 🤔

--- PR #330 | src/payment/models/buy-crypto/services/buy-crypto-out.service.ts ---
Context:   | +    await this.dexService.completeOrders(LiquidityOrderContext.BUY_CRYPTO, batch.id.toString()); | +
David: It's IMO a bit confusing to have the `completeOrder` command in the `validateOutBalance` method.

--- PR #330 | src/payment/models/dex/entities/liquidity-order.entity.ts ---
Context: + | +  reserved(targetAmount: number): this { | +    this.targetAmount = this.referenceAsset === this.targetAsset.dexName ? this.referenceAmount : targetAmount;
David: Duplication of logic (line 66 and 82)

--- PR #330 | src/payment/models/dex/factories/liquidity-order.factory.ts ---
Context: +  // *** PUBLIC API *** // | + | +  createPurchaseOrder(request: LiquidityRequest, chain: string, purchaseStrategy: AssetCategory): LiquidityOrder {
David: `purchaseStrategy: AssetCategory` looks a bit weird

--- PR #330 | src/payment/models/dex/factories/liquidity-order.factory.ts ---
Context: +  // *** PUBLIC API *** // | + | +  createPurchaseOrder(request: LiquidityRequest, chain: string, purchaseStrategy: AssetCategory): LiquidityOrder {
David: It's better 👍 😉

--- PR #330 | src/payment/models/buy-crypto/services/buy-crypto-out.service.ts ---
Context:   | +    await this.dexService.completeOrders(LiquidityOrderContext.BUY_CRYPTO, batch.id.toString()); | +
David: Shouldn't it be moved to buy-crypto-out.service.ts line 85? Order can be completed as soon as the tokens arrived on out node.

--- PR #330 | src/payment/models/dex/services/dex.service.ts ---
Context: + | +@Injectable() | +export class DEXService {
David: Rename to `DexService`

--- PR #330 | src/payment/models/buy-crypto/services/buy-crypto-dex.service.ts ---
Context: -    await this.processNewBatches(newBatches, securedBatches, pendingBatches); | +    await this.processNewBatches(newBatches); |    }
David: I would remove this method completely, would make the code more readable.

--- PR #330 | src/payment/models/buy-crypto/services/buy-crypto-dex.service.ts ---
Context:   | -      return availableAmount >= requiredAmount ? requiredAmount : 0; | +      return (await this.dexService.reserveLiquidity(request)) || 0;
David: Why this `|| 0`? Can the return value of `reserveLiquidity` be undefined? If yes, I would update the return type of `reserveLiquidity` to `number | undefined` and rather use `?? 0`. What do you mean?

--- PR #330 | src/payment/models/buy-crypto/services/buy-crypto-dex.service.ts ---
Context:   | -      return availableAmount >= requiredAmount ? requiredAmount : 0; | +      return (await this.dexService.reserveLiquidity(request)) || 0;
David: I saw that you always use `||` and not `??`. Do you prefer it?

--- PR #330 | src/payment/models/dex/services/dex.service.ts ---
Context: +      return strategy.checkLiquidity(request); | +    } catch (e) { | +      console.error(e);
David: I would remove this log to have the same behaviour as for `reserveLiquidity`.

--- PR #330 | src/payment/models/dex/services/dex.service.ts ---
Context: + | +      if (liquidity !== 0) { | +        const order = this.liquidityOrderFactory.createReservationOrder(request, 'defichain');
David: Use 'DeFiChain' (see enum in src/payment/models/deposit/deposit.entity.ts)

--- PR #330 | src/payment/models/dex/services/dex.service.ts ---
Context: +        order.reserved(liquidity); | + | +        await this.liquidityOrderRepo.save(order);
David: Do we need to think about race conditions in this place?

--- PR #330 | src/payment/models/dex/services/dex.service.ts ---
Context: +      await strategy.purchaseLiquidity(request); | +    } catch (e) { | +      console.error(e);
David: Does this create double logs?

--- PR #330 | src/payment/models/dex/services/dex.service.ts ---
Context: +    if (incompleteOrders.length === 0) { | +      return; | +    }
David: Useless code?

--- PR #330 | src/payment/models/dex/services/dex.service.ts ---
Context: +@Injectable() | +export class DEXService { | +  private readonly verifyPurchaseOrdersLock = new Lock(1800);
David: Could probably decrease the timeout interval here. 30 minutes is IMO quite long 🤔

--- PR #330 | src/payment/models/dex/services/dex.service.ts ---
Context: +  } | + | +  @Interval(60000)
David: Should we decrease this interval to improve transaction time?

--- PR #330 | src/payment/models/dex/services/liquidity.service.ts ---
Context: + | +    // 5% cap for unexpected meantime swaps | +    if (amount * 1.05 > availableAmount - pendingAmount) {
David: This cap is new 🤔

--- PR #330 | src/payment/models/dex/services/liquidity.service.ts ---
Context: +  private async calculateSourceAssetPrice(sourceAsset: string, targetAsset: string): Promise<number> { | +    return (await this.#dexClient.testCompositeSwap(sourceAsset, targetAsset, 0.001)) / 0.001; | +  }
David: Do we need both methods? Can't we just swap parameters?

--- PR #330 | src/payment/models/dex/services/liquidity.service.ts ---
Context: +  private isAssetNotAvailableError(e: Error): boolean { | +    return e.message && e.message.includes('Cannot find usable pool pair'); | +  }
David: TODO

--- PR #330 | src/payment/models/dex/strategies/purchase-liquidity/purchase-crypto-liquidity.strategy.ts ---
Context: +  } | + | +  private async getSwapAsset(
David: Somewhat misleading method name

--- PR #330 | src/payment/models/dex/services/liquidity.service.ts ---
Context: +  ): Promise<number> { | +    if (referenceAsset === targetAsset) { | +      const swapAssetPrice = await this.calculateTargetAssetPrice(referenceAsset, swapAsset);
David: The method name `calculateTargetAssetPrice` is confusing in this context.

--- PR #330 | src/payment/models/dex/services/liquidity.service.ts ---
Context: +    } | + | +    return this.#dexClient.testCompositeSwap(referenceAsset, swapAsset, referenceAmount);
David: This line is only correct, if swap asset is part of the swap path between reference asset and target asset. It IMO doesn't make sense to implement the other case right now ... but we should keep it in mind.

--- PR #330 | src/payment/models/dex/services/liquidity.service.ts ---
Context: +  // *** PUBLIC API *** // | + | +  async getAvailableTargetLiquidity(
David: Rename to `getAndCheckAvailableTargetLiquidity` or similar?

--- PR #330 | src/payment/models/dex/strategies/purchase-liquidity/purchase-poolpair-liquidity.strategy.ts ---
Context: +    const poolPair: [string, string] = [`${leftAmount}@${leftAsset}`, `${rightAmount}@${rightAsset}`]; | + | +    const chainSwapId = await this.chainClient.addPoolLiquidity(Config.node.dexWalletAddress, poolPair);
David: It's actually not a swap 😉

--- PR #330 | src/payment/models/dex/strategies/purchase-liquidity/purchase-stock-liquidity.strategy.ts ---
Context: + | +@Injectable() | +export class PurchaseStockLiquidityStrategy extends PurchaseLiquidityStrategy {
David: I see a lot of code duplicated in `PurchaseStockLiquidityStrategy` and `PurchaseCryptoLiquidityStrategy` (everything except getting swap asset/amount). Did you think about merging them? It could be a could idea, especially if we might at some time also have multiple swap assets for crypto tokens.

--- PR #330 | src/payment/models/dex/services/dex.service.ts ---
Context: + | +      if (liquidity !== 0) { | +        const order = this.liquidityOrderFactory.createReservationOrder(request, 'defichain');
David: Yes, totally makes sense!

--- PR #330 | src/payment/models/dex/services/dex.service.ts ---
Context: +      return strategy.checkLiquidity(request); | +    } catch (e) { | +      console.error(e);
David: Ok 👍

--- PR #330 | src/payment/models/buy-crypto/services/buy-crypto-dex.service.ts ---
Context:   | -      return availableAmount >= requiredAmount ? requiredAmount : 0; | +      return (await this.dexService.reserveLiquidity(request)) || 0;
David: Ok 😁

--- PR #333 | src/user/models/kyc/dto/kyc-user-data.dto.ts ---
Context: +  @ApiProperty() | +  @IsNotEmpty() | +  @IsString()
David: Use `@IsEmail()`

--- PR #333 | src/user/models/kyc/kyc.controller.ts ---
Context: +  async updateKycDataByCode(@Param('code') code: string, @Body() data: KycUserDataDto): Promise<KycInfo> { | +    return await this.kycService.updateKycData(code, data) | +  }
David: Should we use `:code/data` endpoint? Is a bit confusing to have same endpoint (PUT/POST) for two completely different things.

--- PR #333 | src/user/models/kyc/kyc.service.ts ---
Context: +    const user = userId | +      ? await this.getUserById(userId) | +      : await this.getUserByKYCCode(code)
David: Move this logic to a method?

--- PR #333 | src/user/models/kyc/kyc.service.ts ---
Context: +  } | + | +  private async getUserByKYCCode(code: string): Promise<UserData> {
David: We use camel case even for abbreviations (e.g. KYC) 😉

--- PR #333 | src/user/models/kyc/kyc.service.ts ---
Context: +    await this.userDataService.updateSpiderIfNeeded(user, data) | + | +    const updatedUser = await this.userDataRepo.save({ ...user, ...data });
David: Could it make sense to merge this logic with `UserDataService.updateUserData`?

--- PR #333 | src/user/models/kyc/kyc.service.ts ---
Context: +    await this.userDataService.updateSpiderIfNeeded(user, data) | + | +    const updatedUser = await this.userDataRepo.save({ ...user, ...data });
David: Just saw, that `updateSpiderIfNeeded` is missing in `UserDataService.updateUserData` (was missing before). Would add it there as well.

--- PR #333 | src/user/models/kyc/dto/kyc-user-data.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsString() | +  phone: string;
David: How does this work with blanked mail and phone?

--- PR #333 | src/user/models/user-data/user-data.controller.ts ---
Context: @@ -79,7 +79,8 @@ export class UserDataController { |      const user = userData.users[0]; |      if (!user) throw new BadRequestException('User not found');
David: These two lines can be removed

--- PR #333 | src/user/models/user-data/user-data.entity.ts ---
Context: +      const [name, domain] = value.split('@') | +      // -1 on name.length .join is not needed as the join will produce one less character | +      return `${name[0]}${new Array(name.length).join('*')}@${domain}`
David: Perhaps write a util method to create a string of predefined length filled with a given character? Would make it more readable 😉

--- PR #333 | thunder-tests/kyc-incorp-cert/test.json ---
Context: +{ | +    "test": "incorporation certificate" | +}
David: What is this for?

--- PR #333 | thunder-tests/thunderEnvironment.json ---
Context: +      { | +        "name": "userDataId", | +        "value": "ChangeMe"
David: Do these really belong to the environment?

--- PR #333 | thunder-tests/thunderclient.json ---
Context: +      "type": "bearer", | +      "bearer": "{{token}}" | +    },
David: Can be removed (for every call). Just use inherited auth.

--- PR #333 | src/user/models/kyc/kyc.service.spec.ts ---
Context: +} | + | +describe('KycService', () => {
David: Didn't have a close look at the tests yet. There is already some UserData mock logic (src/user/models/user-data/__tests__/mock/user-data.entity.mock.ts). Would it be possible to reuse it?

--- PR #333 | src/user/models/kyc/kyc.service.ts ---
Context: +    await this.userDataService.updateSpiderIfNeeded(user, data) | + | +    const updatedUser = await this.userDataRepo.save({ ...user, ...data });
David: Oh yeah, you're right.

--- PR #333 | src/user/models/kyc/dto/kyc-user-data.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsString() | +  phone: string;
David: Let's say somebody enters the KYC Data on payment, but had mail and phone already saved. You will then call the /data endpoint and mail and phone are required, but you only have the blanked ones.

--- PR #333 | thunder-tests/kyc-incorp-cert/test.json ---
Context: +{ | +    "test": "incorporation certificate" | +}
David: Ok nice 🤣

--- PR #333 | thunder-tests/thunderEnvironment.json ---
Context: +      { | +        "name": "userDataId", | +        "value": "ChangeMe"
David: I actually don't know 🤔  sounded weird to have a user ID in the environment ... but there's probably no better place.

--- PR #333 | thunder-tests/thunderclient.json ---
Context: +      "type": "bearer", | +      "bearer": "{{token}}" | +    },
David: Well, it is working for all other requests 😁 try reloading Thunder or restarting VS Code. Might be some caching issue.

--- PR #333 | src/user/models/user-data/user-data.entity.ts ---
Context: + | +export function Blank(value: string, type: BlankType): string { | +  if (!value || value.length === 0) return;
David: `return value;`

--- PR #334 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +      txAmount: amount, | +      chargeAmount: 0, | +      chargeCurrency: currency,
David: Move currency below amount

--- PR #334 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: +      txAmount: amount, | +      chargeAmount: 0, | +      chargeCurrency: currency,
David: Add `txCurrency`

--- PR #336 | src/monitoring/monitoring.module.ts ---
Context:      PaymentModule, |      UserModule, | +    BankTxBatchRepository,
David: Remove

--- PR #336 | src/monitoring/monitoring.module.ts ---
Context: +    OlkypayObserver, | +    OlkypayService, | +    BankTxBatchRepository,
David: Remove `OlkypayService` and `BankTxBatchRepository`. Export `OlkypayService` in `PaymentModule`.

--- PR #336 | src/monitoring/observers/olkypay.observer.ts ---
Context: +export class OlkypayObserver extends MetricObserver<OlkypayData> { | +  constructor(monitoringService: MonitoringService, private readonly olkypayService: OlkypayService) { | +    super(monitoringService, 'olkypay', 'combined');
David: Change metric name to `balance` instead of `combined`?

--- PR #336 | src/monitoring/observers/olkypay.observer.ts ---
Context: +  } | + | +  @Interval(10000)
David: Interval is a bit small 🤣 would use 15 minutes.

--- PR #336 | src/monitoring/observers/olkypay.observer.ts ---
Context: +  private amount(indicator: BankTxIndicator, amount: number): number { | +    return indicator == BankTxIndicator.CREDIT ? amount : -amount; | +  }
David: Use plain SQL to get the DB balance. Something like this:
```sql
SELECT SUM(CASE WHEN bt.creditDebitIndicator = 'DBIT' THEN bt.amount * -1 ELSE bt.amount END)
FROM dbo.bank_tx bt 
INNER JOIN dbo.bank_tx_batch btb 
ON bt.batchId = btb.id
WHERE btb.iban = 'LU116060002000005040'
```

--- PR #343 | src/payment/models/bank-tx/bank-tx.entity.ts ---
Context:    buyCrypto?: BuyCrypto; | + | +  @OneToOne(() => BuyFiat, (sell) => sell.bankTx, { nullable: true })
David: Replace `sell` by `buyFiat`

--- PR #343 | src/payment/models/history/history.service.ts ---
Context: +            (c.percentFeeAmount || (c.inputReferenceAmount && c.percentFee)) && c.inputReferenceAsset | +              ? this.getAssetSymbol(c.inputReferenceAsset) | +              : null,
David: Why so complicated? 🤣

--- PR #347 | src/config/config.ts ---
Context:    crypto = { |      fee: 0.012, | +    feeUsedRef: 0.011,
David: I would propose to switch to a ref bonus of 0.1% instead. So the fee, when using a ref code, would be `crypto.fee - crypto.refBonus`.
For the buy fee we use a similar scheme, where the referred can adapt the ref bonus.

--- PR #347 | src/user/models/user/user.service.spec.ts ---
Context: + | +    await expect(service.getUserCryptoFee(1)).resolves.toStrictEqual({ fee: 1.1, refBonus: 0.1 }); | +  });
David: Add a unit test for individual crypto fee?

--- PR #348 | src/shared/services/letter.service.ts ---
Context: +    const balance = await this.http | +      .post<BalanceResponse>(`${Config.letter.url}/getBalance`, { | +        auth: { username: Config.letter.userName, apikey: Config.letter.apiKey },
David: Extract auth config to separate method/variable?

--- PR #348 | src/shared/services/iban.service.ts ---
Context:    async getIbanInfos(iban: string): Promise<IbanDetailsDto> { | -    const url = `${this.baseUrl}/${iban}`; | +    const url = `${this.baseUrl}/validate_iban${iban}`;
David: Probably forgot the / here?

--- PR #348 | src/shared/services/letter.service.ts ---
Context: +        auth: { username: Config.letter.userName, apikey: Config.letter.apiKey }, | +      }) | +      .then((r) => r.balance.value);
David: `.then((r) => +r.balance.value);`and return it directly?

--- PR #348 | src/payment/models/bank-tx/olkypay.service.ts ---
Context: -    const url = `ecritures/${Config.bank.olkypay.clientId}/${Util.isoDate(fromDate)}/${Util.isoDate(toDate)}`; | +    const url = `ecritures/${Config.bank.olkypay.clientId}/${Util.isoDate(fromDate)}/${Util.isoDate( | +      Util.daysAfter(1, toDate),
David: I would move the daysAfter to the calling method, because it IMO disturbs the logic of this method (if we one day e.g. want to get all transactions of a period in the past).

--- PR #348 | src/monitoring/observers/olkypay.observer.ts ---
Context:   | -  @Interval(900000) | +  @Interval(10000)
David: Is this intended?

--- PR #348 | src/monitoring/observers/olkypay.observer.ts ---
Context:        .select( |          "SUM(CASE WHEN bankTx.creditDebitIndicator = 'DBIT' THEN bankTx.amount * -1 ELSE bankTx.amount END)", |          'balance',
David: Change to `dbBalance`?

--- PR #350 | src/shared/models/fiat/__tests__/mock/fiat.entity.mock.ts ---
Context: + | +export function createCustomFiat(customValues: Partial<Fiat>): Fiat { | +  return { ...new Fiat(), ...defaultFiat, ...customValues };
David: ```ts
const fiat = {...new Fiat()};
fiat instanceof Fiat // false
```
Perhaps better use `Object.assign(new Fiat(), { ...defaultFiat, ...customValues })` (for all mocks)?

--- PR #350 | src/payment/models/crypto-route/crypto-route.controller.ts ---
Context: +  async getCryptoRouteHistory(@GetJwt() jwt: JwtPayload, @Param('id') id: string): Promise<CryptoInputHistoryDto[]> { | +    return this.cryptoInputService.getHistory(jwt.id, +id); | +  }
David: I think we should not return CryptoInputHistory here, but also BuyCryptoHistory. CryptoInputs are just inputs. Depending on the route, either a BuyFiat, a BuyCrypto or a CryptoStaking is created. So for CryptoRoute, you'll also find the history in the BuyCrypto table.

--- PR #350 | src/payment/models/buy-crypto/dto/buy-crypto-history.dto.ts ---
Context: +  amlCheck: AmlCheck; | +  isComplete: boolean; | +}
David: All 3 history DTOs are the same. Should we use a single class/interface?

--- PR #350 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context: +      isComplete: buyCrypto.isComplete, | +    }; | +  }
David: I think you can use the `toHistoryDto` method for crypto routes as well, can't you?

--- PR #350 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context: +      isComplete: buyCrypto.isComplete, | +    }; | +  }
David: If yes, you could also remove the cryptoInput relation in getCryptoRouteHistory.

--- PR #350 | src/payment/models/buy-fiat/buy-fiat.service.ts ---
Context: +      outputAsset: buyFiat.outputAsset, | +      txId: buyFiat.cryptoInput.inTxId, | +      date: buyFiat.cryptoInput.created,
David: We should use buyFiat.outputDate here, to be consistent with the other history types.

--- PR #350 | src/payment/models/buy-crypto/dto/buy-crypto-history.dto.ts ---
Context: +  amlCheck: AmlCheck; | +  isComplete: boolean; | +}
David: I think it's a good thing to have very similar history entries for different routes. It might therefore be a good idea to have multiple DTOs, but use a common base class. WDYT?

--- PR #350 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context: +      isComplete: buyCrypto.isComplete, | +    }; | +  }
David: Oh I see. On the Dev system, BuyCryptos are not automatically filled. We therefore just entered some random information do to the test. That probably was a bad idea 🤔 

On production however you'll have BTC as an input asset.

--- PR #350 | src/payment/models/buy-fiat/buy-fiat.service.ts ---
Context: +      outputAsset: buyFiat.outputAsset, | +      txId: buyFiat.cryptoInput.inTxId, | +      date: buyFiat.cryptoInput.created,
David: Fair enough. IMO it should be similar for all routes, and for other routes, the outputDate was taken. Perhaps it would help, if we would specifically mark it as completion date? And add the "start" date for other history types as well?

--- PR #353 | src/config/config.ts ---
Context: +    ref: 3, | +    lm: 4, | +  };
David: I would place this as an enum next to api-key.service.

--- PR #353 | src/shared/services/api-key.service.ts ---
Context: +    let hash = Util.createHash(Util.createHash(address + new Date().toISOString(), 'sha256'), 'md5').toUpperCase(); | + | +    return hash.substring(0, hash.length - 5) + this.getFilterHash(queryFilter) + Config.apiKeyVersionCT;
David: We should try to store this magic numbers (in this case 5) into variables.

--- PR #353 | src/shared/services/api-key.service.ts ---
Context: +  } | + | +  private getFilterHash(queryFilter: HistoryFilter): string {
David: It's actually not a hash, more a code 😉

--- PR #353 | src/shared/services/api-key.service.ts ---
Context: +    filterHashSubString = '0000' + (filterHashSubString ? filterHashSubString.toString(16) : ''); | +    filterHashSubString = filterHashSubString.substring(filterHashSubString.length - 4, filterHashSubString.length); | +    return filterHashSubString;
David: ```ts
const filterCode = Util.sum(Object.keys(queryFilter).map((key) => Math.pow(2, Config.HistoryFilter[key])));
return filterCode.toString(16).padStart(4, '0');
```

--- PR #353 | src/shared/services/api-key.service.ts ---
Context: +    filterHashSubString = '0000' + (filterHashSubString ? filterHashSubString.toString(16) : ''); | +    filterHashSubString = filterHashSubString.substring(filterHashSubString.length - 4, filterHashSubString.length); | +    return filterHashSubString;
David: As we use a hex code here, 2 or 3 digits should probably be enough.

--- PR #353 | src/shared/services/api-key.service.ts ---
Context: + | +      return filter; | +    }
David: I would remove the filter parameter and just return the HistoryQuery resulting from the parsing. In the calling function you can use object destructuring to merge the queries (`const a = {...b, ...c};`)

--- PR #353 | src/shared/services/api-key.service.ts ---
Context: +              : filter[key] != null | +              ? filter[key] | +              : null),
David: ```ts
Object.entries(Config.HistoryFilter).filter(([key, value) => apiKeyFilter & Math.pow(2, value) > 0).reduce((prev, [key, value]) => {...prev, [key]: 'true', {});
```

--- PR #353 | src/user/models/user/user.service.ts ---
Context:      private readonly settingService: SettingService, |      private readonly dfiTaxService: DfiTaxService, | +    private readonly apiKeyCT: ApiKeyService,
David: Rename to apiKeyService?

--- PR #353 | src/shared/services/api-key.service.ts ---
Context: +  } | + | +  public checkKeySign(user: User, sign: string, timestamp: string): boolean {
David: Rename to `isValid` or similar? Would make the meaning of the result clear.

--- PR #353 | src/shared/services/api-key.service.ts ---
Context: +    const userSign = this.getSign(user, timestamp); | + | +    return sign.toUpperCase() == userSign || Util.daysDiff(new Date(timestamp), new Date()) <= 1;
David: Are you sure that this is right? 😉

--- PR #353 | src/shared/services/api-key.service.ts ---
Context:    private get suffixLength(): number { | -    return this.versionLength + this.filterLength; | +    return this.versionLength;
David: suffixLength can be removed completely and version can be back to '0'.

--- PR #353 | src/shared/services/api-key.service.ts ---
Context:   | -  public getFilter(apiKey: string): HistoryFilter { | +  public getFilter(apiKey: string, filterCode: string): HistoryFilter {
David: I think the filter is not depending on the (version of the) apiKey, is it?

--- PR #354 | src/payment/models/payout/services/payout.service.ts ---
Context: +      const order = this.payoutOrderFactory.createOrder(request, Blockchain.DEFICHAIN); | + | +      await this.payoutOrderRepo.save(order);
David: Did you think about directly initiating the transfer to speed up processing?

--- PR #354 | src/payment/models/payout/services/payout.service.ts ---
Context: +        const request = { asset, amount, destinationAddress }; | + | +        const transferTxId = await this.dexService.transferLiquidity(request);
David: I think there is a misunderstanding in terms of "cash flow". Liquidity for payout of staking rewards is not on DEX, but on INT wallet directly:
![image](https://user-images.githubusercontent.com/85513542/185620744-c9cc0438-364d-4668-a401-a81b92022d1a.png)
So for the staking reward payout, no transfer is required.

--- PR #354 | src/payment/models/payout/strategies/payout.strategy.ts ---
Context: +      // find nearest non-full group without repeating address | +      const suitableExistingGroups = [...result.entries()].filter( | +        // eslint-disable-next-line @typescript-eslint/no-unused-vars
David: This comment can be removed with newest changes from develop.

--- PR #354 | src/payment/models/payout/strategies/payout.strategy.ts ---
Context: + | +      uniqueAddresses.set(o.destinationAddress, increment); | +    });
David: Use `Util.aggregate`?

--- PR #354 | src/payment/models/payout/strategies/payout-token.strategy.ts ---
Context: + | +@Injectable() | +export class PayoutTokenStrategy extends PayoutStrategy {
David: Same question as last time 😉 I really like the strategy thinking ... but again there is a lot of duplicated code. E.g. the `sendToken` and `sendDFI` methods are practically the same. And it's not some trivial code but a very fundamental logic.
Is there a way to combine it? 😊

--- PR #354 | src/payment/models/payout/services/payout-defichain.service.ts ---
Context: + | +  async getUtxoForAddress(address: string): Promise<string> { | +    return this.whaleService.getClient().getBalance(address);
David: Convert to number?

--- PR #354 | src/payment/models/payout/services/payout-defichain.service.ts ---
Context: +  } | + | +  getWallet(context: PayoutOrderContext): string {
David: `getWalletAddress`?

--- PR #354 | src/payment/models/payout/strategies/payout.strategy.ts ---
Context: +    }); | + | +    return [...uniqueAddresses.entries()].map(([addressTo, amount]) => ({ addressTo, amount }));
David: Why this `[...uniqueAddresses.entries()]`? (same for other occasions in this file)

--- PR #354 | src/payment/models/buy-crypto/services/buy-crypto-out.service.ts ---
Context: -            ); | +            console.error(`Failed to initiate buy-crypto payout. Transaction ID: ${transaction.id}`); |              // continue with next group in case payout failed
David: Comment needs to be updated 😉

--- PR #357 | src/shared/util.ts ---
Context: +        return { amount: deposit[key], asset: key }; | +      }); | +  }
David: Just a suggestion:
```ts
static transformToMinDeposit(deposit: { [asset: string]: number }, filter: string[] | string = []): MinDeposit[] {
   filter = [].concat(filter);
   return Object.entries(deposit)
      .filter(([key, _]) => filter.includes(key))
      .map(([key, value]) => ({ asset: key, amount: value }));
}
```

--- PR #357 | src/payment/models/sell/__tests__/sell.controller.spec.ts ---
Context: +      { | +        minDeposits: [ | +          { amount: 0.1, asset: 'DFI' },
David: This test should fail 🤔

--- PR #357 | src/payment/models/staking/__tests__/staking.service.spec.ts ---
Context: +    await expect(service.toDto(1, createCustomStaking({ deposit: createDefaultDeposit() }))).resolves.toMatchObject({ | +      minInvestment: 100, | +      minDeposits: [{ amount: 1, asset: 'DFI' }],
David: This test should fail 🤔

--- PR #357 | src/shared/util.ts ---
Context: +        return { amount: deposit[key], asset: key }; | +      }); | +  }
David: Oh yes, you're completely right 😉

--- PR #358 | src/payment/models/history/history.service.ts ---
Context: +          c.txId && | +          c.cryptoInput && | +          c.cryptoInput.outTxId &&
David: Why is `outTxId` required? It's not used.

--- PR #358 | src/payment/models/history/history.service.ts ---
Context: +      ) | +      .map((c) => | +        c.cryptoInput.asset.dexName == c.outputAsset
David: Why not take c.inputAsset?

--- PR #358 | src/payment/models/history/history.service.ts ---
Context: +                buyValueInEur: null, | +                sellValueInEur: null, | +              },
David: This deposit looks the same than the upper one. Can we combine it?

--- PR #358 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context: +      .andWhere('(buyUser.id = :id OR cryptoRouteUser.id = :id)', { | +        id: userId, | +      })
David: Use a normal find?

```ts
where: [
   { outputDate: Between(dateFrom, dateTo), buy: { user: { id: userId } } },
   { outputDate: Between(dateFrom, dateTo), cryptoRoute: { user: { id: userId } } }
]
```

--- PR #358 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context: +        'cryptoRoute', | +        'cryptoRoute.user', | +        'cryptoRoute.asset',
David: Asset is always `eager: true` and therefore shouldn't be required here.

--- PR #358 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context:      dateTo: Date = new Date(), |    ): Promise<{ fiatAmount: number; fiatCurrency: string; date: Date; cryptoAmount: number; cryptoCurrency: string }[]> { | +    // Without cryptoInput buyCryptos
David: Rename this method instead of comment?

--- PR #358 | src/payment/models/history/history.service.ts ---
Context: +    const stakingRewards = this.fixDuplicateTxRewards( | +      await this.stakingRewardService.getUserRewards([userId], dateFrom, dateTo), | +    );
David: Can be written a bit nicer?

```ts
const stakingRewards = await this.stakingRewardService.getUserRewards([userId], dateFrom, dateTo)
   .then(this.fixDuplicateTxRewards);
```

--- PR #358 | src/payment/models/history/history.service.ts ---
Context: +    let { deposits, withdrawals } = this.fixDuplicateTxInvest( | +      await this.cryptoStakingService.getUserInvests(userId, dateFrom, dateTo), | +    );
David: Same here.

--- PR #358 | src/payment/models/history/history.service.ts ---
Context: +      ); | + | +    return rewards;
David: Perhaps a more readable solution?

Place the following in util.ts

```ts
  static groupBy<T>(list: T[], key: KeyType<T, string>): Map<string, T[]> {
    return list.reduce(
      (map, item) =>
        map.set(item[key] as unknown as string, (map.get(item[key] as unknown as string) ?? []).concat(item)),
      new Map<string, T[]>(),
    );
  }
```

and use it for all duplicate checks (example below).

```ts
  private fixDuplicateTxRewards(rewards: StakingReward[]): StakingReward[] {
    const duplicates = Array.from(Util.groupBy(rewards, 'txId'))
      .map(([_, rewards]) => rewards)
      .filter((r) => r.length > 1);

    duplicates.forEach((rewards) => rewards.forEach((r, i) => (r.txId += i > 0 ? i : '')));

    return rewards;
  }
```

--- PR #366 | src/payment/models/bank-account/bank-account.controller.ts ---
Context: +  @ApiBearerAuth() | +  @UseGuards(AuthGuard(), new RoleGuard(UserRole.USER)) | +  async updateBuyRoute(
David: Rename the methods

--- PR #366 | src/payment/models/bank-account/bank-account.service.ts ---
Context:   | -  private async createBankAccount(iban: string, userId: number, copyFrom?: BankAccount): Promise<BankAccount> { | +  //
David: Empty comment? 😉

--- PR #366 | src/payment/models/bank-account/bank-account.service.ts ---
Context: +      relations: ['user'], | +    }); | +    if (bankAccounts) throw new ForbiddenException('bankAccount already exists');
David: ```ts
if (bankAccounts) throw new ConflictException('BankAccount already exists');
```

--- PR #366 | src/payment/models/bank-account/bank-account.service.ts ---
Context: +    if (bankAccounts) throw new ForbiddenException('bankAccount already exists'); | + | +    const bankAccount = await this.getBankAccount(dto.iban, userId);
David: Rename `getBankAccount ` to `getOrCreateBankAccount`

--- PR #366 | src/payment/models/bank-account/bank-account.service.ts ---
Context: +    const bankAccount = await this.getBankAccount(dto.iban, userId); | + | +    bankAccount.user = { id: userId } as User;
David: This line can be removed

--- PR #366 | src/payment/models/bank-account/bank-account.service.ts ---
Context: + | +    bankAccount.user = { id: userId } as User; | +    bankAccount.preferredCurrency = { id: dto.preferredCurrency.id } as Fiat;
David: ```ts
  // check currency
  if (dto.preferredCurrency) {
    bankAccount.preferredCurrency = await this.fiatService.getFiat(dto.preferredCurrency.id);
    if (!bankAccount.preferredCurrency) throw new BadRequestException('Currency not found');
  }
```

--- PR #366 | src/payment/models/bank-account/bank-account.service.ts ---
Context: + | +  async createBankAccount(userId: number, dto: CreateBankAccountDto): Promise<BankAccount> { | +    const bankAccounts = await this.bankAccountRepo.findOne({
David: Rename `bankAccounts` to `existing`

--- PR #366 | src/payment/models/bank-account/bank-account.service.ts ---
Context: +    bankAccount.user = { id: userId } as User; | +    bankAccount.preferredCurrency = { id: dto.preferredCurrency.id } as Fiat; | +    if (dto.label) bankAccount.label = dto.label;
David: `if (dto.label)` can be removed IMO

--- PR #366 | src/payment/models/bank-account/bank-account.service.ts ---
Context: +      relations: ['user'], | +    }); | +    if (!entity) throw new NotFoundException('bankAccount not found');
David: **B**ankAccount not found

--- PR #366 | src/payment/models/bank-account/dto/create-bank-account.dto.ts ---
Context: +  @ValidateNested() | +  @Type(() => EntityDto) | +  preferredCurrency: Fiat;
David: Optional?

--- PR #366 | src/payment/models/bank-account/dto/create-bank-account.dto.ts ---
Context: +  @ApiProperty() | +  @IsNotEmpty() | +  iban: string;
David: Missing @IsString()

--- PR #366 | src/payment/models/bank-account/dto/create-bank-account.dto.ts ---
Context: +  @ApiPropertyOptional() | +  @IsOptional() | +  label: string;
David: Missing @IsString()

--- PR #366 | src/payment/models/bank-account/dto/update-bank-account.dto.ts ---
Context: +  @ApiProperty() | +  @IsNotEmpty() | +  iban: string;
David: IBAN cannot be updated

--- PR #366 | src/payment/models/bank-account/dto/update-bank-account.dto.ts ---
Context: +import { Fiat } from 'src/shared/models/fiat/fiat.entity'; | + | +export class UpdateBankAccountDto {
David: Looks very similar to CreateBankAccountDto => use a common base class?

--- PR #366 | src/payment/models/bank-account/bank-account.controller.ts ---
Context: +  } | + | +  @Put()
David: I would have the the id of the BankAccount to be updated in the route (see e.g. BuyController).

--- PR #366 | src/payment/models/bank-account/bank-account.service.ts ---
Context: +    const update = this.bankAccountRepo.create(dto); | + | +    Util.removeNullFields(entity);
David: Useless

--- PR #366 | src/payment/models/bank-account/bank-account.service.ts ---
Context: +    if (!entity) throw new NotFoundException('bankAccount not found'); | + | +    const update = this.bankAccountRepo.create(dto);
David: We would need to check the currency. Maybe in a separate createEntity method (see e.g. StakingService)?

--- PR #371 | src/config/config.ts ---
Context: +    }, | +  }; | +
David: I would put all bank infos together, e.g.:
```ts
bank = {
   maerki: {
      accounts: [
         {
            currency: 'EUR',
            iban: 'CH6808573177975201814',
            bic: 'MAEBCHZZ',
          }
      ]
   },
   olkypay: {
      credentials: {
         clientId: process.env.OLKY_CLIENT,
         username: process.env.OLKY_USERNAME,
         password: process.env.OLKY_PASSWORD,
         clientSecret: process.env.OLKY_CLIENT_SECRET,
      },
      accounts: [...],
   },
   ...
}
```

--- PR #371 | src/payment/models/bank-account/dto/bank-account.dto.ts ---
Context:    iban: string; | + | +  @ApiProperty()
David: `@ApiProperty({ type: Fiat })`

--- PR #371 | src/payment/models/buy/dto/buy-payment-info.dto.ts ---
Context: +  refBonus: number; | + | +  @ApiProperty()
David: Add type here, and also add the ApiProperty to MinDeposit.

--- PR #371 | src/payment/models/buy/dto/buy-payment-info.dto.ts ---
Context: +  @ApiProperty() | +  bic: string; | +}
David: I would use the following fields:
- name
- street
- number
- zip
- city
- country
- iban
- bic

--- PR #371 | src/payment/models/sell/dto/sell-payment-info.dto.ts ---
Context: +  depositAddress: string; | + | +  @ApiProperty()
David: Add type here

--- PR #371 | src/payment/models/buy/dto/create-buy-payment-info.dto.ts ---
Context: +import { Fiat } from 'src/shared/models/fiat/fiat.entity'; | + | +export class CreateBuyPaymentInfoDto {
David: Perhaps `GetBuyPaymentInfoDto` would be a better name?

--- PR #371 | src/payment/models/sell/dto/create-sell-payment-info.dto.ts ---
Context: +import { Fiat } from 'src/shared/models/fiat/fiat.entity'; | + | +export class CreateSellPaymentInfoDto {
David: Perhaps `GetSellPaymentInfoDto` would be a better name?

--- PR #371 | src/payment/models/sell/sell.controller.ts ---
Context: +  async getFees(userId: number): Promise<number> { | +    return this.userService.getUserSellFee(userId); | +  }
David: Use this method on lines 85 and 92 as well.

--- PR #371 | src/payment/models/sell/sell.controller.ts ---
Context: +    @Body() createSellDto: CreateSellPaymentInfoDto, | +  ): Promise<SellPaymentInfoDto> { | +    const fees = await this.getFees(jwt.id);
David: Can be moved into `toPaymentInfoDto`

--- PR #371 | thunder-tests/thunderclient.json ---
Context:      "colId": "62eefab7-84cd-496b-8f93-253c15d4f841", | -    "containerId": "ece042c8-5d43-422a-bca8-7c94c971fde4", | -    "name": "Get My Bank Accounts",
David: I think you deleted the bank account thunder requests.

--- PR #371 | src/payment/models/buy/buy.service.ts ---
Context: -    if (!asset) throw new BadRequestException('Asset not found'); | +  // --- PAYMENT-INFO --- // | +  async getBuyPaymentInfos(
David: I think it would be easier to call the complete `createBuy` method here and catch the ConflictException (or add a parameter to ignore conflict).

--- PR #371 | src/payment/models/sell/sell.service.ts ---
Context:   | +  // --- PAYMENT-INFO --- // | +  async getSellPaymentInfos(userId: number, dto: CreateSellPaymentInfoDto): Promise<Sell> {
David: Same as buy.service

--- PR #373 | src/payment/models/bank-tx/frick.service.ts ---
Context: +  private getPositiveAmount(amount: number): number { | +    return amount < 0 ? amount * -1 : amount; | +  }
David: `Math.abs(amount)` (I would remove this method)

--- PR #373 | src/payment/models/bank-tx/frick.service.ts ---
Context: +    try { | +      if (!Config.bank.frick.key) return; | +      const transactions = await this.getTransactions(new Date(lastModificationTime));
David: `const { transactions } = await this.getTransactions(new Date(lastModificationTime));`

--- PR #373 | src/payment/models/bank-tx/frick.service.ts ---
Context: +    const url = `accounts`; | +    return await this.callApi<Accounts>(url); | +  }
David: ```ts
private async getTransactions(fromDate: Date, toDate: Date = new Date()): Promise<Transactions> {
  const params = {
    fromDate: Util.isoDate(fromDate),
    toDate: Util.isoDate(toDate),
    maxResults: 2500,
    status: TransactionState.BOOKED,
  };
  return await this.callApi<Transactions>('transactions', 'GET', params);
}

async getAccounts(): Promise<Accounts> {
  return await this.callApi<Accounts>('accounts');
}
```

and use the following in the `request<T>` method

```ts
      return await this.http.request<T>({
        url: `${Config.bank.frick.url}/${url}`,
        method: method,
        data: method !== 'GET' ? data : undefined,
        params: method === 'GET' ? data : undefined,
        headers: this.getHeaders(data),
      });
```

--- PR #373 | src/payment/models/bank-tx/frick.service.ts ---
Context: +    if (!tx.md) return null; | +    return { | +      exchangeRate: tx.md == 'D' ? tx.fxrate : Util.round(1 / tx.fxrate, 2),
David: `tx.md === MD.D`

--- PR #373 | src/payment/models/bank-tx/frick.service.ts ---
Context: +    } catch (e) { | +      console.error('Error during get frick transactions:', e); | +    }
David: I would not catch the error here, but in bank-tx.service 🤔 what do you think?
(same for olkypay)

--- PR #373 | src/payment/models/bank-tx/frick.service.ts ---
Context: +  private parseTransaction(tx: Transaction): Partial<BankTx> { | +    return { | +      accountServiceRef: tx.orderId ? tx.orderId.toString() : tx.transactionNr?.toString(),
David: `accountServiceRef: (tx.orderId ?? tx.transactionNr)?.toString(),`

--- PR #373 | src/payment/models/bank-tx/frick.service.ts ---
Context: +    return { | +      accountServiceRef: tx.orderId ? tx.orderId.toString() : tx.transactionNr?.toString(), | +      bookingDate: tx.valutaIsExecutionDate ? new Date(tx.valuta) : new Date(tx.bookingDate),
David: What is this `valutaIsExecutionDate`?

--- PR #373 | src/payment/models/bank-tx/frick.service.ts ---
Context: +      bankName: account.creditInstitution, | +      creditDebitIndicator: | +        tx.direction == TransactionDirection.INCOMING ? BankTxIndicator.CREDIT : BankTxIndicator.DEBIT,
David: Should we get this indicator from the amount? CREDIT if positive, DEBIT if negative?

--- PR #373 | src/payment/models/bank-tx/bank-tx.service.ts ---
Context: +          if (!(e instanceof ConflictException)) console.error(`Failed to import transaction:`, e); | +        } | +      }
David: If we use the same settingKey for multiple banks, we have to break, if any request fails (because we might loose some transactions). It then could be:
```ts
const transactions = await Promise.all([
  this.olkyService.getOlkyTransactions(lastModificationTime),
  this.frickService.getFrickTransactions(lastModificationTime),
]).then(([olky, frick]) => olky.concat(frick));

for (const bankTx of transactions) {
  try {
    await this.create(bankTx);
  } catch (e) {
    if (!(e instanceof ConflictException)) console.error(`Failed to import transaction:`, e);
  }
}
```

--- PR #373 | src/payment/models/bank-tx/sepa-parser.service.ts ---
Context:            ...this.getRelatedPartyInfo(entry), |            ...this.getRelatedAgentInfo(entry), | +          accountIban: Config.bank.maerkiBaumann.ibanEur,
David: Use the IBAN from the batch (there are also multiple accounts), e.g. as method parameter.

--- PR #375 | src/user/models/ident/ident.service.ts ---
Context: @@ -12,7 +12,14 @@ export class IdentService { |    // --- WEBHOOK UPDATES --- // |    async identUpdate(result: IdentResultDto): Promise<void> {
David: Update the log on line 37 to print identification instead of transaction ID.

--- PR #375 | src/user/models/spider-data/spider-data.entity.ts ---
Context:    @Column({ length: 256, nullable: true }) | -  identTransactionId: string; | +  identIdentificationId: string;
David: Rename to identIdentificationId**s** to be clear?

--- PR #375 | src/user/models/user-data/user-data.controller.ts ---
Context: +  async requestVideoId(@Param('id') id: string): Promise<void> { | +    return this.kycService.requestVideoId(+id); | +  }
David: Switch to a generic KycStatus/KycState update API

--- PR #384 | src/user/models/user-data/user-data.service.ts ---
Context:    ConflictException, | +  forwardRef, | +  Inject,
David: Unused

--- PR #384 | src/user/models/user-data/user-data.service.ts ---
Context: -    userData = await this.updateUserSettings(userData, dto); | +    // Is this needed here? | +    userData = await this.updateUserSettings(userData, {});
David: I think so 😉  what are your thoughts? And why `updateUserSettings(userData, {})`?

--- PR #384 | src/user/models/user-data/user-data.service.ts ---
Context: -    } | +    if (dto.kycStatus && userData.kycStatus != dto.kycStatus) | +      this.kycProcessService.goToStatus(userData, dto.kycStatus);
David: KycProcessService doesn't save the UserData. You need to get the result value:
```ts
userData = this.kycProcessService.goToStatus(userData, dto.kycStatus);
```

--- PR #384 | src/user/models/user-data/user-data.service.ts ---
Context:   | -  async updateSpiderIfNeeded(userData: UserData, dto: UpdateUserDto) { | +  async updateSpiderIfNeeded(userData: UserData, dto: UpdateUserDto): Promise<UserData> {
David: Use the return value at all places, this method is called.

--- PR #385 | src/payment/models/bank-tx/frick.service.ts ---
Context:    } |   | +  private async getTransactionAccount(orderId: string): Promise<Transactions> {
David: Name is a bit misleading?

--- PR #385 | src/payment/models/bank-tx/frick.service.ts ---
Context:    } |   | +  private async getTransactionAccount(orderId: string): Promise<Transactions> {
David: I would have orderId parameter as number (orderId: number).

--- PR #385 | src/payment/models/bank-tx/frick.service.ts ---
Context:      if (!transactions) return []; |   | +    for (const transaction of transactions) {
David: Perhaps add a comment?

--- PR #386 | src/payment/models/crypto-route/crypto-route.controller.ts ---
Context: +      depositAddress: cryptoRoute.deposit.address, | +      ...(await this.getFees(userId)), | +      minDeposits: Util.transformToMinDeposit(Config.node.minDeposit.Fiat),
David: Use min. deposits from toDto method (extract to separate method?)

--- PR #386 | src/payment/models/crypto-route/dto/get-crypto-payment-info.dto.ts ---
Context: + | +export class GetCryptoPaymentInfoDto { | +  @ApiProperty()
David: Perhaps a description would help here?
```ts
@ApiProperty({ description: 'Source blockchain' })
```

--- PR #386 | src/payment/models/crypto-route/dto/get-crypto-payment-info.dto.ts ---
Context: +  @ValidateNested() | +  @Type(() => EntityDto) | +  asset: Asset;
David: Perhaps a description would help here?
```ts
@ApiProperty({ description: 'Target asset' })
```

--- PR #386 | src/payment/models/crypto-route/crypto-route.controller.ts ---
Context: +        minDeposits = Util.transformToMinDeposit(Config.node.minDeposit.DeFiChain); | +        break; | +    }
David: ```ts
switch (blockchain) {
      case Blockchain.BITCOIN:
        return Util.transformToMinDeposit(Config.node.minDeposit.Bitcoin);
      case Blockchain.DEFICHAIN:
        return Util.transformToMinDeposit(Config.node.minDeposit.DeFiChain);
    }
```

--- PR #388 | src/payment/models/buy/buy.controller.ts ---
Context:    } | + | +  private getBankInfoAccount(
David: `getBankAccountInfo`?

--- PR #388 | src/payment/models/buy/buy.controller.ts ---
Context: +    let account = bankAccounts.find((b) => b.currency === currencyName); | +    if (!account) account = bankAccounts.find((b) => b.currency === fallBackCurrencyName); | +    return account;
David: ```ts
return bankAccounts.find((b) => b.currency === currencyName) ?? bankAccounts.find((b) => b.currency === fallBackCurrencyName);
```

--- PR #388 | src/payment/models/buy/buy.controller.ts ---
Context:   | -  private getBankInfo(buy: Buy, dto: GetBuyPaymentInfoDto): BankInfoDto { | +  private async getBankInfo(buy: Buy, dto: GetBuyPaymentInfoDto): Promise<BankInfoDto> {
David: I think it would be useful to write some unit tests for this code 😉

--- PR #388 | src/user/models/user-data/__tests__/mock/user-data.entity.mock.ts ---
Context: +  entity.country = createDefaultCountry(); |   |    return entity;
David: Use the same logic as for country mock here (add a default UserData and use the Object.assign())

--- PR #388 | src/payment/models/buy/__tests__/buy.controller.spec.ts ---
Context: +      bic: 'BFRILI22', | +    }); | +  });
David: I would add a test with a special currency (e.g. GBP) to see if it falls back to the EUR account.

--- PR #388 | src/payment/models/buy/__tests__/mock/buy.entity.mock.ts ---
Context: +  entity.bankAccount = createDefaultBankAccount(); |   |    return entity;
David: Same here (as UserData)

--- PR #392 | src/payment/models/bank-account/bank-account.service.ts ---
Context: +  async checkFailedBankAccounts(): Promise<void> { | +    const failedBankAccounts = await this.bankAccountRepo.find({ where: { returnCode: 256 } }); | +    failedBankAccounts.forEach((bankAccount) => this.reloadBankAccount(bankAccount.id, bankAccount.iban));
David: Missing await. You need to use a for-loop for calling async methods

--- PR #392 | src/payment/models/bank-account/bank-account.service.ts ---
Context:    } |   | +  private async reloadBankAccount(bankAccountId: number, iban: string): Promise<void> {
David: Why not have the complete BankAccount as parameter?

--- PR #394 | src/payment/models/masternode/masternode.service.ts ---
Context: +      if (nthTry > 1 && e.response?.status == 401) { | +        return this.request(url, method, data, nthTry - 1); | +      }
David: Is not required here, because we will never get 401 (no access token).

--- PR #394 | src/payment/models/masternode/masternode.service.ts ---
Context: + | +  // --- MASTERNODE SYNC --- // | +  @Interval(1440000)
David: Use a @Cron expression

--- PR #394 | src/payment/models/masternode/masternode.service.ts ---
Context: +    if (!Config.mydefichain.username) return; | + | +    const dbOperatorList = await this.masternodeRepo.find({
David: Rename to masternodes?

--- PR #394 | src/payment/models/masternode/masternode.service.ts ---
Context: +      select: ['operator'], | +    }); | +    const serverList = await this.settingService.get('serverList');
David: Use a clearer name like masternodeServerList or similar?

--- PR #394 | src/payment/models/masternode/masternode.service.ts ---
Context: + | +      for (const operator of missingOperators) { | +        const newOperator = await this.masternodeRepo.create({ operator, server });
David: await is not required

--- PR #395 | src/app.module.ts ---
Context:      AinModule, | +    EthereumModule, | +    BSCModule,
David: We only used CamelCase (BscModule in this case) until now. WDYT? 😉

--- PR #395 | src/shared/models/asset/asset.service.ts ---
Context: +    const { blockchain, isToken } = query; | + | +    if (dexName === 'DFI' && isToken) dexName = 'DFI-Token';
David: This logic does IMO not belong here (even though it was me, who put it here). The only place where the token is used, is in defi-input.service. Let's discuss on how to improve it.

--- PR #395 | src/payment/models/dex/services/dex-defichain.service.ts ---
Context: +      (o) => | +        o.targetAsset.dexName === asset && | +        (o.targetAsset.blockchain === Blockchain.DEFICHAIN || o.targetAsset.blockchain === Blockchain.BITCOIN),
David: Why Bitcoin?

--- PR #395 | src/payment/models/buy-crypto/entities/__mocks__/buy-crypto-batch.entity.mock.ts ---
Context:    entity.status = keys.includes('status') ? status : BuyCryptoBatchStatus.CREATED; | -  entity.outTxId = keys.includes('outTxId') ? outTxId : ''; | +  entity.blockchain = keys.includes('blockchain') ? blockchain : Blockchain.DEFICHAIN;
David: Should we switch to object destructuring (for all mocks)?

--- PR #395 | src/payment/models/dex/services/dex-evm.service.ts ---
Context: +    const pendingOrders = (await this.liquidityOrderRepo.find({ isReady: true, isComplete: false })).filter( | +      (o) => o.targetAsset.dexName === this.nativeCoin && o.targetAsset.blockchain === this.blockchain, | +    );
David: Why only native coin?

--- PR #395 | src/payment/models/exchange/services/currency.service.ts ---
Context: + | +  async getPrice(from: string, to: string): Promise<Price> { | +    const targetPrice = await this.conversionService.convertFiat(1, from, to);
David: Use `conversionService.getFiatRate` ?

--- PR #395 | src/payment/models/exchange/services/fixer.service.ts ---
Context: + | +@Injectable() | +export class FixerService implements PriceProvider {
David: Should we keep it? If not, let's also remove its config.

--- PR #395 | src/payment/models/payout/services/payout.service.ts ---
Context: +    const tokenOrders = orders.filter( | +      (o) => [Blockchain.DEFICHAIN, Blockchain.BITCOIN].includes(o.asset.blockchain) && o.asset.dexName !== 'DFI', | +    );
David: Why Bitcoin?

--- PR #395 | src/payment/models/payout/services/payout.service.ts ---
Context: +    const orders = await this.payoutOrderRepo.find({ status: PayoutOrderStatus.PREPARATION_CONFIRMED }); | + | +    const DFIOrders = orders.filter(
David: `dfiOrders`

--- PR #395 | src/payment/models/payout/services/payout.service.ts ---
Context: +    ); | + | +    const DFIStrategy = this.strategies.getPayoutStrategy(PayoutStrategyAlias.DEFICHAIN_DFI);
David: `dfiStrategy`

--- PR #395 | src/payment/models/dex/services/dex.service.ts ---
Context: +    try { | +      return await this.dexDeFiChainService.transferLiquidity(destinationAddress, asset.dexName, amount); | +    } catch (e) {}
David: Should we throw here, if asset.blockchain is not DeFiChain?

--- PR #395 | src/payment/models/pricing/utils/price-path.ts ---
Context: +      } | + | +      results.push(await step.execute());
David: Would perhaps be easier with if-else instead of break/continue.

--- PR #395 | src/payment/models/pricing/utils/price-path.ts ---
Context: + | +    return price; | +  }
David: Perhaps this should be moved to Price class, as it is used in multiple places.

--- PR #395 | src/payment/models/pricing/utils/price-step.ts ---
Context: +      reference: options.providers?.reference || [], | +    }; | +    this.options.fixedPrice = options.fixedPrice;
David: Why not like this (only initialize the options in constructor)?
```ts
this.options = {
   from: options.from || 'input',
   to: options.to || 'output',
   ...
};
```

--- PR #395 | src/payment/models/pricing/utils/price-step.ts ---
Context: + | +      _to = options.to; | +    }
David: Should we move the validation to the constructor (same as path)?

--- PR #395 | src/payment/models/pricing/utils/price-step.ts ---
Context: +        return [await provider.getPrice(fromCurrency, toCurrency), provider.name]; | +      } catch { | +        return null;
David: Is this correct? Don't we leave the for loop?

--- PR #396 | src/shared/i18n/de/mail.json ---
Context: +      "body": "<table style=\"font-family: Open Sans, Helvetica, Arial, sans-serif; width: 60%; padding: 8px; font-size: 85%\" align=\"center\" ><tr style=\"font-family: Open Sans, Helvetica, Arial, sans-serif\"><td colspan=\"3\" align=\"center\">Hi {firstname} {surname} {organizationName}</td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr style=\"font-family: Open Sans, Helvetica, Arial, sans-serif\"><td colspan=\"3\" align=\"center\">If you want to link your new address to your existing and already verified account.</td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr><td align=\"right\">Existing address</td><td style=\"width: 2%\"></td><td align=\"left\">{existingAddress}</td></tr><tr><td align=\"right\">New address</td><td style=\"width: 2%\"></td><td align=\"left\">{newAddress}</td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr style=\"font-family: Open Sans, Helvetica, Arial, sans-serif\"><td colspan=\"3\" align=\"center\">Please follow instructions <a href=\"{url}\" style=\"color: #ffffff\">here</a>.</td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr style=\"font-family: Open Sans, Helvetica, Arial, sans-serif\"><td colspan=\"3\" align=\"center\">Your DFX Team</td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr style=\"padding: 1px\"><td colspan=\"3\"></td></tr><tr><td colspan=\"3\" align=\"center\"><span style=\"font-family: Zapfino\">Bitcoiners by heart ♥️</span></td></tr></table>", | +      "title": "Link your address" | +    }
David: Let's talk to Oli for exact wording and translations.

--- PR #396 | src/shared/models/asset/asset.controller.ts ---
Context: -    return await this.assetService.getAllAsset(); | +  async getAllAsset(@GetJwt() jwt: JwtPayload): Promise<Asset[]> { | +    return this.assetService.getAllAsset(jwt.blockchain);
David: Already some ideas on how to handle BSC vs. Ethereum?

--- PR #396 | src/shared/models/asset/asset.service.ts ---
Context: -    return this.assetRepo.find(); | +  async getAllAsset(blockchain: Blockchain): Promise<Asset[]> { | +    if (!blockchain) blockchain = Blockchain.DEFICHAIN;
David: blockchain ??= Blockchain.DEFICHAIN;

--- PR #396 | src/user/models/auth/auth.service.ts ---
Context: +    if (blockchain === Blockchain.ETHEREUM || blockchain === Blockchain.BITCOIN) | +      return { message: Config.auth.signMessageGeneral + address, blockchain }; | +    return { message: Config.auth.signMessage + address, blockchain };
David: I would invert the logic (we'll probably add more blockchains):
```ts
return {
   message: blockchain === Blockchain.DEFICHAIN ? Config.auth.signMessage : Config.auth.signMessageGeneral,
   blockchain,
};
```

--- PR #396 | src/user/models/link/__tests__/link-address.entity.spec.ts ---
Context: +  it('should set existing, new address and expiration date to tomorrow on create', () => { | +    const tomorrow = new Date(); | +    tomorrow.setDate(tomorrow.getDate() + 1);
David: ```ts
const tomorrow = Util.daysAfter(1);
```

--- PR #396 | src/user/models/link/link-address.entity.ts ---
Context: +    const tomorrow = new Date(); | +    tomorrow.setDate(tomorrow.getDate() + 1); | +    linkAddress.expiration = tomorrow;
David: ```ts
linkAddress.expiration = Util.daysAfter(1);
```

--- PR #396 | src/user/models/link/dto/link.dto.ts ---
Context: +  @Type(() => AddressInformationDto) | +  linkTo: AddressInformationDto; | +}
David: Is this still used?

--- PR #396 | src/user/models/kyc/kyc.service.ts ---
Context:      if (!dataComplete) throw new BadRequestException('Ident data incomplete'); |   | +    const [users, numberOfUsersWithSameInformation] = await this.userDataService.getUsersByInformation(user);
David: `numberOfUsersWithSameInformation` is not needed?

--- PR #396 | src/user/models/kyc/kyc.service.ts ---
Context: +        const existingAddress = completedUser.users[0].address; | +        // this should be fine, as a new user_data should have only one address | +        const newAddress = user.users[0].address;
David: Comments can be removed?

--- PR #396 | src/user/models/kyc/kyc.service.ts ---
Context: +          }, | +        }); | +        throw new ConflictException();
David: Add an error message

--- PR #396 | src/user/models/user/user.service.ts ---
Context:   | -  async createUser(dto: CreateUserDto, userIp: string, userOrigin?: string): Promise<User> { | +  async createUser(dto: CreateUserDto, userIp: string, userOrigin?: string, userData?: UserData): Promise<User> {
David: Is the userData parameter used somewhere?

--- PR #396 | src/user/models/user/user.service.ts ---
Context:   | -  async createUser(dto: CreateUserDto, userIp: string, userOrigin?: string): Promise<User> { | +  async createUser(dto: CreateUserDto, userIp: string, userOrigin?: string, userData?: UserData): Promise<User> {
David: Ok, makes sense 👍

--- PR #396 | src/user/models/kyc/kyc.service.ts ---
Context: +        const existingAddress = completedUser.users[0].address; | +        // this should be fine, as a new user_data should have only one address | +        const newAddress = user.users[0].address;
David: So what would you propose?

--- PR #396 | src/user/models/kyc/kyc.service.ts ---
Context: +        const existingAddress = completedUser.users[0].address; | +        // this should be fine, as a new user_data should have only one address | +        const newAddress = user.users[0].address;
David: Ok perfect 👍

--- PR #396 | src/user/models/kyc/kyc.service.ts ---
Context:      if (!dataComplete) throw new BadRequestException('Ident data incomplete'); |   | +    const users = await this.userDataService.getUsersByInformation(user);
David: Talked to Cyrill ... we should use only the email to match accounts (no names).

--- PR #396 | src/user/models/kyc/kyc.service.ts ---
Context: +      const completedUser = users.find((data) => KycCompleted(data.kycStatus)); | +      const shouldSendLinkEmail = users.length > 1; | +      if (shouldSendLinkEmail && completedUser) {
David: `shouldSendLinkEmail` is always true, if `completedUser` is not null => can be removed

--- PR #396 | src/user/models/kyc/kyc.service.ts ---
Context: +          }, | +        }); | +        throw new ConflictException('User already has completed Kyc');
David: Is this error handled on payment page?

--- PR #396 | src/user/models/link/link-address.entity.ts ---
Context: + | +  isExpired(): boolean { | +    return this.expiration.getTime() < new Date().getTime();
David: Could this be replace by the following (not sure)?
```ts
return this.expiration < new Date();
```

--- PR #396 | src/user/models/user/dto/linked-user.dto.ts ---
Context: +  isSwitchable: boolean; | + | +  @ApiProperty()
David: ```ts
@ApiProperty({ enum: Blockchain, isArray: true })
```

--- PR #396 | src/user/models/kyc/kyc.service.ts ---
Context: +          }, | +        }); | +        throw new ConflictException('User already has completed Kyc');
David: Perfect 👍

--- PR #396 | src/user/models/kyc/kyc.service.ts ---
Context:   | +    const users = await this.userDataService.getUsersByMail(user.mail); | +    if (users) {
David: Check is not required

--- PR #403 | src/ain/services/crypto.service.ts ---
Context: +      if (!isValid && blockchains.includes(Blockchain.DEFICHAIN)) { | +        isValid = this.fallbackVerify(defichainFallbackMessage, address, signature, blockchains); | +      }
David: Shouldn't we put this logic into `verifyDefichain` method? Would be easier IMO.

--- PR #403 | src/ain/services/crypto.service.ts ---
Context: +      if (!isValid && blockchains.includes(Blockchain.DEFICHAIN)) { | +        isValid = this.fallbackVerify(defichainFallbackMessage, address, signature, blockchains); | +      }
David: The `fallbackVerify` is used to support all address formats (we got this code from U-Zyn and actually never questioned or verified it extensively 😉). So `fallbackVerify` also needs to be executed for both messages.

--- PR #408 | src/payment/models/history/dto/history.dto.ts ---
Context: +  SELL = 'Sell', | +  CRYPTO = 'Crypto', | +}
David: I would move this to RouteHistoryDto. Maybe we can move the route-history.dto.ts to the history folder?

--- PR #408 | src/payment/models/history/dto/history.dto.ts ---
Context: +export class SimplifiedHistoryDto extends RouteHistoryDto { | +  type: HistoryTransactionType; | +}
David: I would move this also to RouteHistoryDto and rename it to something like TypedHistoryDto?

--- PR #408 | src/payment/models/history/history.controller.ts ---
Context: +    //this.historyService.getOceanTransaction('df1qh2as5nzrehxgu4mke4ysrvngydsevtmhedns0x'); | +    //this.historyService.getOceanTransaction('8L3z6CyyLshqnUf8kDHjLQ6GCupLKbiXJx'); | +    //return await this.historyService.getHistory(jwt.id, jwt.address, query);
David: This should be removed?

--- PR #408 | src/payment/models/history/dto/history.dto.ts ---
Context:  } | + | +export class SimplifiedHistoryDto extends RouteHistoryDto {
David: It might be a good moment to also swagger-document the route history return DTOs 😉

--- PR #408 | src/payment/models/history/history.controller.ts ---
Context:      @Query() query: HistoryQuery, |    ): Promise<HistoryDto[] | CoinTrackingHistoryDto[]> { |      return await this.historyService.getHistory(jwt.id, jwt.address, query);
David: Is there something missing? We're not calling the BuyCrypto and BuyFiat Service? 🤔

--- PR #408 | src/payment/models/history/history.controller.ts ---
Context: +        }, | +      ]) | +      .reduce((prev, curr) => prev.concat(curr), []);
David: `.map((c) => ({ ...c, type }))` and remove the reduce?

--- PR #408 | src/payment/models/history/history.controller.ts ---
Context: +      ), | +      this.toSimplifiedHistoryDto(await this.buyFiatService.getHistory(jwt.id), HistoryTransactionType.SELL), | +    ].reduce((prev, curr) => prev.concat(curr), []);
David: Sort by date (descending)?

--- PR #413 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context:        if (!entity.buy) throw new BadRequestException(`Cannot assign BuyCrypto ${id} to a buy route`); |        update.buy = await this.getBuy(dto.buyId); | +      if (entity.bankTx) await this.bankTxRepo.setNewUpdateTime(entity.bankTx.id);
David: Is the bankTx loaded from database?

--- PR #413 | src/user/models/user-data/user-data.service.ts ---
Context:    } | + | +  private async setNewUpdateTimeWithUser(userDataId: number): Promise<void> {
David: I would rename the method to something with bank TX 😉

--- PR #419 | src/payment/models/payout/services/payout-defichain.service.ts ---
Context: +  isUserAddress(address: string): boolean { | +    return ['df1', 'tf1'].includes(address.slice(0, 3)); | +  }
David: I would rename it to `isLightWalletAddress`.

--- PR #421 | src/app.module.ts ---
Context:      UserModule, |      MonitoringModule, | +    NotificationModule,
David: Should we add it to SharedModule only?

--- PR #421 | src/notification/notification.controller.ts ---
Context: +  @ApiExcludeEndpoint() | +  @UseGuards(AuthGuard(), new RoleGuard(UserRole.ADMIN)) | +  async reserveLiquidity(@Body() dto: MailRequest): Promise<void> {
David: Name can be improved 😆

--- PR #421 | src/notification/notification.controller.ts ---
Context: +@ApiTags('notification') | +@Controller('notification') | +export class NotificationController {
David: Do we need this controller?

--- PR #421 | src/notification/services/mail.service.ts ---
Context: +import { Mail } from '../entities/mail/mail'; | + | +export interface MailOptions {
David: Is this used somewhere?

--- PR #421 | src/notification/services/notification.service.ts ---
Context: +    } | + | +    throw e;
David: Should we always throw, even if it's just suppressed?

--- PR #421 | src/payment/models/dex/dex.controller.ts ---
Context: +  @ApiExcludeEndpoint() | +  @UseGuards(AuthGuard(), new RoleGuard(UserRole.ADMIN)) | +  async transferMinimalUtxo(@Param('address') address: string): Promise<string> {
David: Param is missing in URL?

--- PR #421 | src/payment/models/dex/dex.controller.ts ---
Context: +  async fetchTargetLiquidityAfterPurchase( | +    @Param('context') context: LiquidityOrderContext, | +    @Param('correlationId') correlationId: string,
David: Params are missing in URL or switch to body?

--- PR #421 | src/payment/models/dex/dex.controller.ts ---
Context: +  @ApiExcludeEndpoint() | +  @UseGuards(AuthGuard(), new RoleGuard(UserRole.ADMIN)) | +  async checkTransferCompletion(@Param('transferTxId') transferTxId: string): Promise<boolean> {
David: Param is missing in URL?

--- PR #421 | src/payment/models/dex/dex.controller.ts ---
Context: +  async completeOrders( | +    @Param('context') context: LiquidityOrderContext, | +    @Param('correlationId') correlationId: string,
David: Params are missing in URL or switch to body?

--- PR #421 | src/notification/entities/notification.entity.ts ---
Context: + | +    this.context = metadata?.context; | +    this.correlationId = metadata?.correlationId.substring(0, 255);
David: Why do you take a substring here? Column length is 'MAX'.

--- PR #421 | src/payment/models/payout/payout.controller.ts ---
Context: +  async checkOrderCompletion( | +    @Param('context') context: PayoutOrderContext, | +    @Param('correlationId') correlationId: string,
David: Params are missing in URL or switch to body?

--- PR #421 | src/payment/models/pricing/pricing.controller.ts ---
Context: +  @UseGuards(AuthGuard(), new RoleGuard(UserRole.ADMIN)) | +  async getPrice(@Body() dto: PriceRequest): Promise<PriceResult> { | +    if (process.env.ENVIRONMENT === 'test') {
David: Environment `test` does not exist. It's `loc`, `dev` or `prd`.

--- PR #428 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context:                  buyWalletAddress: Util.trimBlockchainAddress(tx.target.address), | -                buyTxId: tx.txId, | +                buyTxId: `${BlockchainExplorerUrls[Blockchain.DEFICHAIN]}/${tx.txId}`,
David: I would rename the parameter to something with transaction link.

--- PR #433 | src/user/models/auth/auth.service.ts ---
Context: -  async signIn({ address, signature }: AuthCredentialsDto): Promise<{ accessToken: string }> { | -    const user = await this.userRepo.getByAddress(address); | +  async signIn({ address, signature }: AuthCredentialsDto, companyLogin = false): Promise<{ accessToken: string }> {
David: I think I would create a different method for the companySignIn. There's not much shared logic. And getting a user from a walletRepo is kind of confusing 😉

--- PR #433 | src/user/models/auth/auth.service.ts ---
Context:    } | + | +  private generateCompanyToken(user: Wallet): string {
David: Rename parameter to `wallet`

--- PR #433 | src/user/models/auth/auth.service.ts ---
Context: -  private verifySignature(address: string, signature: string): boolean { | -    const signatureMessage = this.getSignMessage(address); | +  private verifySignature(address: string, signature: string, companyLogin = false): boolean {
David: For company login we should use the challenge-response method. The wallet signing message can be removed.

--- PR #433 | src/shared/auth/jwt-payload.interface.ts ---
Context: + | +export interface JwtCompanyPayload extends JwtBasicPayload { | +  isKycClient: boolean;
David: I would solve this with different roles, e.g. `Wallet` and `KycWallet` or similar.

--- PR #433 | src/user/models/user/user.service.ts ---
Context:   | +    console.log(`SignUp User-Ip ${userIp}; Ip-country ${ipCountry}`); | +
David: I added this log in a different branch just for testing purposes. It can be removed here.

--- PR #433 | src/user/models/wallet/wallet.repository.ts ---
Context: -export class WalletRepository extends Repository<Wallet> {} | +export class WalletRepository extends Repository<Wallet> { | +  async getByAddress(address: string, needsRelation = false): Promise<Wallet> {
David: Unused parameter

--- PR #433 | src/user/models/kyc/kyc.service.ts ---
Context: +      walletUser = await this.walletRepo.findOne({ where: dto.wallet }); | +      if (!walletUser || !walletUser.isKycClient || !walletUser.apiUrl) | +        throw new NotFoundException('Wallet user not found');
David: `Wallet not found`

--- PR #433 | src/user/models/kyc/kyc.service.ts ---
Context: + | +    try { | +      walletUser = await this.walletRepo.findOne({ where: dto.wallet });
David: `wallet`

--- PR #433 | src/user/models/kyc/kyc.service.ts ---
Context: + | +      const { kycId } = await this.http.get<{ kycId: string }>(`${walletUser.apiUrl}/kyc/kycId`, { | +        headers: { 'x-api-key': Config.lock.apiKey, 'content-Type': 'text/plain' },
David: Is the `'content-Type': 'text/plain'` required?

--- PR #433 | src/user/models/kyc/kyc.service.ts ---
Context: + | +      const slaveUser = await this.userRepo.findOne({ where: { address: kycId }, relations: ['userData'] }); | +      if (!slaveUser) throw new NotFoundException('External service user not found');
David: `KYC user not found `

--- PR #433 | src/user/models/kyc/kyc.service.ts ---
Context: + | +      throw error; | +    }
David: In this method, we should only merge the two users. Updating Lock API with a webhook is a different story. The webhook should be triggered on any KYC state change. Therefore it should be a completely separated method.

--- PR #433 | src/user/models/kyc/kyc.service.ts ---
Context: +          kycStatus: 'Full', | +          kycHash: user.userData.kycHash, | +        };
David: The DTO looks like this:
```ts
export class KycDataDto {
  mail: string;
  firstName: string;
  lastName: string;
  street: string;
  houseNumber: string;
  city: string;
  zip: string;
  phone: string;
  kycStatus: KycStatus;
  kycHash: string;
}
```

--- PR #433 | src/user/models/user-data/user-data.service.ts ---
Context: -    return await this.userDataRepo.save({ ...userData, ...dto }); | +    // KYC change Webhook | +    if (dto.kycStatus && KycCompleted(dto.kycStatus)) await this.kycWebhookService.kycChanged(userData.id);
David: I would do this in the goToStatus method.

--- PR #433 | src/user/models/user-data/user-data.service.ts ---
Context: +    // KYC change Webhook | +    await this.kycWebhookService.kycChanged(masterId); | +
David: I think we should trigger the webhook on the slave, the master didn't change 🤔

--- PR #433 | src/shared/auth/user-role.enum.ts ---
Context: + | +  // external company roles | +  EXTERNAL_COMPANY_OWNER = 'ExternalCompanyOwner',
David: I would use something with KYC client? This role is only for KYC client companies.

--- PR #433 | src/user/models/kyc/kyc-webhook.service.ts ---
Context: + | +  async kycChanged(userDataId: number): Promise<void> { | +    let data: { id?: string; result?: 'StatusChanged' | 'Failed'; data?: {}; reason?: string } = {};
David: It would make sense to add interfaces for this:

```ts
export enum KycStatus {
  NA = 'NA',
  LIGHT = 'Light',
  FULL = 'Full',
}

export enum KycResult {
  STATUS_CHANGED = 'StatusChanged',
  FAILED = 'Failed',
}

export class KycDataDto {
  mail: string;
  firstName: string;
  lastName: string;
  street: string;
  houseNumber: string;
  city: string;
  zip: string;
  phone: string;
  kycStatus: KycStatus;
  kycHash: string;
}

export class KycWebhookDto {
  id: string;
  result: KycResult;
  data: KycDataDto;
  reason: string;
}
```

--- PR #433 | src/user/models/kyc/kyc-webhook.service.ts ---
Context: + | +  async kycChanged(userDataId: number): Promise<void> { | +    let data: { id?: string; result?: 'StatusChanged' | 'Failed'; data?: {}; reason?: string } = {};
David: Remove this line

--- PR #433 | src/user/models/kyc/kyc-webhook.service.ts ---
Context: +        kycStatus: KycCompleted(userData.kycStatus) ? 'Full' : 'NA', | +        kycHash: userData.kycHash, | +      };
David: ```ts
const data: KycWebhookDto = {
  id: user.address,
  result: 'StatusChanged',
  data: {
    mail: userData.mail,
    firstName: userData.firstname,
    lastName: userData.surname,
    street: userData.street,
    houseNumber: userData.houseNumber,
    city: userData.location,
    zip: userData.zip,
    phone: userData.phone,
    //TODO change for KYC Update v2
    kycStatus: KycCompleted(userData.kycStatus) ? 'Full' : 'NA',
    kycHash: userData.kycHash,
  },
};
```

--- PR #433 | src/user/models/kyc/kyc-webhook.service.ts ---
Context: +  } | + | +  async kycFailed(userDataId: number): Promise<void> {
David: Add a reason parameter

--- PR #433 | src/user/models/kyc/kyc-webhook.service.ts ---
Context: + | +  async kycFailed(userDataId: number): Promise<void> { | +    let data: { id?: string; result?: 'StatusChanged' | 'Failed'; reason?: string } = {};
David: Remove this line

--- PR #433 | src/user/models/kyc/kyc-webhook.service.ts ---
Context: +      data.id = user.address; | +      data.result = 'Failed'; | +      //data.reason = '';
David: ```ts
const data: KycWebhookDto = {
  id: user.address,
  result: 'Failed',
  reason: reason,
};
```

--- PR #433 | src/user/models/user-data/user-data.service.ts ---
Context: +    // KYC change Webhook | +    await this.kycWebhookService.kycChanged(masterId); | +
David: The slave UserData will be unused afterwards. All users and bankDatas of the slave will be transferred to the master. Even if the slave had KYC and the master didn't, the user would not have KYC after the merge.

--- PR #439 | src/payment/models/bank-tx-return/bank-tx-return.service.ts ---
Context: +    const entity = this.bankTxReturnRepo.create(); | + | +    entity.bankTx = bankTx;
David: ```ts
const entity = this.bankTxReturnRepo.create({ bankTx });
```

--- PR #440 | src/payment/models/buy/buy.controller.ts ---
Context: -    return { ...Config.bank.dfxBankInfo, iban: account.iban, bic: account.bic }; | -  } | +    if (!bank) throw new ConflictException('No Bank for the given amount/currency');
David: `BadRequestException`

--- PR #440 | src/payment/models/buy/buy.service.ts ---
Context:        }, | -      relations: ['deposit', 'bankAccount', 'user', 'user.userData'], | +      relations: ['deposit', 'bankAccount', 'user', 'user.userData', 'bankAccount.user', 'bankAccount.user.userData'],
David: Do we need this change?

--- PR #440 | src/shared/models/bank/bank.entity.ts ---
Context: +export class Bank extends IEntity { | +  @Column({ length: 256 }) | +  name: string;
David: What about an enum for the bank name?

--- PR #440 | src/shared/models/bank/__mocks__/bank.entity.mock.ts ---
Context: +    }), | +  ]; | +}
David: What about declaring all default banks as constants?
```ts
export const frickEur = createCustomBank({
      name: BankName.FRICK,
      currency: 'EUR',
      iban: 'LI95088110104693K000E',
      bic: 'BFRILI22',
      receive: true,
    });

export const frickChf = createCustomBank({
      name: BankName.FRICK,
      currency: 'CHF',
      iban: 'LI52088110104693K000C',
      bic: 'BFRILI22',
      receive: true,
    });
```
We can still export them in an array:
```ts
export const defaultBanks = [frickEur, frickChf, ...];
```
We could then use them to compare it in the testcases (see comment below).

--- PR #440 | src/shared/models/bank/__tests__/bank.service.spec.ts ---
Context: +      iban: 'LI52088110104693K000C', | +      bic: 'BFRILI22', | +    });
David: ```ts
    await expect(service.getBank(createBankSelectorInput('CHF', 10000))).resolves.toMatchObject({
      iban: frickChf.iban,
      bic: frickChf.bic,
    });
```

--- PR #440 | src/shared/models/bank/__tests__/bank.service.spec.ts ---
Context: +  it('should return BF if amount > 9000', async () => { | +    jest.spyOn(countryService, 'getCountryWithSymbol').mockResolvedValue(createDefaultCountry()); | +    jest.spyOn(bankRepo, 'find').mockResolvedValue(createDefaultBanks());
David: Move the setup methods to a separate setup method (with currency as optional parameter)?

--- PR #440 | src/shared/models/bank/__tests__/bank.service.spec.ts ---
Context: +    }); | +  }); | +});
David: Maybe add a test to verify if disabling a bank works?

--- PR #442 | src/payment/models/buy-crypto/entities/buy-crypto.entity.ts ---
Context: + | +    return this; | +  }
David: This method does exactly the same as `confirmSentMail` 🤔

--- PR #442 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context:      await this.buyCryptoOutService.payoutTransactions(); |      await this.buyCryptoNotificationService.sendNotificationMails(); | +    await this.buyCryptoNotificationService.paybackToAddressInitiated();
David: Remove this additional call here and call it from `sendNotificationMails`. Extract the existing logic in there to a new method (e.g. buyCryptoConfirmed or similar). See `buy-fiat-notification.service`.

--- PR #442 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context: +        entity.paybackInitiated(); | + | +        if (entity.buy?.user.userData.mail || entity.cryptoRoute?.user.userData.mail) {
David: `entity.user.userData.mail`

--- PR #442 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context: +          { id: entity.id }, | +          { mailSendDate: entity.mailSendDate, recipientMail: entity.recipientMail }, | +        );
David: Use this update logic also for the upper sendNotification method (we still use a save there).

--- PR #442 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context: +                lang: entity.buy | +                  ? entity.buy.user.userData.language?.symbol.toLowerCase() | +                  : entity.cryptoRoute.user.userData.language?.symbol.toLowerCase(),
David: `entity.user.userData.language?.symbol.toLowerCase()`

--- PR #442 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context: +              }), | +              userAddressTrimmed: Util.trimBlockchainAddress( | +                entity.buy ? entity.buy.user.address : entity.cryptoRoute.user.address,
David: `entity.user.address`

--- PR #442 | src/payment/models/buy-crypto/services/buy-crypto.service.ts ---
Context: +      entity.amlCheck === AmlCheck.PENDING && update.amlCheck && update.amlCheck !== AmlCheck.PENDING | +        ? await this.buyCryptoRepo.save({ ...update, ...entity, amlCheck: update.amlCheck, mailSendDate: null }) | +        : await this.buyCryptoRepo.save({ ...update, ...entity });
David: Could use something like
```ts
const amlUpdate = entity.amlCheck === AmlCheck.PENDING && update.amlCheck && update.amlCheck !== AmlCheck.PENDING ? {amlCheck: update.amlCheck, mailSendDate: null } : undefined;
entity = await this.buyCryptoRepo.save({ ...update, ...entity, ...amlUpdate });
```

--- PR #442 | src/payment/models/buy-fiat/buy-fiat.service.ts ---
Context: +            mailReturnSendDate: null, | +          }) | +        : await this.buyFiatRepo.save({ ...update, ...entity });
David: Same here
```ts
const amlUpdate = entity.amlCheck === AmlCheck.PENDING && update.amlCheck && update.amlCheck !== AmlCheck.PENDING ? { amlCheck: update.amlCheck, mail2SendDate: null, mailReturnSendDate: null } : undefined;
entity = await this.buyFiatRepo.save({ ...update, ...entity, ...amlUpdate });

--- PR #443 | src/shared/util.ts ---
Context: +    ); | +  } | +
David: I would move this to sell.controller, it's sell specific logic.

--- PR #443 | src/shared/util.ts ---
Context: +    ); | +  } | +
David: Maybe a bit simpler?
```ts
return this.transformToMinDeposit(index[0] ?? Config.blockchain.default.minTransactionVolume.default);
```

--- PR #443 | src/shared/util.ts ---
Context: +    ); | +  } | +
David: And rename `index` to `minVolume` or similar?

--- PR #446 | src/blockchain/shared/evm/evm-client.ts ---
Context: +    const contract = this.getERC20Contract(token.chainId); | +    const decimals = await contract.decimals(); | +    const targetAmount = ethers.utils.parseUnits(`${amount}`, decimals);
David: Should we move this unit parsing thingy into a helper method?

--- PR #446 | src/payment/models/payout/strategies/payout/impl/defichain-dfi.strategy.ts ---
Context:  @Injectable() | -export class PayoutDeFiChainDFIStrategy extends PayoutDeFiChainStrategy { | +export class DeFiChainDfiStrategy extends JellyfishStrategy {
David: Shouldn't it be DeFiChainCryptoStrategy?

--- PR #446 | src/payment/models/dex/strategies/purchase-liquidity/purchase-liquidity.facade.ts ---
Context: +      if (assetCategory === AssetCategory.CRYPTO) return Alias.BSC_CRYPTO; | +      if (assetCategory === AssetCategory.STOCK) return Alias.BSC_TOKEN; | +    }
David: Perhaps we need to discuss this. From what I saw, BSC_CRYPTO is for native BNB and BSC_TOKEN for all BEP-20 tokens, is this right? Then the differentiation based on AssetCategory is not really fitting 🤔 
Same for other facades and Ethereum blockchain.

--- PR #446 | src/payment/models/dex/strategies/purchase-liquidity/purchase-liquidity.facade.ts ---
Context: +      if (assetCategory === AssetCategory.CRYPTO) return Alias.BSC_CRYPTO; | +      if (assetCategory === AssetCategory.STOCK) return Alias.BSC_TOKEN; | +    }
David: It seems to be very similar for all chains. There are the native asset and multiple wrapped tokens, which need to be handled differently.

--- PR #447 | src/payment/models/buy-crypto/entities/buy-crypto.entity.ts ---
Context:        ? 'mail.payment.deposit.buyCryptoCrypto' | -      : 'mail.payment.deposit.buyCryptoFiat'; | +      : this.amlCheck !== AmlCheck.PENDING && this.amlCheck !== AmlCheck.FAIL
David: `this.amlCheck === AmlCheck.PASS`?

--- PR #447 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context: +    }); | + | +    entities.length > 0 && console.log(`Sending ${entities.length} 'payback to address' email(s)`);
David: Wrong log `payback to address`

--- PR #447 | src/payment/models/buy-crypto/entities/buy-crypto.entity.ts ---
Context:    get translationKey(): string { | -    return this.inputReferenceAsset === this.outputReferenceAsset | +    return this.inputReferenceAsset === this.outputReferenceAsset && this.amlCheck !== AmlCheck.PENDING
David: Do we send `buyCryptoCrypto` notifications, even when AML check is failed?

--- PR #447 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context: +          { id: entity.id }, | +          { mailSendDate: entity.mailSendDate, recipientMail: entity.recipientMail }, | +        );
David: Use the new update logic from develop.

--- PR #447 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context: + | +    for (const entity of entities) { | +      if (entity.amlReason !== AmlReason.ANNUAL_LIMIT && entity.amlReason !== AmlReason.DAILY_LIMIT) continue;
David: I would do this check directly in SQL,

--- PR #447 | src/payment/models/buy-crypto/entities/buy-crypto.entity.ts ---
Context: +      : this.amlCheck === AmlCheck.FAIL | +      ? 'mail.payment.deposit.paybackInitiated' | +      : null;
David: Should we return null, or rather throw an error? Users might get weird emails, if we return null here.

--- PR #447 | src/payment/models/buy-crypto/entities/buy-crypto.entity.ts ---
Context: +      : this.amlCheck === AmlCheck.FAIL | +      ? 'mail.payment.deposit.paybackInitiated' | +      : null;
David: Perhaps restructure it a bit?
```ts
if (this.amlCheck === AmlCheck.PASS) {
   return this.inputReferenceAsset === this.outputReferenceAsset ? 'mail.payment.deposit.buyCryptoCrypto' : 'mail.payment.deposit.buyCryptoFiat';
} else if (this.amlCheck === AmlCheck.PENDING) {
   if (this.amlReason === AmlReason.DAILY_LIMIT) return 'mail.payment.pending.startKyc';
   if (this.amlReason === AmlReason. ANNUAL_LIMIT) return 'mail.payment.pending.increaseLimit';
} else {
   return 'mail.payment.deposit.paybackInitiated';
}
throw new Error('some-error-message');
```

--- PR #447 | src/shared/i18n/de/mail.json ---
Context: +        "body": "<table style=\"font-family:Open Sans,Helvetica,Arial,sans-serif;width:60%;padding:8px;font-size:85%\" align=\"center\"><tr><td colspan=\"3\" align=\"center\">Bitte beantrage daher ein neues Jahreslimit</td></tr><tr><td colspan=\"3\" align=\"center\">Sobald das neue Limit genehmigt wurde, <br> wird deine Transaktion weiter bearbeitet.</td></tr><tr><td colspan=\"3\" align=\"center\">Limit erhöhen?</td></tr><tr><td colspan=\"3\" align=\"center\">In der DFX App per \"Staking\" die Paymentseite aufrufen.</td></tr><tr><td colspan=\"3\" align=\"center\">Bei \"Dein Limit\" auf den roten Pfeil gehen und <br> ein neues gewünschtes Limit beantragen.</td></tr><tr><td colspan=\"3\" align=\"center\">Fragen? - Kontaktiere bitte support@dfx.swiss</td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr></tr><tr style=\"font-family:Open Sans,Helvetica,Arial,sans-serif\"><td colspan=\"3\" align=\"center\">Herzlichen Dank für dein entgegengebrachtes Vertrauen</td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"font-family:Open Sans,Helvetica,Arial,sans-serif\"><td colspan=\"3\" align=\"center\">Dein freundliches DFX Team</td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr style=\"padding:1px\"><td colspan=\"3\"> </td></tr><tr><td colspan=\"3\" align=\"center\"><span style=\"font-family:Zapfino\">Bitcoiners by heart ♥️</span></td></tr></table>", | +        "title": "Jährliches Limit überschritten" | +      }
David: Why not use dailyLimit/annualLimit as keys?

--- PR #447 | src/payment/models/buy-crypto/services/buy-crypto-notification.service.ts ---
Context: -      if (entity.amlReason !== AmlReason.ANNUAL_LIMIT && entity.amlReason !== AmlReason.DAILY_LIMIT) continue; |        try { |          entity.confirmSentMail();
David: Can be removed

--- PR #447 | src/payment/models/buy-crypto/entities/buy-crypto.entity.ts ---
Context:   | -    return null; | +    throw new Error('buyCrypto mail error: missing translationKey');
David: ```ts
throw new Error(`Tried to send a mail for BuyCrypto ${this.id} in invalid state`);
```

--- PR #448 | src/payment/models/bank-tx/dto/update-bank-tx.dto.ts ---
Context: +  @ValidateNested() | +  @Type(() => EntityDto) | +  chargebackBankTx: BankTx;
David: Should we just use the chargebackBankTxId here (same as buyId in case of BuyCrypto)?

--- PR #448 | src/payment/models/bank-tx-return/bank-tx-return.service.ts ---
Context: +    entity = this.bankTxReturnRepo.create(bankTxReturn); | + | +    entity.chargebackBankTx = chargebackBankTx;
David: ```ts
entity = this.bankTxReturnRepo.create({ ...bankTxReturn, chargebackBankTx });
```

--- PR #448 | src/payment/models/bank-tx-return/bank-tx-return.service.ts ---
Context:    async create(bankTxReturn: BankTxReturnInterface): Promise<BankTxReturn> { | -    const entity = this.bankTxReturnRepo.create(bankTxReturn); | +    let entity = await this.bankTxReturnRepo.findOne({ where: { bankTx: bankTxReturn.bankTx } });
David: I would only search by ID, everything else might make the check useless and slows it down.

--- PR #448 | src/payment/models/bank-tx-return/bank-tx-return.service.ts ---
Context: +    if (!chargebackBankTx) throw new BadRequestException('ChargebackBankTx not found'); | + | +    entity = await this.bankTxReturnRepo.findOne({ where: { chargebackBankTx } });
David: Same here.

--- PR #450 | src/payment/models/bank-tx-return/bank-tx-return.service.ts ---
Context: +      if (!update.chargebackBankTx) throw new BadRequestException('ChargebackBankTx not found'); | + | +      const entity = await this.bankTxReturnRepo.findOne({
David: I would not name the inner variable equal to the one in the outer context, can cause confusion 🤔

--- PR #453 | src/admin/admin.controller.ts ---
Context: +  customCols?: string[]; | +} | +
David: Did you test the code? 😉  I nowhere see a mapping from filterCols (in DTO) to customCols in interface.

--- PR #455 | src/config/config.ts ---
Context:        signOptions: { |          expiresIn: process.env.JWT_EXPIRES_IN ?? 172800, | +        companyExpiresIn: process.env.JWT_EXPIRES_IN_COMPANY ?? 30000,
David: These are 8 hours?

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: +    return ( | +      new Date().getTime() - challenge.created.getTime() <= | +      Number.parseInt(Config.auth.challenge.company.expiresIn.toString())
David: Why this detour via string?

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: +    return ( | +      new Date().getTime() - challenge.created.getTime() <= | +      Number.parseInt(Config.auth.challenge.company.expiresIn.toString())
David: Might be easier to use `Util.secondsDiff`

--- PR #455 | src/config/config.ts ---
Context: +    challenge: { | +      company: { | +        expiresIn: process.env.COMPANY_CHALLENGE_EXPIRES_IN ?? 10000,
David: I would specify the time in seconds (as for JWT options)

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: +  checkChallengeList() { | +    const keyList = this.challengeList.entries(); | +    for (const [key, challenge] of keyList) {
David: ```ts
for (const [key, challenge] of this.challengeList.entries()) {
```

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: +  } | + | +  private checkChallengeToken(challenge: ChallengeData): boolean {
David: Rename to `isChallengeValid` (makes it clear, what the response value means)

--- PR #455 | src/user/models/auth/dto/auth-company-credentials.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsString() | +  challengeHash: string;
David: It's not a hash but a signature. So we can probably just reuse the normal AuthCredentialsDto?

--- PR #455 | src/user/models/kyc/kyc-process.service.ts ---
Context: -    return await this.goToStatus(userData, KycStatus.CHATBOT); | +    const users = await this.userRepo.find({ where: { userData: { id: userData.id } }, relations: ['wallet'] }); | +    const lockUser = users.find((e) => e.wallet.name === 'LOCK.space');
David: Can be done in a single SQL Query (with findOne)

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: +    const signatureMessage = this.getSignMessage(dto.address); | + | +    if (!this.verifySignature(signatureMessage.message, dto.address, dto.signature)) {
David: ```ts
    const { message } = this.getSignMessage(dto.address);
    if (!this.verifySignature(message, dto.address, dto.signature)) {
```

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: +    const signatureMessage = this.getSignMessage(address); | + | +    const credentialsValid = this.verifySignature(signatureMessage.message, address, signature);
David: ```ts
    const { message } = this.getSignMessage(dto.address);
    if (!this.verifySignature(message, dto.address, dto.signature)) {
```

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: -    const signatureMessage = this.getCompanySignMessage(address); | -    return this.cryptoService.verifySignature(signatureMessage.message, address, signature); | +  private verifyCompanyChallengeHash(dto: AuthCompanyCredentialsDto): boolean {
David: `verifyCompanySignature`

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: +  private verifyCompanyChallengeHash(dto: AuthCompanyCredentialsDto): boolean { | +    const challengeData = this.challengeList.get(dto.address); | +    if (!challengeData || !this.checkChallengeToken(challengeData))
David: Move the null-check to checkChallengeToken?

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: +    const challengeData = this.challengeList.get(dto.address); | +    if (!challengeData || !this.checkChallengeToken(challengeData)) | +      throw new ConflictException('key/challenge Token is not valid anymore');
David: I think I would also throw Unauthorized, or BadRequest and use `Challenge invalid` as a message.

--- PR #455 | src/user/models/kyc/kyc-webhook.service.ts ---
Context: +                : userData.kycStatus === KycStatus.REJECTED | +                ? KycWebhookStatus.REJECTED | +                : KycWebhookStatus.NA,
David: Perhaps a separate method for this to restructure it a bit and make it more readable? E.g.
```ts
if (KycCompleted(...)) {
   return spiderData?.chatbotResult ? KycWebhookStatus.FULL : KycWebhookStatus.LIGHT;
} else if (...) {
   ...
} else {
   return KycWebhookStatus.NA;
}
```

--- PR #455 | src/user/models/kyc/kyc-webhook.service.ts ---
Context: -          headers: { 'x-api-key': Config.lock.apiKey }, | +        const apiKey = this.walletService.getApiKeyInternal(user.wallet.name); | +        if (!apiKey) throw new ConflictException(`ApiKey for wallet ${user.wallet.name} not available`);
David: I would throw a standard error (`throw new Error(...)`), because this method is normally called from within our API. So we don't need HTTP codes.

--- PR #455 | src/user/models/kyc/kyc.service.ts ---
Context:   | +    const apiKey = this.walletService.getApiKeyInternal(wallet.name); | +    if (!apiKey) throw new ConflictException(`ApiKey for wallet ${wallet.name} not available`);
David: About Conflict (HTTP 409):
![image](https://user-images.githubusercontent.com/85513542/196709212-d0d01206-43fc-4911-a376-a29110a1cecb.png)
=> it's probably rather an InternalServerError

--- PR #455 | src/user/models/wallet/wallet.service.ts ---
Context: -  // } | +  async getAllExternalServices(): Promise<Wallet[]> { | +    return await this.walletRepo.find({ where: { apiUrl: Not(IsNull()), isKycClient: true } });
David: API URL can be null IMO. Not all external services might want to use webhooks.

--- PR #455 | src/user/models/wallet/wallet.controller.ts ---
Context: +          : KycCompleted(user.userData.kycStatus) | +          ? KycWebhookStatus.LIGHT | +          : KycWebhookStatus.NA,
David: This logic is shared with KycWebhookService. Maybe we can share it somehow?

--- PR #455 | src/user/models/wallet/wallet.controller.ts ---
Context: +    return { | +      ...wallet, | +    };
David: I think we need an explicit mapping here. Otherwise the API will return all fields of the wallet (I think).

--- PR #455 | src/user/models/wallet/wallet.controller.ts ---
Context: + | +  private async toKycDataDto(user: User): Promise<KycDataDto> { | +    const spiderData = await this.spiderRepo.findOne({ where: { userData: { id: user.userData.id } } });
David: SpiderData can be included in the original SQL query I think?

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: + | +  private isChallengeValid(challenge: ChallengeData): boolean { | +    return !challenge ? false : Util.secondsDiff(new Date(), challenge.created) <= Config.auth.challenge.expiresIn;
David: ```ts
return challenge && Util.secondsDiff(new Date(), challenge.created) <= Config.auth.challenge.expiresIn;
```

--- PR #455 | src/user/models/auth/auth.service.ts ---
Context: + | +  private isChallengeValid(challenge: ChallengeData): boolean { | +    return !challenge ? false : Util.secondsDiff(new Date(), challenge.created) <= Config.auth.challenge.expiresIn;
David: `Util.secondsDiff(new Date(), challenge.created)` will be negative. I think you need to invert it.

--- PR #455 | src/user/models/kyc/kyc-process.service.ts ---
Context:    async startKycProcess(userData: UserData): Promise<UserData> { | -    return await this.goToStatus(userData, KycStatus.CHATBOT); | +    const lockUser = userData.users.find((e) => e.wallet.name === 'LOCK.space');
David: Can we guarantee that users are loaded from DB?

--- PR #455 | src/user/models/kyc/kyc-process.service.ts ---
Context:   | -    if (status === KycStatus.MANUAL) { | +    const externalUser = userData.users.find((e) => e.wallet.isKycClient === true);
David: This logic could be moved to a method in UserData entity? We could then reuse it everywhere.

--- PR #455 | thunder-tests/thunderclient.json ---
Context: +    "body": { | +      "type": "json", | +      "raw": "{\n    \"address\": \"8an2Mi4dCV8GfiqbF2i2pJwDetq87AhFa4\",\n    \"signature\": \"HzRZfDlmtAp4ywy7MuGpwCEEGoemH6rB82VNJUaKM7P1PCmiBRo2MyuuYfIBKE/nakafSymO8ep+84AGIzeEm/I=\"\n}",
David: Should we remove these credentials? 😉  (same for challenge request)

--- PR #455 | src/user/models/user-data/user-data.entity.ts ---
Context: +    const externalUser = this.users.find((e) => e.wallet.isKycClient === true); | +    return externalUser ? true : false; | +  }
David: Cool!

--- PR #455 | src/user/models/user-data/user-data.entity.ts ---
Context: +    const externalUser = this.users.find((e) => e.wallet.isKycClient === true); | +    return externalUser ? true : false; | +  }
David: Simpler
```ts
return this.users.find((e) => e.wallet.isKycClient === true) != null;
```
or
```ts
return !!this.users.find((e) => e.wallet.isKycClient === true);
```

--- PR #458 | src/user/services/spider/spider-sync.service.ts ---
Context: +    if (!version) return null; | + | +    return { document, version: version.name, part };
David: Use a separate method for document fetch:
```ts
  private async getCompletedIdentDocument(
    userData: UserData,
    document: KycDocument,
    contentType: KycContentType,
  ): Promise<{ document: KycDocument; version: string; part: DocumentVersionPart }> {
    const version = await this.spiderApi.getDocumentVersion(userData.id, false, document, KycDocumentState.COMPLETED);
    if (!version) return null;

    const part = await this.spiderApi
      .getDocumentVersionParts(userData.id, false, document, version.name)
      .then((parts) => parts.find((p) => p.contentType === contentType));
    if (!part) return null;

    return { document, version: version.name, part };
```
and use it like 
```ts
...
let result = await this.getCompletedIdentDocument(...);
if (!result) {
   // fallback ...
}
```

--- PR #462 | src/user/models/user/user.service.ts ---
Context:   |    // --- FEES --- // |    async getUserBuyFee(userId: number, annualVolume: number): Promise<{ fee: number; refBonus: number }> {
David: Fix unit tests

--- PR #466 | src/statistic/statistic.service.ts ---
Context: -          ref: await this.userService.getTotalRefRewards(), | +          staking: Util.round(await this.stakingService.getTotalStakingRewards(), 2), | +          ref: Util.round(await this.userService.getTotalRefRewards(), 2),
David: Should we use `Config.defaultVolumeDecimal`?

--- PR #468 | src/payment/models/buy-fiat/buy-fiat-notification.service.ts ---
Context:    } | + | +  async pendingBuyCrypto(): Promise<void> {
David: Naming can be improved 😉

--- PR #468 | src/payment/models/buy-fiat/buy-fiat-notification.service.ts ---
Context: +                  : 'mail.payment.pending.annualLimit', | +              translationParams: { | +                hashLink: `https://payment.dfx.swiss/kyc?code=${entity.sell.user.userData.kycHash}`,
David: Use `Config.payment.url` (also in buy-crypto-notification)

--- PR #472 | src/payment/models/sell/dto/create-sell.dto.ts ---
Context: + | +  @ApiProperty() | +  @IsOptional()
David: Is it possible to change this to IsNotEmpty?

--- PR #472 | src/payment/models/sell/dto/get-sell-payment-info.dto.ts ---
Context: + | +  @ApiProperty() | +  @IsOptional()
David: Same here?

--- PR #474 | src/payment/models/buy-crypto/dto/update-buy-crypto.dto.ts ---
Context: -  outputAsset: string; | +  @Type(() => Asset) | +  outputAsset: Asset;
David: We should use only IDs here (for Google Sheets).

--- PR #474 | src/payment/models/buy-crypto/entities/buy-crypto-fees.entity.ts ---
Context: +    entity.estimatePurchaseFeePercent = Util.round(purchaseFeeAmount / transaction.outputReferenceAmount, 8); | +    entity.estimatePayoutFeeAmount = payoutFeeAmount; | +    entity.estimatePayoutFeePercent = Util.round(payoutFeeAmount / transaction.outputReferenceAmount, 8);
David: Use the methods below?

--- PR #474 | src/payment/models/buy-crypto/services/buy-crypto-batch.service.ts ---
Context: +      return 0; | +    } | +  }
David: These are the same, aren't they?

--- PR #474 | src/payment/models/buy-crypto/entities/buy-crypto-batch.entity.ts ---
Context: +    const feeRatio = Util.round((purchaseFeeAmount + payoutFeeAmount) / this.outputReferenceAmount, 8); | + | +    if (feeRatio > 0.001) {
David: Should we move this ratio somewhere more central?

--- PR #474 | src/payment/models/buy-crypto/services/buy-crypto-out.service.ts ---
Context:          } |   | +        // TODO - refactor process so not rely on DuplicatedEntryException when retrying half-successful batch
David: What about this TODO?

--- PR #474 | src/payment/models/dex/services/dex-defichain.service.ts ---
Context: +      console.warn( | +        `Could not find purchasable amount for swapAsset: ${swapAsset.dexName}, targetAsset: ${targetAsset.dexName}`, | +      );
David: Return `undefined` in case of an error?

--- PR #474 | src/payment/models/dex/strategies/check-liquidity/impl/defichain-default.strategy.ts ---
Context: +      dexName: 'DFI', | +      blockchain: Blockchain.DEFICHAIN, | +      type: AssetType.COIN,
David: On DeFiChain the swap asset is actually the token.

--- PR #474 | src/payment/models/payout/services/payout-bitcoin.service.ts ---
Context: -    return transaction && transaction.blockhash && transaction.confirmations > 0; | +    const isComplete = transaction && transaction.blockhash && transaction.confirmations > 0; | +    const payoutFee = isComplete ? Util.round(-transaction.fee / 100000000, 8) : 0;
David: Fee is negative?

--- PR #474 | src/payment/models/pricing/services/pricing.service.ts ---
Context:      ); | + | +    //*** INDICATIVE PRICES ***//
David: Where do we need the indicative prices?

--- PR #474 | src/payment/models/pricing/services/dfi-pricing-dex.service.ts ---
Context: -      referenceAsset: from, | +      referenceAsset: fromAsset, |        referenceAmount: 0.001,
David: Should we use 0.001 in case of BTC and 1 else? As we have it at another place?

