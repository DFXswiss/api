{"pr": 269, "path": "src/payment/models/buy-crypto/utils/buy-crypto-chain.util.ts", "body": "Instead of currentHeight you could just use height + 1.", "diff_hunk": "@@ -0,0 +1,70 @@\n+import { InWalletTransaction } from '@defichain/jellyfish-api-core/dist/category/wallet';\n+import { Injectable } from '@nestjs/common';\n+import { NodeClient } from 'src/ain/node/node-client';\n+import { NodeService } from 'src/ain/node/node.service';\n+import { Config } from 'src/config/config';\n+import { BuyCryptoBatch } from '../entities/buy-crypto-batch.entity';\n+import { BuyCryptoBatchRepository } from '../repositories/buy-crypto-batch.repository';\n+import { BuyCryptoReposito", "line": 29}
{"pr": 269, "path": "src/payment/models/buy-crypto/utils/buy-crypto-chain.util.ts", "body": "Getting the block is not needed anymore.", "diff_hunk": "@@ -0,0 +1,70 @@\n+import { InWalletTransaction } from '@defichain/jellyfish-api-core/dist/category/wallet';\n+import { Injectable } from '@nestjs/common';\n+import { NodeClient } from 'src/ain/node/node-client';\n+import { NodeService } from 'src/ain/node/node.service';\n+import { Config } from 'src/config/config';\n+import { BuyCryptoBatch } from '../entities/buy-crypto-batch.entity';\n+import { BuyCryptoBatchRepository } from '../repositories/buy-crypto-batch.repository';\n+import { BuyCryptoReposito", "line": 54}
{"pr": 269, "path": "src/payment/models/buy-crypto/utils/buy-crypto-chain.util.ts", "body": "Is this method really belonging to chain-util?", "diff_hunk": "@@ -0,0 +1,70 @@\n+import { InWalletTransaction } from '@defichain/jellyfish-api-core/dist/category/wallet';\n+import { Injectable } from '@nestjs/common';\n+import { NodeClient } from 'src/ain/node/node-client';\n+import { NodeService } from 'src/ain/node/node.service';\n+import { Config } from 'src/config/config';\n+import { BuyCryptoBatch } from '../entities/buy-crypto-batch.entity';\n+import { BuyCryptoBatchRepository } from '../repositories/buy-crypto-batch.repository';\n+import { BuyCryptoReposito", "line": 36}
{"pr": 357, "path": "src/shared/util.ts", "body": "Just a suggestion:\r\n```ts\r\nstatic transformToMinDeposit(deposit: { [asset: string]: number }, filter: string[] | string = []): MinDeposit[] {\r\n   filter = [].concat(filter);\r\n   return Object.entries(deposit)\r\n      .filter(([key, _]) => filter.includes(key))\r\n      .map(([key, value]) => ({ asset: key, amount: value }));\r\n}\r\n```", "diff_hunk": "@@ -161,4 +162,16 @@ export class Util {\n   static trimIBAN(iban: string): string {\n     return '***' + iban.slice(iban.length - 4);\n   }\n+\n+  static transformToMinDeposit(deposit: any, filter?: string[] | string): MinDeposit[] {\n+    return Object.keys(deposit)\n+      .filter((key) => {\n+        if (typeof filter === 'string') return filter === key;\n+        if (typeof filter === 'object') return filter.includes(key);\n+        return true;\n+      })\n+      .map((key) => {\n+        return { amou", "line": 176}
{"pr": 357, "path": "src/shared/util.ts", "body": "Oh yes, you're completely right \ud83d\ude09", "diff_hunk": "@@ -161,4 +162,16 @@ export class Util {\n   static trimIBAN(iban: string): string {\n     return '***' + iban.slice(iban.length - 4);\n   }\n+\n+  static transformToMinDeposit(deposit: any, filter?: string[] | string): MinDeposit[] {\n+    return Object.keys(deposit)\n+      .filter((key) => {\n+        if (typeof filter === 'string') return filter === key;\n+        if (typeof filter === 'object') return filter.includes(key);\n+        return true;\n+      })\n+      .map((key) => {\n+        return { amou", "line": 176}
{"pr": 395, "path": "src/payment/models/pricing/utils/price-path.ts", "body": "Would perhaps be easier with if-else instead of break/continue.", "diff_hunk": "@@ -0,0 +1,76 @@\n+import { cloneDeep } from 'lodash';\n+import { Price } from '../../exchange/dto/price.dto';\n+import { PriceRequest, PriceResult, PriceStepResult } from '../interfaces';\n+import { PricingPathAlias } from '../services/pricing.service';\n+import { PricePathInitSpecification } from '../specifications/price-path-init.specification';\n+import { PriceStep } from './price-step';\n+\n+export class PricePath {\n+  constructor(public readonly alias: PricingPathAlias, private readonly steps: Pri", "line": 34}
{"pr": 395, "path": "src/payment/models/pricing/utils/price-path.ts", "body": "Perhaps this should be moved to Price class, as it is used in multiple places.", "diff_hunk": "@@ -0,0 +1,76 @@\n+import { cloneDeep } from 'lodash';\n+import { Price } from '../../exchange/dto/price.dto';\n+import { PriceRequest, PriceResult, PriceStepResult } from '../interfaces';\n+import { PricingPathAlias } from '../services/pricing.service';\n+import { PricePathInitSpecification } from '../specifications/price-path-init.specification';\n+import { PriceStep } from './price-step';\n+\n+export class PricePath {\n+  constructor(public readonly alias: PricingPathAlias, private readonly steps: Pri", "line": 69}
{"pr": 395, "path": "src/payment/models/pricing/utils/price-step.ts", "body": "Why not like this (only initialize the options in constructor)?\r\n```ts\r\nthis.options = {\r\n   from: options.from || 'input',\r\n   to: options.to || 'output',\r\n   ...\r\n};\r\n```", "diff_hunk": "@@ -0,0 +1,165 @@\n+import { cloneDeep } from 'lodash';\n+import { Price } from '../../exchange/dto/price.dto';\n+import { PriceMismatchException } from '../../exchange/exceptions/price-mismatch.exception';\n+import { PriceStepResult, PriceProvider, PriceProviderName } from '../interfaces';\n+import { PriceStepInitSpecification } from '../specifications/price-step-init.specification';\n+\n+export interface PriceStepOptions {\n+  from?: string | 'input';\n+  to?: string | 'output';\n+  referenceTo?: string", "line": 31}
{"pr": 395, "path": "src/payment/models/pricing/utils/price-step.ts", "body": "Should we move the validation to the constructor (same as path)?", "diff_hunk": "@@ -0,0 +1,165 @@\n+import { cloneDeep } from 'lodash';\n+import { Price } from '../../exchange/dto/price.dto';\n+import { PriceMismatchException } from '../../exchange/exceptions/price-mismatch.exception';\n+import { PriceStepResult, PriceProvider, PriceProviderName } from '../interfaces';\n+import { PriceStepInitSpecification } from '../specifications/price-step-init.specification';\n+\n+export interface PriceStepOptions {\n+  from?: string | 'input';\n+  to?: string | 'output';\n+  referenceTo?: string", "line": 56}
{"pr": 395, "path": "src/payment/models/pricing/utils/price-step.ts", "body": "Is this correct? Don't we leave the for loop?", "diff_hunk": "@@ -0,0 +1,165 @@\n+import { cloneDeep } from 'lodash';\n+import { Price } from '../../exchange/dto/price.dto';\n+import { PriceMismatchException } from '../../exchange/exceptions/price-mismatch.exception';\n+import { PriceStepResult, PriceProvider, PriceProviderName } from '../interfaces';\n+import { PriceStepInitSpecification } from '../specifications/price-step-init.specification';\n+\n+export interface PriceStepOptions {\n+  from?: string | 'input';\n+  to?: string | 'output';\n+  referenceTo?: string", "line": 147}
{"pr": 443, "path": "src/shared/util.ts", "body": "I would move this to sell.controller, it's sell specific logic.", "diff_hunk": "@@ -171,6 +173,16 @@ export class Util {\n     return '***' + iban.slice(iban.length - 4);\n   }\n \n+  static getMinDeposit(outputAsset: string): MinDeposit[] {\n+    const index = Object.entries(Config.blockchain.default.minTransactionVolume)\n+      .filter(([key, _]) => key === outputAsset)\n+      .map(([_, value]) => value);\n+\n+    return this.transformToMinDeposit(\n+      index.length != 0 ? index[0] : Config.blockchain.default.minTransactionVolume.default,\n+    );\n+  }\n+", "line": 185}
{"pr": 443, "path": "src/shared/util.ts", "body": "Maybe a bit simpler?\r\n```ts\r\nreturn this.transformToMinDeposit(index[0] ?? Config.blockchain.default.minTransactionVolume.default);\r\n```", "diff_hunk": "@@ -171,6 +173,16 @@ export class Util {\n     return '***' + iban.slice(iban.length - 4);\n   }\n \n+  static getMinDeposit(outputAsset: string): MinDeposit[] {\n+    const index = Object.entries(Config.blockchain.default.minTransactionVolume)\n+      .filter(([key, _]) => key === outputAsset)\n+      .map(([_, value]) => value);\n+\n+    return this.transformToMinDeposit(\n+      index.length != 0 ? index[0] : Config.blockchain.default.minTransactionVolume.default,\n+    );\n+  }\n+", "line": 185}
{"pr": 443, "path": "src/shared/util.ts", "body": "And rename `index` to `minVolume` or similar?", "diff_hunk": "@@ -171,6 +173,16 @@ export class Util {\n     return '***' + iban.slice(iban.length - 4);\n   }\n \n+  static getMinDeposit(outputAsset: string): MinDeposit[] {\n+    const index = Object.entries(Config.blockchain.default.minTransactionVolume)\n+      .filter(([key, _]) => key === outputAsset)\n+      .map(([_, value]) => value);\n+\n+    return this.transformToMinDeposit(\n+      index.length != 0 ? index[0] : Config.blockchain.default.minTransactionVolume.default,\n+    );\n+  }\n+", "line": 185}
{"pr": 785, "path": "src/integration/blockchain/shared/util/blockchain.util.ts", "body": "<img width=\"506\" alt=\"image\" src=\"https://github.com/DFXswiss/api/assets/85513542/cf77c9ac-3732-423e-b104-89ced10fd8bb\">\r\n\r\nThese changes can be reverted, Lightning is already there.", "diff_hunk": "", "line": 1}
{"pr": 839, "path": "src/shared/utils/util.ts", "body": "This looks like some mail-specific logic, maybe move it there?", "diff_hunk": "@@ -331,4 +333,8 @@ export class Util {\n   static trim({ value }: TransformFnParams): string {\n     return value.split(' ').join('');\n   }\n+\n+  static parseMailKey(mailKey: MailTranslationKey, amlReason: AmlReason): string {\n+    return `${mailKey}.${amlReason.replace(/([a-z])([A-Z])/g, '$1_$2').toLowerCase()}`;\n+  }", "line": 339}
{"pr": 897, "path": "src/shared/utils/util.ts", "body": "Rename to a more general name, maybe `mapBooleanQuery`?", "diff_hunk": "@@ -353,4 +353,8 @@ export class Util {\n   static trim({ value }: TransformFnParams): string | undefined {\n     return value?.split(' ').join('');\n   }\n+\n+  static mapHistoryQueryDto({ value }: TransformFnParams): boolean | undefined {\n+    return Boolean(value || value === '');\n+  }", "line": 359}
{"pr": 920, "path": "src/shared/utils/util.ts", "body": "Use generic names (not fee \ud83d\ude09 )", "diff_hunk": "@@ -29,14 +29,22 @@ export class Util {\n     return list.reduce((prev, curr) => prev + curr, 0);\n   }\n \n-  static sumObj<T>(list: T[], key: KeyType<T, number>): number {\n+  static sumObjValue<T>(list: T[], key: KeyType<T, number>): number {\n     return this.sum(list.map((i) => i[key] as unknown as number));\n   }\n \n-  static minObj<T>(list: T[], key: KeyType<T, number>): number {\n+  static minObjValue<T>(list: T[], key: KeyType<T, number>): number {\n     return Math.min(...list.map((i) => i[key] ", "line": 46}
{"pr": 920, "path": "src/shared/utils/util.ts", "body": "This code currently takes the last element if there are multiple elements with the same value. I would take the first element in this case.", "diff_hunk": "@@ -29,14 +29,22 @@ export class Util {\n     return list.reduce((prev, curr) => prev + curr, 0);\n   }\n \n-  static sumObj<T>(list: T[], key: KeyType<T, number>): number {\n+  static sumObjValue<T>(list: T[], key: KeyType<T, number>): number {\n     return this.sum(list.map((i) => i[key] as unknown as number));\n   }\n \n-  static minObj<T>(list: T[], key: KeyType<T, number>): number {\n+  static minObjValue<T>(list: T[], key: KeyType<T, number>): number {\n     return Math.min(...list.map((i) => i[key] ", "line": 46}
{"pr": 1118, "path": "src/shared/utils/util.ts", "body": "I think the check `visibleLength > value.length ? value.length : visibleLength` is not required.", "diff_hunk": "@@ -378,6 +378,11 @@ export class Util {\n     return '***' + value.slice(value.length - visibleLength);\n   }\n \n+  static blankMail(value: string, visibleLength = 4): string {\n+    const mailSplit = value.split('@');\n+    return `${mailSplit[0].slice(0, visibleLength > value.length ? value.length : visibleLength)}***@${mailSplit[1]}`;", "line": 383}
{"pr": 1125, "path": "src/shared/utils/util.ts", "body": "Duplicate checks", "diff_hunk": "@@ -89,6 +89,17 @@ export class Util {\n     );\n   }\n \n+  static arraysHaveSameElements(arr1: string[], arr2: string[], replaceKey?: string, replaceString?: string) {\n+    return replaceKey\n+      ? arr1.every((element) => arr2.includes(element.split(replaceKey).join(replaceString))) ||\n+          arr2.every((element) => arr1.includes(element)) ||\n+          arr1.every((element) => arr2.includes(element)) ||\n+          arr2.every((element) => arr1.includes(element.split(replaceKey).join(replaceSt", "line": 101}
{"pr": 1209, "path": "src/shared/utils/async-cache.ts", "body": "`EVERY_5_MINUTES`", "diff_hunk": "@@ -1,9 +1,18 @@\n import { Util } from './util';\n \n+export enum CacheItemResetPeriod {\n+  EVERY_SECOND = 0,\n+  EVERY_10_SECONDS = 10,\n+  EVERY_30_SECONDS = 30,\n+  EVERY_1_MINUTE = 60,\n+  EVERY_5_MINUTE = 5 * 60,\n+  EVERY_6_HOURS = 3600 * 6,", "line": 9}
{"pr": 1209, "path": "src/shared/utils/async-cache.ts", "body": "`EVERY_SECOND` -> `ALWAYS`?", "diff_hunk": "@@ -1,9 +1,18 @@\n import { Util } from './util';\n \n+export enum CacheItemResetPeriod {\n+  EVERY_SECOND = 0,\n+  EVERY_10_SECONDS = 10,", "line": 5}
{"pr": 1209, "path": "src/shared/utils/async-cache.ts", "body": "Maybe we should have a method `invalidate(id: string)`?", "diff_hunk": "@@ -15,13 +24,22 @@ export class AsyncCache<T> {\n \n     const entry = this.cache.get(id);\n     if (!entry || forceUpdate?.(entry.data) || !(entry.updated > this.expiration)) {\n-      await this.update(id, update, fallbackToCache);\n+      await this.updateInternal(id, update, fallbackToCache);\n     }\n \n     return this.cache.get(id).data;\n   }\n \n-  private async update(id: string, update: () => Promise<T>, fallbackToCache: boolean) {\n+  update(id: string, update: T): void {\n+    if (!id) throw ne", "line": 42}
{"pr": 1209, "path": "src/shared/utils/async-cache.ts", "body": "`this.cache.delete(id)`?", "diff_hunk": "@@ -15,13 +24,19 @@ export class AsyncCache<T> {\n \n     const entry = this.cache.get(id);\n     if (!entry || forceUpdate?.(entry.data) || !(entry.updated > this.expiration)) {\n-      await this.update(id, update, fallbackToCache);\n+      await this.updateInternal(id, update, fallbackToCache);\n     }\n \n     return this.cache.get(id).data;\n   }\n \n-  private async update(id: string, update: () => Promise<T>, fallbackToCache: boolean) {\n+  async invalidate(id?: string): Promise<void> {\n+    if (!id)", "line": 37}
{"pr": 1456, "path": "src/integration/blockchain/shared/util/blockchain.util.ts", "body": "Do we want undefined for coins?", "diff_hunk": "@@ -11,7 +11,7 @@ export function assetExplorerUrl(asset: Asset): string | undefined {\n   if (asset.type === AssetType.COIN) return undefined;\n ", "line": 12}
{"pr": 1456, "path": "src/integration/blockchain/shared/util/blockchain.util.ts", "body": "What if `BlockchainExplorerUrls[asset.blockchain]` is undefined", "diff_hunk": "@@ -11,7 +11,7 @@ export function assetExplorerUrl(asset: Asset): string | undefined {\n   if (asset.type === AssetType.COIN) return undefined;\n \n   const assetPath = assetPaths(asset);\n-  return assetPath ? `${BlockchainExplorerUrls[asset.blockchain]}/${assetPath}` : undefined;\n+  return `${BlockchainExplorerUrls[asset.blockchain]}/${assetPath ?? ''}`;", "line": 14}
{"pr": 1879, "path": "src/shared/utils/util.ts", "body": "`Center`?", "diff_hunk": "@@ -494,6 +494,10 @@ export class Util {\n     );\n   }\n \n+  static blankCentre(value: string, visibleLength = 4): string {\n+    return value.slice(0, visibleLength) + this.blankStart(value, visibleLength);\n+  }", "line": 499}
{"pr": 1879, "path": "src/shared/utils/util.ts", "body": "True, we normally use American english \ud83d\ude09", "diff_hunk": "@@ -494,6 +494,10 @@ export class Util {\n     );\n   }\n \n+  static blankCentre(value: string, visibleLength = 4): string {\n+    return value.slice(0, visibleLength) + this.blankStart(value, visibleLength);\n+  }", "line": 499}
{"pr": 1944, "path": "src/shared/utils/util.ts", "body": "I would move this as a private method to the DfxCronService", "diff_hunk": "@@ -406,6 +409,36 @@ export class Util {\n     return new Promise((resolve) => setTimeout(resolve, ms));\n   }\n \n+  static async cronJobDelay(expression: DfxCronExpression): Promise<void> {\n+    const random = Math.random() * 1000;\n+\n+    switch (expression) {\n+      case CronExpression.EVERY_10_SECONDS:\n+        return Util.delay(random * 5); // 0 .. 5 sec\n+\n+      case CustomCronExpression.EVERY_15_SECONDS:\n+        return Util.delay(random * 5); // 0 .. 5 sec\n+\n+      case CronExpression.EVERY_", "line": 440}
{"pr": 1960, "path": "src/shared/utils/queue-handler.ts", "body": "```ts\r\n  private parallelWork(work: Promise<void>): void {\r\n    this.workParallelCounter++;\r\n\r\n    void work.finally(() => this.workParallelCounter--);\r\n  }\r\n```", "diff_hunk": "@@ -73,11 +80,21 @@ export class QueueHandler {\n       try {\n         if (this.queue.length > 0) {\n           const work = this.queue.shift().doWork(this.itemTimeout);\n-          if (!this.workParallel) await work;\n+          this.workParallel ? await this.checkWorkParallel(work) : await work;\n         } else {\n+          this.workParallelCounter = 0;\n           await Util.delay(10);\n         }\n       } catch {}\n     }\n   }\n+\n+  private async checkWorkParallel(work: Promise<void>): Promise<void>", "line": 99}
{"pr": 1960, "path": "src/shared/utils/queue-handler.ts", "body": "```ts\r\nif (this.queue.length > 0 && this.workParallelCounter < this.maxWorkParallel) {\r\n```", "diff_hunk": "@@ -73,11 +80,21 @@ export class QueueHandler {\n       try {\n         if (this.queue.length > 0) {\n           const work = this.queue.shift().doWork(this.itemTimeout);", "line": 82}
{"pr": 1960, "path": "src/shared/utils/queue-handler.ts", "body": "Remove `workParallel`, `maxWorkParallel = 1`", "diff_hunk": "@@ -40,18 +40,25 @@ export class QueueHandler {\n \n   private isRunning = true;\n \n+  private workParallelCounter = 0;\n+\n   /**\n    * @param queueTimeout Max. item in queue time (incl. execution time)\n    * @param itemTimeout Max. item execution time\n    */\n   constructor(\n     private readonly queueTimeout?: number,\n     private readonly itemTimeout?: number,\n-    private workParallel = false,\n+    private readonly workParallel = false,\n+    private readonly maxWorkParallel = Infinity,", "line": 53}
{"pr": 2107, "path": "src/shared/utils/util.ts", "body": "Why not the same implementation as in services (using `Object.fromEntries`)? \ud83d\ude09  And maybe also allow undefined  entities => return entity?", "diff_hunk": "@@ -425,8 +426,23 @@ export class Util {\n     return new Promise((resolve) => setTimeout(resolve, ms));\n   }\n \n-  static removeNullFields(entity: any): void {\n-    Object.keys(entity).forEach((k) => entity[k] == null && delete entity[k]);\n+  static removeNullFields<T extends object>(entity: T): Partial<T> {\n+    const result = { ...entity };\n+    Object.keys(result).forEach((k) => result[k] == null && delete result[k]);\n+    return result;\n+  }", "line": 433}
{"pr": 2107, "path": "src/shared/utils/util.ts", "body": "Same here", "diff_hunk": "@@ -425,8 +426,23 @@ export class Util {\n     return new Promise((resolve) => setTimeout(resolve, ms));\n   }\n \n-  static removeNullFields(entity: any): void {\n-    Object.keys(entity).forEach((k) => entity[k] == null && delete entity[k]);\n+  static removeNullFields<T extends object>(entity: T): Partial<T> {\n+    const result = { ...entity };\n+    Object.keys(result).forEach((k) => result[k] == null && delete result[k]);\n+    return result;\n+  }\n+\n+  static removeDefaultFields<T extends object>(s", "line": 446}
{"pr": 2146, "path": "src/integration/blockchain/shared/util/blockchain-client.ts", "body": "BtcClient also extends BlockchainClient but does not return EvmSignedTransactionResponse?", "diff_hunk": "@@ -10,5 +10,5 @@ export abstract class BlockchainClient {\n   abstract getTokenBalances(assets: Asset[], address?: string): Promise<BlockchainTokenBalance[]>;\n   abstract isTxComplete(txHash: string, confirmations?: number): Promise<boolean>;\n   abstract getToken(asset: Asset): Promise<Currency>;\n-  abstract sendSignedTransaction(tx: string): Promise<SignedTransactionResponse>;\n+  abstract sendSignedTransaction(tx: string): Promise<EvmSignedTransactionResponse>;", "line": 13}
{"pr": 2148, "path": "src/integration/blockchain/shared/util/blockchain.util.ts", "body": "`[Blockchain.BINANCE_PAY]: undefined,`", "diff_hunk": "@@ -36,6 +36,7 @@ const BlockchainExplorerUrls: { [b in Blockchain]: string } = {\n   [Blockchain.ARWEAVE]: 'https://arscan.io',\n   [Blockchain.CARDANO]: 'https://cardanoscan.io',\n   [Blockchain.RAILGUN]: 'https://railgun-explorer.com',\n+  [Blockchain.BINANCE_PAY]: '',\n };", "line": 40}
{"pr": 2148, "path": "src/integration/blockchain/shared/util/blockchain.util.ts", "body": "Same here", "diff_hunk": "@@ -54,6 +55,7 @@ const TxPaths: { [b in Blockchain]: string } = {\n   [Blockchain.ARWEAVE]: 'tx',\n   [Blockchain.CARDANO]: 'transaction',\n   [Blockchain.RAILGUN]: 'transaction',\n+  [Blockchain.BINANCE_PAY]: '',\n };", "line": 59}
{"pr": 2410, "path": "src/shared/utils/util.ts", "body": "I think this is not yearsDiff since `yearsDiff(new Date('2024-12-31'), new Date('2025-01-01')) = 1`. Maybe just `this. yearsDiff(from, to) / 365`?", "diff_hunk": "@@ -284,6 +284,10 @@ export class Util {\n     return this.secondsDiff(from, to) / (3600 * 24);\n   }\n \n+  static yearsDiff(from?: Date, to?: Date): number {\n+    return to.getFullYear() - from.getFullYear();\n+  }", "line": 289}
{"pr": 2492, "path": "src/shared/utils/util.ts", "body": "This is bad in performance (O(n\u00b2)) for large lists, maybe use a map?\n```TS\nstatic toUniqueList<T>(list: T[], key: KeyType<T, number> | KeyType<T, Date>): T[] {\n  return Array.from(\n    list.reduce((map, item) => map.set(item[key], item), new Map<T[K], T>()).values()\n  );\n}\n```", "diff_hunk": "@@ -116,6 +116,16 @@ export class Util {\n   }\n \n   // --- LISTS --- //\n+\n+  static toUniqueList<T>(list: T[], key: KeyType<T, number> | KeyType<T, Date>): T[] {\n+    return list.reduce((acc, curr) => {\n+      if (!acc.some((item) => item[key] === curr[key])) {\n+        acc.push(curr);\n+      }\n+      return acc;\n+    }, []);\n+  }", "line": 122}
{"pr": 2492, "path": "src/shared/utils/util.ts", "body": "Yes, very good", "diff_hunk": "@@ -116,6 +116,16 @@ export class Util {\n   }\n \n   // --- LISTS --- //\n+\n+  static toUniqueList<T>(list: T[], key: KeyType<T, number> | KeyType<T, Date>): T[] {\n+    return list.reduce((acc, curr) => {\n+      if (!acc.some((item) => item[key] === curr[key])) {\n+        acc.push(curr);\n+      }\n+      return acc;\n+    }, []);\n+  }", "line": 122}
{"pr": 2549, "path": "src/shared/utils/async-cache.ts", "body": "EVERY_6_MONTH**S**", "diff_hunk": "@@ -9,6 +9,7 @@ export enum CacheItemResetPeriod {\n   EVERY_HOUR = 3600,\n   EVERY_6_HOURS = 3600 * 6,\n   EVERY_24_HOURS = 3600 * 24,\n+  EVERY_6_MONTH = 3600 * 24 * 30 * 6,\n }", "line": 13}
{"pr": 2552, "path": "src/integration/realunit/utils/price-utils.ts", "body": "`Util.daysAfter`", "diff_hunk": "@@ -0,0 +1,53 @@\n+export class PriceUtils {\n+  static fillMissingDates<T extends { created: Date }>(prices: T[]): T[] {\n+    if (prices.length === 0) return prices;\n+\n+    const sortedPrices = [...prices].sort((a, b) => a.created.getTime() - b.created.getTime());\n+    const filledPrices: T[] = [sortedPrices[0]];\n+\n+    for (let i = 1; i < sortedPrices.length; i++) {\n+      const previousPrice = sortedPrices[i - 1];\n+      const currentPrice = sortedPrices[i];\n+\n+      const daysBetween = this.ge", "line": 42}
{"pr": 2552, "path": "src/integration/realunit/utils/price-utils.ts", "body": "`Util.daysDiff` (with rounding?)", "diff_hunk": "@@ -0,0 +1,53 @@\n+export class PriceUtils {\n+  static fillMissingDates<T extends { created: Date }>(prices: T[]): T[] {\n+    if (prices.length === 0) return prices;\n+\n+    const sortedPrices = [...prices].sort((a, b) => a.created.getTime() - b.created.getTime());\n+    const filledPrices: T[] = [sortedPrices[0]];\n+\n+    for (let i = 1; i < sortedPrices.length; i++) {\n+      const previousPrice = sortedPrices[i - 1];\n+      const currentPrice = sortedPrices[i];\n+\n+      const daysBetween = this.ge", "line": 32}
{"pr": 2552, "path": "src/integration/realunit/utils/price-utils.ts", "body": "Should be the same as `Util.daysAfter`", "diff_hunk": "@@ -0,0 +1,39 @@\n+import { Util } from 'src/shared/utils/util';\n+\n+export class PriceUtils {\n+  static fillMissingDates<T extends { created: Date }>(prices: T[]): T[] {\n+    if (prices.length === 0) return prices;\n+\n+    const sortedPrices = [...prices].sort((a, b) => a.created.getTime() - b.created.getTime());\n+    const filledPrices: T[] = [sortedPrices[0]];\n+\n+    for (let i = 1; i < sortedPrices.length; i++) {\n+      const previousPrice = sortedPrices[i - 1];\n+      const currentPrice = sort", "line": 38}
{"pr": 2580, "path": "src/shared/utils/pdf.util.ts", "body": "Move this to CoinGecko. Has nothing to do with PDF", "diff_hunk": "@@ -0,0 +1,188 @@\n+import PDFDocument from 'pdfkit';\n+import { I18nService } from 'nestjs-i18n';\n+import { Blockchain } from 'src/integration/blockchain/shared/enums/blockchain.enum';\n+import { Asset } from 'src/shared/models/asset/asset.entity';\n+import { dfxLogoBall1, dfxLogoBall2, dfxLogoText } from './dfx-logo';\n+import { PdfLanguage } from 'src/subdomains/supporting/balance/dto/input/get-balance-pdf.dto';\n+import { PriceCurrency } from 'src/subdomains/supporting/pricing/services/pricing.ser", "line": 29}
{"pr": 3002, "path": "src/shared/utils/pdf.util.ts", "body": "The only difference is positioning and scale, right?\r\n```TS\r\n    pdf.translate(mm2pt(20), mm2pt(14));\r\n    pdf.scale(0.15);\r\n```\r\nMaybe we can have a size parameter (small/large) then?", "diff_hunk": "@@ -61,6 +62,45 @@ export class PdfUtil {\n     pdf.translate(0, 30);\n   }\n \n+  static drawInvoiceLogo(pdf: InstanceType<typeof PDFDocument>, brand: PdfBrand = PdfBrand.DFX): void {", "line": 65}
{"pr": 3257, "path": "src/shared/utils/util.ts", "body": "Why not use our IBAN tools?\n\n`const { countryCode, bankIdentifier } = IbanTools.extractIBAN(iban)`", "diff_hunk": "", "line": 1}
