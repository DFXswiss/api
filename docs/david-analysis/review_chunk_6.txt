--- PR #2410 | src/subdomains/core/sell-crypto/process/services/buy-fiat-notification.service.ts ---
Context:                      urlText: entity.userData.kycUrl, | +                    phone: entity.userData.phone, |                    },
David: Blank the phone with ***

--- PR #2410 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context:    BANK_TX_NEEDED = 'BankTxNeeded', | +  MANUAL_CHECK_PHONE = 'ManualCheckPhone', |  }
David: I would rather use something like `PhoneVerificationNeeded`

--- PR #2410 | src/shared/i18n/de/mail.json ---
Context: +                "bank_tx_needed": "Um diese Transaktion auszuführen ist zuvor eine Banktransaktion erforderlich", | +                "manual_check_phone": "Wir konnten dich unter deiner angegebenen Telefonnummer nicht erreichen" |              },
David: What is this for?

--- PR #2410 | src/shared/utils/util.ts ---
Context: +  static yearsDiff(from?: Date, to?: Date): number { | +    return to.getFullYear() - from.getFullYear(); | +  }
David: I think this is not yearsDiff since `yearsDiff(new Date('2024-12-31'), new Date('2025-01-01')) = 1`. Maybe just `this. yearsDiff(from, to) / 365`?

--- PR #2410 | src/subdomains/core/buy-crypto/process/services/buy-crypto-notification.service.ts ---
Context:                      urlText: entity.userData.kycUrl, | +                    phone: entity.userData.phone, |                    },
David: Blank phone

--- PR #2420 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context:        if (!nationality) throw new BadRequestException('Nationality not found'); | + | +      data.nationality = Object.assign({ id: nationality.id, symbol: nationality.symbol }, data.nationality);
David: Why not just `Object.assign(data.nationality, { id: nationality.id, symbol: nationality.symbol });`?

--- PR #2423 | src/subdomains/generic/user/models/bank-data/bank-data.entity.ts ---
Context: +      this.comment.includes(e), | +    ); | +  }
David: Maybe this would be simpler?
```TS
get mergeError(): AmlError | undefined {
  if (this.comment.includes(BankDataVerificationError.MERGE_PENDING)) return AmlError.MERGE_PENDING;
  if (this.comment.includes(BankDataVerificationError.MERGE_EXPIRED)) return AmlError.MERGE_EXPIRED;
}
```

--- PR #2447 | src/integration/blockchain/zano/services/zano.service.ts ---
Context: +      } | +    } | +  }
David: Maybe we should do this less often than every minute?

--- PR #2447 | src/integration/blockchain/zano/services/zano.service.ts ---
Context: +      return this.client.addAssetToWhitelist(assetId); | +    } |    }
David: Can we have a `assetIds` array parameter so we don't need to call `this.client.getAssetWhitelist` multiple times?

--- PR #2447 | src/subdomains/supporting/payout/strategies/payout/impl/base/bitcoin-based.strategy.ts ---
Context: +        `Error on sending ${orders.map((o) => o.asset.name)} for payout. Order ID(s): ${orders.map((o) => o.id)}:`, | +        e, | +      );
David: Why not `Error on sending ${orders[0].asset.name} for payout. ...`?

--- PR #2447 | src/subdomains/supporting/dex/strategies/check-liquidity/impl/zano-token.strategy.ts ---
Context: +    throw new Error( | +      `Only native coin reference is supported by Zano CheckLiquidity strategy. Provided reference asset: ${referenceAsset.dexName} Context: ${context}. CorrelationID: ${correlationId}`, | +    );
David: This error message looks wrong 🤔

--- PR #2455 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +      KycLogType.KYC, | +      `UserData deactivated on ${userData.deactivationDate}`, | +    );
David: Maybe `userData.deactivationDate.toISOString()` would be more readable?

--- PR #2461 | src/shared/models/ip-log/ip-log.service.ts ---
Context: +      .where('ipLog.ip = :ip', { ip }) | +      .andWhere('ipLog.user.id IS NOT NULL') | +      .distinctOn(['ipLog.user.userData.id'])
David: - `distinctOn` is not supported in MS SQL => the best way is probably to fetch all distinct user data IDs?
- use the alias for filtering
- use inner join instead of checking for null
- join by address instead of id (id is not set during first login)
- filter by hasIpRisk = false?

So maybe like this?

```TS
this.ipLogRepo
      .createQueryBuilder('ipLog')
      .select('userData.id')
      .distinct()
      .innerJoin(User, 'user', 'ipLog.address=user.address')
      .innerJoin('user.userData', 'userData')
      .where('ipLog.ip = :ip', { ip })
      .andWhere('userdata.hasIpRisk = 0')
      .getRawMany<{ id: number }>()
```

The update of the user datas (KycAdminController) would be very fast afterwards:
```TS
const userDataIdsWithIpRisk = await this.ipLogService.getUserDataIdsWithIpRisk(ip);
await userDataRepo.update(userDataIdsWithIpRisk, { hasIpRisk: true });
```

--- PR #2461 | src/shared/models/setting/setting.service.ts ---
Context: +  async getIpBlacklist(): Promise<string[]> { | +    return (await this.getObjCached<string[]>('ipBlacklist')) ?? []; | +  }
David: `return this.getObjCached<string[]>('ipBlacklist', []));`

--- PR #2461 | src/shared/models/setting/setting.service.ts ---
Context: +      ipBlacklist.filter((blockedIp) => blockedIp !== ip), | +    ); | +  }
David: Invalidate the cache after updates

--- PR #2461 | src/shared/models/ip-log/ip-log.service.ts ---
Context:   | +  async getDistinctUserDataIpLogs(ip: string): Promise<number[]> { | +    return this.ipLogRepo
David: Maybe call it something like `getUserDataIdsWith`?

--- PR #2461 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +    const userDataIdsWithIpRisk = await this.ipLogService.getDistinctUserDataIpLogs(ip); | +    await this.userDataRepo.update(userDataIdsWithIpRisk, { hasIpRisk: true }); | +  }
David: The comment here is quite useless 🤣  and the method name could be improved, maybe something like `flagAllUsersWith`?

--- PR #2461 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:   | +      const ipBlacklist = await this.settingService.getIpBlacklist(); | +      if (ipBlacklist.includes(ip)) await this.userDataService.updateUserDataInternal(account, { hasIpRisk: true });
David: Maybe a helper method would be good?

```TS
private checkIpBlacklistFor(userData: UserData, ip: string): Promise<void> {
   if (userData.hasIpRisk) return;

    const ipBlacklist = await this.settingService.getIpBlacklist();
    if (ipBlacklist.includes(ip)) await this.userDataService.updateUserDataInternal(account, { hasIpRisk: true });
}
```

--- PR #2461 | src/shared/models/setting/setting.service.ts ---
Context: +    const ipBlacklist = await this.getIpBlacklist(); | +    if (!ipBlacklist.some((blockedIp) => blockedIp === ip)) throw new BadRequestException('Blocked IP not found'); | +
David: `    if (!ipBlacklist.includes(ip)) throw new BadRequestException('Blocked IP not found');`

--- PR #2461 | src/shared/models/setting/setting.service.ts ---
Context: + | +    if (!ipBlacklist.some((blockedIp) => blockedIp === ip)) { | +      ipBlacklist.push(ip);
David: `    if (!ipBlacklist.includes(ip)) {`

--- PR #2462 | src/subdomains/supporting/payment/services/transaction-request.service.ts ---
Context: +    } | + |      if (transactionRequest) await this.complete(transactionRequest.id);
David: Looks very complicated 🤣  maybe filter in SQL?
```TS
    const matchingRequests = await this.transactionRequestRepo.find({
      where: {
        routeId,
        sourceId,
        targetId,
        isComplete: false,
        created: MoreThan(Util.daysBefore(Config.txRequestWaitingExpiryDays)),
        status: In([TransactionRequestStatus.CREATED, TransactionRequestStatus.WAITING_FOR_PAYMENT]),
        amount: Between(amount * 0.99, amount * 1.01),
      },
      order: { created: 'DESC' },
      relations: { user: true, custodyOrder: { user: true } },
    });

const pendingRequests = matchingRequests.filter((t) => t.status === TransactionRequestStatus.WAITING_FOR_PAYMENT);

const matchingRequest =
   pendingRequests.find((t) => t.amount === amount) ??
   pendingRequests.at(0) ??
   matchingRequests.find((t) => t.amount === amount) ??
   matchingRequests.at(0);
   
for (const pendingRequest of pendingTxRequests.filter((t) => t.id !== matchingRequest.id)) {
   await this.transactionRequestRepo.update(...pendingRequest.resetStatus());
}

...
```

--- PR #2472 | src/subdomains/supporting/recall/dto/update-recall.dto.ts ---
Context: +  @IsNumber() | +  fee: number; | +}
David: Missing `?`s

--- PR #2473 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +        // Create user bankData | +        if (dto.transaction.senderIban && newIssue.transaction.sourceType === TransactionSourceType.BANK_TX) | +          await this.bankDataService.createIbanForUserInternal(userData, { iban: dto.transaction.senderIban }, false);
David: Should we catch errors here? We don't want the support issue creation to fail because of some bank data error.

--- PR #2474 | src/subdomains/supporting/payin/strategies/register/impl/zano.strategy.ts ---
Context: + | +    return payInEntries; | +  }
David: This looks a bit too complex 🤔 coin and tokens can be handled similarly, both have an ID?
`asset:  supportedAssets.find((a) => Util.equalsIgnoreCase(a.chainId, assetId),`

--- PR #2476 | src/subdomains/core/payment-link/services/payment-link-payment.service.ts ---
Context: +      await this.cancelQuotesForPayment(payment); | +    } |    }
David: - This flag only affects requests to `lnurlp/cancel/:id` (anonymous endpoint)
- Throw an error, if somebody tries to cancel a non-cancellable payment

--- PR #2477 | src/subdomains/generic/kyc/dto/kyc-error.enum.ts ---
Context:    [KycError.USER_DATA_DEACTIVATED]: 'Account deactivated', | +  [KycError.IDENT_IP_COUNTRY_MISMATCH]: 'Due to regulatory requirements', | +  [KycError.IP_COUNTRY_MISMATCH]: 'Due to regulatory requirements',
David: Maybe something like `Regulatory requirements not met` so it fits the other errors (same for other languages)?

--- PR #2477 | src/subdomains/generic/kyc/dto/kyc-error.enum.ts ---
Context:    COUNTRY_NOT_ALLOWED = 'CountryNotAllowed', | +  IDENT_IP_COUNTRY_MISMATCH = 'IdentIpCountryMismatch', | +  IP_COUNTRY_MISMATCH = 'IpCountryMismatch',
David: Improve naming?

--- PR #2478 | src/subdomains/core/sell-crypto/process/services/buy-fiat.service.ts ---
Context:   | +  async getBuyCryptoByTransactionId( | +    transactionId: number,
David: Copy&paste

--- PR #2478 | src/subdomains/supporting/bank-tx/bank-tx/services/bank-tx.service.ts ---
Context:   | +  async getBuyCryptoByTransactionId(transactionId: number, relations?: FindOptionsRelations<BankTx>): Promise<BankTx> { | +    return this.bankTxRepo.findOne({ where: { transaction: { id: transactionId } }, relations });
David: Copy&paste

--- PR #2480 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context:   | +export class KycRecallConfirmationData { | +  @ApiProperty({ description: 'Is the recall condition accepted?' })
David: RecallAgreementData

--- PR #2480 | src/subdomains/generic/kyc/enums/kyc-step-name.enum.ts ---
David: RECALL_AGREEMENT (and maybe move down to PAYMENT_AGREEMENT, since it's not in the normal KYC?)

--- PR #2480 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context:   | +  @Column({ nullable: true }) | +  recallConfirmation?: boolean; // null = deactivated, false = step created, true = step completed by user
David: recallAgreementAccepted?

--- PR #2480 | src/subdomains/generic/kyc/controllers/kyc.controller.ts ---
Context: +  @ApiUnauthorizedResponse(MergedResponse) | +  async updateRecallConfirmation( | +    @Headers(CodeHeaderName) code: string,
David: Rename

--- PR #2483 | src/subdomains/supporting/payin/strategies/register/impl/base/polling.strategy.ts ---
Context: + | +    this.blockHeight = currentBlockHeight; | +    return true;
David: Update `this.blockHeight` only after successful pay-in processing?

--- PR #2483 | src/subdomains/supporting/payin/strategies/register/impl/base/polling.strategy.ts ---
Context: + | +  abstract getBlockHeight(): Promise<number>; | +  abstract processNewPayInEntries(): Promise<void>;
David: protected?

--- PR #2483 | src/subdomains/supporting/payin/strategies/register/impl/monero.strategy.ts ---
Context:   | -  private async processNewPayInEntries(): Promise<void> { | +  async processNewPayInEntries(): Promise<void> {
David: Both methods protected

--- PR #2483 | src/subdomains/supporting/payin/strategies/register/impl/zano.strategy.ts ---
Context:   | -  private async processNewPayInEntries(): Promise<void> { | +  async processNewPayInEntries(): Promise<void> {
David: Same here

--- PR #2483 | src/subdomains/supporting/payin/strategies/register/impl/zano.strategy.ts ---
Context:   | -  private async processNewPayInEntries(): Promise<void> { | +  async processNewPayInEntries(): Promise<void> {
David: Same here

--- PR #2483 | src/subdomains/supporting/payin/strategies/register/impl/bitcoin.strategy.ts ---
Context:   | -  private async processNewPayInEntries(): Promise<void> { | +  async processNewPayInEntries(): Promise<void> {
David: Same here

--- PR #2483 | src/subdomains/supporting/payin/strategies/register/impl/base/polling.strategy.ts ---
Context: +      } catch (e) { | +        this.logger.error('Processing new entries failed:', e); | +      }
David: try-catch is unnecessary, errors are logged by @DfxCron

--- PR #2490 | src/shared/validators/is-not-null.validator.ts ---
Context: +    IsNotNull(validationOptions)(target, propertyName); | +  }; | +}
David: This method seems to be useless. Remove it and export `IsNotNull`

--- PR #2490 | src/shared/validators/is-not-null.validator.ts ---
Context: + | +    // alle anderen Werte = ok | +    return true;
David: Looks like AI code + comments? 🤔 

Can be simplified to `return value !== null;`

--- PR #2491 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +      .createQueryBuilder('buyCrypto') | +      .select('SUM((partnerFeeAmount * (amountInEur/amountInChf )) / (refProvision * 0.01))', 'volume') | +      .addSelect('SUM(partnerFeeAmount * (amountInEur/amountInChf ))', 'credit')
David: Isn't the volume just `amountInEur`?

--- PR #2491 | src/subdomains/core/sell-crypto/process/services/buy-fiat.service.ts ---
Context: +      .createQueryBuilder('buyFiat') | +      .select('SUM((partnerFeeAmount * (amountInEur/amountInChf )) / (refProvision * 0.01))', 'volume') | +      .addSelect('SUM(partnerFeeAmount * (amountInEur/amountInChf ))', 'credit')
David: Same here?

--- PR #2491 | src/subdomains/generic/user/models/user/user.entity.ts ---
Context: +  get completeRefCredit(): number { | +    return this.refCredit + this.partnerRefCredit; | +  }
David: I would rather use `total` instead of `complete` (e.g. `totalRefVolume`)

--- PR #2491 | src/subdomains/core/referral/reward/services/ref-reward.service.ts ---
David: We probably need to update `UserService.getOpenRefCreditUser` (currently doesn't include partner refs)

--- PR #2491 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +      .select('SUM(amountInEur)', 'volume') | +      .addSelect('SUM(partnerFeeAmount * (amountInEur/amountInChf ))', 'credit') | +      .where('usedPartnerFeeRef = :ref', { ref })
David: `partnerFeeAmount` is in in `inputReferenceAsset`?

--- PR #2491 | src/subdomains/core/sell-crypto/process/services/buy-fiat.service.ts ---
Context: +      .select('SUM(amountInEur)', 'volume') | +      .addSelect('SUM(partnerFeeAmount * (amountInEur/amountInChf ))', 'credit') | +      .where('usedPartnerFeeRef = :ref', { ref })
David: `partnerFeeAmount` is in `inputAsset`?

--- PR #2491 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:   | +  @Column({ length: 256, nullable: true }) | +  usedPartnerFeeRef?: string;
David: `usedPartnerRef`?

--- PR #2491 | src/subdomains/core/sell-crypto/process/buy-fiat.entity.ts ---
Context:   | +  @Column({ length: 256, nullable: true }) | +  usedPartnerFeeRef?: string;
David: Same here?

--- PR #2492 | src/shared/utils/util.ts ---
Context: +      return acc; | +    }, []); | +  }
David: This is bad in performance (O(n²)) for large lists, maybe use a map?
```TS
static toUniqueList<T>(list: T[], key: KeyType<T, number> | KeyType<T, Date>): T[] {
  return Array.from(
    list.reduce((map, item) => map.set(item[key], item), new Map<T[K], T>()).values()
  );
}
```

--- PR #2492 | src/shared/utils/util.ts ---
Context: +      return acc; | +    }, []); | +  }
David: Yes, very good

--- PR #2497 | src/subdomains/generic/support/support.service.ts ---
Context: +  private async getUserData(key: string): Promise<UserData> { | +    if (key.length === 14 && key.substring(4, 5) === '-' && key.substring(9, 10) === '-') | +      return this.buyService.getBuyByKey('bankUsage', key, true).then((b) => b?.userData);
David: `Config.formats.bankUsage.test(key)`

--- PR #2497 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: -  async getUsersByMail(mail: string): Promise<UserData[]> { | +  async getUsersByMail(mail: string, onlyValidUser = true): Promise<UserData[]> { |      return this.userDataRepo.find({
David: What does this parameter do?

--- PR #2497 | src/subdomains/generic/support/support.service.ts ---
Context: + | +    if (CryptoService.isBlockchainAddress(key)) { | +      return (
David: `Config.formats.address.test(address)`

--- PR #2497 | src/subdomains/supporting/payin/services/payin.service.ts ---
Context: +        query.orWhere(`${key.includes('.') ? key : `cryptoInput.${key}`} = :param`, { param: value }); | +      } | +    }
David: Maybe this is better?
```TS
      for (const [i, key] of keys.entries()) {
        const params: [string, object] = [`${key.includes('.') ? key : `cryptoInput.${key}`} = :param`, { param: value }];
        i === 0 ? query.where(...params) : query.orWhere(...params);
      }
```

--- PR #2497 | src/subdomains/core/buy-crypto/process/services/buy-crypto.service.ts ---
Context: +        query.orWhere(`${key.includes('.') ? key : `buyCrypto.${key}`} = :param`, { param: value }); | +      } | +    }
David: Same here?

--- PR #2497 | src/subdomains/generic/support/support.service.ts ---
Context: + | +  private async getUserDataByKey(key: string): Promise<UserData[]> { | +    if (key.includes('@')) return this.userDataService.getUsersByMail(key, false);
David: `getUserDatasByKey`

--- PR #2497 | src/subdomains/generic/support/support.service.ts ---
Context: +      this.payInService | +        .getCryptoInputByKeys(['inTxId', 'outTxId', 'returnTxId'], key) | +        .then((c) => c?.transaction?.userData),
David: Maybe add a `userData` getter on crypto input to simplify this like above?

--- PR #2498 | infrastructure/bicep/core/modules/virtualNetworks.bicep ---
Context: +        '10.0.0.0/16' | +      ] | +    }
David: We should use a different address space than the existing VNet (so they can be peered)

--- PR #2498 | infrastructure/bicep/core/modules/sqlServer.bicep ---
Context: +    tier: dbTier | +    capacity: dbCapacity | +  }
David: Can we switch to vCore-based purchasing model (as we now have for the DFX API)? 
<img width="786" height="164" alt="Image" src="https://github.com/user-attachments/assets/53a9f002-8266-4530-9f9f-9aba8b64e594" />

--- PR #2498 | infrastructure/bicep/virtual-machines/load-balancer/main.bicep ---
David: Add PLS

--- PR #2498 | infrastructure/bicep/virtual-machines/deploy.sh ---
Context: +# "slnode": SwissLedger Node | +vmNameOptions=("node" "slnode") | +
David: No specific slnode

--- PR #2498 | infrastructure/bicep/virtual-machines/load-balancer/main.bicep ---
Context: +var loadBalancerName = 'lbi-${compName}-${rg}-${env}' | +var frontendIPConfigurationName = 'feic-${compName}-${vm}-${env}' | +var backendAddressPoolName = 'bep-${compName}-${vm}-${env}'
David: frontendIPConfiguration belongs to the load balancer, so it should be named after it (`feic-${compName}-${rg}-${env}`)?

--- PR #2498 | infrastructure/bicep/virtual-machines/load-balancer/main.bicep ---
Context: +    ] | +  } | +}
David: Why do we need this private endpoint?

--- PR #2498 | infrastructure/bicep/virtual-machines/parameters/dev-core-node.json ---
Context: +    "hardwareProfile": { | +      "value": "Standard_L2as_v4" | +    },
David: `Standard_D2als_v6`

--- PR #2498 | infrastructure/bicep/virtual-machines/parameters/prd-core-node.json ---
Context: +    "hardwareProfile": { | +      "value": "Standard_L2as_v4" | +    },
David: `Standard_D2als_v6`

--- PR #2498 | infrastructure/bicep/virtual-machines/load-balancer/main.bicep ---
Context: + | +var privateLinkServiceName = 'pls-${compName}-${rg}-${env}' | +var autoApprovalSubscriptions array = []
David: LB, FEIC and PLS should be named after the corresponding VM (they are not generic)

--- PR #2498 | infrastructure/bicep/virtual-machines/load-balancer/main.bicep ---
Context: +        properties: { | +          privateIPAddress: privateIPAddress | +          privateIPAllocationMethod: 'Dynamic'
David: How can we specify an IP address, if allocation is dynamic?

--- PR #2498 | infrastructure/citrea/citreascan/scripts/setupEnv.sh ---
Context: +cd ~ | +mkdir update | +
David: update folder not used?

--- PR #2498 | infrastructure/citrea/citreascan/config/docker/docker-compose-nginx.yml ---
Context: +  shared: | +    external: true | +    name: citrea-testnet-network
David: Maybe be could remove the `external: true` here and let docker-compose create the network instead of doing it manually (keep it false in the other files)?

--- PR #2498 | infrastructure/citrea/citreascan/README.md ---
Context: +1. Copy docker compose config `config/docker/docker-compose-citrea-testnet4.yml` to `/home/{user}` | +1. Copy docker compose config `docker-compose-blockscout-citrea-testnet.yml` to `/home/{user}` | +1. Copy docker compose config `config/docker/docker-compose-nginx.yml` to `/home/{user}`
David: Missing `config/docker/` fore blockscout?

--- PR #2498 | infrastructure/citrea/citreascan/README.md ---
Context: + | +1. Create the file `docker-compose-blockscout-citrea-testnet.frontend.env` in `/home/{user}` | +1. Copy the content of the github repo file `https://github.com/CitreaScan/frontend/blob/develop/.env.vm_citrea_testnet_dev` in `docker-compose-blockscout-citrea-testnet.frontend.env`
David: Has been moved from `CitreaScan/frontend/blob/develop/.env.vm_citrea_testnet_dev` to `CitreaScan/frontend/blob/develop/.env.dev`

--- PR #2498 | infrastructure/citrea/citreascan/README.md ---
Context: + | +1. Run shell script `update-blockscout.sh` with the parameter `frontend` or `backend` | +
David: Is this still required?

--- PR #2498 | infrastructure/citrea/citreascan/config/docker/docker-compose-blockscout-citrea-testnet.yml ---
Context: +    ports: | +      - '4000:4000' | +    command: sh -c "bin/blockscout eval \"Elixir.Explorer.ReleaseTasks.create_and_migrate()\" && bin/blockscout start"
David: Check if `cpus` is still required

--- PR #2522 | src/subdomains/generic/support/support.service.ts ---
Context: +        userData: await this.bankTxService.getBankTxByKey('iban', key, true).then((b) => b?.userData), | +      }; | +    }
David: Return multiple UserDatas

--- PR #2522 | src/subdomains/core/sell-crypto/route/sell.service.ts ---
Context: +  async getSellsByIban(iban: string): Promise<Sell[]> { | +    return this.sellRepo.find({ where: { iban }, relations: { userData: true } }); | +  }
David: `userData` is not a relation

--- PR #2522 | src/subdomains/generic/support/support.service.ts ---
David: Remove `toUniqueList` here?

--- PR #2522 | src/subdomains/generic/support/support.service.ts ---
Context: +        t | +          .flat() | +          .filter((t) => t)
David: Can it return an undefined entry?

--- PR #2523 | src/subdomains/generic/kyc/services/integration/kyc-document.service.ts ---
Context: +      ...(accountType !== AccountType.PERSONAL ? await this.listSpiderFiles(userDataId, true) : []), | +    ]; | +  }
David: We just want all entries from KycFile table (no Azure search)

--- PR #2523 | src/subdomains/generic/kyc/services/kyc-file.service.ts ---
Context: +  async getUserDataKycFiles(userDataId: number): Promise<KycFile[]> { | +    return this.kycFileRepository.findBy({ userData: { id: userDataId } }); | +  }
David: Ignore eager relations? We don't want to load the user data and all its relations for every file

--- PR #2524 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +      status: statusBefore, | +    }); | +  }
David: Variable naming is a bit misleading here, maybe it's clearer like this?
```TS
if (oldBankData.approved && newDto.type === BankDataType.BANK_IN) {
   dto.approved = oldBankData.approved;
   dto.status = oldBankData.status;

   await this.bankDataRepo.update(oldBankData.id, { approved: false, status: ReviewStatus.FAILED });
} else {
   dto.approved = false;
   dto.status = ReviewStatus.FAILED
}

return this.createBankDataInternal(oldBankData.userData, newDto);
```

--- PR #2524 | src/subdomains/generic/user/models/bank-data/bank-data.entity.ts ---
Context:    MERGE_EXPIRED = 'MergeExpired', | +  REPLACED_WITH_NEW_BANK_DATA = 'ReplacedWithNewBankData', |  }
David: `REPLACED = 'Replaced'` would probably enough 😉

--- PR #2525 | src/subdomains/generic/support/support.service.ts ---
Context: +    if (IbanTools.validateIBAN(key).valid) return this.bankTxService.getUnassignedBankTx([key]); | +  } | +
David: Check searchResult.type instead of `IbanTools.validateIBAN` again?

--- PR #2527 | src/subdomains/core/referral/reward/ref-reward.entity.ts ---
Context: +  @OneToOne(() => Transaction, { nullable: true }) | +  @JoinColumn() | +  linkedSourceTransaction?: Transaction;
David: Just called `sourceTransaction`, the link is obvious 😉  (or `originTransaction`)

--- PR #2533 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +        ...nameSplitRequest, |        ], |      });
David: I think I would do it like this:
```TS
// util method
Contains(search: string): FindOperator<string> {
   return ILike(`%${search}%`);
}

// code
const where = { status: Not(UserDataStatus.MERGED) };
const wheres = [
        { ...where, firstname: Contains(name) },
        { ...where, surname: Contains(name) },
        { ...where, verifiedName: Contains(name) },
        { ...where, organization: { name: Contains(name) } },
];

const nameParts = name.split(' ');
const first = name.shift();
const last = name.pop();

if (last) wheres.push({ ...where, firstname: Contains(first), surname: Contains([...nameParts, last].join(' '))});
if (nameParts.length) wheres.push({ ...where, firstname: Contains([first, ...nameParts].join(' ')), surname: Contains(last)})

return this.userDataRepo.find({ where: wheres });
```
WDYT?

--- PR #2536 | src/subdomains/core/history/controllers/history.controller.ts ---
Context: +      ExportType.CHAIN_REPORT, | +    )) as ChainReportApiHistoryDto[]; |    }
David: You could combine these two with `@Get(':exportType')`, the code looks very similar

--- PR #2536 | src/subdomains/core/history/services/history.service.ts ---
David: private?

--- PR #2536 | src/subdomains/core/history/services/history.service.ts ---
Context: + | +  async getHistoryTransactions( | +    user: User | UserData,
David: private?

--- PR #2536 | src/subdomains/core/history/services/history.service.ts ---
David: Maybe rename to `getHistoryWithStaking` to make it clear what it does?

--- PR #2536 | src/subdomains/core/history/dto/output/chain-report-history.dto.ts ---
Context: + | +  @ApiProperty() | +  outputChainId: string;
David: Optional

--- PR #2536 | src/subdomains/core/history/dto/output/chain-report-history.dto.ts ---
Context: + | +  @ApiProperty() | +  outputChainId: string;
David: Use `EvmUtil.getChainId`

--- PR #2536 | src/subdomains/core/history/mappers/chain-report-history-dto.mapper.ts ---
Context: +                  buyCrypto.cryptoInput.asset.dexName, | +                  buyCrypto.cryptoInput.asset.blockchain, | +                ),
David: Isn't it `totalFee` for the sell amount?

--- PR #2536 | src/subdomains/core/history/mappers/chain-report-history-dto.mapper.ts ---
Context: +                buyValueInEur: null, | +                sellValueInEur: buyCrypto.amountInEur, | +                inputBlockchain: buyCrypto.cryptoInput.asset.blockchain,
David: Is it really `amountInEur`, shouldn't it be the fee in EUR?

--- PR #2536 | src/subdomains/core/history/mappers/chain-report-history-dto.mapper.ts ---
Context: +                buyCrypto.cryptoRoute?.asset?.dexName, | +                buyCrypto.cryptoRoute?.asset?.blockchain, | +              ),
David: `buyCrypto.outputAsset`

--- PR #2536 | src/subdomains/core/history/mappers/chain-report-history-dto.mapper.ts ---
Context: +          outputBlockchain: null, // TODO: refReward.outputAsset.blockchain, | +          inputChainId: null, | +          outputChainId: null, // TODO: refReward.outputAsset.chainId,
David: Wait for https://github.com/DFXswiss/api/pull/2538

--- PR #2536 | src/subdomains/core/history/controllers/history.controller.ts ---
Context: +      default: | +        throw new BadRequestException('ExportType not supported'); | +    }
David: Maybe this is easier?
```TS
const typeMap = {
  CT: ExportType.COIN_TRACKING,
  CR: ExportType.CHAIN_REPORT,
};

const type = typeMap[exportType];
if (!type) throw new BadRequestException('ExportType not supported');

return this.historyService.getApiHistory(
  user,
  { format: ExportFormat.JSON, ...query },
  type,
);
```

--- PR #2536 | src/subdomains/core/history/mappers/chain-report-history-dto.mapper.ts ---
Context: +                buyValueInEur: null, | +                sellValueInEur: (buyCrypto.amountInEur / buyCrypto.amountInChf) * buyCrypto.totalFeeAmountChf, | +                inputBlockchain: buyCrypto.cryptoInput.asset.blockchain,
David: Maybe we should do some rounding here (also for CT and below)?

--- PR #2536 | src/subdomains/core/history/mappers/chain-report-history-dto.mapper.ts ---
Context: +              fee: buyCrypto.totalFeeAmount | +                ? (buyCrypto.totalFeeAmount / buyCrypto.inputReferenceAmount) * buyCrypto.inputAmount | +                : null,
David: Rounding

--- PR #2536 | src/subdomains/core/history/mappers/chain-report-history-dto.mapper.ts ---
Context: +          fee: buyCrypto.totalFeeAmount | +            ? (buyCrypto.totalFeeAmount / buyCrypto.inputReferenceAmount) * buyCrypto.inputAmount | +            : null,
David: Rounding

--- PR #2536 | src/subdomains/core/history/mappers/chain-report-history-dto.mapper.ts ---
Context: +          fee: buyFiat.totalFeeAmount | +            ? (buyFiat.totalFeeAmount / buyFiat.inputReferenceAmount) * buyFiat.inputAmount | +            : null,
David: Rounding

--- PR #2536 | src/subdomains/core/history/mappers/coin-tracking-history-dto.mapper.ts ---
David: Rounding

--- PR #2536 | src/subdomains/core/history/mappers/coin-tracking-history-dto.mapper.ts ---
David: Rounding

--- PR #2536 | src/subdomains/core/history/mappers/coin-tracking-history-dto.mapper.ts ---
David: Rounding

--- PR #2538 | src/subdomains/core/referral/reward/services/ref-reward.service.ts ---
Context: +  async syncOutputEntity(): Promise<void> { | +    const entities = await this.rewardRepo.findBy({ outputAssetEntity: { id: IsNull() } }); | +
David: Maybe we should limit the number of results?

--- PR #2538 | src/shared/models/reward.entity.ts ---
Context: + | +  @ManyToOne(() => Asset, { eager: true, nullable: true }) | +  outputAssetEntity?: Asset;
David: Rename to `outputAsset` in a second step?

--- PR #2538 | src/subdomains/core/referral/reward/services/ref-reward.service.ts ---
Context: +      outputAssetString: asset.dexName, |        status: dto.amountInEur > Config.refRewardManualCheckLimit ? RewardStatus.MANUAL_CHECK : RewardStatus.PREPARED, |        targetBlockchain: asset.blockchain,
David: Why not also setting `outputAssetEntity`?

--- PR #2538 | src/subdomains/core/referral/reward/services/ref-reward.service.ts ---
Context: +          outputAssetString: payoutAsset.dexName, |            user, |            status: refCreditEur > Config.refRewardManualCheckLimit ? RewardStatus.MANUAL_CHECK : RewardStatus.PREPARED,
David: Same here?

--- PR #2540 | src/shared/models/reward.entity.ts ---
Context:    @ManyToOne(() => Asset, { eager: true, nullable: true }) | -  outputAssetEntity?: Asset; | +  outputAsset?: Asset;
David: Should we drop the string column?

--- PR #2542 | src/integration/blockchain/realunit/controllers/realunit.controller.ts ---
Context: +@Controller('realunit') | +export class RealunitController { | +  constructor(private readonly realunitService: RealunitService) {}
David: I would name everything `RealUnit` instead of `Realunit` 😉

--- PR #2542 | src/integration/blockchain/realunit/dto/realunit.dto.ts ---
David: Add swagger documentation

--- PR #2542 | src/integration/blockchain/realunit/dto/realunit-dto.mapper.ts ---
Context: +      balance: holder.balance, | +      percentage: (100 * Number(holder.balance)) / Number(dto.totalSupply.value), | +    }));
David: I would do some rounding ... JS is very bad with divisions

--- PR #2542 | src/integration/blockchain/realunit/dto/realunit.dto.ts ---
David: What I meant is the thing a bit further down 😉 
<img width="541" height="390" alt="image" src="https://github.com/user-attachments/assets/ea34a488-ef35-4180-93f1-9003cae63580" />
If done correctly, it e.g. looks like this
<img width="679" height="589" alt="image" src="https://github.com/user-attachments/assets/186015a2-667e-43c2-9c42-7ef85228ad3e" />

--- PR #2542 | src/integration/blockchain/realunit/dto/realunit.dto.ts ---
David: The controller documentation is very good indeed 😉 👍

--- PR #2542 | src/integration/blockchain/realunit/realunit.module.ts ---
Context: +  exports: [RealUnitService], | +}) | +export class RealunitModule {}
David: Forgot a rename 😉

--- PR #2543 | src/subdomains/supporting/bank-tx/bank-tx-repeat/bank-tx-repeat.entity.ts ---
Context:   | +  @ManyToOne(() => User, { nullable: true, eager: true }) | +  user: User;
David: Does it need to be eager?

--- PR #2545 | src/integration/blockchain/realunit/controllers/realunit.controller.ts ---
David: Should we do some caching here? Or maybe in a later stage?

--- PR #2545 | src/subdomains/supporting/pricing/services/integration/pricing-realunit.service.ts ---
Context: +    const assetPrice = from === PricingRealUnitService.REALU ? realunitPrice : 1 / realunitPrice; | + | +    return Price.create(from, to, Util.round(assetPrice, 8));
David: Inverse

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.entity.ts ---
David: Add invitation type?

--- PR #2547 | src/config/config.ts ---
Context:      maxIdentTries: 7, | +    allowedTradeApprovalWalletIds: process.env.TRADE_APPROVAL_WALLET_IDS?.split(',')?.map(Number) ?? [], |    };
David: Why not configure it in the wallet table?

--- PR #2547 | src/subdomains/generic/kyc/enums/kyc.enum.ts ---
Context: +    userData.accountType !== AccountType.ORGANIZATION | +      ? KycStepName.RECOMMENDATION | +      : null,
David: Should we still create the step and auto-complete it? I'm not sure on what we settled on 🤔

--- PR #2547 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsString() | +  label: string;
David: What is this for?

--- PR #2547 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @ValidateIf((a: KycRecommendationData) => Boolean(a.recommendationCode || !(a.ref && a.mail))) | +  @IsString() | +  recommendationCode?: string;
David: Maybe we should just have a single field and detect what it is? Or will we have a type-selection in the frontend?

--- PR #2547 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: + | +    return KycStepMapper.toStepBase(kycStep); | +  }
David: We need to make sure the user doesn't progress with the KYC as long as the recommendation step is not completed.

--- PR #2547 | src/subdomains/generic/kyc/enums/kyc.enum.ts ---
Context:   | +  // TODO: userData/users wallet relation | +
David: TODO?

--- PR #2547 | src/shared/i18n/de/mail.json ---
Context: +            "button": "Du kannst ganz einfach den nachfolgenden Button anklicken, um die Einladung zu bestätigen oder abzulehnen:<br>[url:Klick hier]", | +            "link": "Oder du benutzt den nachfolgenden Link:<br>[url:{urlText}]" | +        }
David: Maybe add some hint to be cautious

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.entity.ts ---
Context: +  get isValid(): boolean { | +    return this.isExpired && !this.isUsed; | +  }
David: `!this.isExpired`?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.controller.ts ---
Context: +      .getAllRecommendationForUserData(jwt.account) | +      .then((r) => RecommendationDtoMapper.entitiesToDto(r)); | +  }
David: `getOwnRecommendationForUserData` vs `getAllRecommendationForUserData` is a bit weird. One is for recommender and one for recommended, right?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.controller.ts ---
Context: +      .getAllRecommendationForUserData(jwt.account, true) | +      .then((r) => RecommendationDtoMapper.entitiesToDto(r)); | +  }
David: I think we don't need both APIs, we can filter in the frontend?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/dto/recommendation.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsString() | +  recommendedAlias: string;
David: Either use `recommendedMail` or just `alias`

--- PR #2547 | src/subdomains/generic/user/models/recommendation/dto/recommendation.dto.ts ---
Context: +  @IsOptional() | +  @IsString() | +  mail: string;
David: `  @IsEmail()`

--- PR #2547 | src/subdomains/generic/user/models/recommendation/mapper/recommendation-dto.mapper.ts ---
Context: + | +export class RecommendationDtoMapper { | +  static entityToDto(recommendation: Recommendation, forRecommender = true): RecommendationDto {
David: I would not use a default here, that's a bit shady 😉

--- PR #2547 | src/subdomains/generic/user/models/recommendation/mapper/recommendation-dto.mapper.ts ---
Context: +      name: | +        (forRecommender ? recommendation.recommended?.completeName : recommendation.recommended?.completeName) ?? | +        recommendation.recommendedAlias,
David: Twice `recommendation.recommended?.completeName`?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/mapper/recommendation-dto.mapper.ts ---
Context: +        recommendation.recommendedAlias, | +      mail: | +        (forRecommender ? recommendation.recommended?.mail : recommendation.recommended?.mail) ??
David: Twice `recommendation.recommended?.mail`?

--- PR #2547 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +      }, | +      relations: { userData: { wallet: true }, recommendation: true }, | +    });
David: We will never handle expired recommendations?

--- PR #2547 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +          await this.kycStepRepo.update(...entity.manualReview(comment)); | +        } else if (errors.every((e) => [KycError.EXPIRED_RECOMMENDATION, KycError.DENIED_RECOMMENDATION].includes(e))) { | +          await this.kycStepRepo.update(...entity.fail(undefined, comment));
David: This does not really make sense. The second else if will never be met

--- PR #2547 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +          (recommendationSteps.some((r) => r.comment?.split(';').includes(KycError.BLOCKED)) || | +            recommendationSteps.length > Config.kyc.maxRecommendationTries) && | +          !recommendationSteps.some((r) => r.comment?.split(';').includes(KycError.RELEASED))
David: `>=`?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: + | +    const recruit = dto.mail | +      ? (await this.userDataService.getUsersByMail(dto.mail)?.[0]) ??
David: recommended

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +  ): Promise<Recommendation> { | +    const advertiser: UserData = dto.refCode | +      ? await this.userService.getRefUser(dto.refCode).then((u) => u.userData)
David: recommender

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: + | +  private async sendRecommenderMail(entity: Recommendation): Promise<void> { | +    try {
David: sendInvitationMail?

--- PR #2547 | src/subdomains/generic/kyc/dto/input/kyc-data.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsString() | +  recommendedAlias: string;
David: Is this meant to be my own alias?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +      : undefined; | +    if (advertiser.isBlocked) throw new BadRequestException('Recommender blocked'); | +
David: Can be undefined

--- PR #2547 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +    if (Config.formats.recommendationCode.test(data.key)) { | +      const recommendation = await this.recommendationService.getAndCheckRecommendationByCode(data.key); | +
David: Should we verify that this recommendation was created by the recommender?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +      undefined, | +      dto.mail, | +    );
David: RecommendationType.MAIL even if there is no mail?

--- PR #2547 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +        kycStep, | +        Config.formats.ref.test(data.key) ? { refCode: data.key } : { mail: data.key }, | +      );
David: Maybe do the data.key check all in recommendation service? Currently we do things twice.

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: + | +  async updateRecommendation(userDataId: number, id: number, dto: UpdateRecommendationDto): Promise<Recommendation> { | +    const entity = await this.recommendationRepo.findOne({ where: { id }, relations: { recommender: true } });
David: `confirmRecommendation`?

--- PR #2547 | src/subdomains/generic/kyc/enums/kyc.enum.ts ---
Context: +    !userData.wallet.autoTradeApproval && userData.accountType !== AccountType.ORGANIZATION | +      ? KycStepName.RECOMMENDATION | +      : null,
David: Also if the user already has trade approval date?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +          language: userData.language, | +          currency: userData.currency, | +        }))
David: Already set trade approval date?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.controller.ts ---
Context: +  ): Promise<void> { | +    await this.recommendationService.confirmRecommendation(jwt.account, +id, data); | +  }
David: It should also be possible to reject

--- PR #2547 | src/shared/i18n/de/mail.json ---
Context: +            "title": "DFX-Einladung", | +            "salutation": "Du wurdest zu DFX eingeladen!", | +            "message": "{name} ({mail}) hat dich eingeladen DFX zu nutzen",
David: Add the recommended name (alias from DB?) to the salutation

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.controller.ts ---
Context: +  @UseGuards(AuthGuard(), RoleGuard(UserRole.ACCOUNT), UserActiveGuard()) | +  async getAllRecommendation(@GetJwt() jwt: JwtPayload): Promise<RecommendationDto[]> { | +    return this.recommendationService
David: `  @Get()`

--- PR #2547 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
Context:   |    get naturalPersonName(): string { | -    return [this.firstname, this.surname].filter((n) => n).join(' ');
David: `get naturalPersonName(): string | undefined {`

--- PR #2547 | src/shared/i18n/de/mail.json ---
Context: +        "confirmation": { | +            "title": "DFX-Empfehlung bestätigen", | +            "salutation": "Du hast eine ausstehende DFX-Einladung",
David: `DFX-Empfehlung`?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.entity.ts ---
Context: +  get url(): string { | +    return `${Config.frontend.services}/recommendation?code=${this.code}`; | +  }
David: I think we don't need the code in the URL? The user needs to be logged in anyway.

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.entity.ts ---
Context: + | +  @Column({ type: 'datetime2', nullable: true }) | +  confirmationDate: Date; // only set for recommendations created by Recommender
David: created by Recommended

--- PR #2547 | src/subdomains/generic/user/models/recommendation/dto/recommendation.dto.ts ---
Context: +  @IsNotEmpty() | +  @IsString() | +  recommendedAlias: string;
David: `@Transform(Util.trim)`

--- PR #2547 | src/subdomains/generic/user/models/recommendation/dto/recommendation.dto.ts ---
Context: + | +  @ApiProperty() | +  status: RecommendationDtoStatus;
David: `{ enum: RecommendationDtoStatus }`

--- PR #2547 | src/subdomains/generic/user/models/recommendation/dto/recommendation.dto.ts ---
Context: + | +  @ApiProperty() | +  type: RecommendationType;
David: `{ enum: RecommendationType }`

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +        currency: userData.currency, | +        tradeApprovalDate: new Date(), | +      }));
David: If mailUser exists, set tradeApprovalDate?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +      ? await this.userDataService.getUsersByMail(data.key)?.[0] | +      : undefined; | +    if (recommender?.isBlocked) throw new BadRequestException('Recommender blocked');
David: No recommender null check?

--- PR #2547 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +      // create new recommendation | +      await this.recommendationService.createRecommendationByRecommended(user, kycStep, data); | +    }
David: Move this all to RecommendationService, to have it all in one place?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +      if (entity.recommended.id) throw new Error('Recommended already set'); | + | +      entity.expirationDate = Util.daysAfter(Config.recommendation.confirmationExpiration);
David: Why update the expiration date?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: + | +      if (entity.recommended.id) throw new Error('Recommended already set'); | +
David: `entity.recommended` might be null?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +      relations: { recommended: true, recommender: true }, | +    }); | +  }
David: Is this method still used?

--- PR #2547 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +    const existingRecommendations = dto.recommendedMail | +      ? await this.recommendationRepo.findBy({ recommendedMail: dto.recommendedMail }) | +      : [];
David: `countBy`?

--- PR #2549 | src/shared/models/ip-log/ip-log.service.ts ---
Context: +      user: { userData: { id: userDataId } }, | +    }); | +  }
David: Maybe we should just load all distinct countries? -> Maybe `async getLoginCountries(userDataId: number, since: Date): Promise<string[]>` would be enough?

--- PR #2549 | src/shared/models/ip-log/ip-log.service.ts ---
Context: +      user: { userData: { id: userDataId } }, | +    }); | +  }
David: We now have a general cache (nearestLog) for variable dates? Maybe we should call the method something like `getRecentLogsByUserData` and have the date fixed.

--- PR #2549 | src/shared/models/ip-log/ip-log.service.ts ---
Context: +      .addSelect('MAX(log.created)', 'maxCreated') | +      .leftJoin('log.user', 'user') | +      .leftJoin('user.userData', 'userData')
David: `innerJoin` (more performant)?

--- PR #2549 | src/shared/models/ip-log/ip-log.service.ts ---
Context: +      .groupBy('log.country') | +      .orderBy('maxCreated', 'DESC') | +      .getRawMany<{ country: string; maxCreated: Date }>()
David: Why so complex with groupBy and maxCreated?

--- PR #2549 | src/shared/utils/async-cache.ts ---
Context:    EVERY_24_HOURS = 3600 * 24, | +  EVERY_6_MONTH = 3600 * 24 * 30 * 6, |  }
David: EVERY_6_MONTH**S**

--- PR #2552 | src/subdomains/supporting/pricing/services/integration/pricing-realunit.service.ts ---
Context:   | -  private static readonly ALLOWED_ASSETS = [PricingRealUnitService.REALU, PricingRealUnitService.ZCHF]; | +  private static readonly ALLOWED_ASSETS = [PricingRealUnitService.REALU, PricingRealUnitService.CHF];
David: Is this change necessary? It will destroy the current pricing because we defined the pricing chain for REALU as REALU -> ZCHF -> USD

--- PR #2552 | src/integration/realunit/controllers/realunit.controller.ts ---
Context: + | +  @Get('historicalPrice') | +  @ApiOperation({
David: `@Get('price/history')`?

--- PR #2552 | src/integration/realunit/controllers/realunit.controller.ts ---
Context: +  async getHistoricalPrice(@Query('timeFrame') timeFrame: TimeFrame = TimeFrame.WEEK): Promise<HistoricalPriceDto[]> { | +    return this.realunitService.getHistoricalPrice(timeFrame); |    }
David: Would it make sense to return the same DTO in `price` and `price/history`? Or do we want the current price only in CHF?

--- PR #2552 | src/integration/realunit/dto/realunit.dto.ts ---
Context: + | +  @ApiPropertyOptional({ description: 'Valuation in CHF at this point in time' }) | +  valuationChf?: number;
David: `valueChf`

--- PR #2552 | src/integration/realunit/realunit.service.ts ---
Context: +      [await this.getRealuAsset()], | +      this.genesisDate, | +    );
David: Would it make sense to use the `historicalPriceCache` here as well?

--- PR #2552 | src/integration/realunit/realunit.service.ts ---
Context: +        return new Date(now.getTime() - 7 * 24 * 60 * 60 * 1000); | +    } | +  }
David: Use `Util.daysBefore(xxx)`? (e.g. for month `Util.daysBefore(30)`)

--- PR #2552 | src/integration/realunit/utils/price-utils.ts ---
Context: +    result.setDate(result.getDate() + days); | +    return result; | +  }
David: `Util.daysAfter`

--- PR #2552 | src/integration/realunit/utils/price-utils.ts ---
Context: +    const end = this.stripTime(endDate); | +    return Math.round((end.getTime() - start.getTime()) / (1000 * 60 * 60 * 24)); | +  }
David: `Util.daysDiff` (with rounding?)

--- PR #2552 | src/integration/realunit/dto/realunit-dto.mapper.ts ---
Context: -    dto.historicalBalances = account.historicalBalances.items.map((hb) => ({ | + | +    const historicalPricesMap = new Map(historicalPrices.map((price) => [price.timestamp.getTime(), price]));
David: Can `historicalPrices` be undefined?

--- PR #2552 | src/integration/realunit/utils/price-utils.ts ---
Context: +    result.setDate(result.getDate() + days); | +    return result; | +  }
David: Should be the same as `Util.daysAfter`

--- PR #2552 | src/integration/realunit/dto/realunit-dto.mapper.ts ---
Context: +      timestamp: hb.created, | +      valueChf: Util.round(historicalPricesMap.get(hb.created.getTime())?.chf * Number(hb.balance), 4), |      }));
David: The historical prices are still date + time, right (at least in the DB)? So we probably wouldn't find something in the map here?
In CustodyService we solved it with `Util.isoDate` as map key.

--- PR #2556 | src/integration/realunit/controllers/realunit.controller.ts ---
David: Revert

--- PR #2556 | src/subdomains/core/faucet/dto/faucet.dto.ts ---
Context: +  @ApiProperty({ description: 'Faucet asset' }) | +  asset: Asset; | +}
David: Remove (default to Ethereum/ETH)

--- PR #2556 | src/subdomains/core/faucet/entities/faucet.entity.ts ---
Context: +@Entity() | +export class Faucet extends IEntity { | +  @Column({ nullable: false })
David: FaucetRequest (rename Entity, Repo, Service, methods, variables, ...)

--- PR #2556 | src/subdomains/core/faucet/services/faucet.service.ts ---
Context: + | +    const faucets = await this.faucetRepo.find({ where: { id: userData.id } }); | +    if (faucets.length > 0) throw new BadRequestException('Faucet already exists for this account');
David: `const faucetUsed = await this.faucetRepo.exists({ where: { id: userData.id } });`

--- PR #2556 | src/subdomains/core/faucet/services/faucet.service.ts ---
David: Add a job to check for payout completion?

--- PR #2556 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: + | +    return { txId: 'fake' }; | +  }
David: What is this?

--- PR #2556 | src/subdomains/core/faucet/services/faucet.service.ts ---
Context: +    const userData = await this.userDataService.getUserData(accountId); | +    if (!userData) throw new NotFoundException('Account not found'); | +    if (userData.kycLevel < KycLevel.LEVEL_30) throw new ForbiddenException('Account not verified');
David: `async createFaucet(userId: number): Promise<FaucetDto> {...}` and then load the user by ID (incl. user data)

--- PR #2556 | src/subdomains/core/faucet-request/services/faucet-request.service.ts ---
Context: +    for (const faucet of pendingFaucets) { | +      const client = this.blockchainRegistry.getEvmClient(Blockchain.ETHEREUM); | +      try {
David: `const client = this.blockchainRegistry.getEvmClient(faucet.asset.blockchain);`

--- PR #2556 | src/subdomains/core/faucet-request/services/faucet-request.service.ts ---
Context: +        await this.faucetRequestRepo.update(...faucet.failed()); | +        this.logger.error(`Faucet request failed for tx ${faucet.txId}: ${e.message}`); | +      }
David: `this.logger.error('Faucet request ${faucet.id} failed:', e);`

--- PR #2556 | src/subdomains/core/faucet-request/services/faucet-request.service.ts ---
Context: +    if (!user.blockchains.includes(Blockchain.ETHEREUM)) throw new NotFoundException('No Ethereum address found'); | + | +    if (!user.userData) throw new NotFoundException('Account not found');
David: `user.userData` is never null

--- PR #2556 | src/subdomains/core/faucet-request/services/faucet-request.service.ts ---
Context: +    if (!user) throw new NotFoundException('User not found'); | +    if (!user.blockchains.includes(Blockchain.ETHEREUM)) throw new NotFoundException('No Ethereum address found'); | +
David: More something like `throw new BadRequestException('Faucet not available for this user');`

--- PR #2556 | src/subdomains/core/faucet-request/services/faucet-request.service.ts ---
Context: +    const faucetUsed = await this.faucetRequestRepo.exists({ | +      where: { id: user.userData.id, status: Not(FaucetRequestStatus.FAILED) }, | +    });
David: `where: { userData: { id: user.userData.id} }`

--- PR #2556 | src/subdomains/core/faucet-request/services/faucet-request.service.ts ---
Context: +    }); | +    if (!faucetUsed) throw new BadRequestException('Faucet already exists for this account'); | +
David: `BadRequestException('Faucet already used for this account');`

--- PR #2556 | src/subdomains/core/faucet-request/services/faucet-request.service.ts ---
Context: +    await this.faucetRequestRepo.save(faucetRequest); | + | +    return { txId, amount: sendFee.amount };
David: Add try-catch with ServiceUnavailableException

--- PR #2556 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context:      private readonly bankAccountService: BankAccountService, | +    @Inject(forwardRef(() => KycAdminService)) |      private readonly kycAdminService: KycAdminService,
David: Still required (all forward refs)?

--- PR #2556 | src/subdomains/generic/user/models/user/user.controller.ts ---
Context: +  async faucetRequest(@GetJwt() jwt: JwtPayload): Promise<FaucetRequestDto> { | +    return this.faucetService.createFaucet(jwt.user); | +  }
David: Move to FaucetController
```TS
@ApiTags('Faucet')
@Controller('faucet')
export class FaucetController {

...

  @Post()
  @ApiBearerAuth()
  @ApiExcludeEndpoint()
  @UseGuards(AuthGuard(), RoleGuard(UserRole.USER), UserActiveGuard())
  async faucetRequest(@GetJwt() jwt: JwtPayload): Promise<FaucetRequestDto> {...}
}
```

--- PR #2556 | src/subdomains/core/faucet-request/services/faucet-request.service.ts ---
Context: +      const asset = await this.assetService.getNativeAsset(Blockchain.ETHEREUM); | +      const sendFee = await this.payoutService.estimateBlockchainFee(asset); | +      const txId = await client.sendNativeCoinFromDex(user.address, sendFee.amount);
David: Check if this is enough

--- PR #2556 | src/subdomains/core/faucet-request/services/faucet-request.service.ts ---
Context: +      return { txId, amount: sendFee.amount }; | +    } catch { | +      throw new ServiceUnavailableException('Faucet currently not available');
David: Log error

--- PR #2556 | src/subdomains/core/faucet-request/dto/faucet-request.dto.ts ---
Context: +export class FaucetRequestDto { | +  @ApiProperty({ description: 'Transaction ID of faucet transaction' }) | +  transactionId: string;
David: txId

--- PR #2556 | src/subdomains/core/faucet-request/entities/faucet-request.entity.ts ---
Context: +export class FaucetRequest extends IEntity { | +  @Column({ nullable: false }) | +  transactionId: string;
David: txId (+ update migration)

--- PR #2556 | migration/1763506597242-AddFaucetRequest.js ---
Context: +        await queryRunner.query(`ALTER TABLE "user_data" ADD "phoneCallIpCountryCheckDate" datetime2`); | +        await queryRunner.query(`DROP TABLE "faucet_request"`); | +    }
David: Remove `user_data` things

--- PR #2558 | src/subdomains/generic/user/models/bank-data/bank-data.service.ts ---
Context: +  get bankBalanceObservable(): Observable<BankData> { | +    return this.newUserBankDataSubject.asObservable(); | +  }
David: Copy&paste?

--- PR #2558 | src/subdomains/supporting/bank-tx/bank-tx/services/bank-tx.service.ts ---
Context: +  async checkAssignAndNotifyUserData(iban: string, userData: UserData): Promise<void> { | +    const bankTxs = await this.getUnassignedBankTx([iban], { transaction: { userData: true } }); | +    if (!bankTxs.length) return;
David: This check is quite useless

--- PR #2560 | src/subdomains/core/buy-crypto/process/entities/buy-crypto.entity.ts ---
Context:    } |   | +  get correlationId(): string {
David: networkStartCorrelationId?

--- PR #2560 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context: + | +  @ApiPropertyOptional() | +  networkStartTx?: NetworkStartTxDto;
David: `{ type: NetworkStartTxDto }`

--- PR #2560 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context:            ? Util.roundReadable( | -              (entity.totalFeeAmount - (blockchainFee + networkStartFee)) * referencePrice, | +              (entity.totalFeeAmount - blockchainFee) * referencePrice,
David: Revert

--- PR #2560 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context:          entity.totalFeeAmount != null |            ? Util.roundReadable(entity.totalFeeAmount * referencePrice, feeAmountType(entity.inputAssetEntity)) |            : null,
David: Subtract networkStartFee

--- PR #2560 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context:        externalTransactionId: buyCrypto.transaction.externalId, | +      networkStartTx: buyCrypto.networkStartFeeAmount | +        ? {
David: ```TS
networkStartTx: buyCrypto.networkStartAmount
   ? {
```

--- PR #2560 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context: +export class NetworkStartTxDto { | +  @ApiProperty() | +  transactionId: string;
David: txId

--- PR #2560 | src/subdomains/supporting/payment/dto/transaction.dto.ts ---
Context: + | +  @ApiProperty() | +  transactionUrl: string;
David: txUrl

--- PR #2562 | src/integration/realunit/realunit.service.ts ---
Context: +      this.pricingService.getPrice(PriceCurrency.CHF, realuAsset, PriceValidity.ANY).catch(() => null), | +      this.pricingService.getPrice(PriceCurrency.EUR, realuAsset, PriceValidity.ANY).catch(() => null), | +      this.pricingService.getPrice(PriceCurrency.USD, realuAsset, PriceValidity.ANY).catch(() => null),
David: I think prices need to be inverted

--- PR #2562 | src/integration/realunit/realunit.service.ts ---
Context: +      this.pricingService.getPrice(PriceCurrency.CHF, realuAsset, PriceValidity.ANY).catch(() => null), | +      this.pricingService.getPrice(PriceCurrency.EUR, realuAsset, PriceValidity.ANY).catch(() => null), | +      this.pricingService.getPrice(PriceCurrency.USD, realuAsset, PriceValidity.ANY).catch(() => null),
David: But it also changed
```TS
// from
chf: price.price
// to
chf: chfPrice ? Util.round(chfPrice.convert(1), 8) : undefined
// where
  convert(fromAmount: number, decimals?: number): number {
    const targetAmount = fromAmount / this.price;
    return decimals != null ? Util.round(targetAmount, decimals) : targetAmount;
  }
  ```
  But maybe I'm wrong 😁

--- PR #2566 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context:   | -    const custodyProvider = await this.custodyProviderService.getWithMasterKey(dto.signature); | +    const custodyProvider = !dto.key ? await this.custodyProviderService.getWithMasterKey(dto.signature) : undefined;
David: Maybe just `await this.custodyProviderService.getWithMasterKey(dto.signature).catch(() => undefined);`?

--- PR #2567 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context: +                feeAmountType(entity.inputAssetEntity), | +              ) | +            : Util.roundReadable(entity.totalFeeAmount * referencePrice, feeAmountType(entity.inputAssetEntity))
David: Create a helper variable?
`const totalFee = networkStartAmount ? entity.totalFeeAmount - networkStartFee : entity.totalFeeAmount;`

--- PR #2567 | src/subdomains/core/history/mappers/transaction-dto.mapper.ts ---
Context:      const referencePrice = entity.inputAmount / entity.inputReferenceAmount; |      const networkStartFee = (entity instanceof BuyCrypto && entity.networkStartFeeAmount) || 0; | +    const networkStartAmount = (entity instanceof BuyCrypto && entity.networkStartFeeAmount) || undefined;
David: Looks suspicious

--- PR #2568 | src/subdomains/core/buy-crypto/routes/swap/swap.controller.ts ---
Context: +      [UserDataStatus.BLOCKED, UserDataStatus.DEACTIVATED], | +      [RiskStatus.BLOCKED, RiskStatus.SUSPICIOUS, RiskStatus.BLOCKED_BUY], | +    ),
David: Swap is actually buy+sell, or nothing? 🤔

--- PR #2569 | src/config/config.ts ---
Context:   | +  scrypt = { | +    wsUrl: process.env.SCRYPT_WS_URL ?? 'wss://otc.scrypt.swiss/ws/v1',
David: Remove default

--- PR #2569 | src/integration/exchange/services/scrypt.service.ts ---
Context: +  readonly name: string = 'Scrypt'; | + | +  constructor(private readonly registry: ExchangeRegistryService) {}
David: It looks like we talk to Scrypt with a websocket -> no HTTP requests possible

--- PR #2569 | src/integration/exchange/services/scrypt.service.ts ---
Context: +      ws.on('close', () => { | +        this.logger.verbose('Scrypt WebSocket closed'); | +      });
David: Could it make sense to keep the websocket for the whole service lifetime instead of recreating it for every request? Maybe check the docu.

--- PR #2569 | src/integration/exchange/services/scrypt.service.ts ---
Context: +  ): Promise<ScryptWithdrawResponse> { | +    const clReqId = `dfx-withdraw-${Date.now()}-${Math.random().toString(36).slice(2, 11)}`; | +
David: Replace `${Date.now()}-${Math.random().toString(36).slice(2, 11)}` with `Util.randomString()`?

--- PR #2569 | src/integration/exchange/services/scrypt.service.ts ---
Context: +      reqid: Date.now(), | +      type: 'NewWithdrawRequest', | +      data: [
David: Use type enum

--- PR #2569 | src/integration/exchange/services/scrypt.service.ts ---
Context: +    const transaction = transactions.find( | +      (t) => t.ClReqID === clReqId && t.TransactionType === 'Withdrawal', | +    );
David: Can't we fetch the withdrawal directly by ID?

--- PR #2569 | src/integration/exchange/services/scrypt.service.ts ---
Context: +      subscribeMessage, | +      (message) => message.type === 'BalanceTransaction' && message.initial === true, | +    );
David: Use type enum

--- PR #2569 | src/integration/exchange/services/scrypt.service.ts ---
Context: +      this.logger.warn('Scrypt API credentials not configured'); | +      throw new Error('Scrypt API credentials not configured'); | +    }
David: Can be removed

--- PR #2569 | src/integration/exchange/services/scrypt.service.ts ---
Context: +  ): Promise<ScryptMessage> { | +    const config = GetConfig().scrypt; | +
David: `Config.scrypt`

--- PR #2569 | src/subdomains/core/liquidity-management/adapters/actions/scrypt.adapter.ts ---
Context: +    if (withdrawal.status !== 'Complete') { | +      this.logger.verbose(`Withdrawal ${correlationId} status: ${withdrawal.status}`); | +      return false;
David: Use status enum

--- PR #2569 | src/integration/exchange/services/scrypt.service.ts ---
Context: +      const hmac = createHmac('sha256', config.apiSecret); | +      hmac.update(signaturePayload); | +      const signature = hmac.digest('base64').replace(/\+/g, '-').replace(/\//g, '_');
David: Use Util

--- PR #2569 | src/subdomains/core/liquidity-management/adapters/actions/scrypt.adapter.ts ---
Context: +    } | + | +    return true;
David: - If withdraw to exchange, we need to verify deposit on target exchange
- For withdraw to address I'm not sure 🤔

--- PR #2569 | src/integration/exchange/enums/exchange.enum.ts ---
Context:    MEXC = 'MEXC', | +  SCRYPT = 'Scrypt', |  }
David: Revert?

--- PR #2569 | src/subdomains/core/liquidity-management/adapters/balances/exchange.adapter.ts ---
Context: -      const exchangeService = this.exchangeRegistry.get(exchange); | +      const exchangeService = exchange === 'Scrypt' ? this.scryptService : this.exchangeRegistry.get(exchange); |        const balances = await exchangeService.getTotalBalances();
David: `'Scrypt' -> LiquidityManagementSystem.SCRYPT`

--- PR #2569 | src/subdomains/core/liquidity-management/adapters/actions/scrypt.adapter.ts ---
Context: +  private isBalanceTooLowError(e: Error): boolean { | +    return ['Insufficient funds', 'insufficient balance', 'not enough balance'].some((m) => e.message?.includes(m)); | +  }
David: This is probably not correct for Scrypt?

--- PR #2569 | src/subdomains/core/liquidity-management/adapters/actions/scrypt.adapter.ts ---
Context: +      this.logger.verbose(`Withdrawal ${correlationId} status: ${withdrawal.status}`); | +      return false; | +    }
David: Remove `logger.verbose`s

--- PR #2569 | src/subdomains/core/liquidity-management/adapters/actions/scrypt.adapter.ts ---
Context: + | +    order.outputAmount = withdrawal.amount ?? order.inputAmount; | +
David: I think this is not required

--- PR #2569 | src/subdomains/core/liquidity-management/adapters/actions/scrypt.adapter.ts ---
Context: +    order.outputAmount = withdrawal.amount ?? order.inputAmount; | + | +    return true;
David: Add TX check like
```TS
    const blockchain = paramMap.destinationBlockchain as Blockchain;
    return this.dexService.checkTransferCompletion(withdrawal.txid, blockchain);
```

--- PR #2569 | src/integration/exchange/services/__tests__/scrypt-api-test.ts ---
David: Delete this file

--- PR #2579 | src/subdomains/core/custody/services/custody-pdf.service.ts ---
Context: + | +const dfxLogoText = | +  'M61.5031 0H124.245C170.646 0 208.267 36.5427 208.267 84.0393C208.267 131.536 169.767 170.018 122.288 170.018H61.5031V135.504H114.046C141.825 135.504 164.541 112.789 164.541 85.009C164.541 57.2293 141.825 34.5136 114.046 34.5136H61.5031V0ZM266.25 31.5686V76.4973H338.294V108.066H266.25V170H226.906V0H355.389V31.5686H266.25ZM495.76 170L454.71 110.975L414.396 170H369.216L432.12 83.5365L372.395 0H417.072L456.183 55.1283L494.557 0H537.061L477.803 82.082L541.191 170H495.778H495.76Z';
David: Move to some shared place

--- PR #2579 | src/subdomains/core/custody/services/custody-pdf.service.ts ---
Context: +        return balances.reduce((sum, b) => sum + b.value.usd, 0); | +    } | +  }
David: Use `CustodyService.getUserCustodyBalance`

--- PR #2579 | src/subdomains/core/custody/dto/input/get-custody-pdf.dto.ts ---
Context: +  @IsOptional() | +  @IsEnum(PdfLanguage) | +  language?: PdfLanguage = PdfLanguage.EN;
David: Use language from user data instead

--- PR #2580 | src/subdomains/core/custody/dto/output/custody-balance.dto.ts ---
Context: +  price: CustodyFiatValueDto; | + | +  @ApiProperty({ description: 'Total value in fiat values' })
David: Can this be reverted?

--- PR #2580 | src/shared/utils/pdf.util.ts ---
Context: +  [Blockchain.BASE]: 'ethereum', | +  [Blockchain.GNOSIS]: 'xdai', | +};
David: Move this to CoinGecko. Has nothing to do with PDF

--- PR #2580 | src/subdomains/core/custody/services/custody-pdf.service.ts ---
Context: +    if (asset.chainId && platform) { | +      return this.coinGeckoService.getHistoricalPriceByContract(platform, asset.chainId, date, currencyLower); | +    }
David: Looks like this should be moved to CoinGecko service (is duplicated in BalancePdfService?)

--- PR #2592 | src/integration/bank/controllers/yapeal-webhook.controller.ts ---
Context: + | +    // TODO: Implement actual YAPEAL signature verification once documentation is available | +    return signature.length > 0 && secret.length > 0;
David: Is this documentation not available yet?

--- PR #2601 | src/subdomains/supporting/realunit/dto/realunit-registration.dto.ts ---
Context: +  @IsString() | +  @Transform(Util.sanitize) | +  addressStreet: string;
David: This DTO is a requirement of Aktionariat

--- PR #2630 | src/subdomains/supporting/bank-tx/bank-tx/services/bank-tx.service.ts ---
David: @Yannick1712 improve as discussed

--- PR #2637 | src/subdomains/generic/user/models/user/dto/user-dto.mapper.ts ---
Context: +      address: userData.address.country | +        ? { ...userData.address, country: CountryDtoMapper.entityToDto(userData.address.country) } | +        : undefined,
David: Fix `userData.address`

--- PR #2649 | src/shared/models/setting/setting-schema.registry.ts ---
Context: @@ -0,0 +1,37 @@ | +import { Type } from '@nestjs/common'; | +import { Process } from 'src/shared/services/process.service';
David: Unused import

--- PR #2650 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
David: Remove all methods that are no longer in use, e.g.
- getAllYapealIbans
- getIbansByName
- getAllActiveVirtualIbans

--- PR #2650 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context:   | -  private async getAccountIbanForEntity(entity: FiatOutput, country: any): Promise<string | undefined> { | +  private async getAccountIbanForEntity(
David: Rename this method? E.g. `getPayoutAccount`?

--- PR #2650 | src/subdomains/supporting/fiat-output/fiat-output-job.service.ts ---
Context: +    return bank?.isCountryEnabled(country) | +      ? { accountIban: bank.iban, bank } | +      : { accountIban: undefined, bank: undefined };
David: We should probably have the `isCountryEnabled` check also for virtual IBANs? And only use vIBANs if the related bank has `send = true`?

--- PR #2665 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:        userData.accountType === AccountType.ORGANIZATION | -        ? await this.settingService.get('defaultIdentMethodOrganization', KycStepType.SUMSUB_VIDEO) | +        ? await this.settingService.get('defaultIdentMethodOrganization', KycStepType.SUMSUB_AUTO)
David: Update setting in DB after merge

--- PR #2665 | src/subdomains/core/aml/services/aml-helper.service.ts ---
David: Remove unused import

--- PR #2676 | src/subdomains/core/buy-crypto/routes/buy/buy.service.ts ---
Context: +    ) | +      throw new BadRequestException('Trading not allowed'); | +  }
David: @Yannick1712 move to buyCheck?

--- PR #2694 | src/subdomains/supporting/payin/strategies/register/impl/cardano.strategy.ts ---
Context: +    for (const activeDepositAddress of activeDepositAddresses) { | +      await this.pollAddress(BlockchainAddress.create(activeDepositAddress, this.blockchain)); | +    }
David: This get's us in trouble with `lastCheckedBlockHeight`

--- PR #2694 | src/subdomains/supporting/payin/strategies/register/impl/cardano.strategy.ts ---
Context:          select: ['id', 'blockHeight'], | -        where: { address: { blockchain: this.blockchain } }, | +        where: [{ address: { blockchain: this.blockchain } }, { senderAddresses: depositAddress.address }],
David: depositAddress is address?

--- PR #2700 | src/subdomains/supporting/log/log-job.service.ts ---
Context: +        k.method === 'Bank Frick (SEPA) International' && // TODO adapt to yapeal | +        k.address === 'Maerki Baumann & Co. AG', // TODO adapt to yapeal |      );
David: TODO?

--- PR #2700 | src/subdomains/supporting/log/log-job.service.ts ---
Context:      // EUR: Maerki -> Kraken |      const eurSenderBankTx = recentKrakenBankTx.filter( | -      (b) => b.accountIban === maerkiEurBank.iban && b.creditDebitIndicator === BankTxIndicator.DEBIT,
David: Fix comments

--- PR #2700 | src/subdomains/supporting/log/log-job.service.ts ---
Context:          k.method === 'Bank Frick (SIC) International' && | -        k.address === 'MAEBCHZZXXX', | +        k.address === 'YAPECHZ2XXX',
David: Take BIC from bank?

--- PR #2701 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
David: Add migration

--- PR #2701 | src/subdomains/core/buy-crypto/routes/buy/buy.service.ts ---
Context: +      }) | +      .getRawOne<{ volume: number }>(); | +  }
David: This does not work, we want the total volume of all buy transactions in this month, not of the buy routes created in this month (same or sell and swap).

--- PR #2820 | src/subdomains/supporting/bank/bank/bank.controller.ts ---
Context: + | +    return vIbans.map(VirtualIbanMapper.toDto); | +  }
David: I think I would place this endpoint next to the `PUT /personalIban` as `GET /personalIban`, WDYT?

--- PR #2820 | src/subdomains/supporting/bank/virtual-iban/virtual-iban.service.ts ---
Context: +  async getVirtualIbanForUser(userDataId: number): Promise<VirtualIban[]> { | +    return this.virtualIbanRepo.findCachedBy(userDataId, { userData: { id: userDataId } }); | +  }
David: This method already exists. But I think we should only return active ones.

--- PR #2820 | src/subdomains/supporting/bank/virtual-iban/virtual-iban.service.ts ---
Context: +  async getVirtualIbanForAccount(userDataId: number): Promise<VirtualIban[]> { | +    return this.virtualIbanRepo.findCachedBy(userDataId, { userData: { id: userDataId } }); | +  }
David: I would use `user-${userData.id}` as cache key. Otherwise it might conflict with virtual IBAN id.

--- PR #2820 | src/subdomains/supporting/bank/virtual-iban/virtual-iban.service.ts ---
Context: + | +  async getVirtualIbanForAccount(userDataId: number): Promise<VirtualIban[]> { | +    return this.virtualIbanRepo.findCachedBy(userDataId, { userData: { id: userDataId } });
David: `getVirtualIbansForAccount`

--- PR #2820 | src/subdomains/core/buy-crypto/routes/buy/buy.controller.ts ---
Context: +  @ApiOkResponse({ type: VirtualIbanDto, isArray: true }) | +  async getAllPersonalBanks(@GetJwt() jwt: JwtPayload): Promise<VirtualIbanDto[]> { | +    return this.virtualIbanService.getVirtualIbanForAccount(jwt.account).then((vI) => vI.map(VirtualIbanMapper.toDto));
David: `getAllPersonalBanks` is a bit weird 😄

--- PR #2820 | src/subdomains/core/buy-crypto/routes/buy/buy.controller.ts ---
Context: +  @ApiBearerAuth() | +  @UseGuards(AuthGuard(), RoleGuard(UserRole.USER), UserActiveGuard()) | +  @ApiOkResponse({ type: VirtualIbanDto, isArray: true })
David: `UserRole.ACCOUNT` should be enough? And we should probably do the same for `  @Post('/personalIban')`

--- PR #2820 | src/subdomains/core/buy-crypto/routes/buy/buy.controller.ts ---
Context:      AuthGuard(), | -    RoleGuard(UserRole.USER), | +    RoleGuard(UserRole.ACCOUNT),
David: Use `jwt.account` below

--- PR #2831 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +        isConfirmed: true, | +        confirmationDate: new Date(), | +      });
David: Should we only confirm it on first login?

--- PR #2831 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +        isConfirmed: true, | +        confirmationDate: new Date(), | +      });
David: This would mean we remove 
```TS
      await this.updateRecommendationInternal(entity, {
        isConfirmed: true,
        confirmationDate: new Date(),
      });
```
and confirm it only when the user logs in first time?

--- PR #2832 | src/subdomains/generic/user/models/recommendation/recommendation.service.ts ---
Context: +        (await this.userService.getAllUserDataUsers(entity.recommended.id))) { | +        await this.userService.updateUserInternal(user, { usedRef: refCode }); | +      }
David: Should we override the usedRef, even if the user already has one?

--- PR #2879 | src/subdomains/core/aml/enums/aml-reason.enum.ts ---
Context:    INTERMEDIARY_WITHOUT_SENDER = 'IntermediaryWithoutSender', | +  NAME_TOO_SHORT = 'NameTooShort', |  }
David: @Yannick1712 do we need to add mail texts when adding a new AmlReason?

--- PR #2882 | src/subdomains/core/liquidity-management/adapters/actions/dfx-dex.adapter.ts ---
Context:    SELL = 'sell', |    WITHDRAW = 'withdraw', | +  SWAP = 'swap',
David: Use purchase/sell instead of swap (same functionality), add a trade asset parameter

--- PR #2907 | migration/1768216892259-BankAddressMax.js ---
Context: +        await queryRunner.query(`ALTER TABLE "bank_account" DROP COLUMN "bankAddress"`); | +        await queryRunner.query(`ALTER TABLE "bank_account" ADD "bankAddress" nvarchar(256)`); | +    }
David: Switch to column update (no drop)

--- PR #2915 | src/subdomains/supporting/realunit/realunit.service.ts ---
David: Revert

--- PR #2919 | src/subdomains/supporting/payout/strategies/payout/impl/monero.strategy.ts ---
David: Improve group creation

--- PR #2960 | scripts/setup.sh ---
David: Remove complete changes in scripts folder?

--- PR #2960 | src/integration/infrastructure/azure-storage.service.ts ---
David: Can we remove the dummy files here?

--- PR #2962 | src/subdomains/supporting/payout/services/payout.service.ts ---
Context: +    const strategy = this.payoutStrategyRegistry.getPayoutStrategy(order.asset); | +    await strategy.doPayout([order]); | +  }
David: I would only payout the transaction if we are sure it failed (verify on-chain)

--- PR #2968 | src/subdomains/supporting/support-issue/enums/support-issue.enum.ts ---
Context:    ON_HOLD = 'OnHold', | +  BOT_MESSAGE = 'BotMessage', |  }
David: Do we really need a state for this? Can't we use clerk field?

--- PR #2968 | src/shared/services/process.service.ts ---
Context:    TRADE_APPROVAL_DATE = 'TradeApprovalDate', | +  SUPPORT_ISSUE_AUTO_TEMPLATES = 'SupportIssueAutoTemplates', |  }
David: Weird name `SupportIssueAutoTemplates`. Maybe just `SupportBot` or `SupportVirtualAgent` or similar?

--- PR #2968 | src/subdomains/supporting/support-issue/services/support-issue-job.service.ts ---
Context: +  async sendAutoTemplates() { | +    await this.moneroComplete(); | +  }
David: Weird name `sendAutoTemplates`. More like `sendAutoResponses`?

--- PR #2968 | src/subdomains/supporting/support-issue/services/support-issue-job.service.ts ---
Context: +      await this.supportIssueService.updateIssueInternal(entity, { state: SupportIssueInternalState.BOT_MESSAGE }); | +    } | +  }
David: Maybe we should directly use a generic implementation where you can just specify the conditions and the resulting SupportMessageTranslationKey? In this example it would be:
Condition:
```TS
        type: SupportIssueType.TRANSACTION_ISSUE,
        reason: In([SupportIssueReason.FUNDS_NOT_RECEIVED, SupportIssueReason.TRANSACTION_MISSING]),
        transaction: {
          buyCrypto: { id: Not(IsNull()), isComplete: true, amlCheck: CheckStatus.PASS, outputAsset: { name: 'XMR' } },
        },
```

--- PR #2968 | src/subdomains/supporting/support-issue/entities/support-message.entity.ts ---
Context:   |  export const CustomerAuthor = 'Customer'; | +export const BotAuthor = 'Bot';
David: Maybe more specific, e.g. `AutoResponder`?

--- PR #2968 | src/subdomains/supporting/support-issue/services/support-issue-job.service.ts ---
Context: +        message: `Hi ${entity.userData.firstname ?? entity.name}\n\n${message}\n\n${botHint}\n\nFreundliche Grüsse / Kind Regards DFX Bot`, | +        author: AutoResponder, | +      });
David: `Freundliche Grüsse / Kind Regards DFX Bot` could be translated

--- PR #2990 | infrastructure/bicep/core/modules/storageAccount.bicep ---
Context: +  'kyc' | +  'support' | +]
David: No `db-bak`

--- PR #2990 | infrastructure/bicep/core/core.bicep ---
David: Not really part of this PR, but the log analytics workspace is not connected to the application insights. It therefore creates an additional workspace.

--- PR #3001 | src/subdomains/generic/user/models/wallet/mapper/wallet-dto.mapper.ts ---
Context: +      identMethod: wallet.identMethod, | +      apiUrl: wallet.apiUrl, | +      apiKey: wallet.apiKey,
David: I wouldn't expose those things

--- PR #3001 | src/subdomains/generic/user/models/wallet/mapper/wallet-dto.mapper.ts ---
Context: +      amlRules: wallet.amlRules, | +      address: wallet.address, | +      name: wallet.name,
David: I wouldn't expose `address`

--- PR #3001 | src/subdomains/supporting/support-issue/dto/support-issue-dto.mapper.ts ---
Context: +      wallet: wallet ? WalletDtoMapper.mapWalletDto(wallet) : undefined, | +      isComplete: transaction.buyCrypto?.isComplete ?? transaction.buyFiat?.isComplete, | +    };
David: I would simplify it like:
```TS
const targetEntity = transaction.buyCrypto ?? transaction.buyFiat;
// and then
return {
   ...
   amlReason: targetEntity?.amlReason,
   comment: targetEntity?.comment
   ...
   wallet: targetEntity?.wallet ? WalletDtoMapper.mapWalletDto(targetEntity?.wallet) : undefined
   ...
};
```

--- PR #3001 | src/subdomains/supporting/support-issue/dto/support-issue.dto.ts ---
Context: + | +  @ApiProperty({ type: SupportIssueInternalAccountDataDto }) | +  userData: SupportIssueInternalAccountDataDto;
David: `account: SupportIssueInternalAccountDataDto;`?

--- PR #3001 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +      where: { id }, | +      relations: { transaction: { buyCrypto: true, buyFiat: true, user: { wallet: true } } }, | +    });
David: Should we also load the limit request?

--- PR #3001 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +    const transactionInput = (issue.transaction?.buyCrypto ?? issue.transaction?.buyFiat)?.cryptoInput?.asset; | + | +    return SupportIssueDtoMapper.mapSupportIssueData(issue, transactionInput);
David: Why is `transactionInput` passed separately?

--- PR #3002 | src/subdomains/supporting/payment/services/swiss-qr.service.ts ---
Context: +  private realunitCreditor(): Creditor { | +    const realunitBank = Config.blockchain.realunit.bank; | +    return {
David: Yes, good idea

--- PR #3002 | src/subdomains/supporting/payment/services/swiss-qr.service.ts ---
Context:      reference, | +    brand = PdfBrand.DFX, |    }: TxStatementDetails): Promise<string> {
David: I would probably have the brand as a separate parameter... it's a bit opaque in the interface.

--- PR #3002 | src/shared/utils/pdf.util.ts ---
Context:    } |   | +  static drawInvoiceLogo(pdf: InstanceType<typeof PDFDocument>, brand: PdfBrand = PdfBrand.DFX): void {
David: The only difference is positioning and scale, right?
```TS
    pdf.translate(mm2pt(20), mm2pt(14));
    pdf.scale(0.15);
```
Maybe we can have a size parameter (small/large) then?

--- PR #3012 | migration/1768943778000-AddYapealEurManualBank.js ---
David: Already inserted manually

--- PR #3027 | src/subdomains/supporting/payment/services/swiss-qr.service.ts ---
Context: + | +    return this.generateMultiPdfInvoice(tableDataWithType, language, billData, brand); | +  }
David: There is quite some duplicated code (from `createTxStatement`), can we share it somehow?

--- PR #3027 | src/subdomains/supporting/payment/services/swiss-qr.service.ts ---
Context: +    billData: QrBillData, | +    brand: PdfBrand = PdfBrand.DFX, | +  ): Promise<string> {
David: Same here

--- PR #3057 | src/subdomains/supporting/realunit/realunit.service.ts ---
Context: +        throw new ConflictException('Account already exists'); | +      } | +    }
David: Do we really need to duplicate this from UserDataService? The check is also done in `trySetUserMail`

--- PR #3057 | src/subdomains/supporting/realunit/realunit.service.ts ---
Context: +    if (userData.kycLevel < KycLevel.LEVEL_10) { | +      await this.userDataService.updateUserDataInternal(userData, { kycLevel: KycLevel.LEVEL_10 }); | +    }
David: This might break the KYC state. It's probably better to call `KycService.initializeProcess`

--- PR #3057 | src/subdomains/supporting/realunit/realunit.service.ts ---
Context: +        } else { | +          throw e; |          }
David: Merge is only requested, if error message includes `merge request sent`

--- PR #3074 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +            throw e; | +          } | +        }
David: Approval step might still be null? Maybe remove duplication like this?
```TS
        const newStep = await this.initiateStep(kycStep.userData, KycStepName.DFX_APPROVAL).catch((e) => {
          if (e.message.includes('Cannot insert duplicate key'))
            return this.kycStepRepo.findOneBy({
              name: KycStepName.DFX_APPROVAL,
              status: ReviewStatus.ON_HOLD,
              userData: { id: kycStep.userData.id },
            });

          throw e;
        });

        if (newStep) await this.kycStepRepo.update(...newStep.manualReview());
```

--- PR #3075 | src/subdomains/generic/user/models/user-data/user-data.entity.ts ---
David: Linter warning

--- PR #3075 | src/subdomains/generic/user/models/user-data/user-data.enum.ts ---
Context:   | +export enum PhoneCallPreferredTimes { | +  BETWEEN_9_AND_10 = 'Between9And10',
David: PhoneCallPreferredTime

--- PR #3075 | src/subdomains/generic/user/models/user-data/user-data.enum.ts ---
Context: +  BETWEEN_15_AND_16 = 'Between15And16', | +  BETWEEN_9_AND_16 = 'Between9And16', | +}
David: Maybe `9To10` is easier to read?

--- PR #3075 | src/subdomains/generic/user/models/user/user.controller.ts ---
Context: +  async updatePhoneCall(@GetJwt() jwt: JwtPayload, @Body() dto: UpdatePhoneCallDto): Promise<void> { | +    return this.userService.updatePhoneCall(jwt.account, dto); | +  }
David: Set via `PUT v2/user`?

--- PR #3075 | src/subdomains/core/aml/enums/aml-error.enum.ts ---
Context:    IP_COUNTRY_MISMATCH = 'IpCountryMismatch', | +  USER_DATA_REJECTED_CALL = 'UserDataRejectedCall', |    TRADE_APPROVAL_DATE_MISSING = 'TradeApprovalDateMissing',
David: Maybe failed is better? It can be both?

--- PR #3075 | src/subdomains/generic/user/models/user/dto/update-user.dto.ts ---
Context: +  @IsEnum(PhoneCallPreferredTime) | +  preferredTimes?: PhoneCallPreferredTime[]; | +
David: `preferredPhoneTimes`

--- PR #3075 | src/subdomains/generic/user/models/user/dto/update-user.dto.ts ---
Context: +  @ValidateIf((a: UpdateUserDto) => Boolean(a.repeatCall || !a.rejectCall)) | +  @IsBoolean() | +  repeatCall?: boolean;
David: `rejectPhoneCall`
`repeatPhoneCall`

--- PR #3075 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: -      if (dto.limitRequest) { | -        newIssue.department = Department.COMPLIANCE; | +      if (dto.limitRequest)
David: Why is `newIssue.department = Department.COMPLIANCE;` removed?

--- PR #3075 | src/subdomains/supporting/support-issue/enums/support-issue.enum.ts ---
Context: +  REPEAT_CALL = 'RepeatCall', | +  PREFERRED_CALL_TIME = 'PreferredCallTime', | +
David: Maybe just use other? There is no automatic logic on PREFERRED_CALL_TIME, right?

--- PR #3076 | src/subdomains/supporting/support-issue/services/support-issue-job.service.ts ---
Context: +        return (day === 2 && hour < 14) || (day > 5 && day < 2) || (day === 5 && hour >= 14); | +      }), | +    );
David: I would do the filtering only once. A new util method:
```TS
  static partition<T>(list: T[], accessor: (item: T) => boolean): [T[], T[]] {
    const truthy: T[] = [];
    const falsy: T[] = [];

    for (const item of list) {
      (accessor(item) ? truthy : falsy).push(item);
    }

    return [truthy, falsy];
  }
```
and then
```
const [standard, weekend] = Util.partition(issues, (i) => {
        const day = i.created.getDay();
        const hour = i.created.getHours();

        return (day === 2 && hour >= 14) || (day > 2 && day < 5) || (day === 5 && hour < 14);
});

await this.sendAutoResponse(SupportMessageTranslationKey.SEPA_STANDARD, standard);
await this.sendAutoResponse(SupportMessageTranslationKey. SEPA_WEEKEND, weekend);
```

--- PR #3076 | src/shared/services/process.service.ts ---
Context:    SUPPORT_BOT = 'SupportBot', | +  SUPPORT_BOT_SEPA = 'SupportBotSepa', |  }
David: Use a setting entry instead

--- PR #3076 | src/subdomains/supporting/support-issue/services/support-issue-job.service.ts ---
Context: -    await this.moneroComplete(); | +    const disabledTemplates = await this.settingsService | +      .getObj<string>('SupportBot')
David: `.get()` (no obj?)

--- PR #3076 | src/subdomains/supporting/support-issue/services/support-issue-job.service.ts ---
Context: +  MONERO_COMPLETE = 'MoneroComplete', | +  SEPA = 'Sepa', | +}
David: `enum AutoResponse { ... }`

--- PR #3081 | src/subdomains/supporting/realunit/realunit.service.ts ---
Context: +    if (isNewEmail) { | +      if (userData.mail && this.hasRegistrationForWallet(userData, dto.walletAddress)) { | +        throw new BadRequestException('Not allowed to register a new email for this address');
David: Why don't we use the walletAddress from the JWT?

--- PR #3089 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +    return this.userDataRepo.find({ | +      select: ['id', 'kycFileId', 'amlAccountType', 'verifiedName'], | +      where: { kycFileId: MoreThan(0) },
David: `select: { id: true, ...}`

--- PR #3103 | src/subdomains/generic/support/support.controller.ts ---
Context:    } |   | +  @Get('kycFileStats')
David: It matches `kycFileList`, I think this is ok

--- PR #3113 | src/integration/blockchain/bitcoin-testnet4/bitcoin-testnet4-client.ts ---
David: Can we share more code with BitcoinClient?

--- PR #3113 | src/subdomains/supporting/payout/payout.module.ts ---
David: Are these all required strategies to be able to handle buy-crypto process for Testnet4?

--- PR #3121 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:      // optional master updates | +    if (master.status === UserDataStatus.KYC_ONLY && slave.users && slave.wallet) master.wallet = slave.wallet; |      if ([UserDataStatus.KYC_ONLY, UserDataStatus.DEACTIVATED].includes(master.status)) master.status = slave.status;
David: `slave.users.length`?

--- PR #3125 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context: +  async checkOrTriggerVideoIdent(userData: UserData): Promise<void> { |      await this.kycAdminService.triggerVideoIdentInternal(userData); |    }
David: Missing something?

--- PR #3139 | src/subdomains/core/buy-crypto/process/services/buy-crypto-preparation.service.ts ---
Context:          cryptoRoute: true, | -        transaction: { user: { wallet: true }, userData: true }, | +        transaction: { user: { wallet: true }, userData: { kycSteps: true } },
David: Lazy load KYC steps where needed

--- PR #3179 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:        inputAsset, | -      refundAmount, | +      refundAmount: refundAmount > refundEntity.refundAmount ? refundEntity.refundAmount : refundAmount,
David: `Math.min`?

--- PR #3179 | src/subdomains/core/history/dto/refund-data.dto.ts ---
Context:   | +  @ApiPropertyOptional({ type: Price, description: 'Price refund asset in input asset' }) | +  refundPrice?: Price;
David: I think we can remove the Swagger docu here, this price is not really important

--- PR #3179 | src/subdomains/core/history/dto/refund-data.dto.ts ---
Context:   | +  @ApiPropertyOptional({ type: Price, description: 'Price refund asset in input asset' }) | +  refundPrice?: Price;
David: Can the refundPrice be undefined?

--- PR #3196 | src/integration/exchange/services/mexc.service.ts ---
Context:      Spark: undefined, | +    Firo: undefined, |      Monero: 'XMR',
David: `Firo: 'XZC'`

--- PR #3196 | src/integration/exchange/services/binance.service.ts ---
Context:      Spark: undefined, | +    Firo: undefined, |      Monero: 'XMR',
David: `Firo: 'FIRO'`

--- PR #3196 | src/subdomains/supporting/dex/services/dex-firo.service.ts ---
Context: +    const feeRate = await this.client.estimateSmartFee(1); | +    return (feeRate ?? 10) * 1.5; | +  }
David: Why don't we use FiroFeeService?

--- PR #3196 | src/subdomains/core/payment-link/services/payment-link-fee.service.ts ---
Context:        try { | -        const fee = await this.calculateFee(blockchain); | +        const fee = GetConfig().environment === Environment.LOC ? 0 : await this.calculateFee(blockchain);
David: Why this?

--- PR #3196 | src/subdomains/core/payment-link/services/payment-balance.service.ts ---
Context:    async forwardDeposits() { | -    const chainsWithoutForwarding = [Blockchain.BITCOIN, ...this.chainsWithoutPaymentBalance]; | +    const chainsWithoutForwarding = [Blockchain.BITCOIN, Blockchain.FIRO, ...this.chainsWithoutPaymentBalance];
David: Either forward to liq address, or include payment address in balance/sending

--- PR #3196 | src/subdomains/core/payment-link/services/payment-balance.service.ts ---
David: Make sure `getPaymentBalances` is correct (**for Firo and Bitcoin**)

--- PR #3196 | src/integration/blockchain/firo/services/firo-fee.service.ts ---
Context: + | +    return baseRate * multiplier; | +  }
David: Do the same also for Bitcoin?

--- PR #3202 | src/subdomains/supporting/payment/services/transaction-helper.ts ---
Context:      if (!inputCurrency.refundEnabled) throw new BadRequestException(`Refund for ${inputCurrency.name} not allowed`); |   |      // CHF refunds only to domestic IBANs
David: Move comment to `getRefundCurrency` method

--- PR #3216 | src/config/config.ts ---
Context:    amlCheckLastNameCheckValidity = 90; // days | +  kycStepExpiry = 90; // days |    allowedBorderRegions = ['CH', 'DE']; // aml & kyc
David: Move to KYC subobject?

--- PR #3216 | src/subdomains/generic/kyc/entities/kyc-step.entity.ts ---
Context: +  addComment(comment: string): string | undefined { | +    if (!comment) return this.comment; | +    return !this.comment ? comment : `${this.comment};${comment}`;
David: Maybe `[this.comment, comment].filter(c => c).join(';')`?

--- PR #3221 | src/subdomains/supporting/realunit/realunit.service.ts ---
Context: +        return result?.walletAddress && !Util.equalsIgnoreCase(result.walletAddress, currentWalletAddress); | +      }); | +  }
David: Looks like `findExistingRegistrationStep` could be integrated directly into the search at the top (no need to duplicate the code IMO).

Maybe also join with logic from `hasRegistrationForWallet`.

--- PR #3229 | src/subdomains/supporting/realunit/controllers/realunit.controller.ts ---
Context: -    await this.realunitService.confirmBuy(jwt.user, +id); | +  @ApiOkResponse({ description: 'Payment confirmed', schema: { properties: { reference: { type: 'string' } } } }) | +  async confirmBuy(@GetJwt() jwt: JwtPayload, @Param('id') id: string): Promise<{ reference: string }> {
David: Yes

--- PR #3230 | src/subdomains/supporting/payment/services/special-external-account.service.ts ---
Context:   | +  async getPhoneCallList(types?: SpecialExternalAccountType[]): Promise<SpecialExternalAccount[]> { | +    return this.specialExternalAccountRepo.findCachedBy(`PhoneCallList-${types?.toString()}`, {
David: Why this optional param?

--- PR #3230 | src/subdomains/core/aml/services/aml-helper.service.ts ---
Context: +          ) | +        ) | +          errors.push(AmlError.IBAN_PHONE_VERIFICATION_NEEDED);
David: `blacklist` or `phoneCallList`?

--- PR #3250 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:   |    // --- HELPER METHODS --- // | +  private async createTradeApprovalLog(userData: UserData, tradeApprovalDate: Date): Promise<void> {
David: Could be interesting, yes

--- PR #3250 | src/subdomains/generic/user/models/user-data/user-data.enum.ts ---
Context: +  USER_DATA_MERGE = 'UserDataMerge', | +  PERSONAL_DATA_ORGANIZATION = 'PersonalDataOrganization', | +  KYC_STEP_COMPLETED = 'KycStepCompleted',
David: Just `ORGANIZATION = 'Organization',`?

--- PR #3255 | src/integration/blockchain/icp/services/icp.service.ts ---
Context: +  getPaymentRequest(address: string, amount: number): string { | +    return `internetComputer:${address}?amount=${Util.numberToFixedString(amount)}`; | +  }
David: `icp:...`?

--- PR #3255 | src/integration/blockchain/icp/services/icp.service.ts ---
Context: +  async getTxActualFee(blockIndex: string): Promise<number> { | +    return this.client.getTxActualFee(blockIndex); | +  }
David: We normally (other chains) call the client directly from the respective service, why not here?

--- PR #3255 | src/integration/blockchain/icp/icp.controller.ts ---
David: Can this be removed?

--- PR #3255 | src/subdomains/core/payment-link/enums/index.ts ---
Context:    Blockchain.CARDANO, | +  Blockchain.INTERNET_COMPUTER, |  ];
David: Does it really belong here?

--- PR #3255 | src/integration/exchange/services/binance.service.ts ---
Context:      Tron: 'TRX', | +    InternetComputer: undefined, |      Citrea: undefined,
David: `    InternetComputer: 'ICP',`

--- PR #3255 | src/integration/exchange/services/mexc.service.ts ---
Context:      Tron: 'TRX', | +    InternetComputer: undefined, |      Citrea: undefined,
David: `    InternetComputer: 'ICP',`

--- PR #3255 | src/subdomains/supporting/payout/strategies/payout/impl/base/icp.strategy.ts ---
Context: + | +          // ICP tokens use Reverse Gas Model: fee is paid in the token itself | +          const feeAsset = isToken ? order.asset : await this.feeAsset();
David: `feeAsset` is always `order.asset`?

--- PR #3255 | src/subdomains/supporting/payout/strategies/payout/impl/base/icp.strategy.ts ---
Context: + | +    // ICP tokens use Reverse Gas Model: fee is paid in the token itself | +    const feeAsset = asset.type === AssetType.TOKEN ? asset : await this.feeAsset();
David: same here?

--- PR #3255 | src/subdomains/supporting/payin/strategies/send/impl/base/icp.strategy.ts ---
Context: +        payIn.asset.type === AssetType.TOKEN | +          ? payIn.asset | +          : await this.assetService.getNativeAsset(payIn.asset.blockchain);
David: Same here

--- PR #3255 | src/subdomains/supporting/payin/strategies/register/impl/icp.strategy.ts ---
David: Use Cardano/Citrea logic

--- PR #3255 | src/integration/blockchain/icp/icp-client.ts ---
Context: + | +    const sendAmount = Math.min(amount, balance) - this.transferFee; | +    if (sendAmount <= 0)
David: Is a bit weird to change the amount we want to send?

--- PR #3255 | src/integration/blockchain/icp/icp-client.ts ---
Context: +    const sendAmount = Math.min(amount, balance) - fee; | +    if (sendAmount <= 0) | +      throw new Error(`Insufficient token balance for payment forward: balance=${balance}, fee=${fee}`);
David: Same here?

--- PR #3255 | src/integration/blockchain/icp/icp-client.ts ---
Context: +    canisterId: string, | +    decimals: number, | +  ): Promise<string> {
David: Use asset param instead

--- PR #3257 | src/subdomains/core/aml/services/aml-helper.service.ts ---
Context: +          !entity.userData.phoneCallCheckDate && | +          (!entity.userData.accountType || entity.userData.accountType === AccountType.PERSONAL) && |            phoneCallList.some((b) =>
David: Helper getter?

--- PR #3257 | src/subdomains/supporting/payment/entities/special-external-account.entity.ts ---
Context:    BANNED_ACCOUNT_IBAN = 'BannedAccountIban', |    AML_PHONE_CALL_NEEDED_BIC_BUY = 'AmlPhoneCallNeededBicBuy', | +  AML_PHONE_CALL_NEEDED_BLZ_BUY = 'AmlPhoneCallNeededBicBuy',
David: Duplicate enum member value AmlPhoneCallNeededBicBuy

--- PR #3257 | src/shared/utils/util.ts ---
David: Why not use our IBAN tools?

`const { countryCode, bankIdentifier } = IbanTools.extractIBAN(iban)`

--- PR #3258 | src/subdomains/core/payment-link/services/payment-balance.service.ts ---
Context: + | +    // sweep all UTXOs: send full balance and let Bitcoin Core subtract the fee from the output | +    return client.sendMany([{ addressTo: outputAddress, amount: utxoBalance }], feeRate, inputs, [0]);
David: This logic is already in FiroClient.sendMany. Maybe we should add a method sendManyFromAddress to BitcoinBasedClient and reuse it in both places? Is this possible?

--- PR #3258 | src/integration/blockchain/bitcoin/node/bitcoin-based-client.ts ---
Context: +  ): Promise<string> { | +    const utxos = await this.getUtxoForAddresses(fromAddresses, this.nodeConfig.allowUnconfirmedUtxos); | +    if (!utxos.length) return '';
David: Throw error if no UTXOs?

--- PR #3286 | src/subdomains/generic/user/models/user/user.service.ts ---
Context: +        ? Config.defaultRef | +        : usedRef; |    }
David: Maybe refactor this a bit?
```TS
  private async checkRef(user: User, usedRef?: string, existingUsedRef?: string): Promise<string> {
    if (usedRef) {
      const refUser = await this.getRefUser(usedRef);
      const isValidRef = usedRef !== user.ref && refUser && user?.userData?.id !== refUser?.userData?.id;
      if (isValidRef) return usedRef;
    }

    return existingUsedRef ?? Config.defaultRef;
  }
```

--- PR #3290 | src/shared/i18n/de/mail.json ---
Context: +                "name_too_short": "Dein Name ist zu kurz für die Bankverarbeitung. Banken benötigen mindestens 4 Buchstaben im Namen des Kontoinhabers.", | +                "asset_input_not_allowed": "Der Vermögenswert ist derzeit nicht für den Handel mit DFX verfügbar" |              },
David: Das Asset? (same above)

--- PR #3309 | src/integration/blockchain/icp/icp-client.ts ---
Context: + | +    return transfers; | +  }
David: Is this only ICP or all transfers?

--- PR #3309 | src/subdomains/supporting/payin/strategies/register/impl/icp.strategy.ts ---
Context: +        select: { id: true, blockHeight: true }, | +        where: { address: depositAddress, inTxId: Not(Like('%:%')) }, | +        order: { blockHeight: 'DESC' },
David: Why is this `inTxId: Not(Like('%:%')`?

--- PR #3328 | package.json ---
Context:      "@dfinity/ledger-icp": "^9.1.0", |      "@dfinity/ledger-icrc": "^7.1.0", | +    "@dfinity/principal": "^3.4.3",
David: Wieso das?

--- PR #3347 | src/subdomains/core/buy-crypto/routes/buy/buy.controller.ts ---
Context: +      throw e; | +    } | +
David: try-catch is actually clearer in this case. We e.g. cannot return from the method in a `.catch`, and rethrowing is als cumbersome.

--- PR #3347 | src/subdomains/core/sell-crypto/route/sell.controller.ts ---
Context: +      throw e; | +    } | +
David: Same here

--- PR #3347 | src/subdomains/core/buy-crypto/routes/swap/swap.controller.ts ---
Context: +      throw e; | +    } | +
David: Same here

--- PR #3347 | src/subdomains/core/history/controllers/transaction.controller.ts ---
Context: +      tx = | +        (await this.transactionService.getTransactionByRequestUid(orderUid, relations)) ?? | +        (await this.transactionRequestService.getTransactionRequestByUid(orderUid, { user: { userData: true } }));
David: It's not: `getTransactionByUid` vs `getTransactionByRequestUid`
But we could sumarize the fallback?

--- PR #3347 | src/subdomains/generic/kyc/services/kyc-client.service.ts ---
Context: -    }).then((dto) => dto.flat()); | +    const userIds = wallet.users.map((u) => u.id); | +    const transactions = await this.transactionService.getTransactionsForUsers(userIds, dateFrom, dateTo, limit);
David: Processing each user individually might be quite inefficient, I'll add batching

--- PR #3352 | src/subdomains/generic/user/models/auth/auth.service.ts ---
Context: +          rs.find( | +            (r) => r.isConfirmed && r.type === RecommendationType.INVITATION && r.method === RecommendationMethod.MAIL, | +          ),
David: Why not in SQL query?

--- PR #3375 | src/subdomains/supporting/payin/strategies/register/impl/icp.strategy.ts ---
Context: +        const startBlock = lastBlock > 0 ? lastBlock + (fromBlock ? 0 : 1) : Math.max(0, chainLength - ICRC_BATCH_SIZE); | +        const endBlock = toBlock ?? chainLength; | +        if (startBlock >= endBlock) continue;
David: Store last block to settings (per canister ID)

--- PR #3375 | src/subdomains/supporting/payin/strategies/register/impl/icp.strategy.ts ---
Context: +    fromBlock?: number, | +    toBlock?: number, | +  ): Promise<PayInEntry[]> {
David: Use same logic for native ICP transfers (for all deposit addresses, not only active ones)

--- PR #3375 | src/subdomains/supporting/payin/strategies/register/impl/icp.strategy.ts ---
Context: +const ICRC_BATCH_SIZE = 2000; | +const SETTING_ICP_NATIVE_BLOCK = 'icpLastScannedBlock'; | +const SETTING_ICP_ICRC_BLOCK_PREFIX = 'icpLastScannedBlock:';
David: Use a single setting entry

--- PR #3375 | src/subdomains/supporting/payin/strategies/register/impl/icp.strategy.ts ---
Context: + | +    return { newEntries, highestBlock }; | +  }
David: There is a lot of duplication here (native coin and tokens look very similar). Can we refactor this?

--- PR #3407 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:      const sequenceNumberOffset = master.kycSteps.length ? Util.minObjValue(master.kycSteps, 'sequenceNumber') - 100 : 0; | +    const kycStepMerge = !!slave.kycSteps; |      for (const kycStep of slave.kycSteps) {
David: `slave.kycSteps?.length`?

--- PR #3407 | src/subdomains/generic/user/models/user-data/user-data.service.ts ---
Context:      // Adapt slave kyc step sequenceNumber |      const sequenceNumberOffset = master.kycSteps.length ? Util.minObjValue(master.kycSteps, 'sequenceNumber') - 100 : 0; | +    const kycStepMerge = slave.kycSteps?.length;
David: `!!slave.kycSteps?.length`

--- PR #3428 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
Context: +    const tryCount = Config.payment.defaultFiroTxIdPaymentTryCount; | + | +    for (let i = 0; i < tryCount; i++) {
David: Can we use Util.retry?

--- PR #3428 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
Context: +          quote.txFailed(`Amount mismatch: got ${outputToPayment.value}, expected ${activation.amount}`); | +          return; | +        }
David: Just fail if the sent amount is too small

--- PR #3428 | src/integration/blockchain/firo/firo-client.ts ---
Context:   |      return this.roundAmount(utxos?.reduce((sum, u) => sum + u.amount, 0) ?? 0); |    }
David: This fetches the UTXOs twice. Once to get the addresses (`getNonDepositAddresses`) and afterwards to get UTXOs on non-deposit addresses only. Could be simplified?

--- PR #3428 | src/integration/blockchain/firo/firo-client.ts ---
David: Same here

--- PR #3441 | src/subdomains/core/custody/dto/output/custody-order-list-entry.dto.ts ---
Context: +      created: order.created, | +    }; | +  }
David: I would not use fallbacks (just leave undefined)

--- PR #3477 | src/subdomains/core/payment-link/services/payment-quote.service.ts ---
Context: +      }, | +      Config.payment.defaultEvmHexPaymentTryCount, | +      1000,
David: `defaultEvmHexPaymentTryCount` -> more general naming?

--- PR #3481 | src/subdomains/core/custody/dto/output/custody-order-list-entry.dto.ts ---
Context: -      outputAmount: order.outputAmount, | +      outputAmount: order.outputAmount ?? tr?.estimatedAmount, |        outputAsset: order.outputAsset?.name,
David: Forgot fallback for assets

--- PR #3481 | src/subdomains/core/custody/services/custody-order.service.ts ---
Context:          orderDto.swap = await this.swapService.getById(swapPaymentInfo.routeId); | +        orderDto.inputAsset = targetAsset; |          orderDto.outputAsset = sourceAsset;
David: Revert

--- PR #3485 | src/subdomains/core/referral/reward/services/ref-reward.service.ts ---
Context:        sourceTransaction, | -      status: dto.amountInEur > Config.refRewardManualCheckLimit ? RewardStatus.MANUAL_CHECK : RewardStatus.PREPARED, | +      status: dto.amountInEur > refRewardManualCheckLimit ? RewardStatus.MANUAL_CHECK : RewardStatus.PREPARED,
David: Remove this config entry?

--- PR #3513 | src/subdomains/generic/kyc/controllers/kyc.controller.ts ---
Context: +  async cancelAddressChangeData(@Headers(CodeHeaderName) code: string, @Param('id') id: string): Promise<void> { | +    return this.kycService.cancelStepManual(code, +id); | +  }
David: I would use a general cancel endpoint ` @Delete(':id') `

--- PR #3513 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +      ...kycStep.cancel(kycStep.comment ? `${kycStep.comment};${KycError.USER_CANCELED}` : KycError.USER_CANCELED), | +    ); | +  }
David: Check if the user can cancel this step. I would allow cancelling for all changes (phone, name, address). WDYT?

--- PR #3513 | src/subdomains/generic/kyc/dto/kyc-error.enum.ts ---
Context:    [KycError.NO_GWG_RISK]: undefined, | +  [KycError.USER_CANCELED]: 'You have canceled this kyc step', |  };
David: `this KYC step`

--- PR #3513 | src/subdomains/generic/kyc/controllers/kyc.controller.ts ---
Context: +  async cancelAddressChangeData(@Headers(CodeHeaderName) code: string, @Param('id') id: string): Promise<void> { | +    return this.kycService.cancelStepManual(code, +id); | +  }
David: Rename method

--- PR #3521 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +      }, | +      relations: ['userData'], | +    });
David: Use non-string relations

--- PR #3521 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context: +        const decision = parsed?.complianceReview?.finalDecision; | +        result.set(step.userData.id, decision === 'Abgelehnt' ? OnboardingStatus.REJECTED : OnboardingStatus.COMPLETED); | +      }
David: Constant for `Abgelehnt`?

--- PR #3545 | src/subdomains/generic/kyc/services/kyc.service.ts ---
Context:      const kycStep = user.getPendingStepOrThrow(stepId); | - | +    if (!kycStep) throw new NotFoundException('KYC step not found');
David: Revert (`getPendingStepOrThrow` already throws if not found)

--- PR #3552 | src/shared/auth/role.guard.ts ---
Context: -    [UserRole.SUPPORT]: [UserRole.COMPLIANCE, UserRole.ADMIN, UserRole.SUPER_ADMIN], | +    [UserRole.SUPPORT]: [UserRole.COMPLIANCE, UserRole.MARKETING, UserRole.ADMIN, UserRole.SUPER_ADMIN], | +    [UserRole.MARKETING]: [UserRole.ADMIN, UserRole.SUPER_ADMIN],
David: I think marketing should not be able to access support endpoints

--- PR #3552 | src/subdomains/generic/support/support.controller.ts ---
Context: -  @UseGuards(AuthGuard(), RoleGuard(UserRole.COMPLIANCE), UserActiveGuard()) | +  @UseGuards(AuthGuard(), RoleGuard(UserRole.SUPPORT), UserActiveGuard()) |    async searchUserByKey(@Query() query: UserDataSupportQuery): Promise<UserDataSupportInfoResult> {
David: Do we need to hide some data for non-compliance users?

--- PR #3552 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: -    // mail is required | -    if (!userData.mail) throw new BadRequestException('Mail is missing'); | -
David: Why is this removed?

--- PR #3552 | src/subdomains/supporting/support-issue/dto/support-issue-dto.mapper.ts ---
Context: +      info = issue.additionalInformation as typeof info; | +    } catch { | +      return undefined;
David: How can this throw an error?

--- PR #3552 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +      .map((t) => t.trim()) | +      .filter((t) => t.length > 0) | +      .slice(0, 10);
David: Do we use this server side search?

--- PR #3552 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: -    // mail is required | -    if (!userData.mail) throw new BadRequestException('Mail is missing'); | -
David: The user is notified by mail when a new support message arrives. That's why this check is there 🤔

--- PR #3552 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context:    ): Promise<SupportMessageDto> { | -    return [UserRole.SUPPORT, UserRole.COMPLIANCE, UserRole.ADMIN].includes(jwt?.role) | +    return jwt?.role && [UserRole.SUPPORT, UserRole.COMPLIANCE, UserRole.ADMIN].includes(jwt.role)
David: Is Marketing missing here?

--- PR #3552 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context:   | +  @Get('support/list') | +  @ApiBearerAuth()
David: `  @Get('list')`?

--- PR #3576 | src/subdomains/core/history/dto/transaction-refund.dto.ts ---
Context: +  @IsOptional() | +  @IsNumber() | +  chargebackAmount?: number;
David: Is a bit dangerous when the user can override the chargeback amount?

--- PR #3576 | src/subdomains/generic/support/support.controller.ts ---
Context: +  async getSupportUserInfo(@GetJwt() jwt: JwtPayload): Promise<{ id: number; firstname: string; surname: string }> { | +    return this.supportService.getSupportUserInfo(jwt.account); | +  }
David: What is this used for?

--- PR #3576 | src/subdomains/generic/support/support.controller.ts ---
Context: +  ): Promise<void> { | +    await this.supportService.processChargebackRefund(+id, dto, jwt.account); | +  }
David: Can't we combine this into `@Put('transaction/:id/refund')`?

--- PR #3576 | src/subdomains/supporting/support-issue/support-issue.controller.ts ---
Context: +          ? Department.COMPLIANCE | +          : Department.SUPPORT, | +    };
David: Maybe we should use the user role for this switch?

--- PR #3576 | src/subdomains/core/history/dto/transaction-refund.dto.ts ---
Context: +  @IsOptional() | +  @IsNumber() | +  chargebackAmount?: number;
David: Makes sense, but the "normal" (non-employee) user should not be able to override the chargeback amount for his transactions.

--- PR #3576 | src/subdomains/generic/support/support.controller.ts ---
Context: +  async getSupportUserInfo(@GetJwt() jwt: JwtPayload): Promise<{ id: number; firstname: string; surname: string }> { | +    return this.supportService.getSupportUserInfo(jwt.account); | +  }
David: Does it make sense to pass the data through the frontend? We can just fetch the required data when the user calls API endpoints.

--- PR #3576 | src/subdomains/generic/support/support.controller.ts ---
Context: +  ): Promise<void> { | +    await this.supportService.processChargebackRefund(+id, dto, jwt.account); | +  }
David: Hm, I'm not sure 🤣  for the user endpoints we also have only a single PUT endpoint, DTO validation can be done conditionally.
If we want to keep two endpoints, they should at least be named and use paths so one understands what they are doing. Both do refunds, one is just specific.

--- PR #3585 | src/subdomains/core/aml/services/aml-helper.service.ts ---
Context: +            ? AmlError.USER_DATA_REJECTED_CALL | +            : AmlError.REFERRER_NO_TRADE_HISTORY, | +      );
David: `refUser` ist der Ref User (wird über den Ref bestimmt) und nicht der Recommender.

--- PR #3585 | src/subdomains/core/aml/services/aml.service.ts ---
Context:        entity.user.usedRef !== Config.defaultRef ? await this.userService.getRefUser(entity.user.usedRef) : undefined; | +    const recommendations = await this.recommendationService.getRecommendationsByRecommendedId(entity.userData.id); | +    const recommender = recommendations.find((r) => r.recommender)?.recommender;
David: Find the confirmed recommendation?

--- PR #3587 | src/subdomains/supporting/support-issue/services/support-issue-job.service.ts ---
Context:   | +  @DfxCron(CronExpression.EVERY_MINUTE, { process: Process.SUPPORT_BOT, timeout: 1800 }) | +  async autoOnHold() {
David: Can probably be run a bit less often? 😅

--- PR #3593 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +      [UserRole.SUPPORT]: Department.SUPPORT, | +      [UserRole.COMPLIANCE]: Department.COMPLIANCE, | +    };
David: Maybe we can already reuse it somewhere?

--- PR #3593 | src/subdomains/supporting/support-issue/services/support-issue.service.ts ---
Context: +          `(SELECT TOP 1 m2.author FROM support_message m2 WHERE m2.issueId = m.issueId ORDER BY m2.id DESC)`, | +          'lastAuthor', | +        )
David: Can we use subquery instead of raw SQL?

