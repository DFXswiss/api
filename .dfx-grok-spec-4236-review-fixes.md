# Goal

Implement the seven review fixes for PR #4236 on the already checked-out branch. Make payout designation claim failures fail-safe per order, DRY the conditional claim, make EVM out-of-gas rollback conditional, make failed-order escalation conditional and mail only successfully escalated orders, add the specified mutation-surviving tests, fix the misleading test-helper name, and update the PR body. Preserve all behavior outside these findings.

# Files

Production files:

- `src/subdomains/supporting/payout/strategies/payout/impl/base/payout.strategy.ts`
- `src/subdomains/supporting/payout/strategies/payout/impl/base/bitcoin-based.strategy.ts`
- `src/subdomains/supporting/payout/strategies/payout/impl/base/evm.strategy.ts`
- `src/subdomains/supporting/payout/services/payout.service.ts`
- Remove caller-side duplicate designation warnings and use a plain conditional continue in these eight strategy paths: `impl/base/evm.strategy.ts`, `impl/base/solana.strategy.ts`, `impl/base/tron.strategy.ts`, `impl/base/cardano.strategy.ts`, `impl/base/icp.strategy.ts`, `impl/arkade.strategy.ts`, `impl/spark.strategy.ts`, `impl/lightning.strategy.ts` under `src/subdomains/supporting/payout/strategies/payout/`.

Test files to update/add coverage in:

- `src/subdomains/supporting/payout/strategies/payout/__tests__/payout-designate-before-broadcast.strategy.spec.ts`
- `src/subdomains/supporting/payout/strategies/payout/__tests__/payout-bitcoin-based.strategy.spec.ts`
- `src/subdomains/supporting/payout/strategies/payout/__tests__/payout-nonevm-strategies.spec.ts`
- `src/subdomains/supporting/payout/strategies/payout/__tests__/payout-evm-retry.spec.ts`
- `src/subdomains/supporting/payout/services/__tests__/payout.service.spec.ts`
- Adapt `src/subdomains/supporting/payout/strategies/payout/__tests__/payout.strategy.base.spec.ts` only if existing assertions require it for the new shared helper/error handling.

PR body:

- `/private/tmp/claude-501/-Users-me/3a6d07d7-1d64-4cc1-8535-6f5cd77aa666/scratchpad/pr-body-4232.md`

# Interfaces and exact behavior

1. In `PayoutStrategy`, add one shared protected `claimForBroadcast(order: PayoutOrder, repo: PayoutOrderRepository): Promise<boolean>`. It must atomically update `{ id, status: PREPARATION_CONFIRMED }` to `{ status: PAYOUT_DESIGNATED }`, call `order.designatePayout()` only when affected is truthy, and catch UPDATE errors inside the helper. A lost race logs `Skipping payout order ${order.id}: designation lost to a concurrent payout run`; an UPDATE error logs `Skipping payout order ${order.id}: designation claim failed, order stays re-selectable` with the error. Both return false. Include the explanatory English comment from the user: a failed claim remains unclaimed/re-selectable and must not escape into stale rollback/failure handling. Add a private base logger with a non-colliding name such as `designationLogger = new DfxLogger(PayoutStrategy)` and use it only for these base transition helpers.

2. Keep `designateBeforeBroadcast(order, repo)` signature. If `order.payoutTxId` is set, return true and preserve status. Otherwise delegate to `claimForBroadcast`.

3. In `BitcoinBasedStrategy.designatePayout`, loop through orders, call `claimForBroadcast(order, this.payoutOrderRepo)`, collect only winners, and remove the duplicated update criteria and inline warning. It may remain outside `send()`'s try because per-order claim errors no longer throw.

4. In all eight single-order strategies remove the duplicate caller warning and use the existing style equivalent of `if (!(await this.designateBeforeBroadcast(order, this.payoutOrderRepo))) continue;` without changing the rest of `doPayout`.

5. Add protected `rollbackBroadcastForRetry(order: PayoutOrder, repo: PayoutOrderRepository): Promise<boolean>` to `PayoutStrategy`. It conditionally updates `{ id: order.id, status: PAYOUT_PENDING, payoutTxId: order.payoutTxId }` to `{ status: PREPARATION_CONFIRMED, payoutTxId: null }`. If affected is falsy log `Skipping payout retry for order ${order.id}: state changed concurrently` and return false. Catch UPDATE errors, log `Skipping payout retry for order ${order.id}: rollback claim failed` with the error, return false. Only on success call `order.rollbackPayout()` and return true. Include the supplied English comment explaining that status+exact payoutTxId pinning prevents stale rollback/rebroadcast.

6. In `EvmStrategy.checkPayoutCompletionData`, change only the failed-out-of-gas branch. Replace full-entity rollback/save with the new conditional helper, and ensure `doPayout([order])` happens only if the rollback succeeds. Do not alter the expired branch or any other completion branch.

7. In `PayoutService.processFailedOrders`, first conditionally update each loaded designated order from `{ id, status: PAYOUT_DESIGNATED }` to `{ status: PAYOUT_UNCERTAIN }`. Include only affected winners in one log/mail. For affected=0, skip with a concise log because state moved concurrently. If zero orders escalate, do not mail. Do not call `pendingInvestigation()` or full-entity `save` for this transition. Preserve the existing single-mail behavior.

8. Tests: in the shared single-order designate suite add `repo.update` rejection coverage asserting no dispatch, no save, and resolved `doPayout` with the order unchanged/re-selectable. Add Bitcoin-family coverage that a rejected update excludes that order while winners still dispatch (and no dispatch for a single rejected order is also acceptable, but preferably prove mixed-group continuation). In `payout-bitcoin-based.strategy.spec.ts`, add the ERROR-path loser mutation test: one order loses the claim, dispatch throws a plain error, and `trackPayoutFailure` plus `rollbackPayoutDesignation` receive only winners; the loser must not be saved or mutated. Add EVM OOG tests for conditional rollback success and affected=0/error skip with no retry, and adapt service tests to conditional escalation/mail semantics including mixed/zero-winner cases. Keep assertions precise.

9. In `payout-nonevm-strategies.spec.ts`, split `repoSaveEcho` so update mocking lives in a clearly named helper such as `repoUpdateAffected`, or rename it to accurately describe both update and save behavior. Prefer the sister-spec split pattern; update all call sites.

10. Update the PR-body file while keeping its existing structure. Add the claim-error fail-safe skip semantics and two additional conditional transitions: EVM OOG rollback pinned to status+exact payoutTxId, and `processFailedOrders` escalation conditioned on `PAYOUT_DESIGNATED` with mail covering only successfully escalated orders.

# Constraints

- English comments and existing formatting/style.
- No silent fallback: claim and rollback UPDATE catches must log the fail-safe skip.
- Do not change behavior beyond the findings. Do not touch the EVM expired branch.
- Do not use Git, do not commit, and do not modify unrelated files.
- Do not run npm, npx, package scripts, tests, formatters, type-checkers, linters, or any shell/terminal command.
- Use the repository's existing `DfxLogger` construction idiom and avoid name collisions with subclass loggers.
- Preserve existing user changes.

# Verification information for the supervisor only

The supervisor, not you, will run: `npm run format:check && npm run lint && npm run type-check && npx jest src/subdomains/supporting/payout --silent`.

Run NO shell or terminal commands, including verification commands. Write only the specified files. The supervisor performs verification remotely because the user explicitly prohibited local npm, npx, package-script, and test execution.
